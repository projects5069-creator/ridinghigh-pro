# RidingHigh Pro — Master System Reference
*The single source of truth for the RidingHigh Pro system.*
*If memory fails or confusion arises, this document is the authoritative answer.*

---

## 1. 📋 Document Metadata

| Field | Value |
|-------|-------|
| **Document version** | 3.40 |
| **Last updated** | 2026-06-21 (מתעדכן בכל bump גרסה) |
| **Author** | Generated by Claude (Anthropic) under direction of Amihay Levy |
| **Source of truth** | Live codebase + Sheets reality check (2026-05-02) |
| **Replaces** | v1.x (had drift on 11+ critical facts vs reality) |
| **Latest commit referenced** | `e4d3c84` — refactor(p4.6): AST-based check_02 + hidden bug fix |
| **Active month at generation** | 2026-04 (with 2026-05 active, 2026-06 pre-created) |
| **Codebase size** | ~13,000 lines Python across 60+ active files |
| **Active workflows** | 18 GitHub Actions workflows (incl. `tests.yml` test-CI — TASK-163; `overnight_report_email.yml` dormant — fires only when the overnight runner is armed) |

### Document Changelog
```
v3.40 (2026-06-21): TASK-182 → Done — נסגרה הדליפה של inter-day artifacts דרך **Path 2 (loader recompute-on-missing)**, code-only, ללא sheet backfill / ללא live write. `cross_month_loaders.exclude_interday_artifacts` מבצע כעת recompute לשורות חסרות-דגל (blank/NaN/עמודה-נעדרת) מ-D0-D5 closes דרך `flag_interday_artifact_chain` (SSoT) **לפני** `_coerce_bool` — דגל קיים מנצח, closes חסרים → fail-closed False, copy בזיכרון (ה-df של הקורא וה-Sheet לא נכתבים). נקודת-הזרקה יחידה: Check 29 (`health_audit._interday_artifact_result`) עובר דרך אותה exclude → fix אחד סוגר את שני הצרכנים. אימות חי על research view: contamination **0.47%→4.25% נכון** (8 דולפים נתפסו: ENVB/TDIC/AIIO/PCLA/ASTC/SDOT×3). raw per-month: 04/05 חסרים את העמודה (header נעדר), 06 value-blank — ה-recompute מכסה את כולם בקריאה. TDD: `tests/test_recompute_on_missing_v1.py` 8/8; suite **468 passed** + `test_formulas.py` 125/125, אפס regression. §23 Check 29 עודכן (resolved, לא "open leak"). אפס שינוי לוגיקת מסחר/Score/sizing. §1 version 3.39→3.40.

v3.39 (2026-06-21): TASK-180 D1 reconciliation — דיטקטור split/halt inter-day מתועד מלא. סף `INTERDAY_ARTIFACT_THRESHOLD_PCT=100` (config.py), מכויל מהתפלגות RH post_analysis (median 9.8% / p95 49% / p99≈96%) → 100% מבודד את זנב-הארטיפקט (TDIC +877%, UGRO +417%, PCLA +179%) מ-pumps אמיתיים; **upward-only** (ירידה רצפה ב-−100%; halts-מטה = TASK-149/168, מחוץ לסקופ). parity RH↔DropsLab דרך mirror מאומת (`interday_artifact.py`, **לא** import — ראה Architecture-principles exception). TASK-90/148/173 → Done (detector ported: DropsLab `6be2fae` mirror + `62aff8f` Option-B exclude). DropsLab = **Option B** (on-the-fly exclude בדשבורד+loaders, ללא persist). **FINDING (data-integrity):** `cross_month_loaders.exclude_interday_artifacts` הוא **column-dependent** (קורא עמודת `InterdayArtifact`, לא recompute) → 128 NaN + 51 blank שורות legacy coerce ל-False → דולפות ל-aggregates **וגם** ל-Check 29 (תת-מדווח contamination%) עד backfill; TASK-182 פתוח כדליפה אמיתית (לא קוסמטית). §23: Check 29 (Inter-day Artifacts) תועד (היה drift — חסר). TASK-180/180.1 נשארים פתוחים (180: פער C DropsLab daily-check; 180.1: AC#3 live no-regression לא אומת). אפס שינוי לוגיקת מסחר/Score/sizing. §1 version 3.38→3.39.

v3.38 (2026-06-19): מוזג `feature/overnight-runner` → main — overnight autonomous bug-fix runner (תשתית non-core: `scripts/overnight/`, `.claude/hooks/block_*.sh`+`settings.night.json`, `tests/overnight/`, workflow `overnight_report_email.yml`). **דורמנט:** ה-hooks נורים רק תחת ה-night `--settings`, וה-workflow רק על push ל-`overnight-reports`; אפס השפעה על סשני CC רגילים או על המערכת החיה. 3 ביקורות אדוורסריות, gates 4/5/6+secret-core-hook עברו חי; CI ירוק (460 passed, plists עברו ל-`plistlib` cross-platform). §21 7→8 workflows + תיעוד `overnight_report_email.yml`; §1 Active workflows 17→18. המערכת **מזוינת בסשן זה** ל-launchd 02:00 לימה לריצת TASK-126 בלבד (היחיד auto-safe ב-backlog, ratio 1/59; ראה `docs/overnight/BACKLOG_STATUS_2026-06-19.md`), עם `MAX_CANDIDATES=70` כדי ש-triage יגיע אליו (היה ב-40 הלא-מסווגים). אפס שינוי לוגיקת מסחר. DRY_RUN, Sentinel=shadow. §1 version 3.37→3.38.
v3.37 (2026-06-19): TASK-96 Done — check_06 robustness transient-failure gate. נחלצה פונקציה טהורה `_github_actions_result(recent_runs)` (+ helper `_failing_workflows_recovered`) ב-`health_audit.py`; check_06 הפך לעטיפת-I/O (אפס שינוי ל-fetch/24h-filter). כש-`success_rate<80` (would-be CRITICAL): downgrade ל-WARNING **רק** אם **SAMPLE-SIZE** (`completed < GHA_SAMPLE_MIN=10`) או **RECOVERING** (latest run per failing-workflow ירוק) — אחרת **CRITICAL נשמר** (אילוץ no-masking ממומש בקוד, לא רק בטסט). מטרה: אירוע-checkout חולף (כמו 31/5, TASK-84/85) לא ייצור exit-1/email-אדום שווא, בלי להחליק כשל מתמשך. §23 תיאור check_06 עודכן. TDD: `tests/test_health_audit_actions_v1.py` 7/7 (ה-guard `test_persistent_failure_stays_critical` ראשון); כל health_audit 11/11; suite 436 passed (הכשל היחיד = `test_write_real_decision_to_sheet`, Sheets-integration pre-existing, לא קשור ל-check_06). אפס שינוי ENTER/SKIP/sizing/Score/severity-של-checks-אחרים. DRY_RUN, Sentinel=shadow. §1 version 3.36→3.37.
v3.36 (2026-06-18): Stage 1 — freeze Score sheet-writes (`5c83eca`). `SCORE_WRITE_FROZEN=True`; scoreless-data-era active (daily_snapshots + 6 outputs -> `""` via `score_write_value`/`apply_snapshot_score_freeze`). timeline_live/ScoreType/in-memory selection (≥70/idxmax/calculate_score) untouched — Score still selects entries in-memory until Stage 2+141. §18 banner + score_version updated. rollback=flip flag False. docs-only ב-PK, אפס שינוי קוד. DRY_RUN, Sentinel=shadow. §1 version 3.35→3.36.
v3.35 (2026-06-18): ADR-009 — Score removal, forward-only (decision + Stage 0 landed `bebd901`). Continuous Score confirmed dead (AUC 0.531, value-in-filters; supersedes ADR-002). Score still computed until Stage 1 freeze. §4/§18/§19/score_version updated. Stages 1-3 + TASK-141 pending. docs-only, אפס שינוי קוד/לוגיקת מסחר. DRY_RUN, Sentinel=shadow. §1 version 3.34→3.35.
v3.34 (2026-06-18, סגירת סשן 17/6): **TASK-169 Done** — AC#2 render של Wilson 95% CI (`cc73ed7` dashboard + `cbfea49` emails), משלים את AC#1 (`wilson_ci`, `2f225c3`). נוסף `fmt_rate_ci(successes, n, decimals=1)` ב-formulas.py (formatter טהור, מאציל ל-`wilson_ci`; מחזיר `"53.8% [35–71%]"`, `n<=0`→`"—"`) + שורה 23 ב-§19. **הכרעות נושאות-משקל (לא לדרוס):** CI חל **רק על inferential proportions** (sample k/n — WR/hit-rate/tier-WR); **מוחרגים** census (PRE_FIX% — הרכב ידוע, אפס sampling), means (avg PnL/MaxDrop), counts, price-% (MxV/RunUp/Gap) — הצמדת CI שם = שגיאה סטטיסטית. **dashboard:** טבלאות (Score-comp §1/§3ב/§4) עם **עמודת-CI נפרדת** (שומר sort/color נומרי); metrics/markdown inline; legend "95% CI" פעם אחת לכל surface; Post-Analysis WR שמר optimistic/pessimistic (WHIPSAW) — בלי interval שני; Section-2 bucket chart נדחה (CI לא חל inline, error-bands=future viz); System-Overview TP10 v2 threading בטוח (defaults ב-outer scope). **emails:** daily (LTR) inline; weekly+monthly (`dir=rtl`) עטוף ב-`<span dir="ltr" style="unicode-bidi:isolate">` — **מאומת ויזואלית, אפס היפוך**; subjects נקיים. TDD: helper 7/7 + 6 email-render + dashboard; py_compile נקי; CI ירוק. אפס שינוי ENTER/SKIP/sizing/Score/D0. DRY_RUN, Sentinel=shadow. §1 version 3.33→3.34.
v3.33 (2026-06-17): TASK-180.1 — תיעוד חריג **duplication-with-guard** (§10 עקרון #1) + השלמת §19. (1) §10 "No formula duplication" — נוסף חריג-מותר inline (סגנון חריג TASK-104): ה-detector `is_interday_artifact`+`flag_interday_artifact_chain` שוכפל מכוון מ-`formulas.py` (canonical) ל-repo DropsLab (`interday_artifact.py`, mirror מאומת); אין cross-repo import (רֵפוֹ נפרדים, shared-infra pre-mature ל-edge לא-מאומת); מוגן ב-3 שכבות (golden-fixture ממקרי-הבדיקה של RH + sha256-pin + parity-test local-only מול ה-canonical החי, byte-for-byte); כל שינוי = lockstep. (2) §19 — נוספו שורות 21/22 (`is_interday_artifact`, `flag_interday_artifact_chain`), נסגר ה-blockquote-TODO מ-v3.32. **הקשר (פירוט→docs של DropsLab / TASK-153, לא כאן):** במקביל ב-DropsLab (`6be2fae`/`62aff8f`/`bbb0012`) — on-the-fly exclude בדשבורד (Option B) + `write_post_rows` non-destructive. docs-only ב-RH, אפס שינוי קוד/לוגיקת מסחר. DRY_RUN, Sentinel=shadow. §1 version 3.32→3.33.
v3.32 (2026-06-16): §19 Formulas Inventory completion (docs-only) — נוספה שורה 20 `wilson_ci` (TASK-169 AC#1); נוספה הערת מודול-לוויין `ta_helpers.py` (`rsi14_wilder`/`atr14_wilder`, החריג המתועד ל-"formulas.py only" כי pandas-based); כותרת §19 — הוסרה ספירה מיושנת ("18"). **דווח ולא תוקן (מחוץ ל-scope):** `is_interday_artifact`+`flag_interday_artifact_chain` (TASK-180) עדיין חסרים כשורות §19 — סומנו ב-blockquote לתיקון עתידי. אפס שינוי קוד/לוגיקת מסחר. DRY_RUN, Sentinel=shadow. §1 version 3.31→3.32.
v3.31 (2026-06-16, re-close catch-up): סגירת חוב Anti-Drift על עבודת-היום-שאחרי-הסגירה-המוקדמת (`5abe533`). **TASK-137 Done** — RSI/ATR ב-`update_ticker_follow_up` נותבו ל-`ta_helpers.py` חדש (Wilder, pandas-aware; formulas נשאר scalar-only) על `hist_full` → D1-D3 השוואתי ל-D0; +typical_price_dist→`calculate_typical_price_dist` (DRY). `1cfe31a`+`24f9edb`. **TASK-135 Done** — `orchestrator.is_market_hours` + `utils.is_day_complete` holiday-aware דרך `utils.is_trading_day` (reuse 130). `c508879`. **TASK-182 §0-writer** — collector כותב `str(interday_is_artifact)` (object-dtype, מונע up-cast ל-'1.0'); LIVE-VERIFY post-market. `93c0832`. **CI-gap סגור** — 7 טסטי-pytest הועברו root→`tests/` (נאספים ב-CI; `bfc4604`+`1346a90`). **TASK-169 AC#1** — `wilson_ci()` ב-formulas.py (95% CI, pure+TDD); AC#2 render נפרד. `2f225c3`. **DropsLab-half הוכח read-only** (Full-Recovery 46.6→44.3, −2.3pp, n=3627; `eac8e33`). הכל TDD + CI ירוק מאומת, **אפס שינוי ENTER/SKIP/sizing/Score/D0**. DRY_RUN, Sentinel=shadow. §1 version 3.30→3.31.
v3.30 (2026-06-16): TASK-151 (חלקי, docs-only) — תיקון drift §A SCORE_CAPS_V2: `RSI_LOW` 50→60 (התאמה ל-config.py:57 — מקור-אמת) + הסרת "(alias)" השגוי. **151 נשאר open:** רכיב ה-workflow-count כבר תוקן (PK=17=מציאות, ע"י 138+163); רכיב ה-RSI-semantics ("bell curve false" / SCORE_RSI_PARAMS dead) דורש code-investigation (מחוץ ל-scope docs-only). **חוב Anti-Drift ידוע:** ה-PK עדיין מפגר אחרי עבודת-היום-שאחרי-הסגירה (TASK-137 Wilder · TASK-135 holiday-aware · CI-gap relocation · §0 writer-hardening) — ייסגר ב-catch-up בסגירה הרשמית הבאה. אפס שינוי קוד/לוגיקת מסחר. DRY_RUN, Sentinel=shadow. §1 version 3.29→3.30.
v3.29 (2026-06-16, סגירת יום): **שני תיקוני-CI/data-integrity + ממצא מסגור.** (1) **TASK-184 Done** — CI 'tests' אדום מ-15/6: `test_task172_coverage_v1.py::...union_and_coverage` לא-הרמטי (פיקסצ'ר מקובע 2026-06-14, אבל `collect_borrow_snapshot` מסנן ב-`utils.get_peru_time()`=היום) → נשבר בגבול-יום 14→15/6, **לא רגרסיה מ-commit**; תיקון=הקפאת שעון בטסט (test-only). `3be9571`+`df4b134`, CI ירוק מאומת חי על `df4b134`+`a2bd740`. (2) **TASK-182 §0 — `_coerce_bool` numeric-truthy** (`a2bd740`): bool flag שעבר float up-cast על NaN-mix נכתב `"1.0"/"0.0"`; קוד ישן קרא `"1.0"`→False → דליפת InterdayArtifacts ל-`exclude_interday_artifacts`. עכשיו `str_true | (to_numeric!=0)`; TDD RED→GREEN, מקרי `np.nan`/`-1.0`/floats. **§0 בלבד — לא משלים 182** (backfill 128-NaN/51-blank = live-write post-market, נשאר open). (3) **ממצא מכריע:** "180 recompute 49.5→47.2" — המספר הוא **מטריקת DropsLab** (`max_recovery_5d_pct`, TASK-90/83), **לא RH**; AC#2 של 180 ערבב חצאים. ה-RH-half Done-בקוד (טרם live-verified — לא לסמן Done); ה-DropsLab-half (90/148/173, חופפים) = ה-49.5→47.2 האמיתי + הצעד-הבא (הליבה `flag_interday_artifact_chain` נייד, אפס צימוד-סכמה → port דק ל-`drops_post`). לטיפול: CI-gap (טסטי-root לא נאספים, `testpaths=tests`), writer-hardening §0, TASK-185 בוטל מדעת (reset --soft+archive). 2 commits היום. docs/test/loader בלבד, אפס שינוי ENTER/SKIP/sizing/classification. DRY_RUN, Sentinel=shadow. §1 version 3.28→3.29.
v3.28 (2026-06-15, סגירת יום): **TASK-144 Done end-to-end** — DropsLab collector הוחזר: root cause sleep(0.35) לא-מותנה×4236=24.7דק→timeout 6/5; fix filter_actionable O(actionable) (TDD 3/3, DropsLab 91f9061+52d9b32); drain 1400 שורות (drops_post 2227→3627, 0 כשלונות); CI workflow_dispatch=success 45ש' (מול 20דק cancelled×5); steady-state actionable=0. שחרר TASK-180 DropsLab-half + crossover chain. **TASK_AUDIT_2026-06-15.md** — מיפוי כל 66 (שכבות L0-L4 data-integrity-first) + **באג §0** (_coerce_bool("1.0")→False → exclude_interday_artifacts מחמיץ artifacts numeric שה-collector כתב → חוסם 180-AC2 גם אחרי backfill; תועד, לא תוקן). **TASK-182** — backfill_interday_flags committed (5434861, TDD 3/3); dry-run: scope 179/3-חודשים (לא 51); write-back+תיקון §0 pending, נשאר open. **קיצוץ backlog OPEN 66→63:** archive 34/113, downgrade 33/145→low, close 181 (prune 32 .bak), nest 90/148/173→parent-180. docs/backlog בלבד, אפס שינוי קוד-מסחר/Sheets. DRY_RUN, Sentinel=shadow. §1 version 3.27→3.28.
v3.27 (2026-06-15): TASK-144 — תיקון collector ה-DropsLab (מת 6/5). **שורש (מהלוגים החיים):** time.sleep(0.35) רץ ללא-תנאי לכל שורת raw → 4236×0.35=24.7 דק רצפת-sleep לבדה, גדלה מונוטונית, חצתה timeout-minutes:20 ב-6/5; write-once בסוף → timeout=אפס-התקדמות (כלום לא נכתב מ-6/5). **תיקון (2 commits ב-repo DropsLab 91f9061+52d9b32, טרם push):** filter_actionable (פונקציה טהורה, TDD 3/3) מסננת processed/not-ripe/stale **לפני** הלולאה → O(actionable) לא O(all); existing_keys מנורמל ל-upper (dedup חסין-case); STALE_CUTOFF_DAYS=20 quarantine (logged, לא silent). bootstrap תשתית-טסטים ל-DropsLab (test_drops_collector.py). **dry-run חי (RO):** 1400 actionable (median 11d, 0 מעל 20d, 0 dupes), 1 quarantined, ~20-31 דק. **נשאר (RULE #6):** drain מקומי חד-פעמי (~1400, post-15:00 פרו) → push → CI 16:30 תראה existing_keys מנוקז (אחרת write-once deadlock חוזר). TASK-144 נשאר open עד drain חי. 151 שורות 15-18d = incompletables self-healing (יוקוורנטנו בחציית 20d). אפס שינוי ENTER/SKIP/sizing/classification. DRY_RUN, Sentinel=shadow. §1 version 3.26→3.27.
v3.26 (2026-06-14): TASK-180 RH-half שלב 2ג (אחרון) — **מימוש AC#3 של 180: בדיקה יומית ב-`health_audit.py`**. נוספו `_interday_artifact_result(df)` (טהור/testable, בלי gc) + `check_29_interday_artifacts(gc)` (wrapper) אחרי check_21, ונרשמו ב-runner אחרי check_21. **advisory WARNING — אף פעם לא BLOCK.** ה-check עושה **reuse של `exclude_interday_artifacts`** (cross_month_loaders) → מדווח **בדיוק את ה-contamination%/n שה-loader המחקרי מחריג** (cross-month, v2) — מקור-אמת אחד עם נתיב-המחקר. `details` = רשימת טיקרים מסומנים (capped 10). **טיפול-שגיאות מבודד:** `gc is None` / כשל-טעינה → **INFO** (לא crash); `df` ריק / עמודה חסרה (דאטה ישנה לפני ה-detector) → **PASSED** (backward-compat). check_29 = המספר הפנוי הבא (תפוסים 01-28). **TDD:** קובץ standalone חדש `test_health_audit_interday_v1.py` (root, §12), 4/4 על הפונקציה הטהורה; py_compile נקי. ⚠️ **live-verify של check_29 בריצת health_audit חיה — pending (RULE #6).** **סטטוס RH-half:** שלושת ה-AC של 180 **ממומשים בקוד** — AC#1 detector (`is_interday_artifact`/`flag_interday_artifact_chain`, v3.23), AC#2 collector 2 עמודות + loader exclude (v3.24/3.25), AC#3 בדיקה יומית (v3.26). **נשאר:** recompute אגרגטים נקי (49.5→47.2) + מיזוג TASK-90/148/173 → 180. **TASK-180 נשאר open** (live-verify + DropsLab-half + recompute). §1 version 3.25→3.26. אפס שינוי ENTER/SKIP/sizing/classification. DRY_RUN, Sentinel=shadow.
v3.25 (2026-06-14): TASK-180 RH-half שלב 2ב — exclude מחקרי ב-`cross_month_loaders.py` (מממש **AC#2 של TASK-173**: exclude שורות מסומנות + דיווח contamination%). 2 פונקציות חדשות ליד ה-Helpers: `_coerce_bool(series)` — str→bool עם **הגנת gotcha** (`bool("False")==True` כי כל מחרוזת לא-ריקה truthy; לכן השוואת-מחרוזת מפורשת `in {'true','1','yes'}` ולא `astype(bool)`); ו-`exclude_interday_artifacts(df, flag_col="InterdayArtifact") -> (clean_df, contamination_pct, n_excluded)` — **row-based** contamination%, מסיר שורות עם `InterdayArtifact==True`. **backward-compat:** df ריק / `flag_col` חסר (דאטה ישנה שנכתבה לפני ה-detector) → `(df, 0.0, 0)`, **לעולם לא raise**. **אפס שינוי לחתימת `load_post_analysis_all_months`** → 3 ה-callers (dashboard.py:1046 / smoke_test_port_month.py:70 / main) לא נשברים. **מיקום ב-loader ולא ב-formulas** — `formulas.py` נשאר pandas-free (scalar טהור); ה-helpers הם pandas-based ושייכים ל-loader (שכבר מייבא pandas). **TDD:** קובץ standalone חדש `test_cross_month_loaders_v1.py` (root, §12 versioned), 6/6 כולל **gotcha guard** (`"False"→False`); מורץ `uv run --with-requirements requirements.txt python3 ...`. **שלב 2ב** — נשאר רק **2ג** (`health_audit` בדיקה יומית, AC#3 של 180) + recompute אגרגטים נקי; **TASK-180 נשאר open**. §1 version 3.24→3.25. אפס שינוי ENTER/SKIP/sizing/classification. DRY_RUN, Sentinel=shadow.
v3.24 (2026-06-14): TASK-180 RH-half שלב 2א — wiring של ה-detector ל-`post_analysis_collector.py`. נוספה פונקציה טהורה `flag_interday_artifact_chain(closes, threshold_pct=None) -> (bool, str)` ב-`formulas.py` (מתחת ל-`is_interday_artifact`): **ANY-pair** על שרשרת close-to-close `[D0_Close..D5_Close]` — מחזירה את הזוג הראשון שהדליק (`"D{i}->D{i+1}"`) או `(False,"")`; **מאצילה את הסף ל-`is_interday_artifact`** (אפס שכפול-סף, SSoT §10 נשמר); guard על closes ריק/None ועל זוג עם None/0. ב-collector: `interday_closes = [d0_fund.D0_Close] + [ohlc.D1..D5_Close]` מחושב לפני `new_row`, ונכתבות **2 עמודות חדשות ל-post_analysis (CamelCase): `InterdayArtifact` (bool) + `InterdayArtifactPair` (str)** ליד `audit_flag`. העמודות נכתבות **בסוף** דרך ה-union writer (`gsheets_sync.save_post_analysis_to_sheets:334-340` — existing∪new, reindex, append-at-end), אפס הזזת עמודות קיימות → **תואם TASK-150** (positional readers לא נשברים). **דגל לא-הרסני:** אפס mutation של ערכים. **TDD:** RED (ImportError) → GREEN; 8 טסטים חדשים (`test_interday_artifact_chain`: TDIC→D0->D1, split-אמצע→D2->D3, None-skip, guards, custom-threshold), suite **117→125**, py_compile נקי על 3 הקבצים. **שלב 2א בלבד** — `cross_month_loaders` (exclude שורות מסומנות + דיווח contamination%, AC#2 של 173) ו-`health_audit` (בדיקה יומית, AC#3 של 180) עדיין פתוחים; **TASK-180 נשאר open**. §1 version 3.23→3.24. אפס שינוי ENTER/SKIP/sizing/classification. DRY_RUN, Sentinel=shadow.
v3.23 (2026-06-14): TASK-180 RH-half שלב 1 — detector של ארטיפקטי split/halt ב-inter-day. נוספה פונקציה טהורה `is_interday_artifact(prev_close, next_close, threshold_pct=None)` ב-`formulas.py` (בין `calculate_d1_gap` ל-`calculate_net_pnl`) + const `INTERDAY_ARTIFACT_THRESHOLD_PCT = 100.0` ב-`config.py` (בלוק Raw Data Caps, ליד REL_VOL_CAP). **calibration:** הסף 100% נגזר מהתפלגות אמיתית של post_analysis (snapshots Apr/May/Jun, 289 שורות → 1,342 זוגות-ימים close-to-close): median 9.8%, p95 49%, **p99≈96%** → >100% מבודד נקי את זנב-הארטיפקטים (TDIC +877%, UGRO +417%, PCLA +179%). **דגל לא-הרסני:** מחזיר bool בלבד, אפס mutation של ערכים (loaders/aggregates יחריגו בשלב נפרד). **⚠️ upward-only by design:** תנועת close-to-close כלפי מטה חסומה ב-−100% (מחיר→0) ולכן לעולם לא חוצה סף 100%; ה-detector תופס רק קפיצות-split כלפי מעלה — בדיוק מה שהדאטה הראתה (כל 12 הקיצוניים +ve). down-artifacts (halt/delisting) **out-of-scope** כאן, מכוסים ב-TASK-149/168 (PHASE 1). **TDD:** RED (ImportError) → GREEN; 10 טסטים חדשים (`test_interday_artifact`), suite **107→117**, אפס רגרסיה; py_compile נקי. **שלב 1 בלבד** — ה-wiring ל-`post_analysis_collector` (עמודת flag) / `cross_month_loaders` (exclude + contamination%) / `health_audit` (בדיקה יומית, AC#3) עדיין פתוח; **TASK-180 נשאר open**. המלצת מיזוג 90/148/173→180 בעינה. §1 version 3.22→3.23. אפס שינוי ENTER/SKIP/sizing/classification. DRY_RUN, Sentinel=shadow.
v3.22 (2026-06-14, post-investigation correction): תיקון מסגרת ה-PHASE 0. חקירת READ-ONLY של 4 חוסמי ה-data-integrity גילתה ש-**TASK-150 ו-TASK-105 כבר Done ב-backlog** ולכן אינם חוסמים פתוחים — בניגוד לרישום ב-v3.21 changelog ובגרסה המקורית של `docs/WORK_PRIORITY_2026-06-14.md`. החוסמים הפתוחים בפועל = **TASK-180 + TASK-144 בלבד**. (TASK-150 נסגר `a7897a5`/PK v3.06 = audit-check + §15 doc; `cross_month_loaders.py` כבר מיישר לפי שם — ה-"enforce כל הreaders" הוא flag נפרד מחוץ ל-AC. TASK-105 נסגר PR #4/`dc3ddbf`/PK v2.66.) WORK_PRIORITY תוקן עם בלוק CORRECTION + סימון 150/105 DONE. הוכרע מודל-נתונים ל-TASK-180: **flag לא-הרסני** (עמודת boolean, בלי שינוי ערכים; loaders/aggregates מחריגים) — הגדרת-סף הזיהוי נשארה OPEN. המלצה: למזג TASK-90/148/173 → TASK-180. docs-only, אפס שינוי קוד/לוגיקת מסחר. Sentinel=shadow, DRY_RUN. §1 version 3.21→3.22.
v3.21 (2026-06-14, סגירת יום): תעדוף data-integrity-first. נכתב `docs/WORK_PRIORITY_2026-06-14.md` (`bcfceed`) — עיקרון-ברזל: נתונים מזוהמים = כשל מערכת, לא פריט "איכות"; אף מחקר / נעילת 178 / Decision-Gate לא תקֵף על דאטה מלוכלכת. **4 חוסמי PHASE 0 שחייבים להיסגר לפני כל עבודה פונה-מחקר:** (1) split/halt detector מאחד TASK-90+148+173 (ארטיפקטים של reverse-split/halt מזהמים כל אגרגט: DropsLab d1 mean +124% מול median 0; CTNT +28567%; 5.6% DL + ~3% RH שורות >100% inter-day) → נפתח **TASK-180** (high, data-integrity); (2) TASK-150 schema-drift (Apr 122 עמ' מול May/Jun 105, score_version זז idx 60 → positional readers נשברים בשקט; גם גייט ל-materialization של D6-D25); (3) TASK-105 silent-loss (כתיבת paper_portfolio בולעת 429 → ENTER רשום בלי שורת-פוזיציה, POSITION_SYNC כוזב); (4) TASK-144 DropsLab קפוא (collector מת 6/5, drops_post קפוא 27/5, 1766 שורות גלם — חוסם-על לכל שרשרת ה-crossover, אין אירועים חדשים). **TASK-177 — תיקון מסגור:** code+TDD done, AC#3 live-verify pending, status To Do **by design** (כמו 172) — לא "status-conflict"; §D's "DONE" = שכבת איסוף D6-D25, הטask פתוח עד ריצת collector אמיתית. נוסף note ל-177 (auto-grow gap: D6-D25 נעדרות מ-header חי, 109 עמ'/0 D6+, סיכון silent-drop). **178 — האסטרטגיה הוכרעה 6/14** (entry d1_close של ה-drop-event, exit ≤5 ימי-מסחר או ±10%, forward-only hold-out, NO peeking) אך **הנעילה נדחית עד PHASE 0 נקי**. docs/backlog בלבד, אפס שינוי קוד/לוגיקת מסחר. Sentinel=shadow, DRY_RUN. §1 version 3.20→3.21.

v3.20 (2026-06-14): TASK-172 coverage — הרחבת יקום-הטיקרים של borrow snapshot + טאב borrow_coverage. מקור-הטיקרים ב-collect_borrow_snapshot הורחב מ-existing_positions (~1/יום) ל-union(daily_snapshots Score≥MIN_SCORE_DISPLAY=60, existing_positions) — כל פאמפ-RH נסרק מקבל פסיקת shortability (הגדרת היקום של HYP-001 §D = "fired a scanner trigger"). קריאת daily_snapshots ב-try/except מבודד (כשל → fallback ל-existing בלבד). טאב agent חדש borrow_coverage (8 עמ': CheckDate·CheckTime·ScannedUniverse·WithBorrowData·PctWithBorrowData·ShortableCount·PctShortable·Source) — שתי עמודות-אחוז נפרדות מעל מכנה=היקום (הפער = covered-but-not-shortable, survivorship transparency ל-179), שורה אחת/יום (dedup CheckDate). helpers טהורים: get_scanned_universe, compute_coverage, build_coverage_row, collect_borrow_coverage. TDD: 12 טסטים חדשים (5 commits: schema→universe→coverage→writer→wiring), suite 361 passed (הכשל היחיד test_write_real_decision_to_sheet = pre-existing integration תלוי-Sheets, לא רגרסיה). פער ידוע: יצירת הטאב החי ב-Sheets deferred (RULE #6, OAuth — לא בשוק סגור); TASK-172 AC#3 live-verify פתוח (כמו 177). §1 version 3.19→3.20. אפס שינוי ENTER/SKIP/sizing/classification. Sentinel=shadow, DRY_RUN.

v3.19 (2026-06-13, סגירת יום #2): נספח ל-`SESSION_HANDOFF_2026-06-13`. המשך היום אחרי הסגירה הראשונה (v3.16): TASK-175 Done + TASK-131 (`091e766` — כיוון LONG נסגר מראיה) + **TASK-177 code+TDD complete** (`5a252a9`/`03d4cb9`/`a7bbbee`/`b9a53ea` — scan-anchored D1-D25, D6-25 data-only, forward-only, סיווג קפוא, 9 טסטים כולל regression-guard). **TASK-177 status = To Do — LIVE-VERIFY pending (RULE #6, העמודות מופיעות רק אחרי ריצת collector אמיתית).** הבא: TASK-178 (חסום על 172-coverage + הכרעת עוגן ה-hold-window D6-D15). 8 commits היום. §1 version 3.18→3.19. docs בלבד, אפס שינוי קוד/לוגיקת מסחר/סיווג רשמי. Sentinel=shadow, DRY_RUN.
v3.18 (2026-06-13): TASK-177 — חלון תוצאה מורחב ב-post_analysis ל-scan-anchored **D25** (forward-only מ-`COLLECT_DAYS_FORWARD_FROM=2026-06-13`). D1-D5 = full OHLC (סיווג **קפוא**, `utils.range(1,6)` לא נגע); **D6-D25 = `D{i}_Close` + `D{i}_Low` בלבד (~40 עמ' חדשות), data-only** — לא נקראות ע"י `calculate_stats`/`classify_trade`. מטרה: ה-superset שמכיל את ה-hold window של HYP-001 (crossover-short), שייגזר ב-179 ויינעל ב-178 — **177 לא נעל את החלון, רק הבטיח שהדאטה תכיל אותו** (השאלה "D6-D15 מאיזה עוגן" נשארת פתוחה ל-178). config SSoT: `CLASSIFY_DAYS=5` + `COLLECT_DAYS_FORWARD=25` + cutoff; `is_complete` מבחין legacy (<cutoff → complete ב-D5, אפס מגע) מ-new (≥cutoff → אוסף עד D25, break-on-unsettled נשמר). fetch buffer scaled; `gsheets_sync.ensure_grid_width` (grows-only, never-shrink) לפני `ws.update` (TASK-123). §15 מתעד את העמודות; SCHEMA.json (167) יאמץ. TDD: 9 חדשים — כולל **regression-guard שמוכיח D6-D25@−90% לא מזיז MaxDrop/TP/SL/BestDay/classify_trade**; suite 347 / formulas 107 / utils 38. §1 version 3.17→3.18. אפס שינוי ENTER/SKIP/sizing/official-classification. Sentinel=shadow, DRY_RUN.
v3.17 (2026-06-13): TASK-175 Done (+ resolves TASK-131) — סגירת כיוון LONG ב-§4 על בסיס ראיה (לא רק design): buying DropsLab drops d1→d5 = −1.64% [−2.50,−0.77], n=2,103, WR 43.7% (−3.64% אחרי slip 1%/side; ה-"75% recovery" = hindsight peak-touch) — investigation II phase 7. תוקנה סתירה ב-#N25: DropsLab drop-events הוסבו מ-"context for active longs" ל-crossover-short candidate feed **DRAFT/EXPLORATORY** (HYP-001, לא מאומת; LONG נשאר non-goal סגור §4). TASK-131 נסגר דרך 175. §1 version 3.16→3.17. docs-only, אפס שינוי לוגיקת מסחר. Sentinel=shadow, DRY_RUN.
v3.16 (2026-06-13, סגירת יום): נכתב `docs/SESSION_HANDOFF_2026-06-13.md`. הסשן: TASK-172 refocus (`2e0519c` — borrow_collector אומת אוסף מ-11/6, פרמיסה הופרכה, §1 drift תוקן) + TASK-165 Done (`0792795` — HYPOTHESES.md + HYP-001 crossover-short DRAFT). הצעד הבא: TASK-177 (חלון D6-D15 — השאלה החוסמת את שרשרת ה-crossover). main נקי/מסונכרן, DRY_RUN, Sentinel=shadow. close-ritual §3 (PK bump on handoff); אפס שינוי קוד/לוגיקת מסחר.
v3.15 (2026-06-13): TASK-165 Done (framework + DRAFT, לא נעילה) — נוצר docs/HYPOTHESES.md: מרשם pre-registration למחקר (§0 מטרה · §A policy נעול: purged split+embargo 5d, BH deflation, n≥150/floor n≥30, fitness תלת-רכיבי = net-exp @ borrow500×HOLD_DAYS/365 + slip 2%/side + CI-מובהק, discovery לא ממוחזר · §B template · §C journal · §D HYP-001 crossover-short DRAFT · §E integration). HYP-001 = SHORT על crossover RH→DropsLab ≤10d; discovery 66% (123/185) המשך −17.75% [−24.5,−11.0] n=62 על 5d (EXPLORATORY, נעול). **status=DRAFT — hold window D6-D15 + entry/exit PENDING TASK-177; נעילה רשמית ב-TASK-178 אחרי 172-coverage+177.** ⚠️ discovery (5d) ≠ validation (D6-D15) — החולשה המרכזית גלויה במסמך. PK מצביע למרשם (SoT לממשל-מחקר, לא משכפל §A). §1 version 3.14→3.15. docs-only, אפס שינוי לוגיקת מסחר. Sentinel=shadow, DRY_RUN.
v3.14 (2026-06-13): TASK-172 מומקד-מחדש + תיקון drift. **אימות חי:** borrow_collector (TASK-139) **אוסף מ-11/6** — borrow_data 2026-06 מכיל שורות אמת (ADIL 11/6 Shortable/ETB; EDHL 12/6 NOT-shortable), ריצת EOD 21:00 כותבת 1 שורה/יום, ריצת 22:16 מחזירה 0 (dedup, לא כשל). הפרמיסה "0 rows ever — wire it" (TASK-172) ו-II-0.2 בדו"ח investigation II = **stale** (ה-backup CSV נלקח 11/6 לפני ריצת ה-21:00). TASK-172 מומקד: AC#1 מסופק ל-`existing_positions` (~1/יום = open∪ENTERs); נשאר הרחבת coverage ליקום הנסרק + coverage report. BorrowFeePct נשאר NULL (Alpaca חושף דגלי shortability בלבד) — worst-case borrow כבר פרמטרי ב-`calculate_net_pnl` 50/200/500%/שנה (TASK-140), ולכן 178/179 **לא** חסומים על מקור-fee; fee אמיתי per-ticker = שכבה-2 LOW אופציונלי (כבר מתועד כעתידי ב-v3.02). **תיקון Anti-Drift:** שדה §1 `Document version` פיגר 3.06 מול changelog v3.13 → סונכרן 3.14 + `Last updated` 13/6. docs/backlog בלבד, אפס שינוי קוד/לוגיקת מסחר. Sentinel=shadow, DRY_RUN.
v3.13 (2026-06-12): TASK-152 Done — **PK Google-Sheets mirror RETIRED** (לא resync). ה-mirror "RidingHigh-Pro-System-Reference" (`1SuHj0…`) היה stale (סונכרן לאחרונה 16/5, הציג v2.0 מול live v3.12) ו-redundant — ה-PK SoT הוא **git** (versioned). הוסר מהדאשבורד (בלוק "Google Sheet Master" → "retired" + הוסרה שורת ה-checklist `sync_pk_to_sheet.py`); `sync_pk_to_sheet.py` סומן **DEPRECATED** בכותרת (נשמר לפי §12 versioned-filenames, לא להריץ). בדיקת S-Sync בדאשבורד (repo↔origin git) לא נגעתי — היא לא ה-mirror. display/docs בלבד, אפס שינוי לוגיקת מסחר. Sentinel=shadow, DRY_RUN.
v3.12 (2026-06-12): TASK-26 Done — WHIPSAW + NO_TOUCH analysis (offline, אפס שינוי קוד/מסחר). **WHIPSAW** נפתר ב-TASK-155 (8W/17L/1U). **NO_TOUCH**: n=3 בלבד (SBLX/ARKR/OSS) — איכותי בלבד, כולם Score נמוך ליד הסף (61-66) → תואם השערת weak-signal (ה-Score הוא הסיגנל). **Characterization (`docs/research/TASK26_WHIPSAW_CHARACTERIZATION_2026-06-12/`, gitignored):** חציוני מטריקות-כניסה, WHIPSAW מול decided WIN/LOSS *באותו יקום* — WHIPSAW **יחסית** מתוח יותר (RunUp +82% / Gap +45% / ATRX +19% מול decided; REL_VOL −27% / Float −19%). **⚠️ EXPLORATORY, n=26, LOW (tier 10-29) — השערה, לא ממצא, לא פילטר מאומת.** Δ הם relative-vs-decided (RunUp/Gap גבוהים מאפיינים את כל ה-pump universe = ה-thesis; הממצא הוא ש-WHIPSAW *יותר* מתוח מ-decided, לא absolute). **אסור להחליט פילטר whipsaw-prone על n=26** — דורש re-run על n>50 + מבחן הפרדה = **task עתידי** (לא פתיחת 26). חוסם הדאטה (n>91) הוסר (n=128). Sentinel=shadow, DRY_RUN.
v3.11 (2026-06-12): TASK-164 Done — **resolved (third) bound מ-verdicts אינטראדיי**. נוסף חסם **resolved** ל-headline WR (Post Analysis + Home) ולמשטח ה-expectancy, מעל ה-optimistic/pessimistic הקיימים. מקור: `whipsaw_verdicts.json` **committed** (deploy-visible דרך חריג `.gitignore` `!whipsaw_verdicts.json` — מחקה את `!sheets_config.json`; snapshot סטטי של 26 ה-verdicts מ-TASK-155). `metrics_bounds.resolved_class(verdict, fallback="LOSS")` (WIN/LOSS pass-through, UNRESOLVED→excluded, חסר→**fallback פסימי** כך שלא מנפח כשהדאטהסט גדל). **Resolved WR = 49.2%** (base 53W/46L + intraday 8W/17L; XNDU UNRESOLVED מוחרג) — בין optimistic 53.5% ל-pessimistic 42.4%, **תואם מדויק ל-TASK-155**. guard: קובץ-חסר → ה-resolved מושמט (optimistic/pessimistic עדיין מוצגים, לא קורס). מתויג ב-UI "as-of 2026-06-12 snapshot". **זה שכלול/דיוק מעל ה-edge ה-executable השלילי שכבר תועד ב-v3.10 — לא ממצא חדש.** TDD: resolved_class 5 חדשים; helpers דאשבורד אומתו ב-ast.parse + smoke (49.2%). תפיסת gotcha: inline comment ב-gitignore חוסם negation (תוקן — comment בשורה נפרדת). אפס שינוי schema/persisted/official-WR-metric/ENTER/SKIP/sizing — חסם נוסף additive. Sentinel=shadow, DRY_RUN.
v3.10 (2026-06-12): TASK-162 + TASK-147 Done — **expectancy dual-bound surface (executable D1_Open)** + **ממצא מחקרי: ה-edge ה-executable שלילי**. נוסף משטח expectancy חי ב-Post Analysis: on-the-fly D1_Open (`classify_trade_row_full`→(cls,day) + `calculate_net_pnl` ה-scale-invariant + `metrics_bounds.expectancy_bounds`), optimistic (WHIPSAW מוחרג) מול pessimistic (WHIPSAW=SL), borrow 50/200/500 — **אפס עמודות חדשות, אפס backfill** (עקבי עם איך שה-WR נעשה ב-142). **ממצא (לא רק surface):** ה-expectancy ה-executable על D1_Open **שלילי גם אופטימית** — −1.47pct opt / −3.74pct pess @ borrow50 (−1.98/−3.00 opt @ borrow200/500). הסיבה: עם slip ה-breakeven WR ≈ 60.8%, וה-WR ב-D1_Open ~53.5% < 60.8% → תוחלת שלילית. **מאשר אמפירית את RH-6.3** (ה-edge נהפך שלילי תחת borrow ריאלי). תיקון עוגן: ה-+1.06/−1.28 שב-AC היה **ScanPrice/חקירה-era** (מנופח, אותו look-ahead ש-142 תיקן; NetPnL אף לא היה קיים ב-snapshot 6/10) — לא משוחזר על D1_Open, וזו תכונה לא באג. AC #2/#3 בוטלו (אין net_pnl param; on-the-fly). NetPnL_* המתמשכות תויגו ScanPrice/Table-A diagnostic (comment). **TASK-147 → Done** (dual reporting הושלם: WR ב-142 + expectancy ב-162). resolved third-bound (verdicts מ-155) → **TASK-164** (דורש אחסון verdicts). TDD: expectancy_bounds 6 + classify_trade_row_full 4 = 10 חדשים; net_pnl/classify/formulas 107/107 ירוקים; string-contract של classify_trade_row נשמר. אפס שינוי schema/persisted/ENTER/SKIP/sizing. Sentinel=shadow, DRY_RUN.
v3.09 (2026-06-12): TASK-155 — minute-bars cache + WHIPSAW resolver (**analysis only, אפס שינוי WR רשמי**). מודולים חדשים: `intraday_cache.get_intraday_bars_cached` (disk-cache JSON ב-`data/intraday_cache/`, settled-day-only, atomic, dependency-free — עוטף את `get_intraday_bars` הקיים; תבנית sma20_cache) + `utils.resolve_whipsaw(entry, minute_bars)` (WIN/LOSS **רק** אם שני הצדדים נראים בברים נפרדים — המוקדם מכריע; one-side/same-bar/neither → UNRESOLVED, לעולם לא ניחוש; `classify_trade` לא נגעתי). Phase-0 spike (IEX feasibility GATE): **25/26 RESOLVABLE = 96%** על האוכלוסייה הרשמית (תיקון data-quality 44→26: v2-filter + dedup + entry=D1_Open). ממצא (offline, `docs/research/WHIPSAW_RESOLUTION_2026-06-12/`, gitignored): 26 WHIPSAW → **8 WIN / 17 LOSS / 1 UNRESOLVED** (XNDU — צד אחד ב-IEX). WR D1_Open: optimistic 53.5% / **RESOLVED 49.2%** / pessimistic 42.4% — ה-WHIPSAW מטים שלילה (68% מהמוכרעים LOSS), מאשר אמפירית את RH-6.3. קלט ל-**TASK-162** (expectancy bound) ו-**TASK-26** (WHIPSAW analysis). הערה: v2-filter על snapshot ה-CSV נותן n=128 (לא 149 של ה-REPORT) — WHIPSAW=26 תואם מדויק על v2/D1_Open (פער-ספירה bookkeeping, לא סתירה). TDD: cache 4 + resolver 9 = 13 חדשים; classify/formulas 107/107 ירוקים. Sentinel=shadow, DRY_RUN.
v3.08 (2026-06-12): TASK-163 — נוסף **test-CI אמיתי**. עד כה הריפו לא הריץ pytest ב-CI כלל (רק filename_guard) — הפער התגלה ב-TASK-142 (הנחנו "CI ירוק 221/0" שלא קיים; ה-221/0 המתועד הוא הרצה מקומית). נוסף `.github/workflows/tests.yml` (`uv run --with-requirements requirements.txt --with pytest pytest -m "not integration"` על push+PR; + `test_formulas`/`test_utils` כסקריפטים), `pytest.ini` (`testpaths=tests` — מוציא קבצי-סקריפט root + snapshot `project_sync_20260418` מ-collection), `tests/conftest.py` (auto-mark לפי path של `tests/agent/integration` כ-`integration` → מדולגים ב-CI ללא Sheets creds). ריצה ראשונה על main: **301 passed / 2 skipped / 3 deselected** (integration) + formulas 107/107 + utils 38/38 = success על runner נקי. Active workflows 16→17. אפס שינוי לוגיקת מסחר. Sentinel=shadow, DRY_RUN.
v3.07 (2026-06-12): TASK-142+147(WR-half) — ה-WR הרשמי עבר ל-**D1_Open (executable)** במקום ScanPrice (run_eod peak-Score hindsight, +13.6pp look-ahead; executable ~53-55% מול ~58% הישן). `classify_trade(scan,ohlc,entry_price=None)` + `classify_trade_row(row,entry_basis="D1_Open")` — ליבת WIN/LOSS/WHIPSAW **לא נגעה**, רק עוגן TP/SL זז; D1_Open חסר→PENDING (אין נפילה שקטה ל-ScanPrice). 2 משטחי headline (Post Analysis + Home) → D1_Open + **חסם WHIPSAW-as-loss פסימי** לצד ה-headline (`metrics_bounds.wr_bounds`, policy-layer בלבד; הליבה ממשיכה להחזיר WHIPSAW). §20 Table A→דיאגנוסטי / Table B→רשמי. `calculate_net_pnl`: **אין** entry_price — ה-PnL fraction scale-invariant (entry מצטמצם; ה-basis נישא ע"י (cls,day) מ-classify_trade), נעול ב-test_netpnl_entry_basis_v1. משטח expectancy חי → TASK-162. אתרי Score-research "Win Rate" (TP10_Hit window-touch, ScanPrice) → "**TP10 Hit-Rate**" לדיסאמביגואציה. §15 schema ללא שינוי. TDD: 142(7)+netpnl-lock(3)+wr_bounds(5)=15 חדשים; classify/phase6-equiv/utils 38/38/formulas 107/107 ירוקים. אפס שינוי ENTER/SKIP/sizing. Sentinel=shadow, DRY_RUN.
v3.06 (2026-06-11): TASK-150 — §15 Data Schema: תועד drift סדר/מספר-עמודות חוצה-חודשים ב-post_analysis (docs-only, ממקור-ריבו `phase4_evidence.md`, אפס קריאת-Sheets): אפריל 122 עמ' (17 legacy-only: Volume/MarketCap/Score_B..I/EntryScore...) מול מאי-יוני 105; מאי↔יוני אותה קבוצה בסדר שונה מ-idx 60 (`score_version` הוזז לפני IntraHigh) → קוראים positional נשברים חוצה-חודשים; להשתמש בקריאה header-aware (`cross_month_loaders.py` כבר מיישר לפי שם). דגל (לא בוצע — נדרש scope קוד): enforcement מלא של כל הקוראים להיות header-aware (חלק ה-"audit/enforce" של 150). חלק מ-batch 2c-1 (supervised auto-mode). אפס שינוי לוגיקת מסחר. DRY_RUN, Sentinel=shadow.
v3.05 (2026-06-11): TASK-138 — ניקוי drift תיעודי ב-PK (docs-only, אפס שינוי קוד, מאומת מול הקוד החי): §19 inventory — validate_atrx bool→float (מחזיר atrx/0 על bad-data), normalize_mxv/atrx 0-1→0-100 + סומנו unused (אפס call-sites, רק import ב-dashboard); §1 + §A טבלת-תשתית — Active workflows 7→16 (ספירה חיה `.github/workflows/*.yml` כולל filename_guard); §2 TL;DR — Dashboard "3 pages"→12 (לפי `_PAGE_NAMES` sidebar nav); §29 כותרת "(current)"→"(as of 2026-05-02 snapshot)". נדחו מ-138 ודווחו לעמיחי (לא נגעתי, דורש אימות/scope אחר): ROCKET 16/0 OOS (נתון OOS לא-מאומת), §29 numbers→276/172 (דורש קריאת Sheet חיה), score_tracker §20 (כבר מתועד ב-§A6 שורה 1341), Sheets-mirror (TASK-152), "3 pages" ב-2 דיאגרמות ASCII + ADR-008 (שמירת alignment/היסטוריה). אפס שינוי לוגיקת מסחר. DRY_RUN, Sentinel=shadow.
v3.04 (2026-06-11): TASK-140 — net-PnL cost model ב-post_analysis (slippage 1%/side + borrow pro-rata). formulas.calculate_net_pnl (מודל phase6: fill/cover adverse, net=gross−rate×days/365; WIN/LOSS בלבד, אחרת NULL); config SLIP=0.01 + BORROW_SCENARIOS=[0.50,2.00,5.00]; classify_trade→(cls,resolution_day) [classify_trade_row עוטף [0], dashboard ללא שינוי]; מקופל ל-calculate_stats (SSoT collector+backfill) → 4 עמודות post_analysis NetPnL_SlipOnly/Borrow50/200/500 (+numeric_cols). backfill חד-פעמי backfill_netpnl.py (FILL-ONLY + header-add + grid-resize): 832 תאים נכתבו חי 04/05/06 (04→126 עמ', 05/06→109), idempotent אומת, גיבוי ב-backups/. integration שקילות-phase6 על fixture (aggregate +1.21% לא-נתון לשחזור — דאטה gitignored/אבוד). לקח: grid-resize נדרש לפני הוספת עמודות חי — values_batch_update לא מרחיב grid אוטומטית (נחשף בריצה חיה 400, תוקן 2a335bd). TDD: net_pnl 10 + stats 7 + classify 7 + equiv 3 + backfill 14 = 41 חדשים; formulas 107/107. אפס שינוי ENTER/SKIP/sizing. DRY_RUN, Sentinel=shadow.
v3.03 (2026-06-11): TASK-134 — 16 כשלי-unit קיימים-מראש (baseline) נסגרו, סוויטה ירוקה 221/0. כולם stale-tests מ-evolution מתועד (אפס שינוי קוד-פרודקשן, test-only): C(EOD window×2 — is_eod_window מגודר ב-AGENT_FORCE_EOD_CLOSE) · E(position_manager×3 — מקור-מחיר get_latest_bar/quote, commit 3f4f95d, לא get_current_price) · D(order_manager×2 — paper_portfolio 22→25 עמ', commit 1c26a00, Status idx 14→16) · A(decision_id×4→3 — פורמט timestamp DEC-DATE-TICKER-HHMMSS-ff, Bug#3 commit 6bc930c; max_counter הוסר=קוד-מת) · B(decision_logic×1 — assert FIELD_MAPPING⊆Decision invariant במקום מספר-קסם; Decision=44/FIELD_MAPPING=42) · F(postmortem×4 — בודקים _generate_lessons ישירות; AutoLessons עבר לפרוזה עברית commit cce6c12). הרצה: uv run --with-requirements requirements.txt pytest. סוגיה סמויה שאותרה (TASK נפרד, לא טופלה): postmortem _generate_lessons מחושב-ונזרק (line 104) + PK §3240 טוען '7 rules' בעוד AutoLessons=פרוזה. DRY_RUN, Sentinel=shadow, אפס שינוי לוגיקת מסחר.
v3.02 (2026-06-11): TASK-139 שכבה 1 [BORROW] — borrow_data מקבל writer פעיל (היה ללא writer). חדש: agent/perception/borrow_collector.py — snapshot יומי של shortability מ-Alpaca, 9 עמודות, IsHTB=shortable&&!ETB, BorrowFeePct=NULL מפורש (Alpaca paper לא חושף fee — שכבה 2 עתידית ממקור אמיתי), batched non-fatal כמו flush_skip_summary (12/12 unit). חיווט: orchestrator_eod.collect_borrow_snapshot אחרי Reconciler — broker ייעודי AlpacaBroker(dry_run=False) read-only (רק get_asset_info, אפס מסחר — נאכף בטסט), טיקרים מ-build_account_state().existing_positions (SSoT open∪ENTERs), best-effort לא מגדיל errors (7/7 wiring + 19/19 no-regression). דורש ALPACA_API_KEY_ID/SECRET ב-Secrets בריצה אמיתית, אחרת init נכשל בשקט (non-fatal, טאב ריק). אפס שינוי ENTER/SKIP/sizing. DRY_RUN, Sentinel=shadow.
v3.01 (2026-06-10, נספח-סגירה): task-156 — אג'נדת פתיחת-יום 11/6 (הוראת עמיחי בסגירה, לא בוצע): 5 משימות-חזון (HYPOTHESES.md, lineage sentinel, SCHEMA.json, delisting detector, Wilson CI) + עריכות-איחוד 26→147/27→153/129→151/15+42+70. WORK_PLAN עודכן. docs-only.
v3.00 (2026-06-10, סגירת ערב): TASK-139-INV deep investigation accepted; 6 decisions resolved: Option B shadow gate, D1_Open official WR, dual WHIPSAW reporting, repo stays public w/o research CSVs, 139 renamed [BORROW], 48 closed Done; work plan at docs/WORK_PLAN_2026-06-11.md. דוח מלא: docs/research/INVESTIGATION_2026-06-10/REPORT.md (41 ממצאים; 2 P0: Score kill-criterion met r=-0.02 p=.82 n=123, look-ahead +13.6pp → WR ריאלי ~53-55%; 5 P1: edge שלילי תחת borrow ריאלי/WHIPSAW-פסימי, tradability ממוקה, TDIC split ב-n=123, DropsLab collector מת מאז 5/6, run_eod peak-entry). נפתחו task-139..155 (17); נסגרו 43/48/124/125/130/146; TASK-131 הורד LOW. תיקון ספירה: "150 כולל WHIPSAW" → 149. Sentinel=shadow, DRY_RUN, אפס שינוי לוגיקת מסחר.
v2.99 (2026-06-10, סגירת יום): נכתב SESSION_HANDOFF_2026-06-10. בלוק-דחיפה גדול: 6 commits (307c0e5→4d30a7a, PK 2.95→2.98) — TASK-130 (trading_days_after holiday-aware), TASK-43 (page-visit logger), TASK-124 (backfill v1→v2 cross-month + תיקון נתיב-runner), TASK-125 (skip_summary aggregation, טאב 14, drift §A6 8→14), פתיחת TASK-135..138, follow-up skip_summary ל-2026-07. התממש: backfill --apply שחרר 40 שורות-חג (Good Friday, אלכסון 27/3-2/4); n מוכרע 93→123 (WIN 81+LOSS 42; 150 כולל WHIPSAW). אירוע בוקר: CI מושבת 16ש' ע"י שם-קובץ task-122 347B>255B, תוקן 307c0e5. פתוח: TASK-126 חילוץ SKIP רץ ברקע (446 mismatches ~8% לחקור), 4 תקועים (SBLX delisted+3 יוני). מחר: ולידציית 2.1 על n=123. Sentinel=shadow, DRY_RUN, אפס שינוי לוגיקת מסחר.
v2.98 (2026-06-10): TASK-125 — החזרת נראות SKIP מצטברת (Route B follow-up). טאב agent חדש skip_summary (14-י, 7 עמודות: Timestamp|RunID|SkipReason|Count|Tickers cap-25|ScoreMin|ScoreMax, נוצר חי ל-2026-06: 11Mu1uPBb5izl2eGP4RTsr89wwNqXcmHc62J2fphlo4M): DecisionLogger צובר SKIPs בזיכרון בתוך ריצת-דקה (stateless בין ריצות) ו-flush_skip_summary() כותב batch יחיד (safe_append_rows) בסוף הריצה — שורה פר-סיבה, מקס +1 כתיבה/דקה (≤480/יום) מול סופת ה-80-100 שהולידה את Route B; dedup על RunID; כשל flush לא-פאטלי ולא נספר כ-error (מניעת סופת מיילים); חסימות Sentinel מוחרגות (sentinel_events); שורות stdout [SKIP] ללא שינוי (audit + TASK-126). חיווט: orchestrator קריאה יחידה אחרי לולאת הסיגנלים. תוקן drift ‏§A6: ‏8→14 טאבים (היו 13 בקוד, PK פיגר) + decision_log ‏41→42 עמודות. TDD: ‏15 unit tests חדשים (decision_logger ‏11, schema ‏2, orchestrator ‏2), אפס רגרסיות (16 כשלי-unit קיימים-מראש זהים ל-baseline → TASK-134). פער ידוע: ‏2026-07 נוצר-מראש עם 13 טאבים — דרושה ריצת create_agent_sheets --month 2026-07 להשלמת skip_summary לפני הרוטציה. הקשר היום: תוקן CI מושבת ~16ש' — שם-קובץ task-122 ‏333B>255B שבר checkout בכל ה-workflows (307c0e5). Sentinel=shadow, DRY_RUN, אפס שינוי לוגיקת ENTER/SKIP — נראות בלבד.
v2.97 (2026-06-10): TASK-124 — תיקון דליפת cross-month ב-post_analysis backfill. ה-workflow step עבר מ-backfill_ohlc.py (v1, D1-only, active-month-only) ל-backfill_ohlc_v2.py --recent 2 --apply (any-missing-D-day, current+1 previous month). recent_months() helper חדש (relativedelta, חיתוך עם sheets_config). תוקן באג נתיב קשיח: ~/RidingHighPro literal → REPO_DIR יחסי (היה קורס על GitHub runner). v1 נשאר בריפו (§12 versioned). TDD: test_recent_months_v1 (4 טסטים כולל גבול-שנה). אפס שינוי לוגיקת מסחר.
v2.96 (2026-06-10): TASK-130 — `trading_days_after` (sheets_manager) הפך holiday-aware: התנאי `weekday()<5` הוחלף בקריאה ל-`utils.is_trading_day` (לוח NASDAQ דרך pandas-market-calendars, ‏fallback ל-weekday כש-mcal חסר; import מקומי בתוך הפונקציה — אפס מעגל-import, אומת דו-כיווני). שורש 13 שורות-החג התקועות (Good-Friday 3/4, ‏Memorial-Day 25/5; הבא: Independence נצפה 3/7) — אחרי backfill תלת-חודשי הסלוטים יחושבו נכון. TDD: ‏tests/test_trading_days_holiday_v1.py — ‏5 טסטים (3 חגים מוזרקים, סופ"ש נשמר, fallback), ‏RED→GREEN; ‏107/107 formulas ללא רגרסיה. ‏orchestrator.is_market_hours ה-weekday-only הכפול — בכוונה לא טופל כאן → TASK-135 (דדליין 3/7). אפס שינוי לוגיקת מסחר.
v2.95 (2026-06-09, סגירת יום): נכתב SESSION_HANDOFF_2026-06-09. סיכום היום: (1) ביקורת-עומק read-only → SYSTEM_REVIEW_2026-06-09_v1 + תוכנית RIDINGHIGH_FIX_PLAN (TASK-123…129); (2) מחקר DropsLab 3-כיווני — ורדיקט: אין edge כיווני ברזולוציה יומית (שורט לא-מסונן breakeven; לונג bottom-reversal מפסיד בכניסת D1; המדדים מפרידים תנודתיות-לא-כיוון, rsi_14 הטוב-ביותר AUC .571 בלבד; delay שוחק); (3) TASK-123 Done — backfill_v2 שחרר 56 שורות (stale-PENDING 70→14, מחקר מוכרע 49→93); (4) root-causes: Route B b1a4e4f (SKIP→stdout, 11/5) + trading_days_after weekday-only (→TASK-130, דדליין 4/7). נפתחו TASK-131 (הכרעת מדיניות LONG — דורש עדכון §4 Non-Goals אם תאושר; הראיות מ-9/6 שליליות ברזולוציה יומית) ו-TASK-132 (סימון 14 התקועות). TASK-125 (החזרת SKIP מצטבר) = המשימה הראשונה מחר. Sentinel=shadow, DRY_RUN, אפס שינוי לוגיקת מסחר.
v2.94 (2026-06-09): TASK-123 Done — `backfill_ohlc_v2.py` חדש (v1 נשאר שלם — §12 versioned filenames): טריגר any-missing-D-day (לא רק D1_Open) + scope חוצה-חודשים (`--months`), **fill-only** דרך `ws.batch_update` תאי (אפס שכתוב-גיליון, אפס clobber/race), `--dry-run` ברירת-מחדל, stats מחושבים-מחדש רק לשורות שמולאו. הוחל חי על אפריל (1,156 תאים) + מאי (516): **stale-PENDING 70→14, שוחררו 56 שורות; דאטת מחקר v2 מוכרעת 49→93** (WIN 59/LOSS 34, +17 WHIPSAW). אומת read-back עצמאי: fill-only החזיק (spot-checks KEPT/FILLED), stats תואמים recompute, ספירות שורות 154/78 ללא שינוי, אפס 429. ה-14 הנותרות: SBLX delisted + 13 שורות-חג — **תגלית root-cause חדשה**: `sheets_manager.trading_days_after` (646) הוא weekday-only בלי לוח חגים → סלוטים של Good-Friday 3/4 ו-Memorial-Day 25/5 לא יתמלאו לעולם (אלכסון 27/3-2/4 + 18/5-22/5); דורש TASK נפרד. אפס שינוי לוגיקת מסחר; Sentinel=shadow, DRY_RUN.
v2.93 (2026-06-09): ביקורת-עומק read-only + תוכנית תיקון. נוספו `docs/SYSTEM_REVIEW_2026-06-09_v1.md` (אינוונטר מדדים/נוסחאות עם VALID/SUSPECT/BROKEN, drift PK-מול-קוד, ולידציה על דאטה חיה: Score↔WIN r≈0, היפוך שכבות 80-90, D1_Gap המנבא החזק, Price המובהק היחיד בטריידים אמיתיים) ו-`docs/RIDINGHIGH_FIX_PLAN_2026-06-09.md` (4 קבוצות לפי תלות: שחרור-דאטה → ניתוק Score → gate חדש ב-shadow → ניקוי drift; run-mode לכל פריט לפי RUN_MODE_DECISION). ממצאי-שורש מאומתים: SKIP logging הופסק בכוונה ב-`b1a4e4f` 11/5 (Route B, quota) — counterfactual מת מאז; 70 שורות post_analysis stale-PENDING (51 בלתי-נראות ל-backfill_ohlc — טריגר D1-בלבד + scope חודש-פעיל); SCORE_RSI_PARAMS קוד-מת (מדרגות 80/85/90 hardcoded). נפתחו TASK-123…129 (backfill_v2, דליפת cross-month, החזרת SKIP מצטבר, חילוץ לוגי Actions עד ~10/8, הכרעת Filter-1, gate-shadow, ניקוי RSI). docs-only — אפס שינוי קוד/לוגיקת מסחר. Sentinel=shadow, DRY_RUN.
v2.92 (2026-06-09): TASK-119 Done — שדה `Generated` (קפוא 2026-06-04, ידני — אף סקריפט לא מעדכן אותו: grep `Document version`/`Generated` ב-*.py ריק) שונה ל-**`Last updated`** = 2026-06-09, עם הערה שהוא מתעדכן בכל bump גרסה (חלק מ-Anti-Drift / SESSION_PROTOCOL §3 — version + changelog + Last updated יחד). docs-only, אפס שינוי לוגיקה/מסחר.
v2.91 (2026-06-09): TASK-107 Done — closed-same-day false-positive ב-`position_sync` תוקן. נוסף ענף **ALLOW / POSITION_SYNC_CLOSED_SAME_DAY** בבלוק ה-BLOCK: כש-`today_enters>0`, `open=0`, הפורטפוליו קריא (recognized>0), ו-`closed_today_count>=today_enters` → ALLOW (הפוזיציות נפתחו ונסגרו באותו יום — 0 פתוחות זה נכון, לא drift). `build_account_state` חושף `closed_today_count` — row-count (כולל כפילויות, מקביל ל-today_enters) של שורות `ExitDate==today` + status CLOSED/DRY_RUN_CLOSED. חיסון TASK-106 (unreadable→WARN) נשמר; partial-cover (`closed<enters`) עדיין BLOCK; backward-compat (signal נעדר→0→BLOCK) נשמר. סוגר את ה-FP שגרם ל-283 HALT ב-6/3 אחה"צ. שינוי בלוגיקת ה-gating של position_sync (מתי HALT) — **לא** בניקוד/sizing/לוגיקת-מסחר; רלוונטי בעיקר ל-active mode (כיום Sentinel=shadow, DRY_RUN). אימות TDD: position_sync **8/8→11/11**, account_state 5/5, py_compile OK.
v2.90 (2026-06-08, שמירת-ביניים): TASK-121 דור-שני התקדמות (ACs #1/#2/#3 ✅, נשאר 2c). **2a — TASK-114 attended auto-mode Done**: night/TASK-114 + Run Log → Agent #8 §3.3 כלל-5=✅ (לא 'לא-ודאי') → merged. **כלל-5 mechanism (Run Log)** הוכח חי end-to-end. **2b-prep — TASK-117 auto-mode אמיתי בפיקוח Done**: ה-classifier של `--permission-mode auto` **נצפה עובד** (n=1), Run Log `mode: auto-mode`, night/TASK-117 → Agent #8 verdict **Ready** → PR #15 squash-merged `ff9b1c9` (post-commit hook tracked + installer; תיקון TASK-116 עכשיו clone-survivable). הזרימה `night/*→Agent #8→§3.3→verdict→merge` הוכחה **3×** (120/114/117). נשאר 2c (לא-מפוקח: routine תפעולי + cron-בוקר Peru→UTC + ריצת-לילה עיוורת) = AC#4/#5, מאחורי gates (RULE #6, כלל-בטיחות #7). PROBE routine `trig_01CjcAM88vmBsrCT5Xw6BpdE` נשאר רדום (enabled:false, API ללא delete → מחיקה ידנית ב-UI). הערה: classifier נצפה n=1 בלבד — שווה לשקול עוד ריצת-auto בפיקוח לפני הלא-מפוקח. Sentinel=shadow, DRY_RUN, אפס שינוי לוגיקת מסחר.

v2.89 (2026-06-08): TASK-121 דור-שני prep — Run Log section ב-NIGHT_RUN_TEMPLATE (G5) + חידוד כלל-5 ב-§3.2 (קריאת stop-counters, דטרמיניסטי) — סוגר את הפער מהמבחן (כלל-5 לא-ודאי→✅). שלב 1 (TASK-120) Done; שלב 2a (TASK-114 auto-mode בפיקוח) ממתין ל-GATE 2a. docs-only, אפס שינוי לוגיקת מסחר.

v2.88 (2026-06-08, סגירת יום): TASK-94 epic נסגר (Agent #8 Routine Checker) — 94.1 (§3 closed + §7.2 ל-5 נתיבים + PK 2.87, PR #11) · 94.2 (פרסונת rh-routine-checker read-only מקודד §3, PR #12) · 94.3 (runbook PR #13 + Phase 0 אימות subagent אחרי reload: Task(subagent_type: rh-routine-checker) נטען ללא not-found, פלט בפורמט §3.3, verdict Ready על branch-בדיקה זרוק + Phase 2 אימות-ענן end-to-end: RemoteTrigger create→run→clone→discover→report "אין עבודת-לילה N/A"; routine staged enabled:false / run_once_at 2027 / allowed_tools read-only+Task; ניקוי clear_mcp_connections + מחיקת branches זרוקים). routine trig_01CjcAM88vmBsrCT5Xw6BpdE נשאר רדום (API ללא delete, מחיקה מלאה רק ב-UI). גייט RULE #6 נשמר — כל create/run/update באישור פרטני. נפתחו TASK-118 (sandbox egress קבע/הסדרה) · 119 (PK Generated stale 06-04) · 120 (debris task-high.1/.2). נכתב SESSION_HANDOFF_2026-06-08. OPEN 45→48. Sentinel=shadow, DRY_RUN, אפס שינוי לוגיקת מסחר.

v2.87 (2026-06-08): TASK-94 brainstorming+plan — נסגר §3 של AGENT8_CAPABILITIES_MAP (היה 🔓 "פתוח לדיון"): §3.1 auto-safe paths (3 רשימות 🔴/🟢/🟡), §3.2 ניסוח 7 כללי-בטיחות (קשיח 1/2/6 git-based · best-effort 3/4/5 עם הסתייגות "לא-ודאי" · כלל-7 תזכורת), §3.3 פורמט דו"ח-בוקר. הוכרע v1=ליבה-בלבד (מסלול ב — פרסונת-סוקר ב-prompt); /code-review+/simplify נדחו ל-v2 (בלוקר headless ירד מהנתיב הקריטי). הרחבת נתיבים-אסורים מ-3 ל-5 (+`.github/workflows/*` +`orchestrator.py`) נכתבה ל-RUN_MODE_DECISION §7.2 + NIGHT_RUN_TEMPLATE (Anti-Drift). פיצול TASK-94→94.1/94.2/94.3. docs-only, אפס שינוי לוגיקת מסחר. Sentinel=shadow, DRY_RUN.

v2.86 (2026-06-07): TASK-95: נכתב docs/AGENT8_CAPABILITIES_MAP.md — מיפוי 9 מקורות (3-מצבים×2-סביבות), נרטיב 7/9 תוקן ל-4/3/2 לפי אימות-חי; #8 ליבה=בדיקות system-aware (בטיחות/drift/התבקש-מול-בוצע) first-class + backbone=RemoteTrigger; תפקיד read-only verdict.

v2.85 (2026-06-07, סגירת יום): נכתב SESSION_HANDOFF_2026-06-07. יום ניקוי-backlog: נסגרו 6 משימות — TASK-93 (Cloud Routine end-to-end ⭐, ראה 2.84), TASK-41 (WONTFIX, filter order=attribution-only), TASK-116 (post-commit hook python3→uv run python3, מקומי-בלבד, אומת חי), TASK-115 (WONTFIX/PARTIAL — research/ כבר נקי), TASK-76+79 (תובנות מחקר מתועדות, פעולה→TASK-69/71). נפתחה TASK-117 (post-commit hook עמיד, LOW). OPEN 52→47. אפס שינוי לוגיקת מסחר. Sentinel=shadow, DRY_RUN. המשימה הבאה: TASK-95 (Agent #8 capabilities map).

v2.84 (2026-06-07): TASK-93 Done — Cloud Routine push+PR אומת end-to-end. שורש הכשל מהפיילוט נחשף חי (RemoteTrigger list): config הפיילוט היה ללא שדה `sources` (→ "400 missing source") + ללא GitHub auth (→ push נכשל). תוקן: (1) זהות GitHub חוברה מחדש לחשבון הנכון (Ambroseius→projects5069-creator) + Claude GitHub App; (2) `session_context.sources` נרשם ב-routine config. מנגנון מתועד: `create(sources + run_once_at רחוק)→action:run PROBE→action:update(FULL prompt)→action:run FULL`. אומת: PROBE דחף branch claude/task-93-conn-test ללא 403; FULL פתח PR #10. ניקוי: PR #10 נסגר + branch נמחק + routine trig_01JkqdLBASKN43QsmmNTffmE → enabled:false (מחיקה מלאה רק ב-UI). הערות: `action:run` לא מתעד last_fired_at/session-URL ב-API (נצפה ב-UI/gh); `/fire`+token (Part B2) נשמר כשכבת foundation עתידית ל-Agent #8, לא-מתועד-בסקיל. תיעוד מלא: docs/TASK-93_ROUTINE_RUNBOOK.md. אפס שינוי לוגיקת מסחר. Sentinel=shadow, DRY_RUN.

v2.83 (2026-06-05): TASK-50 Done — ניקוי 138 .bak מקומיים (6.0MB, gitignored, אפס מחיקות עקובות); ארכיון הפיך rh_bak_archive_20260605-160601.tgz. treadmill: מחיקה ידנית חוזרת (TASK-30 Done אך .bak נוצרו מחדש) -> TASK-114 (prune אוטומטי). CLAUDE.md: כלל CLIPBOARD לראש הקובץ. נפתח TASK שיורי. TASK-93 לא נגענו. פתוחות 51, Sentinel=shadow, DRY_RUN.

v2.82 (2026-06-04, סגירת יום): נכתב SESSION_HANDOFF_2026-06-04. יום: 17 commits, 7 משימות Done (110/45/47/104/111/112/59), RULE #11 v3.3 (scan-and-declare hook), PK 2.74->2.81, 58MB pkl מקומי שוחרר, אפס שינוי לוגיקת מסחר. נפתח TASK-113. תועדו/נדחו 58/101/96/41/46/54. main 429b793, פתוחות 50, Sentinel=shadow, DRY_RUN.
v2.81 (2026-06-04, session open): TASK-112 Done — auto_scanner per-run read-counter added + verified LIVE (run 26969287592 17:47Z post-deploy; absent 17:45 pre-deploy). Live finding: timeline_live=1 cache-miss/run, total=1 — caching effective, Phase 2 S1 goal (minimal API pressure) achieved. CAVEAT: the counter measures cache-MISSES, not raw reads — so the v2.70 timeline_live 4->2 (raw reads) is NOT literally observable through it. Opened TASK-113 (LOW) for literal raw-read verification if ever needed.
v2.80 (2026-06-04, session open): consolidation of hook mirrors (Option A). 3 copies -> one canonical mirror scripts/claude_hooks/ (full bundle: skill_enforcement_hook.sh v3.3 + pretooluse_skill_gate.sh + RECOVERY.md). (1) claude_hooks/skill_enforcement_hook synced to v3.3; (2) removed redundant loose scripts/skill_enforcement_hook.sh; (3) PK section-316 unified with section-291 (two conflicting canonical claims -> one); (4) install.sh now deploys scripts/claude_hooks/* -> ~/.claude/hooks/. LIVE (~/.claude/hooks) unchanged.
v2.79 (2026-06-04, session open): TASK-104 Done — system_events multi-writer audit: BENIGN, לא הפרת §910. 2 כותבים append-only (dashboard log_emergency_stop=append_row ידני/נדיר + orchestrator_eod._system_events_alert=safe_append_row EOD); orchestrator.py READ-ONLY על הtab — תיקון ל-premise השגוי שטען pipeline-writes. append טהור אטומי אצל Google (אין RMW clobber) → מקבילות benign. §910 הורחב עם החריג המותר.
v2.78 (2026-06-04): TASK-58 Phase 1+2 אומתו חי (לא סוגר TASK-58 — ה-AC הוא SA-נפרד, לא בוצע). ריצת agent_minute 16:23 UTC (run 26964724118): "Sheets API reads this run: total=4" — system_events 1, timeline_live 1, paper_portfolio 1, decision_log 1. כל tab נקרא פעם אחת בלבד → אין קריאות כפולות, ה-consolidation של build_account_state אומת. הערה: המונה per-tab ולא per-function — אין ייחוס מספרי מדויק ל-build_account_state. timeline_live 4→2 הוא יעד ה-scanner (auto_scanner), לא ה-agent; אימותו דורש לוג scanner נפרד. total=4 רחוק מ-quota 60/min. החלטת S2 (portfolio 2->1): נדחה — אין לחץ quota בריצה בודדת. שאלה פתוחה ל-58: האם peak המשולב (agent+scanner+health_audit באותה דקה) עדיין נוגע ב-60? אם לא — SA-נפרד אולי מיותר. לא נמדד peak משולב עדיין.
v2.77 (2026-06-04, session open): TASK-47 Done — portfolio sheet audit: ALIVE, לא deprecated. auto_scanner WRITES (468 add Score>=sף, 1319 peak) + READS source-of-truth (604 Status=Open 7d, 1139 D1/D2/D3); dashboard DISPLAYS (2819 Score Tracker, 1019 fallback); monthly_rotation מסובב (57/85). §14 שורה 1301 הורחבה לכלול PURPOSE כדי שסשן עתידי לא יחזור על טעות task-44 (שחשב שהוא dead כי ראה רק את צד ה-dashboard). הנחת ה-deprecation המקורית הופרכה.
v2.76 (2026-06-04, session open): RULE #11 v3.3 — skill-scan hardening (TASK-54 progress, NOT closed). ה-PreToolUse hook (skill_enforcement_hook.sh) עודכן: scan-line חובה לפני כל משימה ("נטענו X כי / נשקלו ונדחו Y") + TASK-TYPE MAPPING שמכווין הסקיל הייעודי הנכון (ניתוח->data-quality-checker, backtest->backtest-expert, position->position-sizer, postmortem->signal-postmortem, thesis->trader-memory-core, bug->systematic-debugging). סונכרן live(~/.claude/hooks)->canonical(scripts/skill_enforcement_hook.sh). הערה: זו הכוונה+שקיפות, עדיין fail-open — ה-KNOWN HOLE (any SKILL.md satisfies) לא נסגר טכנית, TASK-54 נשאר To Do עד אכיפה דטרמיניסטית.
v2.75 (2026-06-04, session open): TASK-45 ניקוי קבצים + TASK-110 wrapper. נמחקו 5 קבצים tracked (mark_score_version.py, upload_enriched_pa_v3.py, OPEN_ISSUES_archive.md, NEXT_SESSION.md, README_health_audit.md; ~975 שורות) + תוקנו 2 dangling refs (OPEN_ISSUES.md, dashboard.py:3789). נשמרו 2 שתויגו שגוי one-shot: apply_text_format_v1.py (חי ב-prepare_next_month.yml:42) + setup_health_audit_sheet.py (recovery path ב-health_audit.py). בנוסף שוחררו 58MB מקומיים: 2 .pkl snapshots (df_rh_2026-04/05_timeline_live, gitignored, לא ב-git) ב-research/2026-05-05_phase1_day1. TASK-110: .rh-run.sh עודכן ל-pbcopy byte-exact + self-verify. docs+hygiene, אפס שינוי לוגיקה.

v2.74 (2026-06-03, branch docs/task78-dropslab-status): TASK-78 — תיקון תיעוד DropsLab.
אומת חי (BY NAME, ID 1XM-qId7HAwEu-8-1GGHcy3RoyyAnsYshjZfDrKFnTMI): `drops_raw` 2851 +
`drops_post` 2156 שורות לא-ריקות — הגיליון **מלא**. טענת "ריק" (TASK-62, 30/5) הייתה
Sheet ID שגוי (homoglyph I/l), תוקן ב-TASK-77. עודכנו: A2 Status (PK) + Implementation
Notes של TASK-27 (החסימה "needs data accumulation" הוסרה — #N25 unblocked). המספר "2379"
בכותרת TASK-78 עצמו התיישן (הדאטה צמחה מאז 30/5) → נכתבו המספרים החיים. docs-only, אפס קוד.

v2.73 (2026-06-03, branch feat/task108-reconcile-auto-repair): מומש **TASK-108**
auto-repair (phase-2 של TASK-106) — **דגל כבוי כברירת מחדל** (RECONCILE_AUTO_REPAIR=False,
config.py). חיווט A: הרחבת Reconciler.reconcile_decision_log_vs_portfolio בפרמטר
auto_repair + today, + helpers _build_portfolio_row_from_decision / _append_portfolio_row
(+ portfolio_row_appender DI ל-__init__). שחזור 25 עמ' מ-decision_log ENTER.
**תיקון עובדתי ל-spec:** 3 ה-order-ids (Entry/TP/SL) **כולם** ריקים (log רץ לפני
execute → None בזמן הלוג; "derive from EntryOrderID" בלתי-אפשרי); EntryPrice←Price
(ExecutionPrice ריק בזמן לוג). Status←AgentMode (DRY_RUN→DRY_RUN_CLOSED, LIVE_PAPER→CLOSED);
ExitReason="RECONCILER_BACKFILL"; ExitDate/ExitTime=EntryDate/EntryTime (החלטה a, עקביות
מול orchestrator:249/trade_history/_compute_duration); ריק רק ב-ExitPrice/RealizedPnL/Pct;
DataQuality="BACKFILL". Idempotency דרך **זיהוי-מחדש** (pf טרי כל EOD), לא דרך ה-dedup
העיוור-ב-append-ראשון. GATE: הדגל נשאר כבוי עד שה-flag-only יוכיח דיוק (0 ימי הוכחה).
טסט test_reconciler_autorepair_v1.py 5/5; test_formulas 107/107; sentinel_selftest 16/16.

v2.72 (2026-06-03, רענון סגירה): נפתח **TASK-108** (MEDIUM) — reconciliation
auto-repair (phase-2 של TASK-106): שחזור שורת paper_portfolio חסרה מ-decision_log
(כמעט-lossless; רק TPOrderID/SLOrderID חסרים), GATE: רק אחרי שה-flag-only הוכח מדויק
(auto-repair כותב ל-sheet). מעדכן את הרעיון "auto-repair phase-2" מ-handoff להיות
TASK מעקב רשמי. משימות פתוחות מרכזיות: TASK-107 (closed-same-day FP), TASK-108
(auto-repair), TASK-58 S2 (אופציונלי). SESSION_HANDOFF_2026-06-03 עודכן.

v2.71 (2026-06-03, סגירת יום): סשן תיקון-שורש שלם. 7 שינויים נחתו ב-main:
(1) יישור-עמודות paper_portfolio Option A (1c26a00); (2) חיסון position_sync PR#3;
(3) TASK-105 חשיפת כשל-כתיבה PR#4; (4) TASK-106 reconciliation flag-only PR#5;
(5) TASK-66 SENTINEL_MODE active→shadow (fb923df, אומת חי 14:28 HALTED→SHADOW_LOGGED);
(6) TASK-58 Phase 1 single-pp-read+read-counter+health-cron PR#6; (7) TASK-58 Phase 2 S1
timeline_live cache 4→2 PR#7. נסגרו TASK-105/106; נפתח TASK-107 (closed-same-day FP).
מצב: Sentinel=shadow, DRY_RUN, PK 2.63→2.71. SESSION_HANDOFF_2026-06-03. למחר: לקרוא
מונה-הקריאות בפתיחה (build_account_state ~7→~3, timeline_live 4→2) ולהכריע על TASK-58 S2.
פתוח: TASK-107, TASK-58 S2 (אופציונלי), auto-repair phase-2 (ללא TASK), TASK-66 follow-up
(active-vs-shadow אחרי n רב-רגיים).

v2.70 (2026-06-03, branch feat/task58-phase2-scanner-timeline-cache): TASK-58
Phase 2 S1 — ניתוב קריאות timeline_live ב-auto_scanner דרך ה-cache (60s). נוסף
helper `_read_timeline_live()` שקורא דרך get_sheet_values; נותבו 3 הקריאות הגולמיות
(run_scan tracked-tickers, _save_daily_summary, update_ticker_follow_up) שעקפו את
ה-cache. **קריטי:** נוסף `invalidate_cache("timeline_live")` מיד אחרי כתיבת סריקת-הדקה
ב-run_scan (מותנה ב-_hdr is not None — לא מאבטל אם הכתיבה דולגה) — בלעדיו הקריאות
שאחרי-הכתיבה היו קוראות דאטה ישן בלי שורות-הדקה. timeline_live **4→2 קריאות-API**
לריצה (אחת לפני-כתיבה, אחת אחרי שמשרתת גם daily_summary וגם follow_up; @744=cache-hit).
מחוץ ל-scope (לא נגעו): portfolio (S2), tabs בקריאה-יחידה (daily_snapshots/daily_summary/
portfolio_live/ticker_follow_up/live_trades), ו-timeline_live ב-run_eod (פעם ביום).
נוסף test_scanner_timeline_cache_v1.py (2 מקרים: נתיב 4→2+טריות-אחרי-invalidate+cache-hit;
guard שמוכיח stale ללא invalidate). אימות: scanner-cache 2/2 + account_state 5/5 +
position_sync 8/8 + write_surfaced 3/3 + reconciler 4/4 + test_formulas 107/107 +
sentinel_selftest 16/16 + py_compile. **שקילות-פלט-סריקה מלאה תאומת חי מחר** דרך מונה-
הקריאות מ-Phase 1 (timeline_live צריך לרדת 4→2 בשורת-הסיכום). branch+PR, בלי push ל-main,
בלי כתיבת-Sheets חיה.

v2.69 (2026-06-03, branch feat/task58-phase1-reduce-reads): TASK-58 Phase 1 —
הפחתת קריאות-Sheet בנתיב-הדקה (סיכון נמוך, מנוף גבוה). (R1) build_account_state
קורא כעת את paper_portfolio **פעם אחת** וגוזר ממנה הכל — open/existing,
pf_total_rows/pf_status_recognized_count, entries_today_pf, ו-exited_today (קודם
קריאה שנייה + ws_pf2); הוסרו 3 get_worksheet מיותרים ל-truthiness (get_sheet_records
מחזיר [] לגיליון חסר). decision_log גם בקריאה יחידה. semantics של
paper_portfolio_fetch_failed נשמרו (exception→True). ~7→~3 read-API לריצת agent.
(מונה) sheets_manager.record_read/get_read_counts/reset_read_counts — מונה per-tab
לקריאות-API (cache-miss בלבד), מאופס בתחילת run() ומדווח בשורת-סיכום אחת בסוף.
(R3-trivial) health_audit.yml — ריצת 12:00 פרו ('0 17') הוזזה ל-15:30 ('30 20',
אחרי סגירה) להוצאת burst משעות-המסחר. R2 (ניתוב reads של auto_scanner ל-cache) =
Phase 2 נפרד. אימות: account_state 5/5 (כולל read-once: pp נקרא 1×, exited_today
מקריאה יחידה) + position_sync 8/8 + write_surfaced 3/3 + reconciler 4/4 +
test_formulas 107/107 + sentinel_selftest 16/16 + py_compile. branch+PR, בלי push
ל-main, בלי כתיבת-Sheets חיה. אומדן peak reads/min ~22-27→~11-15 (לא מדוד; המונה
יאפשר מדידה חיה אחרי deploy).

v2.68 (2026-06-03): TASK-66 — SENTINEL_MODE active→shadow (config.py:320).
החלטת מדיניות (עמיחי): ה-counterfactual (TASK-62 ריצה 2ג) מצא שעסקאות שהיו נחסמות
ביום הכניסה הן דווקא המנצחות — would-block WR 64% מול 41% not-blocked, n=36/68
RELIABLE אך **רגיים יחיד**; 6188/7466 מהבלוקים scan_freshness (סקאן ישן=מהלך חד=
שורט טוב → staleness היא סיגנל חיובי, חסימה מסירה alpha). בנוסף, active עצר את כל
המסחר ב-2026-06-03 דרך false-positive של POSITION_SYNC (283 HALT): בבוקר יישור-עמודות
(תוקן v2.64/65), ואחה"צ closed-same-day (כל שורות paper_portfolio DRY_RUN_CLOSED →
open=0 → drift שקרי). shadow רושם SHADOW_LOGGED ומחזיר ALLOW (data_sentinel
check_system/check_signal) → המסחר לא נעצר, וה-counterfactual ממשיך להצטבר נקי.
ההגנה האמיתית של active (price_sanity/completeness — מיעוט) ניתנת להחזרה סלקטיבית
בעתיד כשיהיה n רב-רגיים מהימן. שינוי שורה יחידה; אין שינוי לוגיקה. נפתח TASK-107
(closed-same-day FP, חיסון position_sync שלב-ב). אימות: py_compile + test_formulas
107/107 + sentinel_selftest 16/16.

v2.67 (2026-06-03, branch feat/task106-reconcile-flag): TASK-106 — reconciliation
flag-only (Option 3, defense-in-depth מעל TASK-105). מתודה חדשה
Reconciler.reconcile_decision_log_vs_portfolio() מזהה ENTER ב-decision_log של היום
ללא שורת paper_portfolio תואמת (מפתח PositionID==DecisionID, התאמה לכל הסטטוסים כדי
שלא ליצור false-positive על same-day-close). עובד ב-DRY_RUN (לא תלוי Alpaca). על פער:
alert ל-system_events (EventType RECONCILE_MISSING_PORTFOLIO_ROW) + ספירה ב-EOD summary
(reconcile_missing_portfolio) + log. **flag-only — בלי auto-repair** (phase-2 נפרד).
Reconciler.__init__ קיבל decision_log_reader + portfolio_all_reader (זריקה לבדיקות).
חובר למעבר ה-EOD ב-orchestrator_eod (alert_writer→system_events best-effort). ה-scaffold
הקודם השווה רק Sheet↔Alpaca ודילג DRY_RUN → היה עיוור לתבנית XOS; זה משלים את החסר.
נוסף test_reconciler_missing_portfolio_v1.py (4 מקרים: פער / same-day-close / OPEN תקין /
re-entry). אימות: 4/4 + write_surfaced 3/3 + position_sync 8/8 + account_state 3/3 +
test_formulas 107/107 + sentinel_selftest 16/16 + py_compile. branch+PR, בלי push ל-main,
בלי כתיבת-Sheets חיה. out-of-scope: repair (phase-2).

v2.66 (2026-06-03, branch feat/task105-surface-write-failure): TASK-105 —
חשיפת כשל כתיבת paper_portfolio (Option 2 + נגיעת Option 1). שורש (אבחון מאומת
מלוג ריצת 13:47 UTC): XOS @ 08:48 קיבל order סימולטיבי, ואז "paper_portfolio write
failed: 429" נבלע ב-order_manager._default_sheet_write (except→logger.error בלי
re-raise) → decision_log ENTER עמיד אך 0 שורות ב-paper_portfolio (אסימטריה מבנית).
התיקון: (1) _write_to_portfolio/_default_sheet_write מחזירים bool; execute() מציב
decision.portfolio_written; (2) Decision.portfolio_written חדש (default True);
(3) orchestrator._record_entry_outcome — כשל כתיבה → summary["errors"]++ + WARNING
(כמו decision_log), ו-ENTER לא נספר כהצלחה על כתיבה כושלת; (4) light Option 1:
_APPEND_RETRY_MAX=4 ב-safe_append_row בלבד (reads נשארים _RETRY_MAX=3; dedup-by-PositionID
כבר קיים → retry בטוח מכפילות). נוסף test_order_manager_write_surfaced_v1.py (3 מקרים:
כשל נחשף / הצלחה / ספירת orchestrator). אימות: 3/3 + position_sync 8/8 + account_state
3/3 + test_formulas 107/107 + sentinel_selftest 16/16 + py_compile. branch+PR, בלי push
ל-main, בלי כתיבת-Sheets. out-of-scope: reconciliation (Option 3, defense-in-depth).

v2.65 (2026-06-03, branch feat/possync-dataquality-warn): חיסון position_sync —
הבחנת data-quality מ-drift אמיתי (מועמד auto-mode/goal ראשון, פיקוח ריצה ראשונה).
הענף today_enters>0 ∧ open_count==0 פוצל: אם paper_portfolio בלתי-קריא
(pf_total_rows>0 ∧ pf_status_recognized_count==0 — כמו כשל היישור של v2.64) →
WARN POSITION_SYNC_DATA_QUALITY (לא חוסם); אחרת (קריא+0 פתוחות, או 0 שורות/תרחיש XOS)
→ נשאר BLOCK POSITION_SYNC_FAILED. build_account_state מוסיף 2 סיגנלים — pf_total_rows
+ pf_status_recognized_count — בלולאת ה-records הקיימת, אפס קריאת Sheets נוספת.
תאימות-אחורה: שדות חסרים → BLOCK כבעבר. נוסף test_position_sync_v1.py (8 מקרים).
אימות: 8/8 + test_formulas 107/107 + sentinel_selftest 16/16 + py_compile. branch+PR,
בלי push ל-main, בלי נגיעה בגיליון. out-of-scope: אנומליית XOS, closed-same-day→ALLOW.

v2.64 (2026-06-03): תיקון יישור-עמודות paper_portfolio (Option A) — שורש ה-HALT החי.
ה-writer (order_manager._write_to_portfolio) כותב 25 ערכים כולל TPPrice/SLPrice (idx 11-12),
אך create_agent_sheets.py הגדיר header של 23 עמודות בלי TP/SL → היסט +2: Status נחת בעמודת
ExitDate ועמודת Status נקראה ריקה בכל שורה → build_account_state ספר open_position_count=0 →
Data Sentinel (active) חסם POSITION_SYNC_FAILED/HALT בכל ריצה (203 אירועים היום עד 12:20).
תוקנו 3 מקומות בסנכרון: (1) שורת ה-header בגיליון paper_portfolio (יוני) יושרה ל-25 שמות —
0 תאי-דאטה זזו (הדאטה כבר היה בסדר ה-writer; אומת TP=EP×0.9, SL=EP×1.1 בכל 8 השורות);
(2) create_agent_sheets.py 23→25 (TPPrice,SLPrice אחרי SLOrderID) — מונע חזרת הבאג בגלגול
חודשי; (3) apply_text_format_v1.py: ExitTime idx 17→19 + SHEET_COLUMN_COUNTS 23→25. כל
הקוראים האחרים לפי-שם → תוקנו אוטומטית. אימות: open_count 0→8, Status=DRY_RUN_OPEN,
test_formulas 107/107, אפס POSITION_SYNC_FAILED אחרי התיקון (אחרון 12:20:55; 12:25:54=DEFERRED).
גיבוי: research/backups/paper_portfolio_backup_2026-06-03.csv + .bak_20260603-122358 לשני קבצי
הקוד. פתוח (לא נפתר כאן): אנומליית XOS — ENTER ב-decision_log ללא שורת paper_portfolio (כתיבת
entry נבלעת ב-order_manager:270, כנראה 429; אסימטריה מבנית). PK כבר תיעד 25 עמ' (§schema) —
המקור-אמת היה נכון, ה-create_agent_sheets פיגר.

v2.63 (2026-06-02, סגירת ערב): סשן תשתית night-run + מפת-דאטה. TASK-103 נסגר —
docs/RUN_MODE_DECISION.md (עץ-החלטה ping-pong/auto-mode/goal) + hook ב-SESSION_PROTOCOL §2.
TASK-102 נסגר — docs/NIGHT_RUN_TEMPLATE.md (מבנה פרומפט-לילה; /goal+auto-mode אומתו חי
מול CLI v2.1.156 — /goal קיים בבינארי + --permission-mode auto ב-choices; תנאי-סיום מדידים;
7 כללי בטיחות) + קישור דו-כיווני עם RUN_MODE_DECISION. TASK-64 נסגר — docs/DATA_SOURCES_MAP.md
(מפת כל 22 הגיליונות + קבצים מקומיים: writer/reader/מטרה). כל writer אומת מול הקוד בפועל;
האימות מנע 7 קביעות שגויות שכמעט נכנסו למקור-אמת: The Critic סווג שגוי ככותב decision_log/
paper_portfolio/market_context/sentinel_events/news_findings — בפועל הוא קורא אותם, וכותב רק
ל-4 (postmortems/agent_scorecard/weekly_summary/monthly_summary); score_analytics writer=
agent/analytics/score_analytics.py (גם יוצר שורות SUG); post_analysis writer=gsheets_sync.py;
config_history/borrow_data אומתו ללא writer פעיל. TASK-86 נסגר — ניקוי repo: 4 קבצי .bak
untracked נמחקו, .gitignore הורחב (research/ + *.bak[0-9]*), TASK-62 report נכנס לריפו;
נמנע באג בסקריפט (grep '\.bak' tracked היה מוחק את task-12/task-30 הלגיטימיים — צומצם
ל-untracked בלבד; אין .bak tracked אמיתי). נפתח TASK-104 — system_events נכתב ע"י
orchestrator + dashboard emergency_stop (בדיקה מול §910 no-concurrent-writers). Backlog
51 open. git מסונכרן (4 commits נדחפו). SESSION_HANDOFF_2026-06-02_evening (thread נפרד
מ-handoff הבוקר של מייל/רוטציה). לקח: recon אוטומטי מבוסס grep/docstring מייצר מפה שגויה —
משימות-מיפוי דורשות אימות-קוד מאומת, לא heuristic (חיזוק לכלל ב-NIGHT_RUN_TEMPLATE: 64 לא
היה מועמד אוטונומי טוב, 86 כן). NIGHT_RUN_TEMPLATE+RUN_MODE_DECISION = תשתית ריצת-לילה מוכנה.

v2.62 (2026-06-02): TASK-91 scope-1+4 — atomic-rotation guard at source.
create_agent_sheets.main() now verifies all 13 AGENT_SHEET_NAMES landed in
sheets_config after creation and sys.exit(1) if any missing, so a partial run
aborts the job and prepare_next_month never commits an incomplete config (the
silent gap that left June with only 9 sheets on 1/5, surfacing as the 1/6 Critic
KeyError). Also fixed stale "11 agent sheets" strings -> 13 (scope-4). Combined
with scope-2 (health_audit check_28) this is defense-in-depth: loud at the source
+ detected within hours by the audit. scope-3 (_ensure_month self-heal for agent
sheets) closed as WON'T-DO — high risk (sheets_manager->agent.setup coupling,
needs OAuth) and low marginal value given the guard already covers it. TASK-91
DONE (scope 1/2/4). TASK-98 (Bug A) was merged here. Verify logic unit-tested
(incomplete->exit1, complete->proceed); no false-positive (config persisted to
disk via _save_config before the check). 107/107.

v2.61 (2026-06-02): TASK-99 — build_weekly_row missed cross-month
exits. A Mon-Fri week straddling a month boundary (e.g. Mon 29/6 -> Fri 3/7)
read only the run-day month's paper_portfolio, dropping the earlier month's
exits. Fix: read every month the Mon-Fri window touches (<=2), merge + dedup by
position_id. Same-month weeks read one sheet (unchanged, no extra API). Builds on
the month= param added in TASK-60. Proven: cross-month week reads both June+July
sheets and captures both trades; same-month reads one. Note: this also incidentally
catches TASK-100's cross-month-exit edge WHEN it falls inside a straddling week,
but TASK-100 (general cross-month position ownership) stays open. Unit-tested + 107/107.

v2.60 (2026-06-02): TASK-91 scope-2 — added health_audit check_28
(AS1, agent_sheets_complete). Guards the Bug-A silent-rotation gap: the active
(current-Peru) month must have all 13 AGENT_SHEET_NAMES present in
sheets_config.json, else CRITICAL -> existing email alert. Config-only (no Sheets
I/O), imports AGENT_SHEET_NAMES from create_agent_sheets (single source, not
duplicated). Proven: forcing 2026-04 (which had only 9 core sheets) -> CRITICAL
with the missing list; live June -> PASSED (13/13). This would have caught June's
missing agent sheets within hours instead of surfacing 1/6 via a Critic crash.
TASK-98 (Bug A) merged into TASK-91. TASK-91 remaining scope (atomic rotation,
_ensure_month extension, stale '11 agent sheets' strings) stays open. 107/107.

v2.59 (2026-06-02): TASK-97 — RH-Summaries monthly host failed in CI.
_get_monthly_worksheet read the spreadsheet ID only from the gitignored local
dotfile (~/.rh_summaries_sheet_id), absent in GitHub Actions -> monthly_summary
sheet row was NEVER written in production (email still sent, masking the gap).
Fix: read RH_SUMMARIES_SHEET_ID env var first, dotfile fallback for local; added
GitHub Secret + env line in agent_critic_monthly.yml. The gspread client itself
already worked in CI (GOOGLE_CREDENTIALS_JSON) — only the ID source was broken.
Discovered during TASK-60 verification. weekly_summary unaffected (uses
sheets_config.json, not the dotfile). Unit-tested (env-first / dotfile-fallback /
safe-degrade) + 107/107. CI write path verified by scheduled run 1/7.

v2.58 (2026-06-02): TASK-60 — monthly Critic email read the WRONG
month's paper_portfolio. review_completed_trades() called get_sheet_values
without a month, defaulting to the current (day-1-empty) sheet; build_monthly_row
then filtered ExitDate to the PREVIOUS month, yielding 0 trades -> silent skip.
May's 115 closed trades were never summarized; the 1/6 monthly run failed/empty.
Fix: review_completed_trades(month=None) routes month to BOTH paper_portfolio and
decision_log reads; orchestrator_critic_monthly passes the previous month.
Daily/weekly (month=None=current) unchanged. Unit-tested + 107/107 regression.
Bug A (paper_portfolio absent from SHEET_NAMES -> KeyError on un-rotated month)
logged separately, linked to TASK-91. Cross-month exit edge cases noted (new tasks).

v2.57 (2026-06-01, סגירת לילה): ריצת-לילה אוטונומית (auto mode) על 3 משימות repo-scoped. TASK-85 נסגר — pre-commit guard נגד שמות-backlog >200B (scripts/git_hooks/pre-commit + install.sh, מותקן מקומית; 3 קבצים 200–255B grandfathered, staged-only; PR#2 מוזג b98ae90). TASK-30 נסגר — 97 קבצי .bak untracked >7d נמחקו (בלתי-הפיך, מתועד), נשמרו 11 גבוליים + research/ (→TASK-86). תוך כדי הסגירה נמצא ותוקן שורש-משני: כלל ה-gitignore *.bak_* (שורה 68) בלע את קובץ-המשימה task-30 עצמו (כותרתו מכילה .bak_) — הקובץ מעולם לא נוצר ב-git, וה-Done שלו היה מקומי בלבד; נוסף חריג !backlog/tasks/*.md (שורה 72) שמשחרר קובצי-משימה אך משאיר גיבויי-.bak אמיתיים ignored (task-44.bak אומת עדיין ignored), ו-task-30 נוסף ל-tracking. TASK-84 נסגר already-resolved — אבחנת-המשימה (sheet_id→exit1) הופרכה: sheet_id נקרא רק ב-write_to_sheet (warning, לא משפיע exit); ה-exit1 היה check_06 (Actions success<80%) שצרח על כשלי-checkout משמות-עבריים-ארוכים מ-31/5 — אזעקת-אמת לא באג; שורש תוקן e7dc0dd + גארד TASK-85 מונע הישנות; success 100% (self-healed). לא נגעתי בקוד — 'תיקון' כמבוקש היה השתקת-אזעקה (silent-failure). Backlog 53 open. דוח: docs/NIGHT_RUN_2026-06-01.md. לקח: גארד silent-failure בפרומפט-לילה עצר נכון משימה שאבחנתה שגויה; וכלל-gitignore רחב מדי בולע קובצי-מערכת לגיטימיים — אותו שורש שמות-קבצים, כיוון הפוך.
v2.56 (2026-06-01, סגירת יום): סשן ניקוי+תכנון (ערב 1/6, נפרד מ-v2.55 שתיקן את כותרת timeline_live). TASK-40 נסגר — מחיקת agent/sentinel/checks/dummy_allow.py (orphan Phase-1, לא נטען ב-data_sentinel._load_checks; אפס רגרסיה מול baseline של 17 כשלי-סביבה, מוזג no-ff 5e83513). TASK-77 נסגר — תיקון homoglyph DropsLab Sheet ID (I/l) ב-2 סקריפטי-מחקר מתוארכים בלבד (research/2026-05-05_phase1_day1/phase1_inventory.py + phase6_dropslab.py); אופציה B: PK+production כבר תקינים מסשן קודם, 2 doc-files ששומרים את רשומת-הבאג (task-77 + SESSION_HANDOFF_2026-05-31) נשמרו במכוון, PR#1 מוזג 7b0754d. נפתחו: TASK-93 (GitHub creds ל-Cloud Routines — push/PR אוטומטי בענן), TASK-94 (Agent #8 "Routine-Checker": QA על עבודת-לילה→דוח בוקר עברי מאחד; נבדל מ-The Critic שעושה סיכומי-מסחר), TASK-95 (ניתוח יכולות review רמה-2 כקלט ל-#8). Backlog 56 open. SESSION_HANDOFF_2026-06-01. לקחים: (א) פרומפט משימת-לילה חייב recon+כללי-הכרעה מובנים, לא שאלות A/B באמצע ריצה; (ב) Cloud Routine רץ מבודד ובטוח אך לא דוחף בלי creds (→TASK-93); (ג) ה-PK תמיד מאחורי הזיכרון — לקרוא חי כל סשן. date-gated שחלונו עבר היום: TASK-60/61 (מייל חודשי+weekly אחרי רוטציה), TASK-91 (רוטציה אטומית) — לתעדף מחר.
v2.55 (2026-06-01): תיקון שורש לבאג כותרת timeline_live + הצלת דשבורד יוני. תסמין: 1/6 הדשבורד קרס (KeyError 'Date') — timeline_live יוני הכיל 5638+ שורות נתונים ללא שורת כותרת. שורש: auto_scanner.py run_scan() השתמש ב-ws_timeline.row_count<=1 להחלטת כתיבת-כותרת, אך row_count הוא גודל ה-GRID (גיליון טרי נולד עם 1000 שורות) — התנאי לעולם לא התקיים בחודש חדש, הכותרת לא נכתבה. חלק 1 (data): הזרקת כותרת 28-עמודות ידנית (insert_row index=1) — הצילה 5638+ שורות, דשבורד חזר. חלק 2 (code, commit 4526296): תיקון 3-מצבים — קורא רק שורה 1 (row_values(1)) עם _with_retry: כשל-קריאה(429)→_hdr=None→SKIP (לא כותב חסר-כותרת, לא דורס), שורה1≠כותרת→כותב כותרת+נתונים, שורה1==כותרת→append. סוגר נתיב אובדן-דאטה (429→overwrite) שהתיקון הנאיבי היה מכניס. מונע הישנות ב-1/7. 107/107. נפתח TASK-92 (דיון: צמצום תיעוד-דקה כעומס 429). לקח: 'quota fix' שמחליף get_all_values ב-row_count שבר זיהוי-כותרת — row_count=גודל-גריד, לא תוכן.
v2.54 (2026-05-31, סגירת יום): סשן ערב 31/5. 3 גושים: (1) TASK-80 נסגר — חקירת DropsLab serial fallers, אין edge חזוי (snapshot+5d; דוח research/TASK-80_serial_fallers_2026-05-31). (2) הצלת CI — 5 קבצי Backlog >255B שברו checkout בכל workflow מאז 30/5; תוקן git mv→ASCII (e7dc0dd), הציל רוטציית 1/6+מייל חודשי. (3) TASK-48 — מייל חודשי לדוח עברי עשיר (v2.51→v2.53, commit 1ed42ca). נפתחו TASK 84-90 (CI/mxv/גרפים/חריגים/splits). Backlog 52 open. SESSION_HANDOFF_2026-05-31_evening. לקח: post-commit hook מאמד PROJECT_STATE→divergence ב-push; פתרון reset --soft לא rebase. מחר date-gated: אימות רוטציית 1/6 (TASK-60/61).
v2.53 (2026-05-31): TASK-48 — המייל החודשי בצורתו הסופית: דוח עברי עשיר ב-monthly_brief (render-only), מחליף את ה-7-KPI הדחוס. בלוקים: [1] שורה-תחתונה (טוב/רע + למה: עוצמה avg_win/avg_loss), [2] 4 כרטיסי-מספרים (עסקאות/ברווח/בהפסד/נטו$) + בר ויזואלי לאחוז-הצלחה (Profit Factor חושב ב-build_monthly_detail אך הוסר מהתצוגה לבקשת עמיחי), [3] טבלת "מי זז" — **מאוחדת לפי מניה** (סך-$ + מספר-כניסות + תשואה ממוצעת %), מסירה את הפער המבלבל מול שורת dominant (top5[0]==dominant_pos), + כותרות-עמודה ומשפט-הסבר, [4] טבלת ביצועי-המדדים (הלב) — לכל מדד ממוצע מנצחות/מפסידות, פער, ודירוג איכות חזק/בינוני/חלש (rel=|פער|/max(|win|,|loss|): ≥.20/≥.08), ממוין, + הסבר-עמודות ומילון-מדדים + הערת-אמת "רוב המדדים לא הבדילו" (מאי: Float% חזק לבד), [5] מה-לבדוק (PRE_FIX אדום אם >50% + תמהיל יציאה בעברית). תוספות לוגיקה ב-build_monthly_detail: profit_factor, metric_quality, top5/bottom5 מאוחדים לפי ticker. RSI לא קיים בעסקאות→4 מדדים; mxv מוסתר (TASK-87). detail=None→fallback (verdict+כרטיסים). build_monthly_row/orchestrator/sheet 16-עמודות לא נגעו. אומת מאי 2026 (115 עסקאות). 107/107. Follow-up: גרפים (equity curve, בר-מדדים) + זיהוי-חריגות — ליום אחר.
v2.51 (2026-05-31): TASK-48 — הרחבת המייל החודשי (per-stock + רוחבי, email-only). build_monthly_detail + _month_trades (critic_v1, טהורים): Top5/Bottom5 לפי $PnL, דומיננטית-לחיוב/שלילה לפי RealizedPnL מצרפי-לטיקר, תמהיל exit_reason, MANUAL_CLEANUP + ספירת PRE_FIX, ממוצעי מדדי-כניסה (score/run_up/atrx/float) מנצחות מול מפסידות, ספירת חוזרים סדרתיים. render_monthly_email(row, detail=None) מוסיף 3 בלוקים; detail=None זהה-בייט למייל האגרגטיבי הקודם (אומת). ה-orchestrator בונה detail ב-try — כשל לא חוסם את המייל. סקציה ג מתויגת "🔬 חקרני — תיאורי, לא טריגר". mxv הוסר מסקציה ג עד TASK-87 (sentinel pollution: ממוצע −1053 מול 92 בעסקה בודדת). ה-sheet monthly_summary (16 עמודות) לא נגעה. 107/107 עוברים. אומת על מאי 2026 (115 עסקאות). נפתח TASK-87 (MEDIUM). Lesson: detail=None backward-compat = ביטוח שהפיצ'ר לא מפיל את המייל הראשון.
v2.50 (2026-05-31): TASK-62 — ניתוח שבועי רטרוספקטיבי + חקירת DropsLab (read-only, אפס מוטציות קוד). דוח: research/TASK-62_weekly_analysis_2026-05-30.md. ממצאים: (1) חוזרים סדרתיים — 41.4% מהנופלים (520/1257) נפלו >פעם (BIYA×13/ADTX×11/IPST×11/SMX×10); הזווית היחידה ללא survivorship bias (→TASK-80). (2) survivorship: drops_raw מודד עומק-בתוך-נופלים לא מי-ייפול (→TASK-79). (3) RidingHigh n=73: RunUp r=−0.377 / ScanChange −0.296 מנבאים MaxDrop אך Score r≈0; DaysSinceIPO +0.261 (→75/76). (4) דיוקן: 77% RSI<50, 81%<SMA200, 67% Nano/Micro, 88% בלי דוח. תקלה: DropsLab Sheet ID homoglyph I/l — הנכון 1XM-qId7... (PK חי כבר תקין; שגוי נותר בזיכרון+research →TASK-77). data:statistical-analysis אומת מול קוד ידני (פער 0.000). נפתחו TASK 63-80 (18). Backlog 44 open. Lesson: homoglyph שורד בעין אנושית → gc.open by-name; 'מי שכבר X' לא מנבא 'מי יעשה X'.

v2.49 (2026-05-30): TASK-48 COMPLETE (code-complete; first live monthly email 1/6).
EMAIL.1 daily (a3c84ba), EMAIL.3 weekly schema (a8ec6ac), EMAIL.2 weekly standalone
(cc7a206, live Fri 18:00). bug#1: review_completed_trades never stored RealizedPnL →
TotalPnL=$0 in all emails; fixed (2bd533a). monthly: build_monthly_row + write_monthly_summary
(7be721f); 2b RH-Summaries provisioning + _get_monthly_worksheet seam — SINGLE tab
monthly_summary (NOT per-year; MonthOf carries year); id 1hp9KSZY... (335a091).
Recursion fix in _gc_or_raise + orphan recovery (0caf8c6). agent_critic_monthly.yml
activated, cron '0 6 1 * *' = 1st 01:00 Peru (a64d470). 3 separate emails + 3 workflows.
Lesson: py_compile/AST miss arity+recursion — relayed scripts need live/inspect check.

v2.48 (2026-05-29): Session. 7 commits. Closed TASK-28 (scan_freshness verified), TASK-55 (429 phase-2: _ha_cached_read retries 3->5 + backoff cap 40s + TTL 300->600), TASK-37 (Live Write Verification — live on 6 ENTERs, 0 lost writes), TASK-57 (28/5 stall = one-off outlier, ~25x neighbors, unverifiable trigger, not recurring), TASK-51/52/56 (protocol rules). SESSION_PROTOCOL gained §1 proactive-proposal, §5 Done-requires-verification gate, §6 Clipboard Integrity. Opened TASK-58 (separate SA for health_audit). Backlog 22 open.
v2.47 (2026-05-28): TASK-55 — Sheets 429 read-quota fix. health_audit
get_active_month_sheets was called 11x/run, each doing uncached gc.open_by_key
(1-3 API calls) = up to 33 spurious reads vs Google 60/min cap; 3 checks (16/20/21)
share _load_recent_metrics which calls it before the cached read; amplified by
retries=3 backoff + 17:00 UTC 4-workflow collision. FIX: memoize (rename to
_get_active_month_sheets_uncached + same-name wrapper, 300s TTL, id(gc) key, mirrors
_HA_SHEET_CACHE) -> 11->1 calls, zero blast radius. Pushed 8e3c9ad, IN PROGRESS pending
live verify (next 0 17 UTC health_audit run must show 0x APIError 429). Phase 2 if
insufficient: retries=3->1, move cron 0 17->0 16 UTC. Root found via 6-round recon.

v2.46 (2026-05-28): TASK-53 — PreToolUse hard skill-gate. UserPromptSubmit (soft,
declare-only) PROVEN INSUFFICIENT (17:24 self-audit: declared 5 skills, applied 4).
New PreToolUse hook blocks Bash/Edit/Write/NotebookEdit until a SKILL.md is Read or a
Skill tool loaded in the session (transcript scan: message.content[].name +
input.skill/file_path). matcher exempts Read+Skill. fail-open on ANY hook error.
ROOT-CAUSE history: v1 used "tool_name" (exists only in hook stdin, NEVER in transcript)
-> COUNT always 0 -> blocked everything (Stage D caught it live); fixed to "type":
"tool_use" + "name":"(Read|Skill)" + SKILL.md/"skill", validated on 3 real transcripts
(COUNT 2/3/3) + bug-guard rejecting Bash whose command text contains "SKILL.md".
KNOWN HOLE (Phase 2 / TASK-54): gate verifies SOME skill loaded, not the RELEVANT one
(Test 3 loaded time-check for a pwd and passed). Recovery: disableAllHooks=true via
native terminal (~/.claude/hooks/RECOVERY.md). Hook+RECOVERY mirrored to
scripts/claude_hooks/. Tested 3/3 live: block / Read-unblock / Skill-unblock.

v2.45 (2026-05-28): Session close. 5 commits today across 6.5h:
SESSION_PROTOCOL+rhpro-session (10:17), morning handoff (10:41),
skills integrity guard (13:34, v2.43), UserPromptSubmit hook
(14:41, v2.44), this close. Day theme: 11+ "skills forgotten"
complaints permanently solved via kernel-level hook. 2 new META
TASKs (51 proactive-proposal, 52 finish-what-you-touch) from
explicit user feedback. Tomorrow: skip infra entirely, attack 1
real production issue. 25 Backlog open, ~93 net open.

v2.44 (2026-05-28): UserPromptSubmit hook — kernel-level skill
enforcement. Root cause of 11+ "you forgot skills again" complaints
across 23/5-28/5: every prior fix was soft (memory entry, RULE in
CLAUDE.md) and depended on the model remembering mid-session. Discovery
via web_search + official Anthropic docs (code.claude.com/docs/en/hooks):
Claude Code hooks are the deterministic enforcement mechanism. Built a
hook at ~/.claude/hooks/skill_enforcement_hook.sh that fires on every
UserPromptSubmit and injects "MANDATORY SKILL ACTIVATION" text into
the prompt. Registered via jq merge in ~/.claude/settings.json (5
enabledPlugins + theme preserved, full backup .bak_2026-05-28_143338).
Live verified 14:39 Peru: clean session, user asked "מה השעה?",
Claude responded "🛠️ סקילים פעילים: time-check — שאלת זמן ישירה",
then Skill(time-check) ⎿ Successfully loaded skill, then ran the
correct bash, then answered. Conclusive proof of fire+obey+execute.
Hook scripts mirrored at scripts/claude_hooks/ (skill_enforcement_hook.sh + pretooluse_skill_gate.sh + RECOVERY.md) for git tracking; scripts/git_hooks/install.sh deploys them to ~/.claude/hooks/. Consolidated 2026-06-04 — removed redundant loose scripts/skill_enforcement_hook.sh; unifies with the section-291 claim into one canonical mirror.

v2.43 (2026-05-28): Skills infrastructure restored + integrity guard.
Discovery: skills/superpowers/ was renamed to .bak on 26/5 17:18 because
superpowers was re-installed as a Claude plugin (5.1.0, marketplace) —
but RULE #11 v3 + SKILLS_MAP still pointed to the dead skills/ path.
Result: 14 superpowers sub-skills silently invisible from 26/5 until
today's session. Also: a 14MB copy of anthropics/skills bundle was
sitting under the misleading folder name "systematic-debugging/".
Fix in 4 parts: (1) deleted the manual skills/superpowers/ duplicate
(plugin remains canonical); (2) renamed misnamed bundle to
anthropic-skills/; (3) RULE #11 v3 paths corrected to plugin glob
~/.claude/plugins/cache/superpowers-marketplace/superpowers/*/skills/<name>/SKILL.md;
SKILLS_MAP fully resynced (8 RH-dedicated + 6 superpowers + 17 anthropic
+ 4 trailofbits plugin skills, all path-verified); (4) new
scripts/check_skills_integrity.sh (~70 lines, zero deps) runs at session
open per RULE #13 — checks plugin manifest, resolves every required
SKILL.md path, flags .bak folders and superpowers duplicates. First
run: 26/26 PASS.

v2.42 (2026-05-28): Session infrastructure — docs/SESSION_PROTOCOL.md
(Open/Close rituals, intent-based recognition, dynamic latest-handoff,
full task accounting) + rhpro-session skill (~/.claude/skills/, factless)
+ CLAUDE.md RULE #13. PK update now MANDATORY on every session close /
handoff / task-list update. Also: c4c4750 live-verified in production
(decision_log writing again, real Scores 53-54, Filter 9 active). New
tasks: TASK-48 (Critic email redesign + weekly/monthly) + TASK-49 (NCT
recon mismatch dl=1/pp=2 — investigate, not fixed).

v2.41 (2026-05-27): Fix — decision_logger row-length assert was hardcoded
                    ==41 while TD.2 (v2.39, commit 5b20cbf) grew FIELD_MAPPING
                    to 42. Broke ALL decision_log writes from 26/5; also caused
                    Score=0.00 in the Critic email via empty postmortem lookup.
                    Fixed to ==len(FIELD_MAPPING) (self-healing) + regression
                    test test_field_mapping.py (2 tests) + stale docstrings
                    corrected to accurate counts (42 sheet/mapping, 43 dataclass).
                    Verified live round-trip (write→read→delete, zero residue).
                    Files: agent/logging/decision_logger.py +
                    agent/trader/decision_logic.py (docstrings) +
                    tests/agent/unit/test_field_mapping.py (new). v2.40 was
                    RiskSentinel (built+reverted per request), so this is v2.41.
                    Separate OPEN issue: 429 read-quota on timeline_live /
                    paper_portfolio / sentinel during market hours — Trader
                    sometimes blind, signals=0 (next priority).

v2.39 (2026-05-26): Triple session closure — Stage 1.6 + L3 + TD.2.
                    Stage 1.6: L6 production monitoring verified. 67 runs, 902
                    decisions, 0 failures. L6 fired on AEMD @$2.58 (sub-$3 block).
                    L3 TOXIC_PROFILE (Filter 4d, commit 14452a2): RSI>88 AND
                    Price/SMA20>250%. +145pp vs L6-alone. New module:
                    agent/enrichment/sma20_cache.py. Decision 42->43 fields.
                    TD.2 (commit 5b20cbf): FIELD_MAPPING 41->42. May Sheet
                    PriceVsSMA20 col 23. 14 filters total. 27/27 tests.

v2.38 (2026-05-26): Stage 2 — Filter 4c BLACKLISTED_TICKER (Toxic Blacklist).
                    CHRONIC_DROPPER_BLACKLIST = [AEHL, TDIC] added to config.py.
                    Filter 4c inserted after PRICE_TOO_LOW, before MARKET_CAP.
                    AEHL + TDIC identified via DropsLab cross-reference as
                    chronic droppers (3+ drops in 30d, Apr 2026). Together
                    accounted for ~$120 of DRY_RUN losses Apr+May. 13 filters
                    total. +4 unit tests, all passing. Commit: ec94d9c.

v2.37 (2026-05-26): DropsLab investigation (Stage 0.4 closure).
                    Found: DropsLab scanner running fine, but drops_collect.yml
                    timing out (10m limit) since May 19 due to organic growth
                    of raw_rows (sleep loop over yfinance fetches). Quick fix:
                    timeout-minutes 10 -> 20. PK §A2 sheet ID corrected from
                    1M-ofm... (empty old sheet, pre-migration) to
                    1XM-qId7... (active sheet, post-Apr 19 migration to
                    projects5069 Google account). Better fix (skip processed
                    rows / --date flag) tracked as new Stage 13.6.

v2.36 (2026-05-25): L6 PRICE_TOO_LOW filter (ScanPrice >= $3).
                    Layers paradigm sub-$3 ban — penny-stock-adjacent
                    stocks have wide spreads, halt risk, and questionable
                    borrow availability. Backtest on PA showed L6 alone
                    contributed +129pp improvement (winners median $7.26
                    vs toxic $3.61). Filter inserted after Filter 4
                    (VOLUME), before Filter 5 (MARKET_CAP), as Filter 4b.
                    Treats price=None/0 as PRICE_TOO_LOW (safer than
                    DATA_ERROR). 14 filters total (was 11). New constant:
                    AGENT_MIN_SCANPRICE_USD=3.0 in config.py. +5 unit tests.
                    Commit: e4687e0. PK updates: §A7 decision flow.

v2.35 (2026-05-25): CRITICAL — Comprehensive Audit reveals real WR is
49.4% (not 67.6%/80.5%), mean PnL -0.55%/trade, total DRY_RUN PnL
-$491 over Apr+May. Five tickers (PIII, QUCY, AEHL, STFS, TDIC)
account for ALL losses; without them total = +$2,150. Score-vs-PnL
Spearman r = 0.020 — Score does not predict outcome. Slippage
asymmetric (TP +5.5pp over-run, SL -6.3pp drag). MXV filter may have
wrong polarity. Phase 2 transition BLOCKED until issues resolved and
new 30-day audit shows real WR ≥ 60%. Full audit at
research/2026-05-25_comprehensive_audit/findings.md

v2.34 (2026-05-25): AUDIT.9 — Win Rate audit across 4 dashboard pages.
Found & fixed bug in Home page (dashboard.py:5106): TP10_Hit.mean() counted
WHIPSAW trades as wins, inflating WR from 78% (bug) down to 69% after fix. Replaced with
classify_trade_row() — same SSoT as Post Analysis page. Also documented:
Portfolio Tracker (55.8%) uses sim, Post Analysis (69%) uses classify_trade,
Trade History (43.8%) uses RealizedPnL from paper_portfolio. The "portfolio" sheet (65 rows,
all Open, no PnL) is only a ticker list for Score Tracker — not a WR source.
Research: research/2026-05-25_winrate_audit.md. task-44 closed Done.

v2.33 (2026-05-24): P3.4 — Cron-drift outage detection (Phase 1).
Implemented detect_outage() in orchestrator.py + wired into run() as
Safety check 3 (commit 7dae5e5). Uses timeline_live as proxy for
"last successful run", logs to sentinel_events on gap > 10 min, sends
email alert on gap > 30 min. Phase 2 (catch-up logic) intentionally
deferred after deep research on 16 trading days of data: 95.28% of
gaps are on-time, 80% of data loss is small drift (3-7 min) that
Phase 2 wouldn't fix anyway, severe outages (>10 min) occur 0.5/day
but mostly clustered in single GH Actions incidents (5/8 severe were
on 22/5 alone). Critical outages (>30 min): 1 in 16 days. Phase 1
provides observability for the rare critical events with zero risk;
Phase 2's ROI doesn't justify its complexity (Alpaca bar fetching,
score consistency vs FINVIZ, dedup logic). task-16 closed Done.

v2.32 (2026-05-24): SENT.1 — Sentinel shadow->active verification completed.
4-hour deep investigation revealed: (1) decision to switch active on 20/5
(commit 5f3918a) was data-backed and correct, (2) apparent BLOCK explosion
on 21-22/5 (4565 BLOCKs, 91% rate) was NOT Sentinel malfunctioning — it
was a lex-compare bug in orchestrator.read_latest_signals that fed stale
ScanTime to Sentinel, (3) bug already fixed in commit 5cc658b (parse_hhmm
utility, 22/5) covering 8 of 9 call sites, (4) the 9th call site discovered
in dashboard.py:2757 fallback in _fetch_health_data — fixed in commit
2ef7ceb. Sentinel itself works correctly; no rollback needed. New task
SENT.2 (verify 25/5) added.

v2.31 (2026-05-23): Misc maintenance batch. Closures: P0.2 (Daily Brief
errors mislabel — 1-line filter to exclude SENTINEL_*), P1.2 (cron drift
investigation documented in research/, no fix needed for now — see P3.4),
P1.5 (PIII postmortem — Filter 9 leak from 15/5, root-caused by Fix D),
P1.4 (paper_portfolio PnL empty: 16 cases analyzed — 8 historical, 7
MANUAL_CLEANUP not-a-bug, 1 HCWB resolved by Fix D), P3.5 (D1_Open
outliers — 5 cases all real events, no data bugs), P3.6 (Sentinel
serialization verified — 8632/8632 valid JSON), P3.7 (dashboard.py
parse_hhmm imports moved to top + duplicate pytz removed), P4.1
(OPEN_ISSUES.md archived, replaced with stub pointing to Backlog.md),
P4.5+P4.7+N4 (verified clean). Plus deletes: archive_2026-04-17 (28KB)
and 18 stale .bak files in .github/workflows/.

v2.30 (2026-05-23): META.1 — WORK_ALLOCATION policy v2. Replaces the
4 conflicting rules of v1 with a single decision rule based on
budget ratios (HOURS not tasks, 40% Maintenance / 40% Development /
20% Analysis). Includes P0 override clause. Meta excluded from
budget. Created docs/WORK_LOG.md for per-session tracking with
inline formula. Also: backlog migration — 35 tasks seeded via
Backlog.md (replaces ad-hoc OPEN_ISSUES.md format). N1 cleanup:
25 untracked files → 0 (archived TP10 investigation, moved EDA
artifacts, deleted obsoletes). N3: CLAUDE.md RULE #4 backup format
aligned to .bak_<date> (was .BEFORE_<task_id>) — matches code reality.

v2.29 (2026-05-23): P4.6 — check_02_hardcoded_thresholds refactor
(regex → AST). The old regex-based scanner emitted 7 findings, 6 of
which were false positives (quantile(0.10), total_seconds() >= 60,
docstring text mentioning thresholds, etc). New AST-based version
walks ast.Compare nodes only, checks left-side variable for
score/threshold context, whitelists non-threshold calls (quantile,
total_seconds, sleep, range). Scope extended from root to root +
agent/**. The extended scope discovered a hidden true bug:
agent/dashboard/trade_history_page.py:531 — hardcoded Score >= 60.
Fixed by importing MIN_SCORE_DISPLAY from config in same commit.
check_02 now PASSED, was WARNING with 7 findings.

v2.28 (2026-05-23): P3.2 — Consolidated 4 nested duplicate styling
functions in dashboard.py. Found 2x color_score (identical bodies)
and 2x highlight_score (one with try/except, one without) — all 4
were nested closures across the file. Refactored to 2 module-level
functions (kept the safer try/except variant). CRITICAL_SCORE and
MIN_SCORE_DISPLAY are imported from config.py at module level, so
the refactor is safe. _color_score (private variant at L3406, not
a duplicate) and color_entry_score (sibling nested fn) preserved.
Net: -23 lines, 0 functional change. Also closed N19 (nan/inf
investigation — no active reference, handled in 3 code locations).

v2.27 (2026-05-23): Filter 9 leak — Fix D (union pattern). v2.24
fix assumed decision_log was a reliable SSoT, but HCWB had 5 ENTERs
on 2026-05-20 (post-v2.24). Root cause: Google Sheets eventual
consistency — writes to decision_log don't appear in reads for
seconds-to-minutes. PK v2.24 switched the counter source from
paper_portfolio to decision_log, but both sheets have the same lag
problem; single-source counting is vulnerable regardless of which
sheet it uses. Fix D in build_account_state: maintain TWO counters
(entries_today_by_ticker from decision_log + entries_today_by_ticker_pf
from paper_portfolio), take max() per ticker. Filter 9 now sees the
highest available count regardless of which sheet is propagating.
Counterfactual on real data: HCWB max(5,4)=5 -> ENTER #4 blocked;
PIII max(14,14)=14 -> ENTERs #4-14 blocked. Pre-fix leak history:
PIII x14 (15/5), ERNA x8 (12/5), AEHL x7 (12/5), QUCY x7 (14/5 + 15/5),
HCAI x4 (18/5), ATRA x4 (7/5). Post-v2.24 leak: HCWB x5 (20/5).
Post-Fix D: zero (verified counterfactually).

v2.26 (2026-05-19): Three new tasks logged from end-of-day review. (1) Daily Brief email lacks per-ticker exit details — currently shows aggregate $503.94 P&L + 4 ENTER, but no per-trade exit/P&L/WIN RATE breakdown. User cannot see what happened to each entry without opening dashboard. Live Agent page already shows this info — replicate it in the email body. (2) Trade History page did not show today's 4 entries in the first view — data exists in paper_portfolio (verified), so this is a display/sort bug in agent/dashboard/trade_history_page.py. (3) Daily Brief email arrived at 17:45 Peru, but cron of agent_email_daily.yml is '30 21 * * 1-5' = 21:30 UTC = 16:30 Peru. 75-minute discrepancy — investigate. Verified end-of-day from Live Agent dashboard: 4 entries today (CODX, WNW x3), 6 exits total (2 from 18/5 + 4 from today), $503.94 P&L correct, Filter 9 held WNW at exactly 3 entries (fix from commit d3b6e08 works in production).

v2.25 (2026-05-19): The Critic never ran automatically since deploy (17/5) — 3 days of silence. Root cause: cron was '7 0 * * 2-6' = 00:07 UTC, the global GH Actions peak hour. The :07 offset (commit 2d6f26c) was not enough — schedules near midnight UTC are silently dropped without even a failed-run record. Evidence: Market Context (created same day) runs fine on cron in UTC 13-20; all other repo workflows in UTC 11-21 run reliably. Only Critic targeted midnight. Fix in commit c0c7123: moved cron to '0 22 * * 1-5' = 22:00 UTC = 17:00 Peru (1h after EOD at 16:00 Peru, in the proven working range). Verified manually 19/5 21:31 Peru: workflow Success, scorecard wrote 4 rows for 19/5, RTL Hebrew email delivered. First automatic run: Wed 20/5 17:00 Peru.

v2.24 (2026-05-19): Filter 9 re-entry limit leak fixed (root cause). The Critic flagged HCAI entered 4x on 2026-05-18; decision_log confirmed 4 ENTERs (12:06/12:17/12:20/14:39) — The Critic was RIGHT, Filter 9 leaked. Root cause: entries_today_by_ticker (which Filter 9 reads) was counted from paper_portfolio, whose writes can be lost to a 429 — paper_portfolio had only 3 HCAI rows (12:17 write lost), so the 4th entry saw count<3 and was not blocked. Fix in build_account_state: count per-ticker entries from decision_log (reliable SSoT — ENTER always logged), not paper_portfolio; also increment the counter in-loop after each ENTER so same-run re-entries are seen. Task 15 (teach Critic about SKIP) cancelled — Critic was accurate.

v2.23 (2026-05-19): OAuth token renewed + Warmer truth-fix. The OAuth refresh token was revoked (invalid_grant). Root cause: warm_oauth_token.py refreshes the token but never persists a rotated one — Google occasionally rotates refresh tokens, and the rotation was lost, so 8 'successful' Warmer runs did nothing useful (false confidence). The app is 'In production' (verified in console), so tokens do NOT expire from inactivity — the old docstring claiming 'Testing / 7-day' was wrong. Fix: renewed token manually (get_oauth_token.py), updated GOOGLE_OAUTH_TOKEN_JSON secret, corrected warm_oauth_token.py + .yml docs to state it is a DETECTOR not a preventer. Auto-persist via PAT was considered and rejected (adds a write-secrets key + failure point for a rare event with a working email alert + 5-min manual fix).

v2.22 (2026-05-19): News Detective Finnhub fix. agent_minute.yml did not pass FINNHUB_API_KEY to the orchestrator env, so _fetch_finnhub_news raised 'FINNHUB_API_KEY not set' on every ticker — half the agent (Finnhub) was dead while EDGAR worked. The secret already existed in repo settings; only the workflow env block was missing the line. Added FINNHUB_API_KEY to agent_minute.yml env. Verify finnhub_news columns populate on next run.

v2.21 (2026-05-19): Quota fix — auto_scanner timeline_live read. auto_scanner.py:405 pulled the entire timeline_live sheet (~220k rows) every minute just to test if the sheet was empty via len()<=1. Replaced with ws.row_count (worksheet metadata, 0 API cost). This was the heaviest Sheets read in the system, running ~390x/day, and a primary 429-quota driver. Recon found two minute-cadence workflows (agent_minute + auto_scan) both reading timeline_live; orchestrator-side read consolidation deferred to a separate task.

v2.20 (2026-05-19): Sentinel scan_freshness thresholds widened. Real timeline_live data showed scans run every ~1.6min (gaps up to 3min are normal), so the old 3min WARN threshold collided with normal cadence — shadow audit showed 100% WARN. Widened: SCAN_MAX_AGE_MINUTES 3->5 (WARN), SCAN_MAX_AGE_BLOCK_MINUTES 5->10 (BLOCK). Re-audit: scan_freshness 100% ALLOW, overall 0% BLOCK across 3 scans. SENTINEL_MODE stays 'shadow' — need multi-day clean audit (incl. market open/close) before active.

v2.19 (2026-05-19): Sentinel price_freshness BLOCK->WARN. Shadow audit on real signals showed 17.4% BLOCK rate, all from price_freshness STALE_FINVIZ_PRICE. FINVIZ vs Alpaca price gaps are normal for volatile pump stocks and do NOT risk trades — execution price always comes from Alpaca (3f4f95d), FINVIZ only feeds candidate discovery + Score. Demoted to WARN. Re-audit: 0% BLOCK. Verified via sentinel_selftest_v1 (16/16) + shadow_audit_v1. scan_freshness 100%-WARN still open (pipeline age vs 3min threshold) — not yet active.

v2.18 (2026-05-18): Sheets WRITE resilience — completes v2.15 (which
                    covered reads only). sheets_manager: safe_update,
                    safe_batch_update, safe_append_row — all wrap
                    _with_retry on 429. safe_append_row adds an
                    idempotency guard: append_row is not idempotent,
                    so on retry it reads the dedup column and skips
                    if the row is already there. 8 writers migrated —
                    decision_logger / order_manager / postmortem use
                    dedup (decision_id / PositionID); sentinel / critic /
                    market_context / news_detective use blind retry
                    (low-risk, rare); orchestrator portfolio updates
                    use safe_batch_update. auto_scanner writes NOT yet
                    migrated (no per-row unique id — separate task).
v2.17 (2026-05-18): Sentinel fix — two checks were blind in production.
                    position_sync read open-count from the mixed
                    existing_positions set (open positions + today's
                    ENTER tickers), masking real sync failures; now reads
                    open_position_count. orchestrator fed today_enters
                    from that same mixed set; now feeds cold_start_daily_used.
                    provider_heartbeat imported a non-existent module
                    (providers.data_provider_factory), failing every run
                    with WARN; now imports from data_provider. Verified by
                    sentinel self-test (5 BLOCK checks fire correctly).
v2.16 (2026-05-18): Realistic win-rate. utils.classify_trade() —
                    day-by-day D1-D5 trade classifier (5-day
                    maturity gate) returning WIN/LOSS/WHIPSAW/
                    NO_TOUCH/PENDING. Dashboard post_analysis_page
                    now shows true win-rate (decided trades only)
                    + investigation categories. Replaces misleading
                    TP10_Hit-as-win and the Expected-Value metric.
                    Investigation finding: prior "~81% win rate" was
                    an artifact — whipsaw rows counted as wins. True
                    win-rate on 58 settled trades ~58%. See §20.
v2.15 (2026-05-17): Sheets quota resilience — #1 production risk.
                    sheets_manager._with_retry(): exponential backoff
                    (2/4/8s) on 429 quota errors only. get_sheet_values():
                    60s read cache keyed by (tab,month). Critic weekly_summary
                    cut from 35 to 4 Sheets API calls. gsheets_sync reads
                    wrapped in retry. PnL-empty bug investigated = false alarm.
v2.14 (2026-05-17): ROCKET_GUARD (Filter 11) — blocks shorting stocks
                    still climbing (RunUp>=50% AND PriceToHigh>=-10%).
                    Calibrated on 196 rows: 16 losses blocked, 0 wins lost.
                    Agents #3-5 built (Market Context, News Detective, Critic).
                    System Overview fully refreshed. 11 filters total.
v2.13 (2026-05-16): Added §A15 — Data Sentinel (agent #2, gatekeeper).
v2.12 (2026-05-16): Bug-fix sweep — 5 critical trading-agent bugs.
                    Bug #1: _close_position uses trigger price (no stale re-fetch).
                    Bug #2: sheet writer targets row by number (not PositionID).
                    Bug #3: PositionID now DEC-DATE-TICKER-HHMMSS-ff (collision-proof).
                    Bug #4: concurrent cap counts actual OPEN rows (not unique tickers).
                    Bug #5: AGENT_MAX_REENTRIES_PER_TICKER=3 caps same-ticker churn.
                    DataQuality column added (CLEAN / PRE_FIX).
                    paper_portfolio schema now 25 columns.
                    10 filters in decision_logic (added REENTRY_LIMIT).
v2.11 (2026-05-04): Milestone 8 — Email System.
                    Added agent/notifications/email_sender.py — SMTP wrapper
                    for Gmail with App Password support. Reads creds from
                    env vars (GitHub Actions secrets). Graceful failure —
                    email errors never crash orchestrator.
                    
                    Added 2 HTML email templates: morning_brief (08:30 Peru
                    daily start) + daily_brief (16:30 Peru EOD summary).
                    
                    Added 2 orchestrators: orchestrator_email_morning.py
                    + orchestrator_email_daily.py. Each gathers state from
                    Sheets/broker and renders + sends email.
                    
                    Urgent alerts integrated into orchestrator.py and
                    orchestrator_eod.py — any errors trigger immediate
                    email via send_alert().
                    
                    Added 2 GitHub Actions workflows: agent_email_morning.yml
                    (cron 30 13 UTC M-F) + agent_email_daily.yml (30 21
                    UTC M-F).
                    
                    Required GitHub Secrets: SMTP_HOST, SMTP_PORT, SMTP_USER,
                    SMTP_PASSWORD, EMAIL_TO.
                    
                    8 new tests. Total agent unit suite: 127/127 passing.
                    Phase 1 emails complete.

v2.10 (2026-05-04): Milestone 10 — Production Deployment.
                    Added agent/orchestrator.py (~360 lines) — main run loop.
                    Every minute during market hours: safety checks → build
                    account_state → read timeline_live → evaluate signals →
                    log decisions → execute orders → monitor positions → EOD close.
                    
                    Added agent/orchestrator_eod.py (~100 lines) — daily 16:00 Peru.
                    Reconciler.reconcile + ScoreAnalytics.run_daily + weekly (Sat).
                    
                    Added .github/workflows/agent_minute.yml (cron */1 13-20 UTC M-F).
                    Added .github/workflows/agent_eod.yml (cron 0 21 UTC M-Sat).
                    
                    Emergency Stop reader integrated: checks system_events for
                    unresolved EMERGENCY_STOP_REQUESTED before any trading.
                    
                    19 new tests added. Total agent suite: 121 (118 pass +
                    3 pre-existing failures in decision_id_generator, unrelated).
                    
                    Phase 1 production-ready. DRY_RUN active.

v2.9 (2026-05-04): Milestone 9 — Streamlit Dashboard Pages 9-10.
                    Added agent/dashboard/ package (4 files, 628 lines).
                    Page 9 Live Agent: operational view with KPIs, positions,
                    decisions, Emergency Stop button.
                    Page 10 Score Brain: analytical view with tier winrates,
                    correlations, suggestions with Approve/Reject.
                    Modified dashboard.py (+10 lines: import, page entries, routing).
                    7 robustness fixes applied.

v2.8 (2026-05-03): Milestone 7 — Score Analytics System.
                    Added agent/analytics/score_analytics.py (535 lines).
                    Daily + weekly observational analysis of postmortem data.
                    OBSERVATIONAL ONLY — never auto-modifies score formula.
                    
                    Daily run (16:30 Peru): writes 1 row to score_analytics
                    Sheet (25 columns) with WinRate per tier, 7 metric
                    correlations, surprise findings, recommendations.
                    
                    Weekly run (Saturday 18:00 Peru): same + generates 0-5
                    suggestions to pending_suggestions Sheet (14 columns)
                    awaiting user approval.
                    
                    Sample size tiers determine confidence: INSUFFICIENT (<10),
                    EXPLORATORY (10-29, LOW), RELIABLE (30-99, MEDIUM),
                    STRONG (100+, HIGH).
                    
                    12 new tests added. Total agent suite: 102/102 passing.

v2.7 (2026-05-03): Milestone 6 — Postmortem Engine.
                    Added agent/analytics/postmortem_engine.py (346 lines).
                    Generates postmortem analysis for every closed position
                    with 7 auto-lesson rules, MFE/MAE from real market bars,
                    17-column Sheet schema. Per-position trigger via hook
                    in position_manager._close_position. Both DRY_RUN and
                    LIVE_PAPER positions get postmortems — DRY_RUN benefits
                    most since the whole point is learning.
                    
                    Added AGENT_SCORE_VERSION="v2.6" to config.py for 
                    postmortem schema versioning. Bump when score formula
                    changes (M7+) so old postmortems retain historical accuracy.
                    
                    13 new tests added. Total agent suite: 90/90 passing.

v2.6 (2026-05-03): Milestone 5 — Alpaca Execution.
                    Added 4 execution layer files: alpaca_broker, order_manager,
                    position_manager, reconciler. Upgraded tradability mock to
                    broker-aware with 1-hour cache. 47 new tests added.
                    Total agent suite: 77/77 passing.
                    
                    Critical safety: Triple-layer guard prevents accidental live
                    trading: (1) "paper" assertion in broker base_url, (2) PK key
                    prefix check, (3) AGENT_LIVE_PAPER state machine blocks real
                    submission until M10 enables it.

v2.5 (2026-05-03): Milestone 4 — Decision Logger.
                    Added 2 agent files: decision_id_generator (DEC-YYYY-MM-DD-TICKER-HHMMSS-ff
                    sequential IDs with date reset), decision_logger (writes Decision
                    dataclass to 41-column decision_log Sheet via FIELD_MAPPING).
                    14 new tests added (5+7+2 integration). Total agent: 30/30 passing.
                    Bug discovered & fixed: Drive Move() doesn't propagate file
                    permissions. SA was missing Editor access on 8 Agent Sheets after
                    M2's emergency move. One-time fix: re-shared 8 sheets with SA.

v2.4 (2026-05-03): Milestone 3 — The Trader Core Logic.
                    Added 5 agent files: score_calculator, data_quality,
                    tradability (mock), decision_logic (41-field Decision
                    dataclass + 11 filters), trader (stateless orchestrator).
                    Critical test test_scanner_agent_match: 0 mismatches.
                    Score tolerance set to 0.04 (rounding-only, documented).

v2.3 (2026-05-03): Schema fix — decision_log 40→41 columns.
                    Removed ColdStartRemaining, added ColdStartConcurrentLeft +
                    ColdStartDailyLeft. Renamed Decision→Action.

v2.2 (2026-05-03): Milestone 2 — Agent Sheets Setup.
                    Added 8 agent sheets to 2026-05 folder.
                    Updated prepare_next_month.yml with agent sheet creation step.
                    Bug fixed: SA fallback creating wrong folder (OAuth required).
                    Added §A6 Agent Sheets Schema.

v2.1 (2026-05-03): Added §A5 Agent Module Overview (Phase 1, Milestone 1).
                    Added AGENT_* constants to config.py.
                    Created agent/ directory structure.

v2.0 (2026-05-02): Full rewrite from reality check.
                    Corrected drift on: dashboard pages (10→3),
                    sheets count (7→9), score weights, TP/SL,
                    record counts, repo URL, service account,
                    DATA_PROVIDER (yfinance→Alpaca), workflow count,
                    architecture (inline→modular).
                    Added: ADRs, Glossary, Schema, Lineage, Runbook,
                    Bus factor, KPIs, Cost, DR, Test strategy.

v1.x (deprecated): Earlier hand-written PK. Many facts misaligned with
                    actual code state as of late April 2026.
```

### How to update this document
Per the **Anti-Drift Maintenance Contract** (§35), Claude must update this
document in any session that adds, modifies, or removes:
- A Python script, workflow, sheet, or constant
- A scoring metric, formula, or weight
- A health check, email alert, or schedule
- A phase, KPI, or known issue

Updates increment the patch version (v2.0 → v2.0.1) for content edits, or
minor version (v2.0 → v2.1) for new sections.

---

## 2. ⚡ TL;DR — 60-Second Overview

**RidingHigh Pro** is an automated short-selling research system. It scans
the US equity market every minute, identifies stocks pumping unusually hard,
scores each one 0-100, simulates a $1,000 short trade with TP=−10% / SL=+10%,
and tracks the outcome over 5 trading days.

**No real money is traded.** The system runs in Phase 1: data accumulation
and edge validation. After ~100 clean Score v2 records and verified
TP10 hit-rate stability, the system may transition to live execution.

**Status as of 2026-05-02:**
- 156 records collected (104 v1 deprecated, 50 v2 valid, 2 May v2)
- TP10 hit rate (v2): 75.0%
- Score predictive power: r=0.045 (essentially zero — see ADR-002)
- 18 health checks running 3×/day with email alerts
- Alpaca paper trading active, all systems green

**Owner:** Amihay Levy, Lima Peru (UTC-5).
**Repo:** `projects5069-creator/ridinghigh-pro` (RidingHigh) +
`Ambroseius/DropsLab` (companion data system).
**Dashboard:** `ridinghigh-pro-v2.streamlit.app` (12-page sidebar nav, single-file — see `_PAGE_NAMES`).

---


---

## 🚨 CRITICAL UPDATE — 2026-05-25 Reality Check

A comprehensive audit on 2026-05-25 compared the theoretical
performance numbers in this PK against the actual `paper_portfolio`
data from DRY_RUN trades. **The reality differs significantly from
prior reports.** This section records the audit findings and
supersedes earlier performance claims until the next audit.

### Real Performance — DRY_RUN (Apr+May 2026)

| Metric | Value | Notes |
|--------|-------|-------|
| **Real Win-Rate** | **49.4%** | Wilson 95% CI: 38.6%–60.2%, n=79 decided |
| Mean PnL per trade | -0.552% | |
| Total PnL (DRY_RUN) | -49.13% | -$491.30 at $1000/trade |
| Risk/Reward ratio | 0.95 | Avg win +15.5%, avg loss -16.3% |
| Required WR for breakeven | 51.4% | |
| **Edge over breakeven** | **-2.0pp** | System is below breakeven |
| Sharpe-like (mean/std) | -0.030 | Essentially zero |
| Score↔PnL Spearman r | +0.020 | **Score does NOT predict PnL** |

### Why the prior numbers were wrong

1. **TP10_Hit field was inflated** — used window-level "did price ever
   drop 10% in 5 days" instead of day-by-day classification.
   Old metric: 80.5% → True (classify_trade): 67.6% on ScanPrice
2. **Even 67.6% was theoretical** — based on ScanPrice as entry.
   In reality, mean |delta| between ScanPrice and actual fill is 12%.
   Real WR on actual fills: 49.4%
3. **Slippage is asymmetric and severe**:
   - TP_HIT executes at average +15.8% (vs theoretical +10%) — over-run +5.5pp
   - SL_HIT executes at average -16.6% (vs theoretical -10%) — slippage -6.3pp
   - Net asymmetry: -0.8pp drag per round-trip

### Five tickers explain ALL losses

Without these 5 (PIII, QUCY, AEHL, STFS, TDIC) the remaining 43 trades
would total +215% PnL. With them, total is -49%.

| Ticker | Trades | Total PnL% | Pattern |
|--------|--------|-----------|---------|
| STFS | 5 | -64.07% | 0% WR |
| AEHL | 9 | -64.06% | 33% WR |
| TDIC | 4 | -56.08% | 25% WR, -80% delta outlier |
| PIII | 14 | -47.61% | 29% WR, most traded |
| QUCY | 14 | -33.18% | 29% WR |

### Filter audit findings

- **MXV_TOO_HIGH filter** — counterfactual shows MXV>97 SKIPs would have
  been 100% WIN. The filter may be operating with wrong polarity or
  threshold direction. **Needs urgent code investigation.**
- **RUNUP_TOO_LOW filter** — most SKIPs (RunUp<30%) show 80-100% WR
  in counterfactual. Threshold may be too aggressive.
- **Last-hour entries** — Hour 14 Peru WR=35.7%, total PnL=-65.5%.
  Accounts for most of the loss.

### What this means

- **DRY_RUN works as designed** — it caught these problems before
  real money was at risk. This is the value of Phase 1.
- **System is NOT ready for Phase 2** until these issues are
  resolved and a fresh 30-day audit shows real WR ≥ 60%.
- **Score formula may need redesign** — r=0.02 correlation with PnL
  is the central finding. Filters work, scoring does not.

### Open investigations (must complete before any code changes)

1. ATRX/volatility profile of the 5 bad tickers — is this a
   ticker-bias issue or a volatility-filter need?
2. MXV polarity audit — does the formula match the filter direction?
3. Per-hour WR with confidence intervals — what is the real cutoff?
4. SL slippage architecture — why -6.3pp beyond theoretical?

**Full audit:** `research/2026-05-25_comprehensive_audit/findings.md`

---


## 3. 🧠 Mental Model

> **"RidingHigh Pro is like a fishing fleet that doesn't fish."**

Imagine a fleet of boats casting nets every minute across a vast ocean (the
US stock market). The boats don't actually catch fish (no real trades).
Instead, every fish that swims into a net is photographed, measured,
weighed, scored, and released. Months later, the records reveal which
patterns of fish (which signal combinations) reliably predict which fish
will be edible (profitable shorts).

**Only when patterns prove statistically reliable will the boats start
catching real fish.**

This mental model implies:

- **Speed of catch matters less than completeness of records.** A missed
  scan is acceptable; a corrupted record is not.
- **The catch is the data, not the money.** Every scan, every score,
  every D1-D5 OHLC is the asset.
- **Patience is structural.** Phase 1 lasts months by design.
- **Score is a hypothesis, not a verdict.** Until correlation with wins is
  proven, Score is a guess.

When you ask Claude "should we add X to the scanner?" — the answer
depends on whether X improves catch quality (good) or just adds noise
to the records (bad).

---

## 4. 🚫 Non-Goals — What This System Is NOT

To prevent scope creep and accidental redirection of architectural intent,
the following are explicit non-goals:

| Non-goal | Rationale |
|----------|-----------|
| ❌ **Long positions / buying stocks** | Short side only — **evidence-confirmed (Investigation II phase 7, TASK-175):** buying DropsLab drops d1->d5 close = -1.64% [-2.50,-0.77], n=2,103, WR 43.7% (-3.64% after 1%/side slip); the "75% recovery" was a hindsight peak-touch stat. LONG closed by data, not only design. |
| ❌ **Day trading scalps (intraday in/out)** | TP/SL window is 5 trading days |
| ❌ **Options or derivatives** | Equities only; complexity not justified |
| ❌ **Pre-market or after-hours trades** | Scanner runs 08:30–15:00 Peru only |
| ❌ **Real-money execution (current phase)** | Phase 1 — data validation first |
| ❌ **News-based decision making** | Catalyst data collected for context, not for triggering |
| ❌ **Sentiment analysis (social media, etc.)** | Out of scope; would dilute focus |
| ❌ **Fundamental investing (P/E, DCF)** | Pure technical/statistical short bias |
| ❌ **Crypto, FX, futures, commodities** | US equities only |
| ❌ **Order book / Level 2 data** | Not needed for daily-bar simulation |
| ❌ **Replacement for licensed financial advice** | Personal research tool only |
| ❌ **Multi-user platform / SaaS** | Single-operator system |
| ❌ **Continuous Score as an entry predictor** | Disproven (AUC 0.531, r≈0; the explicit filters carry the signal). Being removed forward-only — **ADR-009**. Historical Score frozen for audit. |

If a request implies any of the above, Claude must flag it as out-of-scope
and ask whether the system definition is changing before implementing.

---

## 5. 👤 User Profile

| Field | Value |
|-------|-------|
| **Name** | עמיחי (Amihay) Levy |
| **Location** | Lima, Peru |
| **Timezone** | **UTC-5, NO daylight saving year-round** (critical — never confuse with US ET which has DST) |
| **GitHub username (legacy)** | `Ambroseius` |
| **GitHub user (machine commits)** | `adilevy@Amihays-MacBook-Pro.local` |
| **Active RidingHigh repo owner** | `projects5069-creator` |
| **Active DropsLab repo owner** | `Ambroseius` |
| **Local dev path** | `~/RidingHighPro/` |
| **Local Python** | 3.9.6 (Mac, system) |
| **Production Python** | 3.11 (GitHub Actions) |
| **Service account email** | `ridinghigh-sheets-v2@ridinghigh-pro-v2.iam.gserviceaccount.com` |
| **Drive root folder ID** | `1mHSdsTENVuMTtlv4XM54SrbadCEF_HHh` |

### Communication preferences

- **Language:** Hebrew with emojis 🎯; technical English inline (variable
  names, error messages, library names stay English).
- **Tone:** Concise and action-oriented. No filler.
- **Numbers:** Scores always 2 decimal places (e.g., `77.63`); percentages
  always include `%` symbol.
- **Code blocks:** Always full files for new artifacts; `str_replace` for
  edits to existing files. Never partial / line-by-line patches.
- **Code-Code blocks:** When delivering work to Claude Code, always provide
  a single copy-paste block with all steps inline.

### Iron rules (declared by user, binding on Claude)

1. `time-check` skill called before any time-sensitive decision.
2. `str_replace` only — never rewrite a file from scratch.
3. Investigation before fix — `awk`/`sed` with line numbers first.
4. `git diff` before commit — verify only intended changes.
5. Syntax check after every Python edit.
6. Manual approval gate before every `git commit`.
7. If `str_replace` uniqueness fails → STOP, do not guess.
8. Backup before any data change.
9. If output is truncated with "+N lines" → request another format until
   the full content is visible.
10. Hebrew RTL without LTR markers.
11. NO `ask_user_input_v0` surveys.
12. NO time pressure / urgency framing.

---

## 6. 🎯 System Purpose & Thesis

### The thesis

Stocks that experience extreme single-day price increases ("pumps") on
elevated volume tend to mean-revert in the days that follow. The thesis
is empirical, not philosophical: certain combinations of pump signals
(low liquidity, high relative volume, intraday range exceeding ATR)
should produce statistically biased short opportunities.

**Research governance (TASK-165).** Every research hypothesis is pre-registered
in `docs/HYPOTHESES.md` — locked fitness + hold-out + experiment journal —
before it touches new data. That register's §A policy is the SoT for research
methodology; this PK points to it and does not duplicate it.

### What the system tries to prove

**H₀ (null hypothesis):** Score has no predictive power above random
selection (TP10 hit rate ≈ 50%).

**H₁ (working hypothesis):** A composite Score that weights MxV, RunUp,
ATRX, RSI, VWAP_dist, ScanChange, and REL_VOL identifies short candidates
with TP10 hit rate ≥ 65% over a 5-day window.

### Current evidence (2026-05-02)

- **TP10 hit rate (v2):** 75.0% — **above** the H₁ threshold ✅
- **Score correlation with hits:** r=0.045 — **near zero** ❌

The dissonance is the central research finding: hit rate is high, but
Score doesn't drive it. This suggests the **filter** (which stocks
even appear) is doing the work, while Score may be measuring pump
intensity rather than reversion probability. See **ADR-002**.

---

## 7. 📅 Current Phase & Exit Criteria

### Phase 1 — Data Accumulation & Validation [ACTIVE]

**Started:** 2026-03-23 (first scan recorded)
**Active since:** 2026-04-13 (Score v2, current formula)
**Target end:** when exit criteria are met (estimated mid-to-late May 2026)

### Phase 1 → Phase 2 transition criteria

| Criterion | Target | Current | Status |
|-----------|--------|---------|--------|
| v2 records accumulated | ≥100 | 50 | ❌ 50% |
| Trading days of v2 data | ≥30 | ~14 | ❌ ~47% |
| Health audit pass rate | 100% for 7 consecutive days | TBD | ⚪ measure |
| Score-TP10 correlation analysis | complete with conclusion | partial | 🟡 in progress |
| TP10 hit rate stability | ±3% range over 4 weeks | TBD | ⚪ measure |
| All 18 health checks passing | yes | yes (18/18 last check) | ✅ |
| **Real WR ≥ 60% over 30 days** (NEW 2026-05-25) | ≥60% | 49.4% | ❌ BLOCKER |

> **🚨 PHASE 2 BLOCKED (2026-05-25):** Real DRY_RUN WR is 49.4%, below the 60% threshold. See the 'Critical Update — 2026-05-25 Reality Check' section above for full audit findings.

### Future phases

| Phase | Theme | Trigger |
|-------|-------|---------|
| **Phase 2** | Dynamic TP via ATRX | Phase 1 criteria met |
| **Phase 3** | Entry timing optimization with minute-level data | Phase 2 stable for 4 weeks |
| **Phase 4** | Real-money execution (Alpaca live, not paper) | Phase 3 shows verified edge |

**Real money is not deployed in Phase 1, 2, or 3.** Phase 4 is gated
on independent statistical validation, not just internal numbers.

---

## 8. 🏗️ System Architecture — 7 Layers

The system is organized into 7 logical layers. Each layer has a clear
responsibility and a single source of truth.

```
┌──────────────────────────────────────────────────────────────────┐
│ LAYER 7 — VISUALIZATION                                          │
│   dashboard.py (1,898+ lines, single-file Streamlit)             │
│   3 pages: Home · Daily Summary · Timeline Archive               │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────┐
│ LAYER 6 — MAINTENANCE & AUTOMATION                               │
│   monthly_rotation.py · prepare_next_month.py                    │
│   warm_oauth_token.py · backup_manager.py                        │
│   mark_score_version.py (one-shot)                               │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────┐
│ LAYER 5 — HEALTH & AUDIT                                         │
│   health_audit.py (18 checks, 3×/day, emails)                    │
│   health_check.py · daily_audit.py · morning_health_check.py     │
│   code_auditor.py (drift detection)                              │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────┐
│ LAYER 4 — COLLECTION & ENRICHMENT                                │
│   post_analysis_collector.py (v5)                                │
│   enrich_post_analysis.py · enrich_data.py                       │
│   backfill_ohlc.py · backfill_fundamentals.py                    │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────┐
│ LAYER 3 — ENGINE                                                 │
│   auto_scanner.py (1,317 lines, main scan + EOD logic)           │
│     ├─ run_scan()          (every-minute scan)                   │
│     ├─ run_eod()           (end-of-day snapshot)                 │
│     ├─ analyze_ticker()    (per-stock scoring)                   │
│     ├─ update_portfolio_live()                                   │
│     ├─ update_ticker_follow_up()                                 │
│     ├─ update_live_trades()                                      │
│     └─ sync_score_tracker()                                      │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────┐
│ LAYER 2 — DATA PROVIDERS (abstraction)                           │
│   data_provider.py (abstract base + factory)                     │
│   providers/alpaca_provider.py    [PRIMARY, paper trading]       │
│   providers/yfinance_provider.py  [FALLBACK + fundamentals]      │
│   validate_providers.py           [A/B comparison tool]          │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────┐
│ LAYER 1 — FOUNDATION                                             │
│   config.py (single source of truth for all constants)           │
│   formulas.py (18 metric calculation functions)                  │
│   utils.py (shared utilities, time, parsing)                     │
│   sheets_manager.py (monthly rotation, multi-month config)       │
│   gsheets_sync.py (post_analysis I/O)                            │
└──────────────────────────────────────────────────────────────────┘
```

### Architecture principles

1. **No formula duplication.** All metrics flow through `formulas.py`.
   This was enforced after early bugs where the same formula existed in
   `auto_scanner` and `dashboard` and diverged silently.

   **Allowed exception (TASK-180.1, 2026-06-17) — cross-repo detector mirror:** the
   interday split/halt detector (`is_interday_artifact` + `flag_interday_artifact_chain`)
   is intentionally duplicated from `formulas.py` (the canonical) into the separate
   DropsLab repo (`interday_artifact.py`, a verified mirror). There is no cross-repo
   import at runtime — the two are independent repos, and shared packaging for an
   unvalidated edge would be premature. Drift is prevented by a three-layer guard on
   the DropsLab side: a golden fixture of RH's own detector test cases, a sha256 pin on
   that fixture, and a local-only cross-repo parity test that imports the live RH
   functions and asserts byte-for-byte agreement over every case. Any change to one
   copy MUST be made in lockstep with the other. This is the only sanctioned
   duplication of a `formulas.py` calculator — a bounded, guarded carve-out (like the
   TASK-104 exception to principle #4), not a license for further copies.

2. **Provider abstraction.** Calling code does not know whether prices
   come from Alpaca or yfinance. The active provider is selected via
   `DATA_PROVIDER` env var.

3. **Config centralization.** Every threshold, weight, and tunable lives
   in `config.py`. The `code_auditor.py` flags hardcoded values.

4. **Read-once, write-once for sheets.** No concurrent writers. Monthly
   rotation creates fresh sheets to avoid quota and integrity issues.
   **Allowed exception (TASK-104, 2026-06-04):** system_events has 2 append-only writers — dashboard `log_emergency_stop` (append_row, manual/rare) + `orchestrator_eod._system_events_alert` (safe_append_row, EOD reconciliation). orchestrator.py is READ-ONLY on this tab. Pure appends are atomic server-side (no read-modify-write clobber), so concurrent append-only writers are benign and permitted — the rule targets RMW races, not appends.

5. **Idempotent maintenance.** Monthly rotation, OAuth warming, and
   backups can run multiple times safely.

---

## 9. 🔄 Data Pipeline

```
                    ┌─────────────────────────┐
                    │   FINVIZ (screener)     │
                    │   pre-market filter     │
                    └────────────┬────────────┘
                                 │  every 60s, 08:30-15:00 Peru
                                 ▼
        ┌──────────────────────────────────────────────┐
        │  cron-job.org → GitHub Actions dispatch      │
        │  workflow: auto_scan.yml                     │
        └──────────────────┬───────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────┐
        │  auto_scanner.py · run_scan()                │
        │  ─────────────────────────────────           │
        │  1. fetch FINVIZ (current snapshot)          │
        │  2. for each ticker:                         │
        │       a. fetch Alpaca daily bars (verify)    │
        │       b. fetch yfinance fundamentals         │
        │       c. calculate 7 metrics                 │
        │       d. calculate Score v2                  │
        │  3. write to multiple sheets                 │
        └──────────────────┬───────────────────────────┘
                           │
        ┌──────────────────┴──────────────────────────────────────┐
        │                                                          │
        ▼                                                          ▼
 timeline_live      portfolio_live     score_tracker    ticker_follow_up
 (every scan)       (live tracking)    (5-min sample)   (5-day journey)


At 16:05 Peru (post-close + 5 min for FINVIZ to settle):

        ┌──────────────────────────────────────────────┐
        │  workflow: post_analysis.yml                 │
        │  ───────────────────────────────             │
        │  Step 1: auto_scanner.py --eod               │
        │   → daily_summary, EOD portfolio close       │
        │  Step 2: post_analysis_collector.py          │
        │   → fetch 5-day OHLC for today's hits        │
        │   → calculate TP10_Hit, MaxDrop, etc         │
        │  Step 3: enrich_post_analysis.py             │
        │   → intraday TP10, D0_Drop%, peak score      │
        │  Step 4: backfill_ohlc_v2.py --recent 2      │
        │   → fill any missing D1-D5, current + prev   │
        │     month (TASK-124 cross-month leak fix)    │
        └──────────────────┬───────────────────────────┘
                           │
                           ▼
                  post_analysis sheet
                  (the research dataset)
                           │
                           ▼
        ┌──────────────────────────────────────────────┐
        │  dashboard.py (Streamlit Cloud)              │
        │  Reads from sheets, displays live            │
        │  3 pages: Home / Daily Summary / Archive     │
        └──────────────────────────────────────────────┘
```

---

## 10. ⏱️ Daily Timeline

All times in **Peru (UTC-5, no DST)**.

```
00:00 ────┬────────────────────────────────────────────────────
          │
00:01 ────┤ 🌙 [1st of month only] monthly_rotation.yml
00:05 ────┤ 🌙 [1st of month only] prepare_next_month.yml
          │
03:00 ────┤
          │
06:00 ────┤ 🩺 health_audit #1 (pre-market check) → email
          │     emails: 🔴/🟡/✅ status
          │
07:00 ────┤ 🔑 [every 3 days] warm_oauth_token.py → email if fail
          │
08:30 ════╪═══════════════════════════════════════════════════
          │ 🟢 NYSE OPEN
          │
08:30 ────┤ ⚡ auto_scanner starts (cron-job.org → GH Actions)
          │   ⚡ scans every minute, ~390 scans/day
          │   ⚡ writes: timeline_live, portfolio_live,
          │              score_tracker, live_trades,
          │              ticker_follow_up
          │
12:00 ────┤ 🩺 health_audit #2 (mid-day check) → email
          │
15:00 ════╪═══════════════════════════════════════════════════
          │ 🔴 NYSE CLOSE
          │
15:00 ────┤ 💼 backup #1 (post_analysis CSV → GH artifact)
16:00 ────┤ 💼 backup #2 (offset cron)
16:05 ────┤ 📊 post_analysis pipeline (4 steps, ~10 min)
          │   ↓ EOD snapshot
          │   ↓ collect 5-day OHLC
          │   ↓ enrich intraday
          │   ↓ backfill missing
          │
16:30 ────┤ 💼 backup #3
          │
22:00 ────┤ 🩺 health_audit #3 (EOD check) → email
          │
24:00 ────┴────────────────────────────────────────────────────

Legend:
  ⚡ = scanner activity (fast)
  🩺 = health check (email-generating)
  💼 = backup (artifact-generating)
  📊 = data collection (sheet-writing)
  🔑 = OAuth maintenance
  🌙 = monthly maintenance (1st of month only)
```

---

## 11. 🌐 Infrastructure

| Component | Provider | Tier | Purpose |
|-----------|----------|------|---------|
| Code hosting | GitHub | Free | Repo, Actions, Secrets |
| Automation | GitHub Actions | Free (~3000 min/mo) | 17 workflows |
| Minute-level cron | cron-job.org | Free | Triggers `auto_scan.yml` |
| Data storage | Google Sheets | Free | All operational data |
| Long-term backup | Google Drive | Free (15 GB) | CSV archives |
| Dashboard hosting | Streamlit Cloud | Free | `ridinghigh-pro-v2.streamlit.app` |
| Market data (primary) | Alpaca | Free (paper) | SIP historical, IEX live |
| Market data (fallback) | yfinance | Free | Fallback prices, fundamentals |
| Screening | FINVIZ | Free (web scrape) | Pre-market filter |
| Email alerts | Gmail SMTP | Free | Health audit notifications |

---

## 12. 🔗 External Dependencies & SLAs

| Service | SLA | Failure Impact | Mitigation |
|---------|-----|----------------|------------|
| **FINVIZ** | none (free scraper) | Scanner blind | None — single point. If down, scanner exits cleanly. |
| **Alpaca** | "best effort" (paper) | No prices for verification | Auto-fallback to yfinance via `data_provider.py` |
| **Yahoo Finance** | none | No fundamentals for new tickers | Stale `data/market_cap_cache.json` used |
| **Google Sheets** | 99.9% per Google | No data persistence | Quota guard in `gsheets_sync.py`; CSV backup 3×/day |
| **GitHub Actions** | varies | No automation | Manual `python3 auto_scanner.py` from local |
| **cron-job.org** | none (free) | No minute-level triggers | `auto_scan.yml` also has internal cron `* * * * *` as backup |
| **Gmail SMTP** | high | No email alerts | Health audit still runs and writes to sheet |
| **Streamlit Cloud** | best-effort free | No dashboard | Sheets accessible directly via Google UI |

### Single points of failure (no fallback)

1. **FINVIZ** — there is no automatic fallback for the screener.
2. **GitHub** — code, secrets, and automation all live there.
3. **Google account** — Sheets, Drive, OAuth all tied to one account.

These are accepted risks for a single-operator research system.

---

## 13. 📊 Data Sources

| Source | Role | Endpoint | Update freq | Cost |
|--------|------|----------|-------------|------|
| **FINVIZ** | Pre-market filter; raw signal source | Web scrape via `finvizfinance` | Real-time | Free |
| **Alpaca** | Verification prices, D1-D5 OHLC | SIP historical, IEX live (paper account) | Real-time | Free (paper) |
| **yfinance** | Fundamentals (Float, Sector, Industry, etc.) + fallback prices | Yahoo public API | Real-time | Free, rate-limited |

### Why three sources

- **FINVIZ** is the only source that provides cheap, real-time market-wide
  screening with the filters we need. It is the funnel.
- **Alpaca** is the price-of-record for the system. Its data feeds
  simulation today and live trades tomorrow (Phase 4).
- **yfinance** provides what Alpaca does not: floatShares, sharesOutstanding,
  sector, industry, P/E, beta. It also serves as failover.

### Critical data quirks

- ⚠️ **FINVIZ Change% comes as a decimal** (e.g., `0.3726`). Must be
  multiplied by 100 to get a percentage. Hardcoded handling in
  `auto_scanner.analyze_ticker()`.
- ⚠️ **yfinance returns NaN in pre-market** for some tickers — handled
  with retries in providers.
- ⚠️ **Alpaca SIP feed has 15-minute delay on free tier for live quotes;
  historical bars are fine.** This is acceptable because the scanner uses
  FINVIZ for live prices and Alpaca only for verification + historical.
- ⚠️ **REL_VOL outliers** can be in the thousands (yfinance bug). Capped
  at 100.0 in `formulas.calculate_rel_vol()` via `REL_VOL_CAP`.

---

## 14. 🗄️ Sheets Architecture

The system uses **9 Google Sheets per month, across 3 active months**.
Each month is a separate Drive folder containing 9 standalone Sheet files
(not tabs in one file).

### Configuration file: `sheets_config.json`

```json
{
  "2026-04": { "timeline_live": "<id>", "daily_snapshots": "<id>", ... 9 keys },
  "2026-05": { ... 9 keys },
  "2026-06": { ... 9 keys, pre-created empty }
}
```

### The 9 sheets per month

| Sheet | Writer | Frequency | Typical row count (full month) |
|-------|--------|-----------|----------------------------------|
| **timeline_live** | `auto_scanner.run_scan()` | every minute during market | ~250,000-300,000 |
| **daily_snapshots** | `auto_scanner` (best per ticker per day) | end of each scan | ~15-20 per day |
| **daily_summary** | `auto_scanner.run_eod()` + scan | every scan + EOD | ~50-60 per day |
| **post_analysis** | `post_analysis_collector.py` | once per day at 16:05 | ~5-15 per day |
| **portfolio** | `auto_scanner.update_live_trades()` | every minute | source-of-truth ל-Score Tracker page + D1-D3 tracking (לא deprecated — נבדק TASK-47); ~5-15/day |
| **portfolio_live** | `auto_scanner.update_portfolio_live()` | every minute | ~5-20 active rows |
| **score_tracker** | `auto_scanner.sync_score_tracker()` | every 5 minutes | ~800-1000 per day |
| **live_trades** | `auto_scanner.update_live_trades()` | every minute (intraday) | ~800-1000 per day |
| **ticker_follow_up** | `auto_scanner.update_ticker_follow_up()` | every minute (5-day journey) | ~5-15 per day |

### Special-purpose sheets (outside monthly rotation)

| Sheet | Purpose | Tabs |
|-------|---------|------|
| **RidingHigh-Health-Audit** | Health check results | History (append) · Latest (overwrite) · Failed (only failing) |
| **MarketCap cache** (local, not Sheet) | `data/market_cap_cache.json` | reduces yfinance calls |

### Monthly rotation behavior

- On the **1st of each month at 00:01 Peru**, `monthly_rotation.yml` runs
  `monthly_rotation.py` to make the new month "active" (writers switch).
- At **00:05 Peru**, `prepare_next_month.yml` runs `prepare_next_month.py`
  to **pre-create** the month after next (e.g., on May 1, it creates June
  ahead of May 31's rollover).
- Both workflows commit `sheets_config.json` back to the repo.
- Old months remain readable (read-only), forming a permanent archive.

### Sheet ownership and sharing

- **Owner:** OAuth user (Amihay) — owns the files in Drive.
- **Editor (writer):** Service account `ridinghigh-sheets-v2@...` —
  shared automatically via `_share_with_service_account()`.
- **Reader (dashboard):** Service account, same as above.

---

## 15. 📋 Data Schema Reference

### `post_analysis` sheet (the research dataset)

This is the **single most important sheet**. Each row = one stock that
scored ≥60 on a given scan day, plus its forward OHLC outcome (D1-D5 for the
classification; extended to D1-D25 for rows scanned on/after 2026-06-13 — see the
TASK-177 note below).

> ⚠️ **Cross-month column drift — read header-aware, never positional (TASK-150).**
> The post_analysis column set AND order are NOT stable across monthly sheets
> (TASK-139-INV phase4):
> - **April = 122 columns; May/June = 105.** April carries 17 legacy-only columns
>   (`Volume, MarketCap, CurrentPrice, EntryChange%, Float_pct_raw, PriceToHigh_raw,
>   PriceTo52WHigh_raw, Score_B..Score_I, EntryScore, Score_recalc_date`) — v1/variant
>   research residue absent from later months.
> - **May vs June = same column set, DIFFERENT order from index 60**: June moved
>   `score_version` ahead of the `IntraHigh` block (May: `…ScoreStd, audit_flag,
>   IntraHigh…score_version` last; June: `audit_flag, score_version, IntraHigh…`).
>
> Any positional reader (fixed column index) breaks across months → read by **header
> name**. `cross_month_loaders.py` already aligns columns by name; route all
> cross-month reads through it. Evidence: `docs/research/INVESTIGATION_2026-06-10/phase4_evidence.md`.

**TASK-177 — extended outcome window (D6-D25, data-only, forward-only).** Rows scanned
**on/after 2026-06-13** (`config.COLLECT_DAYS_FORWARD_FROM`) collect the scan-anchored window
out to **D25**. D1-D5 carry full OHLC and feed the **FROZEN** classification
(`utils` `range(1, 6)`, unchanged); **D6-D25 carry `D{i}_Close` + `D{i}_Low` ONLY (~40 columns)
and are DATA-ONLY** — never read by `calculate_stats`/`classify_trade`. They exist so the
HYP-001 crossover-short hold window can be re-anchored to the drop-event in analysis (TASK-179);
the window itself is locked by **TASK-178**, not here — TASK-177 only guarantees the data
contains it. Rows scanned **before** the cutoff stay D1-D5 (legacy, never re-touched —
forward-only). `SCHEMA.json` (TASK-167) will adopt these columns as canonical.

#### Identity columns
| Column | Type | Source | Description |
|--------|------|--------|-------------|
| `Ticker` | str | FINVIZ | Stock symbol |
| `ScanDate` | str (YYYY-MM-DD) | scanner timestamp | Day stock was scanned |
| `ScanTime` | str (HH:MM) | scanner timestamp | Time of best scan |
| `Score` | float | computed | Composite score 0-100 |
| `score_version` | str | added 2026-05-02 | `'v1'` / `'v2'` (Score era) · **`'v3_scoreless'`** = forward-only scoreless rows — **active since 2026-06-18 (Stage 1 freeze, `5c83eca`)** |

#### Scan-day price/volume
| Column | Type | Source | Description |
|--------|------|--------|-------------|
| `ScanPrice` | float | FINVIZ at scan | Last trade price at scan time |
| `Open` | float | FINVIZ | Day's open |
| `High`, `Low` | float | FINVIZ | Day's range so far |
| `Volume` | int | FINVIZ | Cumulative day volume |
| `MarketCap` | float | FINVIZ + cache | Market cap at scan |
| `Float` | float | yfinance | Float shares |

#### Computed metrics (raw)
| Column | Source | Formula |
|--------|--------|---------|
| `MxV_calc` | `formulas.calculate_mxv` | `(MarketCap - Price×Volume) / MarketCap` |
| `RunUp_calc` | `formulas.calculate_runup` | `(Price - Open) / Open × 100` |
| `ATRX_calc` | `formulas.calculate_atrx` | `(High - Low) / ATR14` |
| `RSI` | `ta.RSIIndicator` | RSI(14) |
| `TypicalPriceDist_calc` | `formulas.calculate_typical_price_dist` | dist from typical price |
| `ScanChange%` | `formulas.calculate_scan_change` | `(Price - PrevClose) / PrevClose × 100` |
| `REL_VOL_calc` | `formulas.calculate_rel_vol` | `Volume / AvgVolume(20d)`, capped at 100 |

#### D0 (scan day) outcomes
| Column | Source | Description |
|--------|--------|-------------|
| `D0_Open` | yfinance/Alpaca | Day's opening price |
| `D0_High` | yfinance/Alpaca | Day's highest price |
| `D0_Low` | yfinance/Alpaca | Day's lowest price |
| `D0_Close` | yfinance/Alpaca | Day's closing price |
| `D0_Volume` | yfinance/Alpaca | Day's total volume |
| `D0_Drop%_from_High` | computed | `(D0_Close - D0_High) / D0_High × 100` |
| `IntraDay_TP10` | enrichment | Boolean — did intraday low hit -10% from scan? |

#### D1-D5 forward outcomes (5 trading days)
For each of D1, D2, D3, D4, D5:
| Column | Description |
|--------|-------------|
| `Dn_Date` | Trading day date |
| `Dn_Open` | Day's open |
| `Dn_High` | Day's high |
| `Dn_Low` | Day's low |
| `Dn_Close` | Day's close |
| `Dn_Volume` | Day's volume |

#### Computed outcomes
| Column | Source | Description |
|--------|--------|-------------|
| `MaxDrop%` | `calculate_max_drop` | Lowest of all 5-day lows vs ScanPrice |
| `TP10_Hit` | computed | `1` if any D1-D5 low ≤ ScanPrice × 0.9 |
| `SL_Hit_D5` | computed | `1` if any D1-D5 high ≥ ScanPrice × 1.1 |
| `TimeToTP10` | computed | Days until TP10 hit (1-5, or 0 if missed) |
| `Status` | computed | `WIN` / `LOSS` / `PENDING` / `OPEN` |
| `NetPnL_SlipOnly` | `calculate_net_pnl` (via calculate_stats) | Net short-PnL fraction after 1%/side slippage, no borrow — WIN/LOSS only, else NULL (TASK-140) |
| `NetPnL_Borrow50` | `calculate_net_pnl` | + borrow 50%/yr pro-rata (rate×resolution_day/365) |
| `NetPnL_Borrow200` | `calculate_net_pnl` | + borrow 200%/yr |
| `NetPnL_Borrow500` | `calculate_net_pnl` | + borrow 500%/yr |

#### Fundamentals (enrichment)
| Column | Source | Description |
|--------|--------|-------------|
| `Sector` | yfinance | Company sector |
| `Industry` | yfinance | Company industry |
| `RealFloat`, `RealFloat_M` | yfinance | Float shares (raw + millions) |
| `MarketCapCategory` | computed | `Micro` (<300M) / `Small` (300M-2B) / `Mid` (2B+) |
| `Price_vs_SMA20` | computed | % distance from 20-day SMA |
| `Consecutive_Up` | computed | Up days before scan |
| `DaysSinceIPO` | computed | Trading days since IPO |

#### Timeline statistics (from timeline_live aggregation)
| Column | Description |
|--------|-------------|
| `FirstScanTime` | First time stock appeared in scan that day |
| `LastScanTime` | Last scan time that day |
| `ScanCount` | How many times scanned that day |
| `ScoreAtFirst` | Score at first scan |
| `ScoreAtLast` | Score at last scan |
| `ScoreMax`, `ScoreMin`, `ScoreStd` | Score statistics across the day |

### Other sheets — abbreviated

**`timeline_live`**: `Date · ScanTime · Ticker · Price · Score · MxV · RunUp · REL_VOL · all 7 metrics`

**`daily_snapshots`**: One row per ticker per day (best snapshot of the day) — same columns as timeline_live but deduplicated.

**`daily_summary`**: `Date · TotalScans · UniqueStocks · AvgScore · TopScore · etc.`

**`portfolio`**: `Ticker · ScanDate · EntryPrice · Status · PnL · ExitDate · ExitPrice`

**`portfolio_live`**: `Ticker · EntryPrice · CurrentPrice · RunningHigh · RunningLow · Status · DropPct`

**`score_tracker`**: `Ticker · ScanDate · ScanTime · Score · Price` (5-min sample)

**`live_trades`**: Same as portfolio but minute-by-minute intraday simulation.

**`ticker_follow_up`**: 5-day journey of selected tickers — `Ticker · ScanDate · D0/D1/D2/D3/D4/D5 prices and outcomes`.

### Agent sheets (Phase 1, added 2026-05-03)

8 additional sheets per month, in the same folder as scanner sheets.
Full schema details in §A6.

| Sheet | Cols | Purpose |
|-------|------|---------|
| `decision_log` | 41 | Every signal evaluated by agent |
| `paper_portfolio` | 25 | Alpaca paper positions mirror |
| `score_analytics` | 25 | Daily/weekly analysis |
| `postmortems` | 17 | Per-position outcome analysis |
| `system_events` | 7 | System events log |
| `pending_suggestions` | 14 | Suggestions awaiting user |
| `config_history` | 10 | Config change audit trail |
| `borrow_data` | 9 | HTB/borrow cache — written by borrow_collector (EOD daily) |
| `borrow_coverage` | 8 | Daily borrow/shortability coverage of the scanned universe (TASK-172) |

---

## 16. 🧬 Data Lineage Examples

### Example 1: How `TP10_Hit` is computed

```
SOURCES:
  ScanPrice  → FINVIZ snapshot (last trade)
                ↓
                logged in timeline_live by auto_scanner.run_scan()
                ↓
                copied to post_analysis on EOD
  D1-D5 OHLC → Alpaca historical bars (provider abstraction)
                ↓
                fetched by post_analysis_collector.fetch_ohlc_for_days()
                ↓ (or)
                fetched by backfill_ohlc.fetch_ohlc() if missed

COMPUTATION:
  formulas.calculate_max_drop(min_low, scan_price)
    where min_low = min(D1.low, D2.low, D3.low, D4.low, D5.low)
  TP10_Hit = (max_drop / scan_price) ≤ -0.10
            = max_drop_pct ≤ -10%

WRITTEN BY:
  post_analysis_collector.py · run() → save_post_analysis_to_sheets()

READ BY:
  dashboard.py · daily_summary_page() and timeline_archive_page()
  Aggregations: hit rate, win rate, conditional win rate by Score tier

VALIDATED BY:
  health_audit.check_05 (post_analysis completeness)
  health_audit.check_07 (score range)
  health_audit.check_09 (duplicates)
```

### Example 2: How `Score` flows from raw FINVIZ to dashboard

```
1. FINVIZ row arrives in auto_scanner.fetch_finviz()
2. analyze_ticker() called per-row
3. Raw fields parsed: parse_market_cap, parse_volume (utils.py)
4. Each metric calculated via formulas.py (no inline math)
5. calculate_score(metrics) returns 0-100
6. Row written to timeline_live with all metrics + Score
7. Best-of-day deduplication writes to daily_snapshots
8. EOD collector writes selected rows to post_analysis
9. Dashboard reads cached_post_analysis() and renders
```

### Why lineage matters

When debugging an anomaly like *"Why did ticker X get Score 100?"*, the
lineage tells you exactly which file to read first (formulas.py:380),
which input was responsible, and which sheet to inspect for raw values.

---

## 17. 📖 Glossary

| Term | Definition |
|------|------------|
| **D0** | Scan day itself (the day a signal fired) |
| **D1, D2, ..., D5** | The 1st through 5th *trading day* after D0 (skips weekends/holidays) |
| **Table A** | Trade simulation entering at `ScanPrice` (EOD of D0) |
| **Table B** | Trade simulation entering at `D1_Open` |
| **TP / TP10** | Take Profit at -10% from entry (short position win) |
| **SL** | Stop Loss at +10% from entry (short position loss). Was +7% before #1 closed. |
| **MxV** | Market cap minus dollar volume, normalized — measures illiquidity vs market cap |
| **RunUp** | Intraday percentage gain from open to current price |
| **ATRX** | Today's range (High-Low) divided by ATR14 — volatility multiplier |
| **REL_VOL** | Today's volume divided by 20-day average volume (capped 100) |
| **VWAP** | Volume-weighted average price; "VWAP_dist" is % distance from typical price |
| **ScanChange%** | Percent change since previous close at scan time |
| **Float%** | Volume as a percentage of float shares — turnover metric |
| **Score** | Composite 0-100 weighted index of 7 metrics (Score v2) |
| **Score v1 / v2** | v1 = pre-2026-04-11 formula (deprecated). v2 = current. See ADR-001. |
| **Critical / High / Med / Low** | Score tiers: 85+/60-84/40-59/<40 |
| **TRADE_ENTRY_MIN_SCORE** | 70 — minimum score for a stock to enter portfolio simulation |
| **MIN_SCORE_DISPLAY** | 60 — minimum score to appear in "top picks" views |
| **EOD** | End of Day — process run after market close at 16:00-16:30 Peru |
| **Phase 1/2/3/4** | System lifecycle phases (data accumulation → real money) |
| **Paper trading** | Simulated trades on Alpaca with no real money |
| **SIP / IEX** | US market data feeds. SIP = consolidated. IEX = single venue. |
| **PK** | Project Knowledge — this document, source of truth for Claude |
| **ADR** | Architecture Decision Record — see §30 |

---

## 18. 🧮 Score v2 — Weights, Caps, Formula

> **⚠️ Removal in progress (forward-only, ADR-009).** DECISION: Score leaves every
> decision role. **Stage 1 freeze landed (`5c83eca`, 2026-06-18): `SCORE_WRITE_FROZEN=True`**
> — scoreless-DATA era active (daily_snapshots + 6 output sheets now write `""`). Score is
> **still computed in-memory** and still selects entries (>=70 / sort / idxmax) until
> Stage 2+141; `calculate_score` not yet removed. The formula below is live for that
> in-memory compute + interpreting frozen history.

### Weights (sum must = 100)

```python
SCORE_WEIGHTS_V2 = {
    "MxV":        25,   # illiquidity signal
    "RunUp":      25,   # pump intensity
    "ATRX":       20,   # volatility expansion
    "RSI":        10,   # overbought (with bell curve, peak 50-70)
    "VWAP":       10,   # price above VWAP
    "ScanChange": 5,    # change since prev close
    "REL_VOL":    5,    # unusual volume
}
```

### Caps (per-metric thresholds for max contribution)

```python
SCORE_CAPS_V2 = {
    "MxV":        200,  # abs value (scores based on |mxv|/200)
    "RunUp":      30,   # percentage
    "ATRX":       5,    # ratio (range/ATR)
    "RSI_HIGH":   70,   # bell curve center high
    "RSI_LOW":    60,   # bell curve center low
    "VWAP":       8,    # percentage
    "ScanChange": 60,   # percentage
    "REL_VOL":    15,   # ratio (for score contribution; raw cap is 100)
}
```

### Score tiers (display)

| Range | Tier | Color | Action |
|-------|------|-------|--------|
| 85+ | Critical | 🔴 Red | Strongest signal — flagged |
| 60-84 | High | 🟠 Orange | Above display threshold |
| 40-59 | Medium | 🟡 Yellow | Watch only |
| 0-39 | Low | ⚪ Gray | Filtered out |

### Action thresholds

| Constant | Value | Meaning |
|----------|-------|---------|
| `MIN_SCORE_DISPLAY` | 60 | Minimum to show in dashboards |
| `TRADE_ENTRY_MIN_SCORE` | 70 | Minimum to enter portfolio simulation |
| `CRITICAL_SCORE` | 85 | Critical tier |
| `HIGH_SCORE` | 60 | High tier |
| `MEDIUM_SCORE` | 40 | Medium tier |

### Score v2 formula (high level)

```
For each metric m in [MxV, RunUp, ATRX, RSI, VWAP, ScanChange, REL_VOL]:
    contribution_m = weight_m × clamp_to_cap(raw_m, cap_m)
score = sum(contributions) capped at 100
```

The exact implementation lives in `formulas.calculate_score()` and is the
**canonical authoritative version**.  Any other implementation anywhere
in the codebase is a bug.

---

## 19. ⚗️ Formulas Inventory

Source: `formulas.py` (v2.0, 470 lines). **Single source of truth for all
metric calculations.** Importing from anywhere else is forbidden.

| # | Function | Returns | Used by |
|---|----------|---------|---------|
| 1 | `calculate_mxv(market_cap, price, volume)` | float (negative for pumps) | scanner, collector |
| 2 | `calculate_runup(price, open_price)` | percentage | scanner, collector |
| 3 | `calculate_atrx(high, low, atr)` | ratio | scanner, collector |
| 4 | `validate_atrx(atrx, atr, price)` | float (atrx, or 0 on bad-data pattern) | scanner |
| 5 | `calculate_gap(open_price, prev_close)` | percentage | scanner (display only — Gap removed from v2 score) |
| 6 | `calculate_typical_price_dist(price, high, low)` | percentage | scanner |
| 7 | `calculate_vwap_dist(price, high, low)` | percentage | scanner (alias of typical price dist) |
| 8 | `calculate_rel_vol(volume, avg_volume)` | ratio (capped at REL_VOL_CAP=100) | scanner, collector |
| 9 | `calculate_float_pct(float_shares, shares_outstanding)` | percentage | scanner |
| 10 | `calculate_price_to_high(price, high_today)` | percentage | dashboard |
| 11 | `calculate_price_to_52w_high(price, high_52w)` | percentage | dashboard |
| 12 | `calculate_scan_change(price, prev_close)` | percentage | scanner |
| 13 | `calculate_drop_from_high(current_price, intraday_high)` | percentage | live tracking |
| 14 | `calculate_max_drop(min_low, scan_price)` | percentage | post_analysis |
| 15 | `calculate_d1_gap(d1_open, scan_price)` | percentage | post_analysis |
| 16 | `calculate_pnl_pct(entry_price, exit_price, is_short=True)` | percentage | portfolio |
| 17 | `normalize_mxv(mxv, min_val=-5000, max_val=0)` | normalized 0-100 | helper (imported, no call-sites — unused) |
| 18 | `normalize_atrx(atrx, min_val=0, max_val=50)` | normalized 0-100 | helper (imported, no call-sites — unused) |
| 19 | `calculate_net_pnl(scan_price, classification, resolution_day, borrow_annual_rate, slip=SLIP)` | fraction | post_analysis |
| 20 | `wilson_ci(successes, n, z=1.96)` | (low, high) fractions [0,1] | small-n honesty on rates (TASK-169 AC#1; rendered via `fmt_rate_ci` — AC#2 done) |
| 21 | `is_interday_artifact(prev_close, next_close, threshold_pct=None)` | bool | split/halt flag; post_analysis collector + cross_month_loaders + health_audit (mirrored in DropsLab — see §10 principle #1 exception) |
| 22 | `flag_interday_artifact_chain(closes, threshold_pct=None)` | (bool, `"D{i}->D{i+1}"`) | ANY-pair split/halt scan over a close chain; delegates the threshold to `is_interday_artifact` (mirrored in DropsLab) |
| 23 | `fmt_rate_ci(successes, n, decimals=1)` | str e.g. `"53.8% [35–71%]"` | display formatter: proportion rate + Wilson 95% CI (TASK-169 AC#2); delegates to `wilson_ci`, `n<=0`→`"—"`; used in dashboard + email WinRate. **Inferential proportions only** — never means/counts/census/price-% |
| **+** | `calculate_score(metrics)` | float 0-100 | scanner, dashboard — **scheduled for removal forward-only (ADR-009, Stage 2)**; not yet retired |

> **Companion module `ta_helpers.py`** (the one documented exception to "formulas.py only" — pandas+ta based, so it cannot live in scalar-only formulas.py): `rsi14_wilder(close, ...)` + `atr14_wilder(high, low, close, ...)` — canonical 14-period Wilder RSI/ATR (TASK-137), shared by the D0 scan path and ticker_follow_up.

### Why this module exists

Before `formulas.py` was created (2026-04-17), the same formulas existed
inline in `auto_scanner.py` AND `dashboard.py` AND `post_analysis_collector.py`
— frequently with subtle differences (ATRX as ratio in one place,
percentage in another). This created silent bugs.

The rule now: **any metric calculation lives in formulas.py or it is a bug.**
`code_auditor.py` enforces this with the `audit_formulas()` check.

---

## 20. 💼 Trade Simulation

### The simulated short trade

For each stock with Score ≥ `TRADE_ENTRY_MIN_SCORE` (70):

```
Entry:    BORROW & SELL at entry price (short)
              ↓
              wait up to 5 trading days (D1-D5)
              ↓
Outcome is classified day-by-day, D1->D5, by utils.classify_trade().
The first day that resolves the trade decides it:
  Day's Low ≤ entry × 0.90  AND  Day's High < entry × 1.10  →  WIN
  Day's High ≥ entry × 1.10 AND  Day's Low  > entry × 0.90  →  LOSS
  Same day hits BOTH                                        →  WHIPSAW
  5 full days, neither hit                                  →  NO_TOUCH
  Fewer than 5 settled days                                 →  PENDING

Position size: fixed $1,000 per trade.
```

### Why day-by-day classification (added v2.16)

The old rule — "intraday_low ≤ entry×0.90 anywhere in D1-D5 ⇒ WIN" —
overstated performance. A row could touch -10% on D5 yet have already
hit the +10% SL on D1: a real-world LOSS scored as a WIN. A 2026-05-18
investigation found this inflated the dashboard win-rate to ~81%.

classify_trade() walks the days in order and stops at the first day that
resolves the trade. WHIPSAW (both ends same day) is genuinely unresolvable
with daily bars — it is NOT counted as win or loss, but flagged for
investigation (entry-timing / entry-point problem). NO_TOUCH is likewise
flagged (why was this stock entered at all?). Only WIN and LOSS feed the
win-rate. On 58 fully-settled trades the true win-rate is ~58%
(vs the ~81% the old metric reported).

### Two parallel tables

| Table | Entry price | Reasoning |
|-------|-------------|-----------|
| **Table B** | `D1_Open` (next-day open) | ✅ **OFFICIAL** (executable, gap risk included) — TASK-142 |
| **Table A** | `ScanPrice` (EOD of scan day) | ⚠️ **DIAGNOSTIC** (theoretical peak-Score hindsight — inflates WR) |

Historical performance gap: Table A vs Table B — see ADR-003. (NOTE: earlier ~80%/~60% figures used the inflated TP10-as-win metric; recompute with classify_trade — true Table-A win-rate ~58%.)
The difference is itself useful data (shows magnitude of overnight gap risk).

### Official WR basis & WHIPSAW dual reporting (TASK-142 + TASK-147, 2026-06-12)

The official "real Win Rate" headline (Post Analysis + Home pages) now classifies on the
**executable `D1_Open` entry**, not `ScanPrice`. The ~58% figure above and the Table-A
narrative were on the inflated ScanPrice basis (run_eod peak-Score, +13.6pp look-ahead);
executable WR is ~53–55%. Implementation: `classify_trade(scan_price, ohlc, entry_price)`
+ `classify_trade_row(row, entry_basis="D1_Open")` — the core WIN/LOSS/WHIPSAW mapping is
**unchanged**, only the TP/SL anchor moves to the entry; a missing `D1_Open` → PENDING (never
a silent ScanPrice fallback). Table A is demoted to diagnostic; Table B is official.

**WHIPSAW dual reporting (TASK-147):** every official WR shows the headline (WHIPSAW
excluded, optimistic) PLUS a pessimistic bound counting WHIPSAW as losses in the denominator
(`metrics_bounds.wr_bounds`, policy-layer only — `classify_trade` still returns WHIPSAW).
The expectancy half (live expectancy surface + ScanPrice-NetPnL demotion) is **TASK-162**.

`calculate_net_pnl` is deliberately **not** parametrized by entry: the PnL fraction is
scale-invariant (the entry cancels), so the D1_Open expectancy is carried by the
`(cls, resolution_day)` from `classify_trade` — locked by `tests/test_netpnl_entry_basis_v1.py`.

The Score-research "Win Rate" surfaces (the `TP10_Hit` window-touch metric off ScanPrice, in
the Score Comparison page) were renamed **"TP10 Hit-Rate"** to disambiguate from the official
D1_Open WR. `§15` data schema is unchanged (no new columns).

### Status colors (in dashboard)

| Color | Meaning |
|-------|---------|
| 🟢 Green | TP10 hit — WIN |
| 🔴 Red | SL hit — LOSS |
| 🟡 Yellow | Pending / Open (no exit yet) |
| ⚪ Gray | Closed at timeout (D5) |

### `portfolio_live` real-time tracking

During market hours, every minute:
- Fetch current price for every Open position
- Update `RunningHigh`, `RunningLow`
- If TP/SL levels crossed → close position with appropriate status
- Calculate intermediate `PnL` for live display

---

## 21. ⚙️ 8 Workflows

All workflows live in `.github/workflows/`. Times shown are Peru (UTC-5).

### `auto_scan.yml` — the main scanner
- **Trigger:** `cron: '* * * * *'` (every minute, all the time)
- **Filter:** Logic inside `auto_scanner.py` checks `is_market_hours()`
  and exits cleanly outside market window. cron-job.org also sends dispatch
  triggers as redundancy.
- **Steps:** install deps → run `python auto_scanner.py`
- **Env:** `GOOGLE_CREDENTIALS_JSON`, `GOOGLE_OAUTH_TOKEN_JSON`,
  `ALPACA_API_KEY_ID`, `ALPACA_SECRET_KEY`, `DATA_PROVIDER=alpaca`,
  `ALPACA_PAPER=true`
- **Timeout:** 8 minutes
- **Typical runtime:** 30-90 seconds per scan

### `post_analysis.yml` — daily research collector
- **Trigger:** `cron: '5 21 * * 1-5'` (16:05 Peru, Mon-Fri)
- **Steps (sequential):**
  1. `python auto_scanner.py --eod` — EOD snapshot
  2. `python post_analysis_collector.py` — fetch 5-day OHLC
  3. `python enrich_post_analysis.py` — intraday enrichment
  4. `python backfill_ohlc.py` — fill gaps from prior days
- **Env:** Alpaca + Google secrets
- **Timeout:** 15 minutes

### `health_audit.yml` — system health
- **Triggers (3×/day, every day including weekends):**
  - `0 11 * * *` (06:00 Peru, pre-market)
  - `0 17 * * *` (12:00 Peru, mid-day)
  - `0 3 * * *` (22:00 Peru — actually 03:00 UTC next day)
- **Steps:** install deps → `python3 health_audit.py`
- **Env:** Google secrets, Alpaca, Gmail SMTP, REPORT_TO
- **Continues on error:** Yes — failure to send email does not abort report

### `backup.yml` — daily CSV backups
- **Triggers (3 cron times to span the day):**
  - `7 13-21 * * 1-5` (every hour from 08:07 to 16:07 Peru)
  - `7 20 * * 1-5` (15:07 Peru, secondary)
  - `30 21 * * 1-5` (16:30 Peru, post-EOD)
- **Steps:** install deps → `python backup_manager.py`
- **Output:** `backups/post_analysis_*.csv` uploaded as GitHub artifact
  (90-day retention, no Drive quota)

### `monthly_rotation.yml` — month-1 active switch
- **Trigger:** `cron: '1 5 1 * *'` (00:01 Peru, 1st of month)
- **Steps:** `python monthly_rotation.py` → commit `sheets_config.json`
- **Permission:** `contents: write` (pushes to repo)

### `prepare_next_month.yml` — pre-create next-next month
- **Trigger:** `cron: '5 5 1 * *'` (00:05 Peru, 1st of month, after rotation)
- **Steps:** `python3 prepare_next_month.py` → commit config
- **Why offset 4 minutes after rotation:** rotation must complete first

### `warm_oauth_token.yml` — OAuth token preservation
- **Trigger:** `cron: '0 12 */3 * *'` (07:00 Peru, every 3 days)
- **Why:** Google revokes refresh tokens after 7 days inactivity for apps
  in 'Testing' OAuth consent. Periodic refresh prevents this.
- **Failure mode:** sends email with manual renewal instructions if token
  cannot be refreshed.

### `overnight_report_email.yml` — overnight runner morning report (DORMANT)
- **Trigger:** `push` to the `overnight-reports` branch, path `docs/overnight/**`
- **Fires ONLY when the overnight runner is armed** (launchd 02:00 Lima writes the report
  branch). Until armed, never triggers — no impact on the live system.
- **Steps:** checkout `overnight-reports` → `pip install -r requirements.txt` →
  `agent/notifications/email_sender.send_email` of the latest `docs/overnight/REPORT_*.md`
- **Env:** `SMTP_*` / `EMAIL_TO` (GitHub Secrets only); no model call, no Anthropic credential
- **Source:** merged from `feature/overnight-runner` (non-core infra; runbook in `docs/overnight/SCHEDULE_INSTALL.md`)

---

## 22. 📧 Email Alerts

The system sends emails via Gmail SMTP. Recipient: `REPORT_TO` secret.

### Subject line conventions

| Sender | Subject pattern | When |
|--------|-----------------|------|
| **health_audit** | `🔴 RidingHigh Health Audit — N CRITICAL issue(s)` | ≥1 critical check failed |
| **health_audit** | `🟡 RidingHigh Health Audit — N WARNING(s)` | warnings only, no critical |
| **health_audit** | `✅ RidingHigh Health Audit — All clear` | all 18 checks PASSED (heartbeat) |
| **warm_oauth_token** | `🔴 RidingHigh: OAuth Token EXPIRED — Manual Renewal Required` | refresh failed |

### Schedule (when emails arrive in your inbox)

```
Mon-Sun, every day:
  ~06:00 Peru — Health audit pre-market
  ~12:00 Peru — Health audit mid-day
  ~22:00 Peru — Health audit EOD

Every 3 days at ~07:00 Peru:
  → Only if OAuth refresh fails

Ad-hoc (rare):
  → If a critical check failure occurs mid-cycle (currently same as schedule)
```

### Email body structure

Each email contains:
- Summary line (counts of CRITICAL/WARNING/PASSED)
- For each CRITICAL: full description of the failure
- For each WARNING: brief description
- Links: GitHub repo, Google Sheets (Health Audit sheet)
- Heartbeat note: "If you're not seeing these 3×/day, the system is broken."

### Heartbeat mode

The audit always sends an email — even when everything is green — so the
**absence** of an email is itself a signal that something's wrong (e.g.,
GH Actions disabled, Gmail credentials revoked).

---

## 23. 🛡️ 18 Health Checks

Source: `health_audit.py` (1,377 lines). 18 checks across 5 categories.
Each check runs 3×/day; results are written to the **RidingHigh-Health-Audit
Google Sheet** with 3 tabs (History, Latest, Failed) and emailed.

### 📁 Code integrity (3 checks)

| # | Name | What it checks |
|---|------|----------------|
| 01 | duplicate_functions | No function defined in 2+ files |
| 02 | hardcoded_thresholds | Values like 0.10, 60, 70 are imported from config not hardcoded |
| 03 | imports_consistency | Modules consistently import from formulas/utils/config |

### 📊 Data freshness (3 checks)

| # | Name | What it checks |
|---|------|----------------|
| 04 | timeline_freshness | timeline_live has rows from today (during market hours) |
| 05 | post_analysis_completeness | post_analysis filled for prior trading days |
| 06 | github_actions_health | Workflows ran successfully recently (via GH API). Bands: ≥95 PASSED / ≥80 WARNING / <80 would-be CRITICAL. **TASK-96 transient gate** — a <80 result is downgraded to WARNING **only** when SAMPLE-SIZE (`<10` completed runs) **or** RECOVERING (every failing workflow's latest run is green). A persistent true failure — **≥10 completed AND a failing workflow's latest run still red** — stays **CRITICAL → `sys.exit(1)` + red email** (no-masking). Decision in pure `_github_actions_result`; `check_06` is the I/O wrapper |

### 🔢 Data quality (4 checks)

| # | Name | What it checks |
|---|------|----------------|
| 07 | score_range | All Score values between 0-100, no NaN |
| 08 | required_columns | post_analysis schema intact, all expected columns present |
| 09 | duplicate_post_analysis_rows | No (Ticker, ScanDate) duplicates |
| 10 | outliers | No extreme outliers (e.g., D1_Open mean = 80% — see issue #N24) |
| 29 | interday_artifacts | Inter-day split/halt artifacts in post_analysis (`is_interday_artifact`: a `>100%` **upward** close-to-close move — downward moves are out of scope, see TASK-149/168), reports contamination%. Reuses `exclude_interday_artifacts`, which **recomputes** blank/NaN/missing-column legacy rows from the D0-D5 close chain before counting (TASK-182, Path 2) — so the count is complete (research view 4.25%, not the old column-dependent 0.47%). In-memory recompute only; the sheet is never written. |

### ⚙️ Config consistency (3 checks)

| # | Name | What it checks |
|---|------|----------------|
| 11 | sheets_config_current_month | Active month is registered in `sheets_config.json` |
| 12 | score_weights_sum | `SCORE_WEIGHTS_V2` sums to exactly 100 |
| 13 | critical_files | Files like `config.py`, `formulas.py`, `auto_scanner.py` exist |

### 🔐 Repo health (2 checks)

| # | Name | What it checks |
|---|------|----------------|
| 14 | uncommitted_count | (when run locally) low uncommitted file count |
| 15 | gitignore_enforcement | Sensitive files like `google_credentials.json` in `.gitignore` |

### Additional severity-aware checks (3, added later)

| # | Name | What it checks |
|---|------|----------------|
| 16 | rel_vol_stuck | REL_VOL not stuck at constant (provider issue) |
| 20 | float_pct_stuck | Float% not stuck (yfinance staleness) |
| 21 | gap_outliers | Gap outliers flagged (corporate actions filtered) |

### Provider health checks (2)

| # | Name | What it checks |
|---|------|----------------|
| 17 | fundamentals_provider | yfinance fundamentals reachable |
| 18 | daily_bars_provider | Alpaca daily bars reachable |

### Cross-sync check (1)

| # | Name | What it checks |
|---|------|----------------|
| 19 | pk_sync | (Currently checks PK doc consistency — relates to System Overview drift) |

### Severity rules

- **CRITICAL** = system cannot function: trigger immediate red email
- **WARNING** = notable degradation: warning email
- **PASSED** = healthy

A check producing CRITICAL automatically triggers off-cycle alerting.

---

## 24. 📈 KPIs & SLIs

### System health KPIs (operational)

| KPI | Target | Current | Source |
|-----|--------|---------|--------|
| Scanner uptime during market hours | ≥99% | TBD (target check_19) | `timeline_live` row counts |
| post_analysis fill rate (D5 within 7 days) | ≥95% | TBD | `health_audit.check_05` |
| Email delivery latency | <5 min from cron | not measured | manual |
| Dashboard load time (typical query) | <3s | not measured | manual |
| Sheets quota usage (peak) | <70% of cap | not measured | not yet instrumented |
| GitHub Actions minutes used | <50% of 3000/mo free | TBD | GH billing |

### Research / analysis KPIs

| KPI | Target | Current (2026-05-02) | Status |
|-----|--------|----------------------|--------|
| v2 records accumulated | ≥100 | 50 | ❌ 50% |
| Trading days of v2 data | ≥30 | ~14 | ❌ ~47% |
| TP10 hit rate (v2) | ≥70% (acceptable) | 75.0% | ✅ |
| TP10 hit rate (all, including v1) | reference only | 80.6% | informational |
| Score-TP10 correlation | r ≥ 0.20 (target) | 0.045 | ❌ near zero (ADR-002) |
| MaxDrop% median (v2) | < -15% | TBD (overall: -20.63%) | likely ✅ |
| % records hitting -10%+ drop | ≥70% | 80.6% | ✅ |

### Benchmarks (to contextualize numbers)

| Reference | TP10-equivalent rate |
|-----------|----------------------|
| Random selection (50/50 stocks) | ~50% over 5 trading days, ATR-scaled |
| S&P 500 average daily decline | ~5% chance of -10% drop in 5 days |
| Filtered pump screen (industry standard) | ~60-70% mean reversion within 5 days |
| Our system | **80.6% all / 75.0% v2** |

The high rate is consistent with academic literature on the "shorting
premium" of recent extreme winners. The novelty here is operational
infrastructure for systematic capture.

---

## 25. 🔒 Secrets Inventory

### What secrets exist

| Secret name | Stored where | Used by | Rotation policy |
|-------------|--------------|---------|-----------------|
| `GOOGLE_CREDENTIALS_JSON` | GitHub Secrets + `~/RidingHighPro/google_credentials.json` (gitignored) | All sheets I/O | Annual or on compromise |
| `GOOGLE_OAUTH_TOKEN_JSON` | GitHub Secrets only | Drive API (sheet creation, file move) | Auto-warmed every 3 days |
| `ALPACA_API_KEY_ID` | GitHub Secrets only | Alpaca provider | On compromise |
| `ALPACA_SECRET_KEY` | GitHub Secrets only | Alpaca provider | On compromise |
| `GMAIL_USER` | GitHub Secrets only | Email sending | On compromise |
| `GMAIL_APP_PASS` | GitHub Secrets only | Email sending | Annual or on compromise |
| `REPORT_TO` | GitHub Secrets only | Email recipient | When email changes |

### Where they are NOT stored

- ❌ Never in committed code
- ❌ Never in PR descriptions, issues, or commits
- ❌ Never in `.env` files in the repo
- ❌ Never in chat with Claude (Claude must refuse if asked to write a file
  with credentials)

### Local credentials handling

- `~/RidingHighPro/google_credentials.json` is the local copy of the
  service account key (mirror of secret). It is in `.gitignore`.
- The check `health_audit.check_15 (gitignore_enforcement)` verifies this.
- Backups in `backups/` are also gitignored.

### Rotation procedure (when needed)

1. Generate new credential at provider (Google IAM / Alpaca dashboard)
2. Update GitHub Secret
3. Update `~/RidingHighPro/google_credentials.json`
4. Trigger `warm_oauth_token.yml` manually to verify
5. Watch next health audit email — should be 🔴 if revocation incomplete
6. Revoke old credential at provider

---

## 26. 💾 Recovery & Disaster Recovery (DR)

### Recovery Point Objective (RPO)
The maximum acceptable data loss is **1 trading day**.

### Recovery Time Objective (RTO)
The maximum acceptable downtime is **48 hours** (one weekend).

### Recovery assets (where everything lives)

| Asset | Primary | Backup | Tertiary |
|-------|---------|--------|----------|
| **Code** | GitHub repo (canonical) | Local clone on Mac | None needed |
| **post_analysis data** | Google Sheets | CSV in `backups/` | GitHub artifacts (90-day) |
| **Other sheets** | Google Sheets | Manual export only | None |
| **Secrets** | GitHub Secrets | (TBD: 1Password recommended) | None |
| **OAuth token** | Auto-refreshed every 3 days | Manual re-grant via `get_oauth_token.py` | None |

### Disaster scenarios & playbooks

#### Scenario 1: Local Mac dies
- **Impact:** None to running system. Local-only tooling unavailable.
- **Recovery:**
  1. Get a new Mac
  2. `git clone https://github.com/projects5069-creator/ridinghigh-pro.git ~/RidingHighPro`
  3. Copy `google_credentials.json` from secure backup
  4. `pip install -r requirements.txt`
  5. Test: `python3 morning_health_check.py`
- **RTO:** ~2 hours

#### Scenario 2: Sheet accidentally deleted
- **Impact:** Loss of in-progress data; backups partial.
- **Recovery:**
  1. Check Google Drive trash (30-day recovery window)
  2. If lost: restore from CSV backup in `backups/` or GH artifact
  3. Use `migrate_*` scripts to repopulate if available
- **RTO:** 4-8 hours

#### Scenario 3: GitHub Actions disabled / repo wiped
- **Impact:** No automation. Existing data preserved in Sheets.
- **Recovery:**
  1. Re-create repo from local clone: `git push <new-url>`
  2. Re-add all secrets
  3. Re-enable Actions
  4. Trigger workflows manually to verify
- **RTO:** 4 hours

#### Scenario 4: Google account suspended
- **Impact:** Catastrophic. Sheets, Drive, OAuth all gone.
- **Recovery:**
  1. Recover Google account or create new
  2. Set up new project, service account
  3. Re-create sheet structure (`prepare_next_month.py` for one month)
  4. Update `sheets_config.json` and `google_credentials.json`
  5. Historical data in CSV backups (≤90 days) and Drive backups (manual)
- **RTO:** 24-48 hours
- **Data loss:** Anything not backed up to CSV. Up to weeks of in-Sheets data
  not captured by `backup_manager.py`.

#### Scenario 5: Alpaca account suspended
- **Impact:** Provider abstraction kicks in — yfinance fallback engages.
- **Recovery:** Sign up for new Alpaca paper account; update env vars.
- **RTO:** 1 hour
- **Note:** No data loss; system runs on yfinance during outage.

---

## 27. 💰 Cost & Quota Tracking

### Monthly costs

```
GitHub Actions:      ~3,000 min/mo free  →  using ~XX min/mo (TBD measure)
GitHub Storage:      500 MB free          →  artifacts ~100 MB
Sheets API:          100 reads/100s/proj  →  peak XX% (TBD)
Drive Storage:       15 GB free           →  using <1 GB
Alpaca:              FREE (paper)         →  $0
yfinance:            FREE (rate-limited)  →  $0
cron-job.org:        FREE tier            →  $0
Streamlit Cloud:     FREE tier            →  $0

TOTAL MONTHLY COST: $0
```

### Quota risks (potential cost triggers)

1. **GitHub Actions minutes** — workflows that fail and retry (e.g.,
   intermittent Alpaca outages) could double minute consumption. At ~390
   minute-scans/day × 30 days = ~11,700 scan invocations. Free tier ends
   at 3,000 min/mo. Each scan averages ~30s = 0.5 min → ~5,850 min/mo.
   ⚠️ **Already over free tier** if we count strictly.
   *Mitigation:* GitHub appears to bill in seconds for free tier. Verify
   billing dashboard monthly.

2. **Sheets API reads** — dashboard refreshes hit Sheets. With 5-min cache,
   risk is low. Without cache, dashboard could hit quota under sustained
   load.

3. **Drive backups** — CSV backup files accumulate. `backup_manager.py`
   should rotate (TBD verify).

### Cost monitoring

- ❌ No automated cost monitoring exists.
- ✅ Health audit `check_06 (github_actions_health)` indirectly catches
  workflow failures.
- ⚠️ TODO: add `check_quota_usage` to health audit.

---

## 28. 🚨 Operational Runbook

### Common incidents and responses

#### Incident: 🔴 Health audit email shows CRITICAL

**Diagnosis steps:**
1. Open the email, identify which check(s) failed
2. Open the **RidingHigh-Health-Audit** Sheet → "Failed" tab for details
3. Look at "Latest" tab for full system state

**Common critical issues:**

| Failure | Likely cause | Fix |
|---------|--------------|-----|
| `check_04 timeline_freshness` | Scanner stopped | Check GH Actions tab in repo. Re-trigger `auto_scan.yml` manually. |
| `check_05 post_analysis_completeness` | Collector failed yesterday | Manually run `python post_analysis_collector.py` from local. |
| `check_07 score_range` | Bad data in v2 records | Investigate ticker/date in the email. May indicate provider returned NaN. |
| `check_12 score_weights_sum` | Someone edited config.py wrong | `cat config.py | grep -A 10 SCORE_WEIGHTS_V2` — verify sums to 100 |
| `check_13 critical_files` | A core file was deleted | Check `git log` recent commits |

#### Incident: 🔴 OAuth Token EXPIRED email

**Cause:** `warm_oauth_token.py` could not refresh.

**Steps:**
1. SSH/local to Mac
2. `cd ~/RidingHighPro && python3 get_oauth_token.py`
3. Follow browser flow → grant access → token saved to local file
4. Copy token JSON content
5. Update GitHub Secret `GOOGLE_OAUTH_TOKEN_JSON`
6. Trigger `warm_oauth_token.yml` manually to verify
7. Wait for next health audit — should clear

#### Incident: No emails arriving

**Diagnosis:**
1. Check spam folder
2. Check Gmail "All Mail" — search for "RidingHigh"
3. Check `health_audit.yml` in GitHub Actions tab — recent runs?
4. If runs are succeeding but no email: secret `GMAIL_APP_PASS` may be
   revoked. Generate new app password at Google Account → Security.

#### Incident: Sheets quota exceeded (rare)

**Symptoms:** `health_audit.check_04/05/07` failures showing "API quota
exceeded" type errors.

**Steps:**
1. Wait 100 seconds (the API limit window)
2. Retry the failing operation manually
3. If recurring, reduce dashboard refresh frequency or increase cache TTL

#### Incident: post_analysis missing today's row

**Causes:**
- Scanner didn't run today (check_04 should have caught it)
- Collector failed (check_05 should have caught it)
- Today's stocks all had Score < 60 (legitimate)

**Steps:**
1. Check `daily_summary` for today — were there any stocks?
2. If yes but post_analysis empty: run `python post_analysis_collector.py`
3. Then `python backfill_ohlc.py` to fill any missing OHLC

#### Incident: Dashboard shows stale data

**Causes:**
- Cache hasn't expired (5 min default)
- Streamlit Cloud cold-started

**Steps:**
1. Click "🗑️ Clear Local Cache" button in sidebar
2. Click "🔄 Refresh data"
3. If still stale: check sheet directly in Google Drive

### Manual interventions

| Task | Command |
|------|---------|
| Run scanner now | `python3 auto_scanner.py` (from local) |
| Run EOD now | `python3 auto_scanner.py --eod` |
| Run health audit now | `python3 health_audit.py --local` |
| Run post-analysis collector for specific date | `python3 post_analysis_collector.py --date 2026-04-30` |
| Backfill OHLC for old rows | `python3 backfill_ohlc.py` |
| Verify all sheets exist for current month | `python3 sheets_manager.py` (functions are importable) |
| Audit code for drift | `python3 code_auditor.py` |


---

## 29. 📊 Performance Statistics (as of 2026-05-02 — snapshot, not auto-refreshed)

*Generated from live Sheets query, 2026-05-02.*

### Volume statistics

| Metric | Value |
|--------|-------|
| Total scans logged (Apr+May) | **283,677** |
| Unique trading days | **18** (Apr 23 first daily_snapshot → May 1) |
| Average scans per trading day | **15,760** |
| Peak: timeline_live April | 263,774 rows |
| post_analysis records (total) | **156** |
| post_analysis (Score v1, deprecated) | 104 |
| post_analysis (Score v2, valid) | **52** (50 April + 2 May) |
| Active months in config | 3 (Apr/May/Jun) |
| Active sheets per month | 9 |
| Total sheets | 27 |

### Trade simulation outcomes

| Metric | Value (all) | Value (v2 only) |
|--------|-------------|-----------------|
| TP10 hit rate | 80.6% | **75.0%** |
| Records analyzed | 154 | 50 |
| Score-TP10 correlation (Pearson r) | 0.053 | **0.045** |

### Drop magnitude statistics

| Metric | Value (all v1+v2) |
|--------|-------------------|
| Mean MaxDrop% | -22.58% |
| Median MaxDrop% | -20.63% |
| % records hitting -10%+ | **80.6%** |

### Entry timing

| Metric | Value |
|--------|-------|
| Mean D1_Open vs ScanPrice gap | **+80.84%** (⚠️ outlier-distorted, see #N24) |
| Median D1_Open vs ScanPrice gap | **-4.24%** |

The mean is wildly distorted by ≥1 outlier. The median (-4.24%) is the
representative value: stocks open ~4% lower than scan EOD, which is
favorable for shorts (already partial profit at open).

### Date range covered

```
2026-03-23  →  2026-05-02   (full active range)
2026-04-13  →  2026-05-02   (Score v2 valid range)
```

---

## 30. 📜 Architecture Decision Records (ADRs)

ADRs document **why** key technical decisions were made. They never get
deleted; superseded ones are marked but kept for context.

### ADR-001: Score v1 → v2 transition (2026-04-11, commit f3d96ca)

- **Status:** Accepted
- **Context:** Score v1 (the original) had 7 metrics with weights:
  MxV=30, RunUp=20, REL_VOL=20, RSI=10, ATRX=10, Gap=5, VWAP=5.
  Empirical correlation analysis suggested some weights were misaligned.
- **Decision:** Replace with Score v2:
  MxV=25, RunUp=25, ATRX=20, RSI=10, VWAP=10, ScanChange=5, REL_VOL=5.
  Drop Gap entirely. Add ScanChange. Reweight ATRX up, REL_VOL down.
- **Consequences:**
  - 104 historical records (v1) became inconsistent with new formula
  - Per ADR-005, rather than recompute, those records were tagged
    `score_version='v1'` for filtering.
  - Score predictive power did not improve (r still ~0.05).
- **Status notes:** v1 records remain in post_analysis but are filtered
  out of all primary analyses by `score_version='v2'` filter.

### ADR-002: Why Score-TP10 correlation is near zero, even with v2

- **Status:** Active investigation
- **Context:** TP10 hit rate is 75-80% (excellent), but
  correlation between Score and TP10 is r≈0.05 (essentially zero).
- **Working theory:** The **filter** (`MIN_SCORE_DISPLAY=60` and
  fundamental gates like min volume, min market cap) is doing the
  predictive work. Once a stock passes the filter, its specific Score
  inside the 60-100 range carries no additional signal.
- **Implication:** Score may need redesign in v3 (or eliminated as
  a binary flag rather than a continuous predictor).
- **Action:** Phase 1 must continue accumulating v2 data to confirm.
  Score v3 design deferred.

### ADR-003: Two trade tables (A vs B)

- **Status:** Accepted
- **Context:** Need to model both theoretical (enter at scan) and
  realistic (enter at next-day open) trade outcomes.
- **Decision:** Maintain two parallel simulations.
- **Consequences:** Table A's outperformance over Table B (~80% vs ~60%)
  quantifies overnight gap risk — useful for sizing real Phase 4 trades.

### ADR-004: Tag instead of recompute v1 records (2026-05-02, commit bf2892b)

- **Status:** Accepted (Issue #N10 closed)
- **Context:** 104 of 156 records (67%) had Score v1. Fully recomputing
  v2 over 5 sheets would take ~6 hours and risk inconsistencies.
- **Decision:** Add `score_version` column to `post_analysis` and
  `daily_snapshots`. Tag each row v1 or v2. Filter analyses to v2.
- **Consequences:**
  - Raw metrics (MxV, ATRX, RunUp) remain unchanged → still usable for
    primary analyses regardless of tag.
  - 30-minute fix vs 6-hour fix.
  - Idempotent script (`mark_score_version.py`) safe to re-run.
- **Considered alternatives:** Full recompute (rejected: too risky given
  r=0.05 marginal value of Score itself).

### ADR-005: Provider abstraction (data_provider.py, 2026-04-25, Issue #9)

- **Status:** Accepted (Phase 2 of Issue #9)
- **Context:** yfinance was the only data source. Risks: rate limits,
  silent NaN returns, no SLA. For Phase 4 (real money), need an actual
  broker connection anyway.
- **Decision:** Build `data_provider.py` abstract base class with two
  implementations: `AlpacaDataProvider` (primary, paper trading) and
  `YFinanceDataProvider` (fallback + fundamentals).
- **Consequences:**
  - Calling code (collector, scanner, dashboard) is provider-agnostic.
  - Switching provider is one env var change: `DATA_PROVIDER=alpaca`.
  - Testable via `validate_providers.py` A/B comparison.
  - Phase 4 transition will reuse same code, just unset `ALPACA_PAPER`.

### ADR-006: 9 sheets per month, not 1 sheet with 9 tabs

- **Status:** Accepted
- **Context:** Original design used one sheet with multiple tabs.
  Hit Sheets API quota (per-sheet per-100-seconds limits).
- **Decision:** Each "tab" became its own sheet file. Total of 9 per
  month. Folder per month in Drive.
- **Consequences:**
  - Quota distributed across files (each sheet has its own quota bucket).
  - More complex config (one ID per sheet).
  - Easier rotation (pre-create files for next month idempotently).

### ADR-007: TP=10%, SL=10% (changed from SL=7%, Issue #1)

- **Status:** Accepted
- **Context:** Initial design had asymmetric TP=-10%, SL=+7%. Across
  early data, +7% SL was triggered too often by normal noise, causing
  premature losses on positions that would later have hit TP10.
- **Decision:** Symmetric ±10%.
- **Consequences:**
  - Win rate improved (fewer premature SL exits).
  - Position size kept at $1000 (-/+$100 outcomes).
  - SL still overrides TP if both hit same bar (conservative).

### ADR-008: Single-file dashboard.py (not multi-file Streamlit pages)

- **Status:** Accepted (with caveat — see roadmap)
- **Context:** Streamlit supports multi-page apps via `pages/` folder.
  Considered splitting dashboard.
- **Decision:** Keep single-file (currently 1,898+ lines).
- **Reasoning:** State sharing simpler in single-file; no pages/ overhead.
  Three pages (Home / Daily Summary / Timeline Archive) implemented as
  radio button + conditional rendering.
- **Future:** When System Overview page is added (per #N26), may split.

### ADR-009: Score removal — forward-only (2026-06-18)

- **Status:** Accepted; implementation staged (Stage 0 landed `bebd901`; Stages 1-3 pending)
- **Context:** ADR-002 suspected the continuous Score carries no signal — now CONFIRMED
  across two investigations: Score AUC 0.531 [0.43,0.63] n=140, all 7 components dead
  (max MxV 0.552), nothing survives Benjamini-Hochberg; random-in-filter WR .659 ≈
  top-Score-half .629 (p=.56), r(Score,WIN)=-0.02 p=.82 n=123. The "pass-all-filters
  WR .846 vs .608 blocked" value lives in the explicit FILTERS (volume/price/float/
  MxV/...), not in the composite Score number.
- **Decision:** Score is removed from EVERY decision role — gate (decision_logic
  Filter 1), scanner threshold (TRADE_ENTRY_MIN_SCORE), activity-filter, and the
  compute itself (calculate_score). forward-only: computation stops; new rows carry no
  Score. History is PRESERVED (freeze, not delete) as a frozen audit layer.
- **Over-principle:** the system evolves; the entry method will change again. Every
  change is forward-only. History-reading code MUST be two-shape tolerant — absence is
  empty (""), never a 0 that masquerades as a real value.
- **Stages:** 0 writer-integrity (absence-safe writers + `v3_scoreless` anchor — landed
  `bebd901`, 2026-06-18) → 1 freeze (stop compute+writes) → 2 driver-removal (Filter 1,
  config consts, calculate_score, health check_12) → 3 viewer-hardening (dashboard/
  score_analytics kept + two-shape tolerant). TASK-141 (replacement gate) = data-
  dependent investigation, deferred to a clean recompute.
- **Consequences:** new anchor `score_version="v3_scoreless"` (queryable era boundary);
  writers emit "" on absence; viewers kept+hardened, not deleted (future history
  analysis); the 0-default masquerade in metric readers (MxV/RunUp `_safe_float(...,
  0.0)`) is a documented known-issue, OUT of this scope (metrics stay).
- **Supersedes:** ADR-002 (suspicion → confirmed + actioned).

---

## 31. 🐛 Failure Modes & Known Issues

### Currently open issues (severity ordered)

#### 🟠 Important (P1)

- **#N21 — Live Write Verification:** No check that scanner is actually
  writing during market hours (vs only running). Risk: silent failure
  where scanner runs but writes 0 rows.
- **#9 (Phase 2) — yfinance reliability:** May be partially resolved by
  data_provider abstraction. Verification needed.

#### 🟡 Should-fix (P2)

- **#N11 / [C2] — Hardcoded thresholds:** A few values (~4 thresholds)
  remain hardcoded outside `config.py`. Health check WARNING active.
- **#N22 — `_get_root_folder_id` fallback:** Local-only edge case in
  `sheets_manager.py:219-249`. CI works fine.
- **#N23 — pandas_market_calendars Python 3.9 incompatibility:** Affects
  local 3.9 development; CI on 3.11 unaffected.

#### 🟢 Discoveries / Tracking (P3)

- **#21 — Gap Outliers:** By design (corporate actions). Filtered, not
  fixed.

### Newly opened today (2026-05-02, this session)

- **#N24 — D1_Open mean gap = 80.84% (outlier-distorted):** Median is
  -4.24% (sensible), but mean is wildly off due to ≥1 outlier in the data.
  Need to investigate which row(s) and whether to clean or annotate.
  *Priority:* P2. *Created:* this session for investigation tomorrow.

- **#N25 — DropsLab integration:** DropsLab data should feed into RidingHigh
  as an additional input layer. Currently the two systems are separate.
  DropsLab source verified full (2026-06-03); the "empty source" block is
  removed — integration is now unblocked.
  Plan: create a dedicated PK for DropsLab, design a daily import bridge,
  decide which DropsLab fields are useful for RidingHigh analysis.
  *Priority:* P2. *Estimated effort:* 1 session for investigation, 1 for
  integration design, 1 for implementation.

- **#N26 — System Overview page in dashboard:** Add 4th page to the
  Streamlit dashboard that renders this PK content live, with auto-sync
  via `health_audit.check_25` (drift detection between PK and code).
  *Priority:* P1. *Estimated effort:* 2-3 hours.

### Failure modes catalog (silent failures to watch for)

| Failure mode | Detection | Mitigation |
|--------------|-----------|------------|
| Scanner runs but writes 0 rows | check_04 + #N21 (planned) | Will be caught by check_22 once added |
| FINVIZ schema change (column missing) | First scan logs error | None — manual fix |
| FINVIZ Change% interpretation flip (bug if they switch to %) | Score values become absurd | check_07 score_range catches |
| Alpaca SDK breaking change | Provider exception | yfinance fallback engages |
| OAuth token revoked unexpectedly | Daily warm_oauth fails | Email alert on next 3-day cycle |
| Service account permissions changed | `_share_with_service_account` fails | Manual investigation required |
| Sheet quota exceeded mid-write | `gsheets_sync` guard distinguishes from fresh sheet | Retry logic |
| Score formula bug | check_12 (weights sum) catches if total ≠100 | Limited — bad logic with sum=100 not caught |
| Holiday on cron day | `is_trading_day` should return False | Tested via `pandas_market_calendars` |
| Daylight Saving confusion | None — Peru has no DST | Hardcoded UTC-5 throughout |

### Historical issues (closed)

| # | Closed date | Title | Resolution |
|---|-------------|-------|------------|
| #1 | 2026-04-25 | SL Unification (TP=10/SL=10/5d window) | ADR-007 |
| #6 | 2026-05-02 | Gap removed from Score v2 despite r=-0.256 | Deferred to Score v3 |
| #N10 | 2026-05-02 | Score v1/v2 validation | ADR-004 (tag, not recompute) |
| #N9, #N8, #22 | 2026-04-30 | Misc fixes | Various |
| #15 | 2026-04-29 | WEIGHTS_V1_LEGACY dead code removed | Deletion |

Full historical issue log: `OPEN_ISSUES.md` in repo.

---

## 32. 🗺️ Roadmap

### Phase 1 — Data Accumulation [ACTIVE, ~50% complete]
- ✅ System running daily
- ✅ 18 health checks live
- ✅ Score v2 active
- ✅ Provider abstraction (Alpaca + yfinance)
- ⏳ 100 v2 records (currently 50)
- ⏳ 30 v2 trading days (currently 14)
- ⏳ TP10 stability validation

### Phase 2 — Dynamic TP via ATRX [PLANNED]
- Goal: Set TP threshold per-stock based on ATRX (high-volatility stocks
  need wider TP).
- Requires: Phase 1 exit criteria met.
- Estimated start: late May 2026.

### Phase 3 — Entry Timing Optimization [PLANNED]
- Goal: Use minute-level data within scan day to identify optimal entry
  point (currently EOD or D1_Open are both rough).
- Requires: Phase 2 stable for 4 weeks.

### Phase 4 — Real Money Execution [GATED, no date]
- Goal: Switch `ALPACA_PAPER=false`, deploy small initial position size.
- Gate criteria:
  - Phase 3 shows verified edge across diverse market conditions
  - Independent statistical review confirms results
  - Capital allocation decision made by user
  - Risk limits hardcoded into trade execution

### System Overview & Documentation Roadmap

- [#N26] System Overview page in dashboard (this session's planning)
- [#N25] DropsLab integration as data source
- [#N24] D1_Open outlier investigation
- check_25: PK auto-sync drift detection (one-time addition to health audit)

---

## 33. 🧪 Test Strategy

### What's tested

| Layer | Test type | File | Coverage |
|-------|-----------|------|----------|
| **Formulas** | Unit | `test_formulas.py` | Most metric functions |
| **Utils** | Unit | `test_utils.py` | Time/parsing helpers |
| **Providers** | Integration | `test_data_provider.py` + `validate_providers.py` | Alpaca↔yfinance A/B |
| **System health** | Continuous | `health_audit.py` (18 checks, 3×/day) | Operational reality |
| **Code structure** | Static | `code_auditor.py` (formula duplication, hardcoded values) | Code drift |
| **Smoke** | Manual | `morning_health_check.py` | Quick eyeball before market |

### What's not tested

- ❌ End-to-end pipeline (FINVIZ → score → write → read → display)
- ❌ Regression tests for Score v2 changes (no fixed test fixtures)
- ❌ Streamlit dashboard (manual testing only)
- ❌ Backtest framework (no formal one)

### Testing philosophy

The system is more of a **data collection rig** than a typical software
project. Bugs that produce silent bad data are worse than bugs that crash.
Hence the emphasis on:
- Health checks > unit tests
- Provider validation > mocking
- Code auditor > coverage metrics

### Recommended additions (future)

1. End-to-end pipeline test running in dry-run mode
2. Regression fixtures: a frozen set of FINVIZ rows + expected scores,
   checked on every formula change
3. Visual diff tests for dashboard pages

---

## 34. 🚌 Bus Factor Checklist

The "bus factor" measures how operational a system is if its primary
operator is unavailable. A new operator should be able to take over in
1-2 hours using only this PK and access credentials.

### Can a new operator…

| Task | Possible? | Path |
|------|-----------|------|
| Find the repo? | ✅ | `projects5069-creator/ridinghigh-pro` (§5) |
| Clone and install? | ✅ | `requirements.txt` exists |
| Get credentials? | ✅ | Secrets inventory (§25) lists what's needed |
| Run a scan locally? | ✅ | `python3 auto_scanner.py` (§28) |
| View the dashboard? | ✅ | `ridinghigh-pro-v2.streamlit.app` |
| Understand the data? | ✅ | Schema reference (§15) + Glossary (§17) |
| Trace any number to its source? | ✅ | Lineage examples (§16) |
| Diagnose a failure? | ✅ | Runbook (§28) |
| Modify scoring? | ⚠️ | `formulas.py` documented, but no testing protocol for changes |
| Add a new metric? | ⚠️ | Pattern is clear from formulas.py but no contributor guide |
| Add a new sheet? | ⚠️ | `sheets_manager.py` shows pattern but rotation logic complex |
| Replace data provider? | ✅ | Abstract base in `data_provider.py` |

### Bus factor assessment: **High (~90%)**

Almost everything an operator needs is documented here. Gaps:
- No formal contributor guide for adding metrics
- No regression test protocol for formula changes
- DropsLab integration TBD (separate system)

---

## 35. 🤝 Anti-Drift Maintenance Contract

This section defines Claude's binding obligations to keep this PK accurate.

### The Contract

```
Whenever I (Claude) help you with a task that:
  1. Adds, modifies, or removes a Python file in the repo
  2. Adds, modifies, or removes a workflow in .github/workflows/
  3. Adds, modifies, or removes a sheet in sheets_config.json
  4. Adds, modifies, or removes a constant in config.py
  5. Adds, modifies, or removes a metric, formula, or weight
  6. Adds, modifies, or removes a health check
  7. Adds, modifies, or removes an email alert or schedule
  8. Adds, modifies, or removes a phase, KPI, or known issue
  9. Closes or opens an issue in OPEN_ISSUES.md

THEN, before the final git commit/push of that session, I MUST:
  ✓ Identify which sections of this PK are affected
  ✓ Update those sections with the new reality
  ✓ Increment the version (v2.0 → v2.0.1 patch, → v2.1 minor)
  ✓ Add a changelog line in §1
  ✓ Inform you ("I've updated PK §X to reflect Y")
  ✓ If you're updating the Google Sheets master copy of PK
    (per §A1), include that in the commit message
```

### Self-check at end of every session

Before closing a session, Claude asks itself:

1. Did this session change anything in the 9 categories above?
2. Are the changes reflected in PK?
3. Is the version bumped?
4. Is the changelog updated?

If any answer is "no" but a change was made, **PK update is owed before
the session ends.**

### How violations are caught

- **`health_audit.check_25`** (planned, this is #N26-related) will
  compare PK content to actual code state and email if drift detected.
- Periodic manual audit at start of every 4th-or-so session.
- The Bus Factor Checklist (§34) becomes an exam: if Claude can't answer
  every question from PK alone, PK has gaps.

### What happens if PK becomes stale

Worst case: a future session opens with PK as context, but PK lies. Claude
makes architectural suggestions based on lies. User implements them.
Reality drifts further. **This is the worst possible outcome for the user.**

The Anti-Drift Contract is the strongest mitigation.

---

## 36. 🔍 Cross-References to Code

Use this index to find where any concept lives in the codebase.

### Configuration & constants

| Concept | File | Line(s) |
|---------|------|---------|
| Score v2 weights | `config.py` | 40-49 (`SCORE_WEIGHTS_V2`) |
| Score v2 caps | `config.py` | 52-62 (`SCORE_CAPS_V2`) |
| RSI bell parameters | `config.py` | 64-72 (`SCORE_RSI_PARAMS`) |
| TP/SL thresholds | `config.py` | 117, 120 (`TP_THRESHOLD_PCT`, `SL_THRESHOLD_PCT`) |
| Position size | `config.py` | 114 (`POSITION_SIZE_USD`) |
| Min Score for trade entry | `config.py` | 104 (`TRADE_ENTRY_MIN_SCORE`) |
| Min Score for display | `config.py` | 88 (`MIN_SCORE_DISPLAY`) |
| Max holding days | `config.py` | 131 (`MAX_HOLDING_DAYS`) |
| Cutoff date | `config.py` | 143 (`DATA_CUTOFF_DATE`) |
| Min price/volume/market cap filters | `config.py` | 167-174 |
| REL_VOL hard cap | `config.py` | 78 (`REL_VOL_CAP`) |
| Sheet config file path | `config.py` | 181 |

### Formulas

| Metric | Function | Line |
|--------|----------|------|
| MxV | `formulas.calculate_mxv` | 84 |
| RunUp | `formulas.calculate_runup` | 96 |
| ATRX | `formulas.calculate_atrx` | 108 |
| Gap | `formulas.calculate_gap` | 134 |
| TypicalPriceDist | `formulas.calculate_typical_price_dist` | 146 |
| VWAP_dist | `formulas.calculate_vwap_dist` | 167 |
| REL_VOL | `formulas.calculate_rel_vol` | 178 |
| Float% | `formulas.calculate_float_pct` | 193 |
| ScanChange | `formulas.calculate_scan_change` | 245 |
| MaxDrop | `formulas.calculate_max_drop` | 287 |
| PnL% | `formulas.calculate_pnl_pct` | 325 |
| Score (composite) | `formulas.calculate_score` | 380 |

### Engine

| Functionality | File | Function |
|---------------|------|----------|
| Per-minute scan | `auto_scanner.py` | `run_scan` (line 309) |
| Per-stock analysis | `auto_scanner.py` | `analyze_ticker` (line 107) |
| FINVIZ fetch | `auto_scanner.py` | `fetch_finviz` (line 298) |
| EOD process | `auto_scanner.py` | `run_eod` (line 1230) |
| Save daily summary | `auto_scanner.py` | `_save_daily_summary` (line 495) |
| Update portfolio_live | `auto_scanner.py` | `update_portfolio_live` (line 546) |
| Update ticker_follow_up | `auto_scanner.py` | `update_ticker_follow_up` (line 689) |
| Update live_trades | `auto_scanner.py` | `update_live_trades` (line 928) |
| Sync score_tracker | `auto_scanner.py` | `sync_score_tracker` (line 1080) |
| Snapshot time check | `auto_scanner.py` | `is_snapshot_time` (line 57) |

### Time, market, parsing

| Functionality | File | Function |
|---------------|------|----------|
| Peru time | `utils.py` | `get_peru_time` (line 66) |
| Trading day check | `utils.py` | `is_trading_day` (line 75) |
| Market hours check | `utils.py` | `is_market_hours` (line 110) |
| Trading days after | `utils.py` | `get_trading_days_after` (line 166) |
| Day complete check | `utils.py` | `is_day_complete` (line 125) |
| Parse market cap | `utils.py` | `parse_market_cap` (line 187) |
| Parse volume | `utils.py` | `parse_volume` (line 222) |
| Smart MC fetch | `utils.py` | `get_market_cap_smart` (line 264) |
| Calculate stats (D1-D5) | `utils.py` | `calculate_stats` (line 377) |

### Storage

| Functionality | File | Function |
|---------------|------|----------|
| Load monthly config | `sheets_manager.py` | `load_config` |
| Get worksheet | `sheets_manager.py` | `get_worksheet` |
| Ensure month created | `sheets_manager.py` | `ensure_monthly_setup` |
| Trading days calculation | `sheets_manager.py` | `trading_days_after` — holiday-aware since TASK-130 (10/6): delegates per-day to `utils.is_trading_day` (NASDAQ calendar via mcal, weekday fallback); returns trading days, not weekdays. All callers (auto_scanner, backfill_v2, dashboard, utils delegation) inherit the fix |
| Archive live trades | `sheets_manager.py` | `archive_live_trades` (line 430) |

### Health & audit

| Functionality | File | Function |
|---------------|------|----------|
| Health audit main | `health_audit.py` | `main` (line 1291) |
| Email alert | `health_audit.py` | `send_email_alert` (line 1210) |
| Print report | `health_audit.py` | `print_report` (line 1124) |
| Write to sheet | `health_audit.py` | `write_to_sheet` (line 1164) |
| All 18 checks | `health_audit.py` | `check_01..check_19` |

### Workflows (cron schedules)

| Workflow | Cron | Peru time | Purpose |
|----------|------|-----------|---------|
| `auto_scan.yml` | `* * * * *` | every minute | Scanner |
| `post_analysis.yml` | `5 21 * * 1-5` | 16:05 | Daily collector |
| `health_audit.yml` | `0 11,17 * * *` + `0 3 * * *` | 06:00, 12:00, 22:00 | Health |
| `backup.yml` | `7 13-21 * * 1-5` etc | hourly during market | Backups |
| `monthly_rotation.yml` | `1 5 1 * *` | 00:01 of 1st | Rotate active month |
| `prepare_next_month.yml` | `5 5 1 * *` | 00:05 of 1st | Pre-create future month |
| `warm_oauth_token.yml` | `0 12 */3 * *` | 07:00 every 3 days | OAuth refresh |

---

## A1. 🔗 Master Backup — Google Sheets Copy

This document is also stored as a **Google Sheets master copy** for
redundancy. The Sheets copy is the offline backup of the canonical PK.

### Setup (one-time) — COMPLETED 2026-05-02

1. ~~Create a new Google Sheet named `RidingHigh-Pro-System-Reference`~~ ✅
2. ~~Place it in the same Drive folder as the operational sheets~~ ✅
3. ~~Share with service account `ridinghigh-sheets-v2@...`~~ ✅
4. ~~Initial population via `sync_pk_to_sheet.py`~~ ✅
5. **Sheet ID:** `1SuHj0joCfT7kAoSEvrqepJJcUG8uBU5J4zmxkx9e3J0`
   **URL:** https://docs.google.com/spreadsheets/d/1SuHj0joCfT7kAoSEvrqepJJcUG8uBU5J4zmxkx9e3J0
6. Sync script: `sync_pk_to_sheet.py` (OAuth-based, idempotent — detects existing sheet)

### Sync model

- **Source of truth:** This `.md` file in the repo (`docs/RidingHigh_Pro_PK_v2.md`)
- **Sheets copy:** Identical content; updated whenever PK is updated
- **Update direction:** Always repo → Sheets (Sheets is read-only mirror)

### When to update the Sheets copy

Per the Anti-Drift Contract (§35), every time PK changes:
1. Update the markdown file in repo
2. Generate a copy of the markdown content
3. Update the Sheets copy (paste new content)
4. The Sheets copy's "Last sync" timestamp updates
5. Commit message references both updates

### Why this exists

If the GitHub repo is somehow lost, corrupted, or inaccessible, the
Sheets copy gives a recoverable copy of system documentation. Combined
with the secrets inventory and the runbook, it makes recovery
self-contained.

---

## A2. 🔬 DropsLab — Companion System Reference

DropsLab is a **separate research system** that complements RidingHigh.
DropsLab is active and populated (verified live 2026-06-03). A dedicated
DropsLab PK is planned (#N25); this appendix is the current reference.

### What DropsLab does (summary)

- **Purpose:** Daily database of stocks that drop -10%+ in a single
  trading day — the inverse of RidingHigh's pump scanner.
- **Schedule:** GitHub Actions workflows
  - `drops_scan.yml` at 21:15 UTC (16:15 Peru, post-close)
  - `drops_collect.yml` at 21:30 UTC (16:30 Peru)
- **Filters:** Min market cap $50M, min volume 500K, no ETFs.
- **Source:** FINVIZ + Yahoo Finance (38 metrics per stock).
- **Storage:** Google Sheet `1XM-qId7HAwEu-8-1GGHcy3RoyyAnsYshjZfDrKFnTMI`
- **Dashboard:** `dropslab.streamlit.app`
- **Repo:** `Ambroseius/DropsLab`
- **First live scan:** 2026-04-04 (71 stocks day 1)
- **Status:** Verified populated 2026-06-03 — opened by name (ID `1XM-qId7HAwEu-8-1GGHcy3RoyyAnsYshjZfDrKFnTMI`): `drops_raw` 2851 + `drops_post` 2156 non-empty rows. No RidingHigh integration yet (#N25 — data-accumulation precondition now met, unblocked). The earlier "empty" reading (TASK-62, 2026-05-30) was a wrong-Sheet-ID homoglyph (I/l), fixed in TASK-77.

### Why DropsLab exists alongside RidingHigh

> "RidingHigh = pumps to short · DropsLab = drops to study · together
> they map extreme moves on both sides of the market."

Drops are studied for patterns (what was the cause, what happened next),
which can inform RidingHigh's understanding of mean reversion. They are
not directly tradable signals.

### Planned integration (#N25, separate session)

1. Create dedicated `DropsLab_PK.md` (parallel to this PK)
2. Reality Check on DropsLab codebase (like we did for RidingHigh)
3. Define which DropsLab fields are useful to import to RidingHigh:
   - Daily set of "what dropped today" → crossover-short candidate feed (DRAFT/EXPLORATORY — HYP-001, not yet validated; see HYPOTHESES.md). LONG remains a closed non-goal (§4)
   - Sector concentration of drops → market regime indicator
   - Specific tickers crossing both systems (pumped → fell → caught)
4. Build daily import bridge:
   - New script: `import_dropslab.py`
   - New sheet in RidingHigh: `dropslab_daily` (per-day summary)
   - Workflow: runs after DropsLab collection (~17:00 Peru)
5. Add DropsLab data to dashboard System Overview page
6. Add health check #25 to verify DropsLab feed is fresh

---

## A3. 📝 Communication Conventions

### Language
- **Primary:** Hebrew with emoji headers
- **Inline technical:** English (variables, error messages, libraries,
  function names)
- **Code:** Always English (Python is English; Hebrew identifiers are
  accident-prone)

### Emoji palette
- 🎯 Goal / objective
- ⚡ Speed / scan / immediate
- 🩺 Health check
- 🔴 / 🟡 / 🟢 / ⚪ / ✅ / ❌ Status indicators
- 💼 Backup / archive
- 📊 Data / analysis
- 🛡️ Protection / safety
- 🚨 Critical alert / runbook
- 📋 Document / metadata
- 🧠 Mental model / thinking
- 🔍 Investigation
- 🔗 Reference / link
- 🌐 Infrastructure
- ⚙️ Workflow / settings
- 🧮 Math / formula
- ⏱️ Time / schedule
- 📅 Calendar / phase
- 🗄️ Storage / database
- 🧬 Lineage / DNA
- 💾 Recovery / save
- 💰 Cost / money
- 🚌 Bus factor
- 🤝 Contract / agreement

### Number formatting
- **Scores:** 2 decimal places — e.g., `Score: 77.63`
- **Percentages:** Always include `%` symbol — e.g., `Hit rate: 75.0%`
- **Currency:** USD with `$` prefix — e.g., `$1,000`
- **Dates:** ISO 8601 — `2026-05-02`
- **Times:** 24-hour with timezone — `08:30 Peru`

### Document tone
- Concise, action-oriented
- No filler ("Let me explain...", "Basically...", "Essentially...")
- Direct ("Score is X" not "Score, which is calculated as...")
- Verb-led for instructions

---

## A4. 📋 Analysis Response Template

When Claude analyzes data and reports findings, this format is the
default unless context demands otherwise:

```
📊 סיכום ממצאים
───────────────
[KPI ראשי / מה התגלה — 1-2 שורות]

🔍 פירוט
[ניתוח לפי מטריקות, תמיד עם מספרים]

⚠️ דגלים / אזהרות
[חריגות, חשד לבאג, patterns חסרים]

💡 המלצה הבאה
[צעד אחד ספציפי שמשפר את המערכת]
```

This template appears verbatim in conversations as a copy-paste reference.

---

## A5. 🤖 Agent Module Overview

The Agent module is a Phase 1 addition that extends RidingHigh Pro
with autonomous paper trading on Alpaca.

**Status:** In development — Milestone 1 of 10 complete (2026-05-03)

**Location:** `agent/` directory at repo root

**Configuration:** All agent constants in `config.py` under
"AGENT MODULE CONFIGURATION" section (lines 260-291).

### Key differences from scanner (intentional)

| Setting | Scanner | Agent | Reason |
|---------|---------|-------|--------|
| Min Score for entry | `TRADE_ENTRY_MIN_SCORE = 70` | `AGENT_MIN_SCORE = 60` | Agent needs more data points for learning |
| Position duration | `MAX_HOLDING_DAYS = 5` | `AGENT_NO_TIME_LIMIT = True` | Only TP/SL/EOD exits; time-based exits are arbitrary |
| Purpose | Detect signals + simulate | Execute on Alpaca Paper |

### Module structure (Phase 1)

```
agent/
├── __init__.py
├── orchestrator.py              (main entry point, per-minute)
├── trader/                      (decision logic)
├── perception/                  (data quality, tradability)
├── execution/                   (Alpaca orders, position management)
├── analytics/                   (Score Analytics, postmortems)
├── monitoring/                  (health checks, self-test, emergency stop)
├── logging/                     (decision logger)
├── notifications/               (email system)
├── dashboard/                   (Streamlit pages 9-10)
├── utils/                       (sheets_cache, helpers)
└── setup/                       (one-time sheet creation scripts)
```

### Dependencies on existing code

| What Agent imports | From where | Purpose |
|-------------------|------------|---------|
| `calculate_score()` | `formulas.py` | Score calculation (NEVER re-implemented) |
| `AGENT_*` constants | `config.py` | All thresholds and settings |
| `get_provider()` | `data_provider.py` | Market data (prices, bars) |
| `is_trading_day()`, `get_peru_time()` | `utils.py` | Time utilities |

### What the Agent does NOT do

- Does NOT modify scanner code or behavior
- Does NOT use real money (Alpaca Paper only)
- Does NOT auto-modify scoring formula or weights
- Does NOT skip Cold Start Mode (first 30 days)

### Phase 1 milestones (10 total)

| # | Name | Status |
|---|------|--------|
| M1 | Foundation & Configuration | ✅ Complete (2026-05-03) |
| M2 | Agent Sheets Setup | ✅ Complete (2026-05-03) |
| M3 | The Trader Core Logic | ✅ Complete (2026-05-03) |
| M4 | Decision Logger | ✅ Complete (2026-05-03) |
| M5 | Alpaca Execution | ✅ Complete (2026-05-03) |
| M6 | Postmortem Engine | ✅ Complete (2026-05-03) |
| M7 | Score Analytics System | ✅ Complete (2026-05-03) |
| M8 | Email System | Planned |
| M9 | Streamlit Dashboard Pages 9-10 | Planned |
| M10 | Critical Infrastructure & Production | Planned |

---

## A6. 📊 Agent Sheets Schema (Phase 1)

14 Google Sheets in monthly folder (alongside scanner sheets).
(v2.98 drift-fix: this section previously listed 8 while the code's
AGENT_SHEET_NAMES had grown to 13; now aligned + skip_summary added.)

**Status:** Created 2026-05-03 (Milestone 2); skip_summary added 2026-06-10 (TASK-125)

**Location pattern:** ROOT/{YYYY-MM}/RH-{YYYY-MM}-{name}

| Sheet | Columns | Purpose |
|-------|---------|---------|
| decision_log | 42 | Every signal evaluated by agent (ENTER rows only since Route B 11/5) |
| paper_portfolio | 25 | Mirror of Alpaca paper positions |
| score_analytics | 25 | Daily/weekly score analysis observations |
| postmortems | 17 | Per-position outcome analysis |
| sentinel_events | 7 | Data Sentinel BLOCK/WARN events |
| system_events | 7 | System events and errors log |
| market_context | 11 | Market context snapshots |
| news_findings | 11 | News Detective findings (log-only) |
| pending_suggestions | 14 | Score Analytics suggestions awaiting user |
| config_history | 10 | Audit trail of config changes |
| borrow_data | 9 | Alpaca HTB/borrow info cache — written by borrow_collector (EOD daily) |
| borrow_coverage | 8 | Daily coverage of scanned universe: % with borrow data, % shortable (TASK-172) |
| agent_scorecard | 7 | Critic per-agent daily scorecard |
| weekly_summary | 16 | Weekly performance + activity rollup |
| skip_summary | 7 | TASK-125: per-run aggregated SKIP counts by reason |

### skip_summary (TASK-125, 2026-06-10) — Route B + aggregation

Route B (b1a4e4f, 11/5) keeps per-SKIP rows OUT of decision_log (stdout
`[SKIP]` lines only — the 80-100 rows/min were blowing the 429 quota).
skip_summary restores aggregate visibility WITHOUT reopening the storm:
- DecisionLogger accumulates SKIPs in-memory during one minute-run
  (stateless across runs — one GH-Actions container per run).
- At end-of-run, orchestrator calls `flush_skip_summary()` — ONE batched
  `safe_append_rows` call, one row per skip-reason:
  `Timestamp | RunID | SkipReason | Count | Tickers (cap 25) | ScoreMin | ScoreMax`
- Quota: max +1 write/run (≤480/day); dedup on RunID makes 429-retries safe.
- Flush failure is non-fatal and NOT counted as a run error (no email storm).
- Sentinel BLOCKs are excluded (they bypass the logger; live in sentinel_events).
- stdout `[SKIP]` lines unchanged — Actions-log audit + TASK-126 extraction.

### Creation flow

- **Initial:** `python3 -m agent.setup.create_agent_sheets`
- **Monthly auto:** prepare_next_month.yml runs at 00:05 Peru on 1st of month
  - Step 1: prepare_next_month.py creates folder + scanner sheets
  - Step 2: create_agent_sheets.py --next-month creates all agent sheets (len(AGENT_SHEET_NAMES), currently 14)
  - Step 3: commit sheets_config.json with all updates

### Authentication pattern (CRITICAL)

Files MUST be created via OAuth (user-owned), not Service Account.
SA has 0 GB Drive quota — files created via SA fall to fallback folder.

The script uses:
- `sheets_manager._get_drive_service_oauth()` for file/folder creation
- `sheets_manager._share_with_service_account()` to grant SA Editor access
- `sheets_manager._get_gc()` (SA-based) for writing headers post-creation

This pattern matches prepare_next_month.py exactly.

### Idempotency (3-layer check)

The script is safe to run multiple times:
1. Check sheets_config.json — if all agent-sheet IDs present, exit early
2. Check sheets_config.json — if specific sheet ID present, use it
3. Check Drive folder — if file with matching name exists, use that ID
4. Otherwise, create new file

---

## A7. 🧠 Agent Decision Logic (Phase 1, M3)

The trader's brain. Takes a raw signal + account state, returns a
Decision with full reasoning.

**Status:** Implemented 2026-05-03 (Milestone 3)

### Module structure

| File | Purpose | Lines |
|------|---------|-------|
| `agent/trader/score_calculator.py` | Wrapper around formulas.calculate_score | 45 |
| `agent/perception/data_quality.py` | 5 quality checks → quality_score 0-1 | 132 |
| `agent/perception/tradability.py` | Mock shortable check (M5 will replace) | 59 |
| `agent/trader/decision_logic.py` | 43-field Decision (42 in Sheet + 1 internal: reentries_used_today) + 14 entry filters | 305+ |
| `agent/trader/trader.py` | Stateless orchestrator | 94 |

### Decision flow (14 filters in order)

1. SCORE_TOO_LOW: score < AGENT_MIN_SCORE (50)
2. MXV_TOO_HIGH: mxv > AGENT_MXV_MAX (-100)
3. RUNUP_TOO_LOW: run_up < AGENT_RUNUP_MIN (30%)
4. VOLUME_TOO_LOW: volume < AGENT_VOLUME_MIN (100K)
4b. PRICE_TOO_LOW: price < AGENT_MIN_SCANPRICE_USD ($3) — sub-$3 stocks have wide spreads, halt risk, questionable borrow. Backtest: +129pp improvement (winners median $7.26 vs toxic $3.61). Added 2026-05-25 (Layers paradigm).
4c. BLACKLISTED_TICKER: ticker in CHRONIC_DROPPER_BLACKLIST = [AEHL, TDIC] — chronic droppers from DropsLab x-ref (3+ drops in 30d, Apr 2026). Together ~$120 of DRY_RUN losses. Added 2026-05-26 (Stage 2 — Layers).
4d. TOXIC_PROFILE: RSI > 88 AND Price/SMA20 > 250% — both conditions required. Toxic median DNA: RSI=92.61, Price/SMA20=305%. Winner median: RSI=83.62, Price/SMA20=194%. Missing SMA data defaults to PASS (safe). Backtest: +145pp vs L6-alone. Added 2026-05-26 (L3 — Layers).
5. MARKET_CAP out of range: [$5M, $2B]
6. QUALITY_TOO_LOW: quality_score < 0.5 (>=3 flags)
7. EXISTING_POSITION: already short this ticker
8. COLD_START_LIMIT: concurrent (5) or daily (10) cap reached
9. REENTRY_LIMIT: same ticker already entered 3x today (Bug #5 fix)
10. INSUFFICIENT_BUYING_POWER: < $1000
11. ROCKET_GUARD: RunUp>=50% AND PriceToHigh>=-10% — stock still climbing vertically, shorting fights the trend. Calibrated on 196 rows: blocks 16 losses, 0 winners.

If all 11 pass → ENTER. Otherwise → SKIP with specific skip_reason.

### Critical: stateless design

The Trader does NOT cache state between calls. Account state
(open positions, cold_start counters, buying_power) must be passed
as `account_state` parameter. This makes:
- Testing trivial (no fixture state)
- Concurrency safe (no shared state)
- M5 transition simple (state moves to Alpaca, no refactor)

### Test coverage

- 16/16 tests passing as of 2026-05-03
- Critical: `test_scanner_agent_match` validates byte-for-byte
  parity with scanner's score on 100 random rows from real
  timeline_live data.
- Score tolerance: 0.04 (max observed rounding delta = 0.03)

---

## A8. 📝 Agent Decision Logger (Phase 1, M4)

Persists every Decision to decision_log Sheet (41 columns).

**Status:** Implemented 2026-05-03 (Milestone 4)

### Module structure

| File | Purpose | Lines |
|------|---------|-------|
| `agent/logging/decision_id_generator.py` | Sequential IDs DEC-YYYY-MM-DD-TICKER-HHMMSS-ff | 122 |
| `agent/logging/decision_logger.py` | Maps Decision → 41-column Sheet row | 161 |

### Decision ID format

`DEC-{YYYY-MM-DD}-{NNNNN}`
- Date: Peru timezone, current day
- Counter: 5-digit, zero-padded, max 99999/day
- Counter resets at midnight Peru
- Counter initialized from existing Sheet rows on logger init
- Fallback format on Sheet error: `DEC-{YYYY-MM-DD}-T{HHMMSS}`

### Field mapping (Decision → Sheet)

41 fields in explicit ordered list (`FIELD_MAPPING`).
Validation: `test_field_mapping_has_41_entries` +
`test_field_mapping_field_names_exist_in_decision` ensure schema integrity.

### Value transformations

| Python type | Sheet value | Reason |
|-------------|-------------|--------|
| `None` | `""` | Matches scanner's `df.astype(str)` convention |
| `bool` | `"True"` / `"False"` | Python's `str(bool)` default |
| Other | as-is | Numbers, strings pass through |

### Write strategy

- **Immediate write** (not batched): every decision matters, no loss on crash
- **Failure handling**: `try/except` returns `None`, signal still processed
- **Errors logged to stderr**: `[DecisionLogger] ...`

### Test coverage

- 14 new M4 tests (5 generator + 7 logger + 2 integration)
- Total agent test suite: **30/30 passing**
- Critical: `test_field_mapping_has_41_entries` ensures Decision schema
  matches Sheet headers byte-for-byte
- Integration: `test_write_real_decision_to_sheet` writes real row to
  decision_log and reads it back — proves end-to-end correctness

### Test data in production Sheet

Two test rows exist in 2026-05/decision_log:
- `TEST_INTEGRATION_M4` (test_write_real_decision_to_sheet)
- `TEST_INTEGRATION_M4_2` (test_decision_id_format_in_sheet)

These can be deleted manually if desired, or left as evidence M4 worked.

---

## A9. ⚡ Agent Execution Layer (Phase 1, M5)

The order execution layer. Translates ENTER decisions into actual
broker calls (Alpaca paper) and tracks position lifecycle.

**Status:** Implemented 2026-05-03 (Milestone 5)

### Module structure

| File | Purpose | Lines |
|------|---------|-------|
| `agent/execution/alpaca_broker.py` | Alpaca SDK wrapper, DRY_RUN simulation, safety guards | 270 |
| `agent/execution/order_manager.py` | Bracket order submission, retry, paper_portfolio writes | 265 |
| `agent/execution/position_manager.py` | Open position monitoring, TP/SL detection, EOD close | 250 |
| `agent/execution/reconciler.py` | Sheet ↔ Alpaca drift detection and correction | 218 |

### Critical safety architecture

Three independent guards prevent accidental live trading:

1. **Broker-level**: `assert "paper" in base_url` in `AlpacaBroker.__init__`
2. **Key-level**: API key must start with "PK" (paper convention)
3. **State-level**: `AGENT_LIVE_PAPER` flag blocks `submit_bracket_order`
   when False (current state). Only M10 enables this.

State machine:
- `AGENT_DRY_RUN=True` → simulated orders, no API calls (current)
- `AGENT_DRY_RUN=False + AGENT_LIVE_PAPER=False` → blocked (RuntimeError)
- `AGENT_DRY_RUN=False + AGENT_LIVE_PAPER=True` → executes (M10 enables)

### Order flow (ENTER decision → completed position)

1. Trader returns Decision(action="ENTER")
2. OrderManager.execute(decision):
   - Pre-flight: market open, broker available
   - Submit bracket order (entry limit + TP + SL as OCO)
   - Poll status up to 60s, cancel on timeout
   - Update Decision: order_id, order_status, execution_price
   - Write to paper_portfolio Sheet (Status="OPEN" or "DRY_RUN_OPEN")
3. PositionManager.monitor_all() (per-minute loop):
   - For each open position: check TP/SL fills
   - Update CurrentPrice, UnrealizedPnL/Pct
   - On TP filled: close position with ExitReason="TP_HIT"
   - On SL filled: close position with ExitReason="SL_HIT"
4. PositionManager.eod_close_all() (14:55 Peru):
   - Close all open positions before market close
   - Cancel dangling TP/SL orders

### Reconciler drift handling

Runs daily after market close. 4 scenarios:

| Sheet Status | Alpaca Reality | Action |
|--------------|----------------|--------|
| OPEN | Position exists | OK (no action) |
| OPEN | No position | PHANTOM_OPEN: mark CLOSED + alert |
| (none) | Position exists | ORPHAN_POSITION: alert only (don't auto-close) |
| DRY_RUN_OPEN | (any) | Skipped (no Alpaca counterpart) |

### Tradability upgrade (M5)

`agent/perception/tradability.py` evolved from M3 mock to broker-aware:
- No broker passed → mock fallback (M3 behavior preserved)
- AGENT_DRY_RUN=True → mock fallback regardless
- Broker provided + LIVE → real `broker.is_shortable()` with 1-hour cache
- On API failure → graceful fallback to mock

### Retry strategy (Spec §8.4)

- 3 retries with exponential backoff: 2s, 4s, 8s
- No retry on rejection (logical error, not transient)
- After all retries exhausted → mark Decision REJECTED, alert

### DRY_RUN markers in paper_portfolio

DRY_RUN positions are clearly distinguishable:
- `Status`: "DRY_RUN_OPEN" (vs "OPEN" for LIVE)
- `EntryOrderID`: "SIM-{uuid}" prefix
- `TPOrderID`/`SLOrderID`: "SIM-{uuid}-tp", "SIM-{uuid}-sl"

This allows M7 Score Analytics to filter LIVE vs DRY_RUN trades.

### Test coverage

- 47 new M5 tests + 2 tradability upgrade tests = 49 added
- Total agent test suite: **77/77 passing**
- All tests use mocked Alpaca SDK (no real API calls in unit tests)
- Critical guards verified: TestSafetyChecks (4) + TestLivePaperGuard (2)

---

## A10. 📝 Agent Postmortem Engine (Phase 1, M6)

Generates postmortem analysis for closed positions. Foundation for
M7 Score Analytics correlations.

**Status:** Implemented 2026-05-03 (Milestone 6)

### Module structure

| File | Purpose | Lines |
|------|---------|-------|
| `agent/analytics/postmortem_engine.py` | Per-position postmortem generator | 346 |

### Trigger

Per-position: hooked into `position_manager._close_position()`. After
position is marked CLOSED in paper_portfolio, postmortem generates
within ~1 minute (acceptance criteria from spec).

### postmortems Sheet schema (17 columns)

PostmortemID, PositionID, Ticker,
EntryDate, EntryPrice, ScoreAtEntry, MetricsAtEntry,
ExitDate, ExitPrice, PnLPct, ExitReason,
DurationHours, MaxFavorable, MaxAdverse, AutoLessons,
GeneratedAt, ScoreVersion

### Computed metrics

| Metric | Formula | Source |
|--------|---------|--------|
| DurationHours | (exit_dt - entry_dt) in hours | paper_portfolio dates |
| MaxFavorable (MFE) | min(Low) during hold (short: lower=better) | data_provider bars |
| MaxAdverse (MAE) | max(High) during hold (short: higher=worse) | data_provider bars |
| ScoreAtEntry | preserved from decision_log | decision_reader |
| MetricsAtEntry | JSON: MxV, RunUp, ATRX, RSI, REL_VOL, ScanChange, ConfidenceScore | decision_log |

### Auto-lesson rules (7 rules + special case)

1. LOSS + ATRX>3 → "High ATRX — consider archetype-specific limits"
2. LOSS + RSI>90 → "RSI 90+ at entry — momentum may have continued"
3. **LIVE_PAPER only** + duration<1h → "Very fast outcome — entry timing optimization"
4. WIN + MAE>SL → "MAE exceeded SL — stop-loss prevented larger loss"
5. MFE drop > 1.5× TP → "Stock fell X% (TP=10%) — trailing TP candidate"
6. EOD_CLOSE + profit → "EOD forced close with profit — consider extending hold"
7. EOD_CLOSE + loss → "EOD forced close with loss — earlier exit signal needed"
- Special: no bar data available → "MFE/MAE not computed"

Rule 3 excludes DRY_RUN to avoid false positives (DRY_RUN has artificial
exit timing within same minute as entry).

### Score historical accuracy

ScoreAtEntry preserves the score AT THAT TIME using AGENT_SCORE_VERSION
constant from config. When M7 changes the score formula:
1. Bump AGENT_SCORE_VERSION (e.g., "v2.6" → "v2.7")
2. New postmortems get tagged with new version
3. Old postmortems retain their original ScoreVersion
4. M7 Analytics can filter by version for apples-to-apples comparison

### Wire-in (position_manager hook)

`PositionManager.__init__` accepts optional `postmortem_engine` parameter.
After `_close_position()` updates the Sheet, if engine is provided:
1. Merges entry data (pos) + exit data (updates) into pos_with_exit
2. Calls engine.generate(pos_with_exit)
3. Wraps in try/except — postmortem failure doesn't fail the close

This makes M6 fully optional. Existing M5 tests pass with `postmortem_engine=None`.

### Test coverage

- 13 new M6 tests across 5 test classes
- Total agent test suite: **90/90 passing**
- Critical: test_writes_17_column_row enforces schema integrity
- DRY_RUN exclusion verified: test_dry_run_skips_fast_outcome_rule

---

## A11. 📊 Agent Score Analytics System (Phase 1, M7)

Daily + weekly observational analysis of postmortem data. Foundation
for M9 Score Brain dashboard page.

**Status:** Implemented 2026-05-03 (Milestone 7)

### Module structure

| File | Purpose | Lines |
|------|---------|-------|
| `agent/analytics/score_analytics.py` | Daily + weekly analysis engine | 535 |

### Phase 1 constraints

- **OBSERVATIONAL ONLY** — never auto-modifies score formula or thresholds
- All suggestions PENDING — user approves/rejects via M9 dashboard or M8 email
- DRY_RUN postmortems included (the primary data source during cold start)

### Schedule

| Run | Time | Output |
|-----|------|--------|
| Daily | 16:30 Peru (after post_analysis) | 1 row to score_analytics Sheet |
| Weekly | Saturday 18:00 Peru | 1 row to score_analytics + 0-5 to pending_suggestions |

### Sample size tiers

Determines suggestion confidence and whether to generate suggestions at all.

| Tier | n | Confidence |
|------|---|------------|
| INSUFFICIENT | <10 | none — "INSUFFICIENT_DATA" recommendation, no suggestions |
| EXPLORATORY | 10-29 | LOW |
| RELIABLE | 30-99 | MEDIUM |
| STRONG | 100+ | HIGH |

### score_analytics Sheet schema (25 columns)

Date, AnalysisType, Period, TotalTrades, WinRate, TotalPnL, AvgPnL,
MedianPnL, WinRate_60_70, WinRate_70_80, WinRate_80_90, WinRate_90_plus,
Corr_MxV, Corr_RunUp, Corr_ATRX, Corr_RSI, Corr_TypicalPriceDist,
Corr_ScanChange, Corr_REL_VOL, StrongestPredictor, WeakestPredictor,
SurpriseFinding, Recommendation, SampleSize, GeneratedAt

### pending_suggestions Sheet schema (14 columns)

SuggestionID, GeneratedDate, WeekOf, Type, Description, Reasoning,
Confidence, AffectedMetric, CurrentValue, ProposedValue, Status,
UserResponse, ResponseDate, SampleSize

### Computed metrics

- TotalTrades, WinRate, TotalPnL, AvgPnL, MedianPnL
- WinRate per score tier (60-70, 70-80, 80-90, 90+)
- 7 Pearson correlations: MxV, RunUp, ATRX, RSI, TypicalPriceDist,
  ScanChange, REL_VOL ↔ PnLPct
- StrongestPredictor / WeakestPredictor (highest/lowest |correlation|)

### Surprise findings (3 rules)

1. Score 90+ underperforms 70-80 by >10%
2. WinRate variance >30% across tiers
3. Strong correlation |corr| > 0.4

### Recommendation format (pipe-separated)

`PRIMARY: <best tier> | SECONDARY: <strongest predictor> | ACTION: <next step>`

Allows easy parsing by M9 dashboard while remaining human-readable.

### Suggestion types (3 types, max 5 per weekly run)

| Type | Trigger | Affected |
|------|---------|----------|
| THRESHOLD_CHANGE | 60-70 tier WR < 50% | AGENT_MIN_SCORE |
| WEIGHT_ADJUSTMENT | metric corr > 0.3 | WEIGHT_<metric> |
| OBSERVATION | score 90+ WR < 50% | HIGH_SCORE_HANDLING |

### Test coverage

- 12 new M7 tests across 5 test classes
- Total agent test suite: **102/102 passing**
- Sample classification, daily/weekly flows, correlations, tier winrates

---

## §A12: M9 — Streamlit Dashboard Pages 9-10

**Goal:** Make the agent visible in the existing Streamlit dashboard.

**Architecture:** New `agent/dashboard/` package with 4 files; `dashboard.py`
modified minimally (10 lines: import + 2 _PAGE_NAMES entries + 2 elif clauses).

**Page 9 — Live Agent** (operational):
- Status banner (DRY_RUN/LIVE_PAPER + market open/closed)
- 4 KPIs (open positions, today's PnL, decisions, ENTERs)
- Open positions table (filtered to OPEN/DRY_RUN_OPEN)
- Today's decisions table with unified Why column
- Emergency Stop button (logs to system_events; M10 reads it)

**Page 10 — Score Brain** (analytical):
- Latest analytics KPIs (sample size, win rate, PnL)
- Win rate by tier (plotly bar chart)
- Metric correlations (horizontal bar)
- Recommendation parsed from pipe-separated format
- Pending suggestions with Approve/Reject buttons (atomic update via ws.find)

**Robustness fixes (lessons learned):**
- Atomic ws.update with range vs 3 sequential ws.update_cell (prevents partial writes)
- ws.find() to locate row by ID vs enumerate(get_all_records, start=2) (immune to empty rows)
- math.isnan/isinf guards before plotly chart data (prevents silent render bugs)
- Unique button keys with idx prefix (prevents DuplicateWidgetID on duplicate suggestion IDs)
- astype(str) before .str.startswith() (handles datetime-typed columns)
- Combined Reason+SkipReason → Why column (better UX than half-empty columns)

**Status:** Phase 1 constraints: Approve/Reject only updates Sheet status,
Emergency Stop only logs. Real apply/halt logic lives in M10.

---

## §A13: M10 — Production Deployment

**Goal:** Agent runs autonomously in DRY_RUN every minute during market hours.

**Architecture:** 2 orchestrator scripts + 2 GitHub Actions workflows.

**`agent/orchestrator.py`** (~360 lines): Main run loop.
1. Safety checks: market hours (08:30-15:00 Peru, Mon-Fri) + emergency stop reader
2. Build account_state from paper_portfolio (open positions) + decision_log (today's ENTERs) + broker (buying_power)
3. Read latest scan from timeline_live (today's max ScanTime → list of signals)
4. For each signal (try/except continue): trader.evaluate → decision_logger.log → if ENTER: order_manager.execute
5. position_manager.monitor_all (update prices, detect TP/SL)
6. If 14:55-14:59 Peru: position_manager.eod_close_all
7. Log summary

**`agent/orchestrator_eod.py`** (~100 lines): Daily 16:00 Peru.
1. Reconciler.reconcile (drift detection between paper_portfolio and Alpaca)
2. ScoreAnalytics.run_daily (write daily analytics row)
3. If Saturday (weekday==5): ScoreAnalytics.run_weekly (suggestions)

**GitHub Actions:**
- `agent_minute.yml` — cron `*/1 13-20 * * 1-5` (every minute, 13:00-20:00 UTC = 08:00-15:00 Peru)
- `agent_eod.yml` — cron `0 21 * * 1-6` (21:00 UTC = 16:00 Peru, Mon-Sat)
- Both pip-install requirements.txt and run via `python -m agent.orchestrator(_eod)`
- Secrets: GOOGLE_CREDENTIALS_JSON, GOOGLE_OAUTH_TOKEN_JSON, ALPACA_API_KEY_ID, ALPACA_SECRET_KEY

**Helper functions in orchestrator.py:**
- `_signal_from_timeline_row()` — maps capitalized timeline_live keys to snake_case Trader signal keys
- `is_market_hours()` / `is_eod_window()` — Peru-aware time gates
- `check_emergency_stop()` — reads system_events for unresolved EMERGENCY_STOP_REQUESTED
- `build_account_state()` — assembles dict from 3 sources (Sheet + Sheet + broker)
- `read_latest_signals()` — filters timeline_live to today's max ScanTime

**Iron Rule additions:**
- Orchestrator MUST log-and-continue on per-signal errors (one bad signal won't stop loop)
- Orchestrator MUST exit early on emergency stop OR outside market hours (no side effects)

**Status:** DRY_RUN active. 30-day data collection phase begins. After 30 days of clean data,
user reviews and may approve LIVE_PAPER switch (config.AGENT_LIVE_PAPER=True).

---

## §A14: M8 — Email System

**Goal:** Email notifications for daily start, EOD summary, and urgent alerts.

**Architecture:** SMTP via Gmail App Password (16-char ASCII). Credentials in
GitHub Actions secrets (SMTP_HOST/PORT/USER/PASSWORD + EMAIL_TO).

**Files:**
- `agent/notifications/email_sender.py` — `EmailSender` class + `send_email()` + `send_alert()` helpers. Strips Gmail UI whitespace from password (xxxx xxxx xxxx xxxx → xxxxxxxxxxxxxxxx). Graceful: never raises, returns False on failure.
- `agent/notifications/templates/morning_brief.py` — `render_morning_email(state)` → (subject, html). Includes mode (DRY_RUN/LIVE_PAPER), open positions, buying power, recent postmortems count.
- `agent/notifications/templates/daily_brief.py` — `render_daily_email(stats)` → (subject, html). Includes realized PnL with color, decisions count (ENTER/SKIP), win rate, top-10 decisions table, errors warning if any.
- `agent/orchestrator_email_morning.py` — gathers state from paper_portfolio/postmortems/broker → renders → sends.
- `agent/orchestrator_email_daily.py` — aggregates today's decision_log/paper_portfolio/system_events → renders → sends.

**Urgent alerts:** Integrated in `orchestrator.py` and `orchestrator_eod.py` — if `summary["errors"] > 0` at end of run → `send_alert()` triggered immediately. Provides timestamp + run summary + error count.

**GitHub Actions:**
- `agent_email_morning.yml` — cron `30 13 * * 1-5` (08:30 Peru, weekdays)
- `agent_email_daily.yml` — cron `30 21 * * 1-5` (16:30 Peru, weekdays)
- Both pip-install requirements.txt and run via `python -m agent.orchestrator_email_*`
- Both load all SMTP secrets in env

**Iron Rule additions:**
- Email sender MUST be graceful — never raise exception that crashes orchestrator
- App Password MUST strip whitespace (Gmail UI displays with spaces, SMTP wants none)
- Alerts only on actual errors (errors > 0) — not on every run

**Status:** Phase 1 complete. 10/10 milestones done. DRY_RUN active. 30-day data collection phase begins tomorrow.

---

## §A15: Data Sentinel — Agent #2 (Gatekeeper)

**Goal:** A validation layer that runs before The Trader, catching bad signals
and broken system state before they corrupt trading decisions. Born from a week
of silent-failure bugs (stale prices, 429 quota, None prices).

**Mental model:** Data Sentinel is a security guard at the entrance. Every signal
from FINVIZ passes through Sentinel's checks before The Trader evaluates it.
If something is wrong, Sentinel can BLOCK the signal (in active mode) or just
log it (in shadow mode).

**Two agents in production:**
1. The Trader — `agent/orchestrator.py` + `agent/trader/` — makes ENTER/SKIP decisions
2. Data Sentinel — `agent/sentinel/` — gatekeeper, runs before Trader

**Files:**
- `agent/sentinel/data_sentinel.py` — `DataSentinel` class, `SentinelResult` dataclass, `_log_sentinel_event()`, `get_sentinel()` singleton.
- `agent/sentinel/checks/` — 7 check modules.

**The 7 checks:**

Per-signal (4) — run on every signal:
- `completeness.py` — 7 required metrics present (ticker, score, price, mxv, run_up, atrx, rsi)
- `scan_freshness.py` — scan age: WARN at 5 min, BLOCK at 10 min (widened 2026-05-19 commit b1353ba)
- `price_sanity.py` — price within [$0.01, $10,000] + OHLC consistency (low ≤ price ≤ high)
- `price_freshness.py` — FINVIZ price vs Alpaca live, WARN if delta > 2% (demoted from BLOCK 2026-05-19 commit 30a8bc4 — FINVIZ-Alpaca gaps normal for volatile pumps and don't risk trades since execution price always from Alpaca). 60s in-memory cache.

System-level (3) — run once per orchestrator run:
- `position_sync.py` — if today_enters > 0 but open_positions = 0 → HALT (paper_portfolio failed to load). today_enters = cold_start_daily_used; open count = open_position_count (NOT len(existing_positions), which is a mixed set — fixed v2.17).
- `quota_health.py` — Sheets writes/min: WARN at 50, BLOCK at 60
- `provider_heartbeat.py` — AAPL canary fetch, 3 consecutive failures within 5 min → HALT

**Modes (config.SENTINEL_MODE):**
- `shadow` — checks run, decisions logged to system_events, but NEVER block (DEFAULT)
- `active` — BLOCK actually prevents trader.evaluate()
- `off` — Sentinel disabled entirely

**Config flags (config.py):**
- `DATA_SENTINEL_ENABLED` (bool), `SENTINEL_MODE` (shadow/active/off)
- 7 per-check toggles: `SENTINEL_CHECK_*`
- Thresholds: `SENTINEL_PRICE_DELTA_MAX_PCT=2.0`, `SENTINEL_SCAN_MAX_AGE_MINUTES=5`,
  `SENTINEL_SCAN_MAX_AGE_BLOCK_MINUTES=10`, `SENTINEL_QUOTA_DEFENSIVE_THRESHOLD=50`,
  `SENTINEL_QUOTA_HALT_THRESHOLD=60`, `SENTINEL_PRICE_MIN_USD=0.01`, `SENTINEL_PRICE_MAX_USD=10000.0`

**Observability:**
- Events written to `system_events` sheet (schema: Timestamp, EventType, Severity, Component, Message, Details, ActionTaken). Only BLOCK/WARN written (never ALLOW — quota).
- Dashboard page "🛡️ Sentinel Events" — KPIs, events table, breakdown by check/reason.
- Health check S1 (`check_24_sentinel_health`) — verifies 9 files, config flags, system_events reachability.

**Integration in orchestrator.py:**
- `sentinel.check_system()` runs once after build_account_state — HALT if BLOCK
- `sentinel.check_signal()` runs per signal before `trader.evaluate()` — skip signal if BLOCK
- Counter `sentinel_blocks` in run summary

**Activation path (shadow -> active):**
- ACTIVE mode since 2026-05-20 11:41 Peru (commit 5f3918a).
- Pre-switch criterion (met): two clean shadow audits (08:50 + 11:28 Peru, 0% BLOCK).
- Post-switch verification (2026-05-24): 4-hour deep dive concluded no Sentinel
  rollback needed; apparent issues were upstream lex-compare bugs (commits
  5cc658b + 2ef7ceb), now fully fixed.

**Iron Rule additions:**
- Sentinel checks MUST never raise — exceptions caught, treated as WARN, never block the loop
- Sentinel writes MUST be graceful — system_events write failure logged, never breaks trading
- Only BLOCK/WARN written to system_events — ALLOW never written (quota protection)

**Status:** Built 2026-05-11 (Phase 1-3). Observability completed 2026-05-16
(system_events logging + dashboard page + health check). Switched to ACTIVE
mode 2026-05-20 11:41 Peru (commit 5f3918a) after two clean shadow audits.
Verified post-switch via 4-hour investigation on 2026-05-24 — see PK v2.32.
The apparent BLOCK explosion of 21-22/5 was traced to a lex-compare bug in
orchestrator.read_latest_signals (fixed commit 5cc658b) + a remaining
dashboard.py call site (fixed commit 2ef7ceb). Sentinel logic itself is sound.

---

*— END OF DOCUMENT —*

*Total length: ~2900 lines · Sections: 44 · Generated: 2026-05-02 · Updated: 2026-05-31*
*Next review: when first PK section drifts from reality (per check_25 future addition)*

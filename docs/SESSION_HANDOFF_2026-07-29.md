# Session Handoff — 2026-07-29 (בוקר)

## מה אומת חי בסשן הזה

**TASK-239 פליאטיב — אומת.** ריצת Post Analysis Collector של 28/7 22:13 UTC
הסתיימה success ב-23m22s תחת התקרה החדשה של 30 דקות (run 30403746436).
Collector שמר 77 שורות, Enrich רץ ועדכן 77. שלוש הריצות שלפניה (23/7, 24/7, 27/7)
נקטעו ב-15m1Xs. **הפליאטיב בלבד אומת — התיקון המהותי של 239 עדיין פתוח.**

**שורש ה-timeout (מאושר קוד):** `yfinance_provider.py:316` `yf.Ticker(t).info` חוסם
~18ש' לכל סמל לא-קיים, בלי timeout/skip-cache/dedup. `post_analysis_collector.py:638`
שומר batch יחיד אחרי הלולאה — ריצה שנקטעת מאבדת הכל, אין שמירה אינקרמנטלית.

**שורש שיבוש ה-Ticker (TASK-238):** צד-שרת. finviz הוסיפו אלמנט לוגו לתא הטיקר
שהטקסט שלו הוא האות הראשונה, ו-`base.py:127 col.text` של finvizfinance משרשר אותו.
הקוד המקומי נקי — אין שום מניפולציה על ticker מלבד `.strip()`.
**הצמדת גרסה לא תפתור** — החילוץ צריך לבוא מ-attribute. הסמלים עדיין מופיעים
בריצת הלילה (BBIYA, VVEEE, SSTKH, DDFNS, IINLF, WWLDS).

**שחזור יולי — אין דדליין.** monthly_rotation ו-prepare_next_month הם create-only
("NEVER deletes"), sheets_config צובר את כל החודשים, ו-cross_month_loaders קורא
מכולם. הדאטה של יולי שורדת. מה שנשבר ב-1/8 הוא `--date` בלבד, וזה TASK-202.

## הבעיה הפתוחה עם הדדליין — אוגוסט

**מצב:** `sheets_config` 2026-08 מכיל 9 טאבי ליבה בלבד. חסרים 16 גיליונות סוכן
כולל `paper_portfolio` ו-`decision_log`. יולי מכיל 25.

**שורש:** פיצול זהויות. `_get_root_folder_id` (sheets_manager:249) בודק את
ROOT_FOLDER_ID דרך ה-**SA**, שמקבל 404, ונופל ל-RidingHigh-Data — בזמן שכל היצירה
בפועל רצה דרך **OAuth** שכן רואה את הקנוני. הבדיקה בוחנת את הזהות הלא-נכונה.
כתוצאה: monthly_rotation (SA) יצר את תיקיית 2026-08 ב-RidingHigh-Data,
prepare_next_month (OAuth) יצר אחת בקנוני, והגארד של TASK-143 ירה על שתי תיקיות
באותו שם ונעצר לפני `create_agent_sheets`. **זו לולאה חודשית, לא אירוע.**

**טופולוגיה מאומתת:**
- root קנוני `1mHSds...` בשם RidingHighPro, owner projects5069@gmail.com,
  מכיל 2026-04 עד 2026-08.
- קנוני/2026-08 = `1k-N29HSIX13KHAgyg4qXZ6H6V_KcS0RQ` — **ריק לגמרי, 0 children.**
- 9 גיליונות הליבה של אוגוסט יושבים ב-`1S5bAu4...` תחת RidingHigh-Data.
- יולי יושב בקנוני ב-`1U2Syqbfq...`.

**ארבע עובדות שנסגרו:**
1. `_share_with_service_account` משתף רק את ה-SA המרכזי. ארבעת השיתופים של יולי
   מגיעים **בירושה מהתיקייה** הקנונית. לכן אוגוסט תת-משותף — `_AS`, `_AM`, `_HA`
   עלולים להינעל מחוץ לנתוני אוגוסט ב-1/8.
2. OAuth תקין. כשלי prepare_next_month היו הטרוגניים: 1/7 גארד, 1/5 JSONDecode ו-pytz.
3. **העברת גיליונות בין תיקיות בטוחה** — אין addParents/removeParents בקוד,
   sheets_config כולו ID-based, שום דבר לא נשען על מיקום.
4. health_audit check_28 בודק רק את החודש **הפעיל** — יתריע ב-1/8, לא לפני.

**הכיוון שהתגבש (טרם אושר):** לתקן את `_get_root_folder_id` שיפתור דרך OAuth;
להעביר את 9 גיליונות אוגוסט לקנוני/2026-08 הריק; ליצור שם את 16 גיליונות הסוכן.
**עובדה אחת חסרה:** האם `_AS`/`_AM`/`_HA` מוענקים ברמת התיקייה הקנונית — זה מכריע
אם ההעברה מספיקה או שצריך re-share מפורש.

**דדליין:** רוטציית 1/8 ב-05:01 UTC.

## מצב git
- `origin/main` = `827bc66` (פליאטיב 239 מוזג).
- `origin/fix/auto-dancer-planmd` = `131a88a`, דחוף, **מוקפא**, לא ממוזג.
- `main` מקומי = 29 commits קדימה ומפוצל, לא נדחף. **החלטה נדחתה.**
- שני קבצי `docs/auto-dancer/queue/QUEUE_2026-07-0[45].md` untracked ונודדים בין ענפים.

## תיקים פתוחים מהיום
238 (finviz ticker), 239 (collector perf — פליאטיב בוצע), 240 (audit דאטה),
241 (ביקורת סוכנים). Backlog פתוח: 57.

## מה לא נעשה
לא עודכן סטטוס אף משימה. לא בוצע bump ל-PK. שתי הבדיקות שעמיחי ביקש (240, 241)
טרם התחילו.

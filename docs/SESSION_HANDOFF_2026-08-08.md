# HANDOFF — שבת 2026-08-08 → הסשן הבא

> **זה לא PK ולא רשימת-משימות.** שניהם חיים בריפו ומתעדכנים שם:
> `docs/RidingHigh_Pro_PK_v2.md` (v4.19) · `docs/WORK_PLAN.md` (v1.6) ·
> `docs/MASTER_REGISTER.md` · `backlog/tasks/`.
> כאן רק: **מצב חי · מה ממתין לעמיחי · מה רץ בשני · ולקחי-שיטה.**

---

## 1 · מצב חי

```
origin/main        44923b0   (Merge PR #44)
הענף               docs/handoff-2026-07-29 — מסונכרן, 0 לא-דחוף
‏PR שמוזגו היום     #40 #41 #42 #43 #44
order_manager.py   b05c963f…  — לא נגע כל היום, לא מקומית ולא ב-main
```

**מה יושב בעץ-העבודה ולא מקומט, במכוון:**
```
 M .github/workflows/agent_minute.yml   גוש concurrency — ממתין להכרעת-259
 M .github/workflows/auto_scan.yml      אותו גוש
?? docs/auto-dancer/                    לא-במעקב, לא נגעתי בו היום
```

**שני שערים חדשים, מחוץ לריפו, בדוקים דו-כיוונית:**
```
~/rhpro_audit_run/audit_gate/gate265_watchdog.py    (AC#1 של כלב-השמירה)
~/rhpro_audit_run/audit_gate/gate266_timeout.sh     (266 + 259 במדידה אחת)
```
⚠️ ‏`gate6_` שמור לשער-הטוהר של TASK-277 — שניהם נושאים מספר-**תיק** בכוונה.

---

## 2 · שלוש הכרעות שממתינות לעמיחי

| # | מה | מתי נדרש | ההמלצה הרשומה |
|---|---|---|---|
| 1 | **הכרעת-259** — לפרוס `concurrency`? | לפני שני 08:30 | **להמתין למדידת-שני.** ‏8/7 הראה חפיפה 10.2% מול 92.5% ב-8/5; אם המספר מחזיק עם סיגנלים אמיתיים, הדיף מתייתר |
| 2 | **270 — גיבוי CSV הדחיות מחוץ למק** | דדליין נפרד ~9/8 (פקיעת לוגי-Actions) | רבע שעה, בלתי-הפיך |
| 3 | **בדיקת-CI לתגובות PR/issues** | מתי שתרצה | השער הנוכחי מכסה גוף-PR בלבד; תגובות דורשות `issue_comment` |

⚠️ **‏(1) היא הפעולה היחידה שנותרה לפני פתיחת החלון. אין עבודת-קוד.**

---

## 3 · שני 10/8 — מה רץ, באיזה סדר

```
לפני 08:30 לימה   עמיחי: הכרעת-259. זהו.
14:45-19:45Z      Claude: cd ~/rhpro_audit_run/audit_gate
                          /usr/bin/python3 gate265_watchdog.py 2026-08-10
                  ⇒ AC#1 של 265: ≥3 שורות רצופות ~5 דק׳ בתוך החלון.
                  ⚠️ הטריגר כבר הוכח יורה-מעצמו; מה שנבדק הוא שה-watchdog
                     **סופר** — Recent15/Prior60 מתמלאים ו-Status מפסיק
                     להיות OUTSIDE_WINDOW.
                  ⚠️ עד ש-AC#1 עובר — אין לגעת ב-detect_outage (TASK-268).
אחרי סגירת-הייצור  Claude: ./gate266_timeout.sh 2026-08-10
                  ⇒ מבוטלות <5% ⇒ 266 נסגר · ≥5% ⇒ נפתח מחדש עם הנתונים.
                  ⇒ חציון <60ש עם סיגנלים אמיתיים ⇒ שאלת-הפריסה של 259
                     מתייתרת מאליה. **שתי משימות, מדידה אחת.**
                  ואז gate4 (M1 — שער-4 מוריק).
```

---

## 4 · ⚠️ מה **לא** נסגר, למרות שנראה כך

זה הסעיף שהכי חשוב לא לפספס בסשן הבא.

```
TASK-265   In Progress — לא Done.  הותקן ויורה, AC#2+AC#3 נסגרו,
                                   אבל AC#1 (שעות-מסחר) ו-AC#4 (PK) פתוחים.
TASK-266   To Do — הוכרע ג׳, נסגר רק אם שער-שני יעבור.
TASK-276   To Do — הוכרע ג׳, נכנס אחרי 4/9 עם TASK-279.
TASK-279   To Do — אחרי 4/9. 276 תלוי בו, סדר מחייב.
TASK-280   To Do — נפתח היום (pip install = ~90% מריצה). אחרי 4/9.
TASK-257   To Do — נעצר: אין לו שער-קבלה. צריך ניסוח שער לפני עבודה.
TASK-259   To Do — ההכרעה ממתינה.
```

**הכרעה ≠ סגירה. התקנה ≠ סגירה.** שני הבידולים האלה הופיעו היום ושניהם
כמעט נרשמו כ-Done.

---

## 5 · שלושה חורים ידועים שהחלון נפתח איתם

כולם **מכוונים ומתועדים**, ו**כולם חייבים להירשם כהסתייגויות בדוח-הכרעת-7/9**:

1. **אין תקרת-חום.** חום נמדד היום **$11,060 = 11.1%** — מעל 8% של D3 ומעל
   טווח 6-8% של position-sizer. ⇒ פרופיל-החשיפה של כל המדגם אינו רפרודוקטיבי
   בחשבון חי. הנימוק לדחייה: 205/291 שורות מפרות ⇒ חסימה על E×Q שגוי.
2. **B-07 מכרסם במדגם** עד שהגלאי (65/49) ייכנס בשבוע-1.
3. **MFE/MAE הם חסם-תחתון** — feed=IEX, חציון 28/390 נרות-דקה למיקרו-קאפ.

---

## 6 · לקחי-שיטה — הדבר שהכי שווה להעביר הלאה

### מחלקת-הכשל של היום: "נראה כמו הגנה, ואינו מחובר"

היא הופיעה **חמש פעמים**, בכל פעם בתחפושת אחרת:

| # | מה נראה תקין | מה באמת היה |
|---|---|---|
| 1 | ארבעת מנגנוני-התהליך "קיימים" | לא נטענו — הסשן נפתח מ-`~/` ולא מהפרויקט |
| 2 | ‏hook מחווט ורשום | נתיב-בית מוחלט ⇒ clone במכונה אחרת = שומר מת |
| 3 | שער שרץ ועבר | ‏`gh` דרש הקשר-git; מהמיקום החדש מת מיד |
| 4 | עותק-שער בגוף התיק | נשא את הגרסה השבורה אחרי שהמקור תוקן |
| 5 | טריגר "מותקן" | רשום ≠ יורה. רק `watchdog_log` הוכיח |

**הכלל שנגזר: קיום-קובץ אינו ראיה. רק ירייה בפועל, מהמיקום האמיתי.**

### בקרה דו-כיוונית לכל שער — לא מותרות

שלושה שערים נכתבו היום, ו**כל אחד תפס באג בעצמו רק בזכות הבדיקה השלילית**:

```
gate266     grep גס של "429" דיווח 9 פגיעות-שווא על יום נקי
            (צירופי-מקרים בשמות-תיקיות). שער כזה היה מדווח סערה שלא קרתה.
gate265     ההרצה השלילית לבדה נכשלה על "אפס שורות בחלון" — כשל ריק
            שלא נגע במונה-הרצף. נדרש override כדי לבחון את המונה.
tool-guard  ‏RED ברנר, לא רק מקומית: גוף מלוכלך → FAIL, ניקוי → PASS.
```

⚠️ **וקוד-יציאה לבדו הטעה פעמיים.** תמיד לקרוא את הפלט ולוודא שהכשל
מהסיבה הנכונה.

### שלוש מלכודות טכניות שנפלתי בהן וכדאי לזכור

```
git ls-tree --name-only ללא -z   → C-quoting של מקף-em דיווח "MISSING" על קובץ קיים
grep | head                       → $? הוא של head; מדד-הצלחה חסר-משמעות
&& בשרשרת עם ls על קובץ חסר       → קוטע את שאר הפקודה בשקט
env ב-settings.local.json         → נטען חם, אך **אינו מתבטל** בהסרת השורה
```

### סתירות-מסמכים

ארבע פעמים היום מסמך אחד עודכן והשני נשאר עם הגרסה הישנה. **הדיף המקומי
תפס את כולן לפני הקומיט.** ⇒ לקרוא `git diff --staged` כמסמך, לא כפורמליות.

---

## 7 · ⚠️ פער-פרוטוקול — נסגר לגבי היום, ופתוח כמדיניות

`docs/SESSION_PROTOCOL.md` §3 שלב-2 מחייב **`docs/SESSION_HANDOFF_<תאריך>.md`**,
ופתיחת-הסשן קוראת את החדש ביותר דרך `ls -1t docs/SESSION_HANDOFF_*.md | head -1`.
ה-handoff של היום נכתב תחילה ל-`~/rhpro_audit_run/` בלבד — כלומר פתיחת-הסשן
הבא הייתה מוצאת את זה של **6/8**, ישן ביומיים.

✅ **נסגר לגבי היום:** הקובץ הועתק **במלואו** ל-`docs/SESSION_HANDOFF_2026-08-08.md`
(‏עותק זהה, לא מצביע). ‏`ls -1t` מחזיר אותו. **פתיחת-הסשן הבא לא צריכה לצאת
מהריפו כדי לקרוא הכל.** העותק ב-`~/rhpro_audit_run/` נשאר גם הוא.

⚠️ **מה שנשאר פתוח הוא המדיניות, לא המקרה:** דוחות-סשן נכתבים ל-`audit_run`
(מחוץ לריפו) בעוד ריטואל-הפתיחה קורא מ-`docs/`. ‏**TASK-281** נפתח לזה, עם שתי
האפשרויות ובלי הכרעה — **הכרעה של עמיחי בפתיחת שני.**

---

## 8 · קריאה חובה לסשן הבא

```
docs/WORK_PLAN.md          v1.6 — תיבת "שני 10/8" בראש הקובץ
docs/RidingHigh_Pro_PK_v2  v4.19 — ⚠️ לקרוא, לא להדפיס (RULE #14)
CLAUDE.md                  RULE #15 (חדש היום)
backlog/tasks/task-259     ההכרעה שממתינה
~/rhpro_audit_run/         TASK266_RECON · TASK276_RECON · METHOD_LANDED ·
                           TOOL_GUARD_NEGATIVE · GATE266_READY · TASK265_GATE
```

**חתימה:** ‏2026-08-08, ‏21:2x לימה. ‏5 PR מוזגו · ‏7 קומיטים · אפס שינוי
בקוד-מסחר · `order_manager.py` לא נגע.

# RidingHigh Pro — Session Protocol
*הפרוטוקול החי לפתיחת יום וסגירת יום.*
*מקור האמת היחיד לטקסים האלה. אם הזיכרון או הסקיל מתנגשים — הקובץ הזה קובע.*

---

## 0. עיקרון העל

כל העבודה נשמרת בקבצים חיים בריפו. הזיכרון של Claude (chat) מחזיק רק
**מצביעים וחוקים** — לא מצב. בכל פתיחת יום אני בונה את התמונה מאפס
מהקבצים, לעולם לא מהזיכרון. בכל סגירת יום כל בקשה חדשה הופכת ל-TASK
רשמי ב-Backlog לפני הסגירה, ונכתב SESSION_HANDOFF.

**מצביע, לא משכפל:** ההודעות וה-handoff מצביעים על מקורות חיים
(PK, MASTER, Backlog) — הם לא משכפלים עובדות לתוכם, כי עובדות משוכפלות
מתיישנות. דוגמה: לא לכתוב גרסת PK קשיחה אלא "ה-PK העדכני" + לקרוא אותו.

---

## 1. זיהוי כוונה — לא מילת קוד (Intent, not keyword)

עמיחי **לא** משתמש במילת-קוד קבועה. בכל יום הוא מנסח אחרת. תפקידי לזהות
את **הכוונה** מההקשר ולהפעיל את הטקס הנכון — בלי להתקבע על ביטוי מסוים.

**כוונת פתיחת יום** — כל ניסוח שמשמעו "מתחילים לעבוד / איפה עומדים / מה
על הפרק". דוגמאות לא ממצות: "בוקר טוב מתחילים", "מה המצב היום", "קדימה
ליום חדש", "איפה עצרנו", "תן סטטוס", "יאללה עבודה", או הדבקת פלט recon
בבוקר.

**כוונת סגירת יום** — כל ניסוח שמשמעו "מסכמים / עוטפים / שומרים לפני
שהולכים". דוגמאות לא ממצות: "סוגרים", "מספיק להיום", "תעטוף את היום",
"סיימנו", "תשמור מה שעשינו", "לקראת סיום".

**בספק — לשאול, לא להניח.** אם לא ברור אם זו כוונת טקס או שאלה רגילה,
שאל קצר ("להפעיל פתיחת יום מלאה?") לפני הפעלת טקס שלם.

**הצעה יזומה — עיקרון על.** תוך כדי כל עבודה, אם אני מזהה בעיה / סיכון /
כפילות / הזדמנות לשיפור — אני מעלה אותה תמיד, גם בלי שנשאלתי. לא עוצרים
את ההצעות. ההצעה נעשית תוך כדי העבודה על המשימה הנבחרת (לא במקום בחירת
משימה ולא כרדיפה משנית באמצעה); מה שלא מטופל עכשיו נרשם כ-TASK.

---

## 2. פתיחת יום (Session Open)

כשמזוהה כוונת פתיחת יום, בצע לפי הסדר:

### שלב 1 — סקיל + אימות תקינות
1. קרא ~/.claude/skills/rhpro-live/SKILL.md והצג שורת סקילים עם line count אמיתי.
2. **הרץ `bash ~/RidingHighPro/scripts/check_skills_integrity.sh`** — חובה. אם FAIL, עצור ותקן לפני שממשיכים. (מונע את באג 26/5 שבו ה-superpowers שונו ל-.bak והסקילים נעלמו בשקט.)

### שלב 2 — פרומפט recon אחד ל-Claude Code
פרומפט יחיד, עטוף ב-.rh-run.sh, פותח ב-wc -l של הסקיל המקומי, שקורא
מהקבצים בלבד (דינמי — בלי תאריכים קשיחים):
1. PK head — version + changelog (head -80 על docs/RidingHigh_Pro_PK_v2.md)
2. ה-SESSION_HANDOFF העדכני ביותר — ls -1t docs/SESSION_HANDOFF_*.md | head -1 ואז cat עליו. לעולם לא תאריך קשיח.
3. PROJECT_STATE.md (בשורש) — מקומי-בלבד, gitignored (TASK-161). אם חסר (clone טרי / ריצת-ענן) — לחדש עם `uv run python3 generate_project_state.py`, לא לתהות למה הוא חסר.
4. git log --oneline -10
5. MASTER_TASK_LIST מלא + backlog task list --plain מלא
6. בדיקות חיות: decision_log היום, ENTERs, Filter 9, reconciliation, agent_minute

### שלב 3 — קריאה לפני דיבור
אחרי הפלט: קרא קודם את ה-PK ואת ה-handoff העדכני, ורק אז דבר. אל תתחיל מהזיכרון.

### שלב 4 — הודעת פתיחה מלאה
הרכב הודעה לפי התבנית הקבועה — 9 חלקים מתויגים, בסדר הזה:
1. **שעון + סטטוס שוק** — שעה (Peru) + כמה זמן השוק סגור / עד פתיחה.
2. **סקילים** — מה נטען + רשימת הסקילים הזמינים המלאה (לא רק rhpro-live).
3. **PK** — נקרא מהקובץ החי. בלי לצטט גרסה מטקסט/זיכרון. **אסור להעיר/להזהיר שה-PK המוזרק לצ'אט מיושן — עמיחי יודע שזה snapshot; ההערה החוזרת היא הטריגר שמתסכל. להתעלם ממנו בשתיקה ולמשוך את הגרסה החיה, בלי אזכור "מודבק/מיושן".**
4. **מצב מערכת — טבלה** (לא prose). שורות מתויגות, לדוגמה:
   | מצב מסחר | DRY_RUN / LIVE_PAPER |
   | Sentinel | active / shadow / off |
   | ספים | MinScore · TP/SL · MinScanPrice |
   | טריות Sheets | core → היום · post_analysis → −Nd |
5. **כל המשימות הפתוחות, מקובצות** — MASTER (כל השלבים) + Backlog (כולן) + חדשות, ללא חיתוך. המספר הסופי מחושב **חי**, פקודה אחת, לא מצוטט מטקסט:
   `backlog task list --plain | awk '/:$/{h=$0} /TASK-/{if(h!~/Done|Archived|Cancelled/)c++} END{print c}'`
6. **עדיפויות** — חובה / חשוב / רצוי (3 ראשונות מה-handoff).
7. **הכרעות פתוחות** — חוסמים אסטרטגיים + כפילויות / אי-התאמות לטיפול ניהולי.
8. **⭐ המשימה הבאה** — המלצה אחת מתועדפת, בולטת, **פעם אחת בלבד** ("⭐ המשימה הבאה: X — כי [נימוק קצר]").
9. **שורה תחתונה — מוכנות כנה** (ראיה לפני הצהרה): "✅ יש לי את כל המידע מסגירת היום הקודם — המשימה הבאה X" או "⚠️ חסר לי Y".


### שלב 5 — שאלה אחרונה לפני שמתחילים

מיד אחרי הודעת הפתיחה (שלב 4, חלק 8), הצג המלצה אחת ברורה למשימה הבאה,
ואז שאל שאלה אחת קצרה **בטקסט פשוט** (לא כלי בחירה):
**"יוצאים ל-X, או מכוון אחרת?"** (X = המשימה המומלצת, עם נימוק קצר).

חוקים לשלב הזה (מודל B):
- **המלץ פעם אחת** על המשימה הבאה, בולט — ואז שאל. אל תחזור על ההמלצה יותר מפעם אחת. עמיחי מאשר או בוחר אחרת.
- **הצהר מצב ריצה.** לכל משימה מועמדת, החל את עץ-ההחלטה מ-`docs/RUN_MODE_DECISION.md` והצהר מצב מתאים (ping-pong / auto-mode / goal) + נימוק; אם עמיחי לא ציין מצב — הצע את המתאים. (TASK-103)
- **לא להתחיל לחקור** או לפעול לפני שמקבל אישור על משימה.
- **לא להציע פעולה משנית** (תיקון תשתית, סנכרון, ניקוי) כצעד ראשון של היום.
  אם זיהית בעיה כזו — רשום אותה כ-TASK ב-Backlog והמשך לשאלה.

זה הגייט בין "פתיחת יום" ל"עבודה אמיתית".
פתיחת יום הסתיימה ברגע שעמיחי בחר משימה — ולא לפני.

לפי TASK-52 META.3: לא להתחיל פעולה משנית באמצע פעולה ראשית.
לפי TASK-51 META.2: כן להציע בעיות שאתה רואה — אבל **תוך כדי עבודה על המשימה הנבחרת**, לא במקום בחירת משימה.

---

## 3. סגירת יום (Session Close)

כשמזוהה כוונת סגירת יום, בצע לפי הסדר:

### שלב 1 — סריקת בקשות היום
עבור על הסשן וזהה כל בקשה/משימה חדשה שעלתה — כולל בקשות בעל-פה. כל אחת
נכנסת ל-Backlog מיד כ-TASK (backlog task create). בסגירה נדון אם רלוונטית.

### שלב 2 — כתיבת SESSION_HANDOFF חדש
פרומפט ל-Claude Code שכותב docs/SESSION_HANDOFF_<תאריך-היום>.md בפורמט
הקבוע (ראה §4). התאריך נקבע דינמית מ-TZ=America/Lima date.

### שלב 3 — עדכון PK (חובה — ללא יוצא מן הכלל)
עדכן PK בכל אחד מאלה: סגירת יום / כתיבת SESSION_HANDOFF / כל בקשה
לעדכון משימות. תמיד קרא את הגרסה החיה (grep Document version) ואז bump
לגרסה הבאה + שורת changelog על מה שנעשה. גם אם רק תיעוד/תשתית — מעדכנים.
אם נגעת בקוד מהותי (נוסחאות / workflows / sheets / config / health
checks) — זה חוזה Anti-Drift, חובה כפולה.

### שלב 4 — עדכון סטטוס משימות
סמן ב-Backlog: הושלמו -> Done, בוטלו -> Cancelled/archive (לא להשאיר תקוע).

### שלב 5 — אימות חי
ודא: git status נקי, קובץ ה-handoff קיים, Backlog מעודכן.

### שלב 6 — שאלת ביקורת לפני סגירה
שאל את עצמך במפורש: "האם יש בקשה מהיום שלא הפכה ל-TASK?" אם כן —
היא נכנסת לפני שסוגרים. הטקס לא נסגר עד שהתשובה היא "לא".
שאל גם: עלתה היום השערת-מחקר / edge חדש שלא נרשם ב-`docs/HYPOTHESES.md`? אם כן —
רישום DRAFT לפני סגירה (pre-registration לפני הצצה לדאטה; ה-SoT לממשל-מחקר).

---

## 4. פורמט SESSION_HANDOFF (התבנית הקבועה)

כותרת (תאריך+יום) / משך / commits / משימות שנסגרו / עקרון תיעוד /
קריאה חובה לסשן הבא / טבלת commits / בעיות פתוחות מדורגות + תיוג
מאומת-מול-השערה / משימות פתוחות (מספרים מול הרשימה החיה) / תעדוף למחר /
לטיפול ניהולי / חתימה.

הפורמט המדויק נלקח מה-handoff העדכני ביותר בריפו
(ls -1t docs/SESSION_HANDOFF_*.md | head -1) — לא מקובץ עם תאריך קשיח.

---

## 5. חוקי ברזל החלים על שני הטקסים

- חקירה לפני כתיבה — קרא קוד/סכמה/נתונים אמיתיים לפני כל מסקנה.
- Claude Code בלבד — כל הוראה = בלוק inline אחד, עטוף .rh-run.sh.
- עברית, לשון זכר, בלי ask_user_input_v0.
- בלי לערבב מטרות — טקס פתיחה/סגירה לא מערבב בתוכו חקירת מערכת לא
  קשורה. אם עולה אנומליה תוך כדי — רושמים אותה כ-TASK, לא רודפים אחריה
  באמצע הטקס.
- **אין סגירה בלי אימות ואישור.** הכרזת "Done" על משימה מחייבת שני תנאים
  לפני הסימון: (1) אימות חי — ראיה מהקבצים/הקוד/הנתונים שה-acceptance מולא
  במלואו, לא מהזיכרון; (2) אישור מפורש של עמיחי. משימה לא נסגרת על סמך
  הנחה, זיכרון, או "נראה שבוצע".

---

## 6. אימות פלט מ-Claude Code (Clipboard Integrity)

כל פלט מ-.rh-run.sh עטוף בבאנר פתיחה `🎯 RH Run — HH:MM:SS Peru`
ובבאנר סגירה `✅ Done — HH:MM:SS Peru`. שתי השורות הן checksum ויזואלי.

- **כלל הבדיקה (Claude בצ'אט):** לפני עבודה על כל פלט שעמיחי מדביק,
  הדבר הראשון — ודא שקיימת שורת הסיום `✅ Done`. אם היא חסרה, הפלט
  נחתך: עצור מיד, אמור "נחתך, הדבק שוב", אל תעבוד על פלט חלקי ואל
  תמלא חורים מהזיכרון.
- **כלל הגשת תוצרים (מתוקן 2026-05-29):** heredoc **לא** שובר את האוטו-קופי — הוא רק כותב קובץ (בלי stdout), ו-.rh-run.sh מעתיק את פלט ה-tee שלו עצמו (אומת: 389 שורות עם heredoc הועתקו). הכשל האמיתי שנצפה = קריסה *לפני* צעד ההעתקה. לכן: כל תוצר מוגש דרך קובץ ומועתק דרך .rh-run.sh — לעולם לא דרך "pipe של ההודעה הסופית".

---

## 7. Changelog

- 2026-05-29: שלב 4 → תבנית קבועה מתויגת (9 חלקים) עם מצב-מערכת כטבלה, ספירת משימות חיה (one-liner), "⭐ המשימה הבאה" פעם אחת, ושורת מוכנות כנה. שלב 5 → מודל B (המלצה אחת בולטת + שאלה בטקסט; הגייט נשמר). §6 → תיקון כלל ה-heredoc (הופרך אמפירית; הגשת תוצרים דרך קובץ + .rh-run.sh).

*— END —*

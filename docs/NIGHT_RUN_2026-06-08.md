# NIGHT_RUN — 2026-06-08 (מבחן בפיקוח · TASK-94 דור-שני, שלב 1)

> **מצב:** SUPERVISED TEST (attended) — **לא** auto-mode, **לא** ריצת-לילה לא-מפוקחת.
> נועד לאמת את הזרימה `night/* → Agent #8 → verdict` עם branch אמיתי, לפני לילה אוטונומי (כלל-בטיחות #7).
> מתועד כאן כ-G5: מקור-metadata ל-best-effort של Agent #8 (כללים 4/5).

## משימה
**TASK-120** — ניקוי debris: מחיקת 2 יתומי-backlog כפולים.

## goal (תנאי-סיום מדיד)
`until backlog/archive/tasks/task-high.1 + task-high.2 are removed and committed on this branch, and git status is clean`

## מה בוצע
- `git rm` על שני קבצי-היתום (בשמות מפורשים):
  - `backlog/archive/tasks/task-high.1 - SENT.3-…-CRON_DRIFT…md`
  - `backlog/archive/tasks/task-high.2 - SENT.3-…-CRON_DRIFT…md`
- שניהם debris מ-glitch של כלי-ה-backlog (29/5): id התקבל כ-`TASK-HIGH.1`/`TASK-HIGH.2` במקום מספר, עם `parent_task_id: TASK-HIGH` (הורה לא-קיים). תוכן זהה ל-SENT.3 הקנוני = **TASK-57** (קיים בנפרד).

## כללי-בטיחות (RUN_MODE_DECISION §7 / NIGHT_RUN_TEMPLATE)
- **branch:** `night/TASK-120` (G1) · **base:** `main` · **no merge, no push ל-main** (G3, כלל #1).
- **נתיבים:** `backlog/archive/tasks/` = תחת `backlog/` → 🟢 auto-safe. אפס נגיעה ב-🔴 (config/formulas/skills/workflows/orchestrator).
- **תקרת לילה (#6):** branch יחיד (≤3).
- **קבוצה A (#3):** אין IO חיצוני (אין finviz/gspread/news/requests) — מחיקת קבצים בלבד.
- **/goal (#4):** תנאי-סיום קונקרטי ומדיד (לעיל).

## verdict צפוי מ-Agent #8
**Ready** — diff backlog-בלבד (🟢), אפס נתיב אסור, branch יחיד.

# הכרעות 2026-08-08 — D3 · D4 · T-206

**הוכרעו ע"י עמיחי, 8/8/2026.** מסמך זה מקבע את ההכרעות ואת אילוצי-המימוש
שהוכחו בביקורת; המימוש עצמו — משימות נפרדות, לא היום.

---

## D3 — תקרת חום-תיק

| פרמטר | ערך |
|---|---|
| תקרת חום | **8% מההון** (Σ 0.1×EntryPrice×Quantity על פוזיציות OPEN) |
| ε זהות (E×Q≈PS) | **0.02** יחסי |
| ε בדיקת-סיכון (Q×(SL−E)≈0.1×E×Q) | **0.05** |
| אתר-אכיפה | **אתר-כתיבת-הפקודה (order_manager), לא שלב-ההחלטה** |

**נימוק מיקום-האכיפה:** הוכח שבשלב-ההחלטה נקראת תמונת-מצב שיכולה לשקר —
ריצה חיה 22/7 (run 29940103210) קיבלה 429 על paper_portfolio והמשיכה עם
"Account state: 0 open positions" בזמן ש-69 פוזיציות היו פתוחות
(TASK-244:36-41). אכיפה שנשענת על ה-snapshot הזה תאשר כניסות בדיוק ברגעים
שבהם אסור.

**נימוק-העומק:** המערכת ב-DRY_RUN — התקרה אינה מגנה על כסף. היא גורמת לדאטה
לשחזר התנהגות של חשבון אמיתי: בלי תקרה, ה-win-rate והאחוזים שנאספים מגיעים
מפרופיל-חשיפה שלא יכול להתקיים בחשבון חי, והמדידה כולה לא ניתנת-לשחזור. זו
דרישת נאמנות-מדידה (`backtest-expert §"Punish the Strategy"`), לא ניהול-סיכון.

**שלושה אילוצי-מימוש מחייבים** (נקראו במקור, מצוטטים עם שורה):

1. **TASK-128:22** — "would-enter is MxV below -100 AND price at least 3 ONLY,
   in a SEPARATE path, **NOT the shared _check_filters**, since editing it
   would change live" ⇒ אסור לגעת ב-`_check_filters` כל עוד HYP-002 רץ;
   אכיפה ב-order_manager עוקפת את האיסור — עוד סיבה למיקום שנבחר.
2. **TASK-244:51** — "Filter 6b in decision_logic._check_filters returns
   ACCOUNT_STATE_UNAVAILABLE when the flag is set" + :40 (ה-snapshot משקר
   תחת 429, fail-open) ⇒ חישוב-החום חייב לכשול-סגור: אם קריאת-הפוזיציות
   נכשלה — אין כניסה, לא "אין חום".
3. **TASK-259:44** — "This is a time-of-check to time-of-use gap BETWEEN
   PROCESSES" (ריצות agent_minute חופפות ב-92.5%, TASK-244:94) ⇒ בדיקת-החום
   חייבת לרוץ צמוד-לכתיבה, על הנתון הטרי ביותר, ולא לרשת snapshot שנלקח
   דקות קודם בתחילת run().

---

## D4 — שורות יולי המזוהמות

**ההכרעה: לסמן ולהחריג. לעולם לא למחוק.**

- **מנגנון הסימון כבר הוכרע ואין להמציא שני** — TASK-246, כלל שתי-השכבות
  שנקבע 3/8 ("TIER RULE SETTLED 2026-08-03", TASK-246:32): 
  `PhantomTicker=PHANTOM/PHANTOM_SUSPECT` +
  `paper_portfolio.DataQuality=PHANTOM_TICKER/PHANTOM_TICKER_SUSPECT`;
  הכלי כבר מחווט — `mark_phantom_rows_v1` על `classify_phantom_tier`
  (TASK-246:46). ה-blocker של 246 ("the finviz ticker fix") הוסר ב-8/8.
- **מה שנשאר פתוח — סינון הצרכנים:** `critic_v1` (מצרפי שבוע/חודש) ·
  `daily_brief` (המייל היומי) · עמודי-הדשבורד — כולם חייבים
  `DataQuality not in (PHANTOM_TICKER, PHANTOM_TICKER_SUSPECT)`. זו הזרוע-
  האוכפת של שער 6 (T-601/T-602-שארית).
- **85 הפוזיציות התקועות נשארות.** הן בסיס-הראיות של תקרית-יולי ואין להן
  undo; מחיקה הייתה משמידה את היכולת לאמת כל ניתוח עתידי של התקופה.

---

## T-206 — quota_health יימחק

**ההכרעה: מחיקה** (לא כוונון-ספים — נפסל). שלוש סיבות בלתי-תלויות, כל אחת
לבדה מספיקה, שהוכיחו שהגלאי **אינו יכול** לירות:

1. **מודד ציר לא-נכון** — סופר כתיבות בעוד תקריות-ה-429 הן על קריאות
   (מקור: S5_DEEP_v1.md, אשכול K; S7_TASKS_v1.md §T-206).
2. **סדר-גודל לא-נכון** — ≤6 כתיבות/תהליך מול סף 50; הסף לעולם לא נחצה
   (מקור: S7_TASKS_v1.md §T-206; STAGE2_RANKING_v3_delta.md §2 K-01).
3. **תזמון לא-נכון** — הבדיקה רצה לפני שהכתיבות קורות, וה-deque הוא
   in-process וריק בזמן הבדיקה (מקור: S5_DEEP_v1.md; delta §2: "רץ לפני
   שהכתיבות קורות, והמונה in-process").

הניסוח המדויק של הממצא (delta §2): הטענה אינה "אפס אירועים ב-120,259 שורות"
אלא **"לא יכול לקרות"** — גלאי שמבנהו מונע ממנו לירות שווה פחות מאין-גלאי,
כי הוא נראה ככיסוי. חלופה עתידית אם תידרש: חיווט `record_read`/
`get_read_counts` (`sheets_manager:431-438` — קיימים, לא מחוברים) לסף-קריאות.
**המחיקה עצמה = תיק-קוד נפרד (נפתח היום), לא בוצעה במסמך זה.**

---

# חמש הכרעות שהומרו מתיקי-בקלוג לתיעוד (2026-08-08)

חמישה תיקים נסגרו Done לא כי בוצעו, אלא כי **ההכרעה לגביהם כבר התקבלה** והם
היו מסמך שהתחזה למשימה. התוכן נשמר כאן; קבצי-התיקים עצמם נשארים בריפו
(status: Done) ואינם נמחקים.

## D5 · TASK-9 — מודול-אנליטיקה ל-Sentinel: **לא נדרש כמשימה**

**מה ביקש:** מודול שמחשב false-positive-rate פר-בדיקה, פילוח BLOCK לפי סיבה,
ומגמה לאורך זמן — כתנאי-קדם למעבר shadow→active.

**ההכרעה: נסגר.** שתי סיבות בלתי-תלויות:
1. **הליבה כבר קיימת** — `agent/sentinel/shadow_audit_v1.py` (אומת חי): "runs
   every Sentinel check against TODAY's real latest scan ... reports the
   would-be BLOCK/WARN/ALLOW distribution. Used to measure the real
   false-positive BLOCK rate before switching SENTINEL_MODE shadow→active".
   כלומר FP-rate + פילוח כבר נמדדים; חסר רק "מגמה לאורך זמן".
2. **המעבר שהוא משרת מוטל בווטו** — ראו D6 להלן.

מודול-מגמה ייבנה רק אם ומתי הווטו של D6 יוסר.

## D6 · TASK-66 — ה-counterfactual ההפוך: **וטו עומד על SENTINEL active**

**הממצא:** העסקאות שה-Sentinel *היה* חוסם הן דווקא המנצחות — win-rate 64%
מול 41% (n=36, רג'ים יחיד). ההסבר המוצע: סקאן ישן = מהלך חד = שורט טוב, ולכן
`scan_freshness` חוסם בדיוק את הסטאפים הטובים. `scan_freshness` הוא הרכיב
הדומיננטי: 25,311 מתוך 45,008 אירועי `sentinel_events` (price_freshness 15,456).

**ההכרעה: `SENTINEL_MODE` נשאר `"shadow"` (config.py:359) עד שתהיה מדידה
רב-רג'ימית.** זו הכרעה עומדת, לא משימה פתוחה.

⚠️ **הבחנה שחייבת להישמר:** `SENTINEL_MODE` ו-`EXPLICIT_GATE_MODE` הם שני
מתגים נפרדים. השני עבר ל-`"active"` ב-29/6 (config.py:374) — זה **אינו** מבטל
את הווטו על הראשון. כל מי שיקרא "הגייט active" ויסיק ש-Sentinel הופעל, טועה.

## D7 · TASK-83 — הרחבת drops_post ב-DropsLab: **לא מתבצע**

**מה ביקש:** להרחיב את מעקב-drops_post מ-5 ימים ל-15/20 (נקודות בדידות
d1,d2,d3,d5,d7,d10,d15,d20 + `max_further_drop_20d` + `max_recovery_20d`),
ובעדיפות עליונה — VWAP ביום-הנפילה ומיקום-הסגירה יחסית אליו, וכן 3-4 נקודות
מחיר תוך-יומיות בפתיחת יום-המחרת.

**ההכרעה: נסגר.** ה-AC שהוגדר "עדיפות עליונה" (VWAP אמיתי) **נפסל
מערכתית** ברולינג 5/8 מטעמי כיסוי-feed (IEX-בלבד — ראו TASK-82 §"VWAP אמיתי:
נסגר. לא ייושם"). מה שנשאר הוא הרחבת-חלון בריפו חיצוני
(`Ambroseius/DropsLab`) שחיותו לא אומתה מאז 2026-06-15. ההשערה עצמה (קריסה
עשויה להעמיק אחרי 7-15 יום, וחלון-5 חותך אותה) **נשמרת כאן** ותיבחן, אם
בכלל, מתוך `post_analysis` D6-D25 שכבר נאסף אצלנו — לא מ-DropsLab.

## D8 · TASK-88 — גרפים במייל החודשי: **לא מתבצע כעת**

**מה ביקש:** equity curve (רווח/הפסד מצטבר לאורך החודש) + בר-לכל-מדד
(Score/RunUp/ATRX/Float/MxV) כתמונות base64 במייל, בלי לגעת ב-build_monthly_row.

**ההכרעה: נסגר.** שני המדדים המרכזיים שהוא אמור להציג מתים: `Score` קפוא
(`SCORE_WRITE_FROZEN=True`, אפס מילוי בטאבי-הסוכן מאז יולי) ו-`MxV` הוצא
מטענת-ההפרדה מראש (`MEASUREMENT_HORIZON` — 93% מהדחיות). גרף על עמודות ריקות
מייצר ביטחון-שווא. ⚠️ בנוסף, ה-equity-curve הוא **מצרף חדש** ולכן יעד של
שער-6 (T-601) — אם ייבנה בעתיד, חייב לרוץ אחרי מסנן-ה-DataQuality של D4.

## D9 · TASK-92 — צמצום תיעוד-הדקה ב-timeline_live: **הדיון נסגר**

**מה ביקש:** להכריע אם לצמצם/לבטל כתיבה-כל-דקה ל-`timeline_live` (ה-writer
הכבד ביותר: ~250-300K שורות/חודש, ~390 כתיבות/יום), כחשוד בלחץ-429.

**ההכרעה: נסגר כדיון — הפרמיסה התפוררה ושלושת המנופים עברו בעלים.**
1. **בעלות ה-429 עברה** ל-TASK-215 (SA ייעודי ל-auto_scan) — זה השורש
   הארכיטקטוני, לא נפח-הכתיבה.
2. **מנוף (ב) "להפסיק לכתוב רק עמודת Score"** — הוכרע במקום אחר: TASK-260
   מתעד ש-`timeline_live` **כן** כותב Score מלא למרות ההקפאה, וההכרעה אם זו
   שמירה-מכוונת-למחקר או פספוס שייכת שם.
3. **מנוף (א) הסתרת דף-הדשבורד** — הפיך וזניח; **מנוף (ג) הפסקת-כתיבה
   מלאה** — נפסל: `orchestrator.read_latest_signals` קורא סיגנלים מהטאב הזה
   (וכך גם `shadow_audit_v1`), ו-post_analysis בונה ממנו את dataset-המחקר.
   ביטול = שבירת הסוכן והמחקר כאחד.

⚠️ **מה שכן שורד מהתיק ועבר לתיק אחר:** ניטור נפח/תקרת-התאים של
`timeline_live` — זה T-204 (בדיקת-בריאות מול תקרת 10M), ו-TASK-253
(כפילויות מריצות חופפות שמנפחות את אותו מונה).

---

# Score — הכרעת 8/8

**ההחלטה: Score יוסר מהמערכת.** הביצוע בשני שלבים.

## למה שני שלבים ולא אחד

חלון-המדידה נפתח 2026-08-10 ונסגר 2026-09-04. `auto_scanner.py:429`
(`sorted(results, key=Score, reverse=True)`) קובע את סדר-השורות ב-`timeline_live`,
ומשם את סדר-העיבוד של הסוכן — ותחת ה-caps (`AGENT_COLD_START_MAX_CONCURRENT` /
`MAX_DAILY`) **מי-שנבדק-ראשון תופס את תקציב-הכניסות**. שינוי הסדר בתוך החלון
משנה אילו טיקרים נכנסים ⇒ משנה את הדאטה ⇒ פוסל את החלון. זה בדיוק מה שקרה
ל-HYP-002 ביולי, ולא נחזור על זה.

```
שלב א — תצוגה   עכשיו (8/8)      אתרים שאינם במורד-זרם של שום כתיבה
שלב ב — התנהגות אחרי 2026-09-04  :429 · :504 · :1349 · decision_logic:270/:372
```

## המיון החלופי: **MxV עולה** (השלילי-ביותר ראשון) — הכרעת עמיחי, 8/8

**הנימוק:** `MxV <= AGENT_MXV_MAX` הוא כבר השער הקובע היחיד בפועל (Filter-1
כבוי מ-29/6; `MXV_TOO_HIGH` = 93% מכלל הדחיות), ועומק ה-MxV הוא הנהג העיקרי
של התוצאה. מיון לפי אותו מדד שמחליט = עקביות בין הסינון לתיעדוף.

⚠️ **פתוח למימוש — חובה טיפול מפורש: התנהגות המיון כשחסר MxV.** ב-Python
`None`/`NaN` במפתח-מיון אינו נופל לסוף מעצמו, ובמיון-עולה ערך חסר עלול לצוף
לראש ולתפוס תקציב-כניסות בלי שנמדד עליו דבר. המימוש חייב לקבע במפורש
ש-MxV חסר/לא-מספרי יורד **לסוף** הרשימה, עם טסט שמוכיח זאת. בלי זה, החלפת
המיון מכניסה באג-שקט חמור מזה שהיא באה לפתור.

## ⚠️ המשקלים מעולם לא הוצדקו בכתב

`SCORE_WEIGHTS_V2 = 25/25/20/10/10/5/5` נכנסו בקומיט `f3d96ca` (2026-04-11).
**גוף-הקומיט ריק** — השורה היחידה היא רשימת הערכים עצמם; אפס docs באותו קומיט,
אפס נימוק ב-PK, ואפס תוצאה בחיפוש רוחבי ב-`docs/`. ה-V1 שקדם (MXV=30%, Gap=10%)
ירש מ-"v14.1" שקדם לריפו — גם לו אין מקור.
**החריג היחיד:** מדרגות ה-RSI, שיש להן הערת-קוד עם מקור ("research 22/4/2026:
RSI 90+=100% TP20, bell curve 50-70 was weakest zone").
ומה שכן כתוב — כתוב **נגד**: `RIDINGHIGH_FIX_PLAN_2026-06-09.md:17` — "70%
מהמשקל (MxV 25 + RunUp 25 + ATRX 20) על מדדים עם ~0 קורלציה לתוצאה; Price —
המנבא המובהק היחיד (r=+0.249) — לא במשקלים בכלל".

זה מתועד כאן **כדי שלא ייווצר נימוק בדיעבד.** אם מישהו יסביר בעתיד למה
המשקלים "נכונים" — ההסבר נולד אחרי המספרים, לא לפניהם.

## מה כבר אינו Score — למניעת בלבול עתידי

Score כבר **אינו** שער-כניסה (Filter-1 כבוי, `decision_logic:309`), **אינו**
שער ל-`daily_snapshots` (`auto_scanner:489-490` לוקח את כל הסריקה), ו**אינו**
שער לעולם-ה-borrow (`get_scanned_universe` עבר ל-MxV ב-TASK-208-B). מה שנשאר
לו בהתנהגות: סדר-עדיפות אחד ושני שערי-כתיבה כמעט-מתים (`Score>=70`, מקס-נמדד
60.86 ב-3/8). המפה המלאה: `rhpro_audit_run/SCORE_MAP_2026-08-08.md`.

---

# שני שעוני-ההכרעה

בניית תוכנית-העבודה (8/8) חשפה שני תאריכי-הכרעה שחיו במקביל בלי שנאמר במפורש
שהם **שני דברים שונים**. שניהם לגיטימיים, ואף אחד מהם אינו מחליף את השני.

## 7/9 — הכרעת חלון-המדידה

**השאלה:** האם מדדי-הכניסה של המועמדים שנדחו מבדילים אותם מאלה שנכנסו.

חלון: 10/8 → 4/9 (20 ימי-מסחר) · רצפה חוסמת `n_ENTER ≥ 100` · MxV מוחרג
מטענת-ההפרדה מראש (93% מהדחיות ⇒ מעגלי) · Bonferroni על 5 המדדים הנותרים,
α=0.01 · AUC<0.60 = שלילי. הפירוט המלא: `docs/MEASUREMENT_HORIZON.md`.

זו שאלה על **איכות-הסינון**: האם השער בכלל מבחין. היא נענית על דאטת-החלון
בלבד ואינה מצריכה תוצאות-מסחר.

## אוקטובר — ורדיקט HYP-002

**השאלה:** האם יש edge שמצדיק כסף אמיתי.

ההשערה נפסלה ב-3/8 ונרשמה מחדש מאפס; הגייט החדש: 45 ימי-מסחר מ-3/8 או
n≥150 כניסות פוסט-flip. זו שאלה על **רווחיות**, והיא דורשת עסקאות שנסגרו,
לא רק כניסות שנרשמו.

## למה ההבחנה חשובה מעשית

**אשכול-Score קפוא על השני, לא על הראשון.** רולינג 5/8 הצמיד את
TASK-208/209/128/273 ואת המשך-הסרת-Score ל"אחרי ורדיקט HYP-002" — כלומר
אוקטובר. מי שיקרא "הכרעה 7/9" ויסיק שאפשר להפשיר את האשכול בספטמבר —
טועה, ועלול לשנות התנהגות בזמן שההשערה עדיין רצה.

ובכיוון ההפוך: אין להשהות את עבודת-החלון (‏194 ניטור · 202 חוצה-חודש ·
73 KPI) בטענה ש"ממילא מחכים לאוקטובר" — הן מזינות את ההכרעה של 7/9,
שהיא שער עצמאי.

```
7/9        מדידה   האם השער מבחין        → קובע אם יש בכלל מה לייעל
אוקטובר    edge     האם זה מרוויח כסף     → קובע אם מפשירים את אשכול-Score
```

⚠️ **פתוח, לא הוכרע כאן:** מה קורה אם 7/9 יוצא שלילי (המדדים אינם מבדילים)
בעוד HYP-002 עוד רצה עד אוקטובר — האם זה מקצר את ההשערה, משנה אותה, או
לא נוגע בה. השאלה נשארת פתוחה עד שנגיע אליה.

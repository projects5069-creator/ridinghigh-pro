# הכרעות 2026-08-08 — D3 · D4 · T-206

**הוכרעו ע"י עמיחי, 8/8/2026.** מסמך זה מקבע את ההכרעות ואת אילוצי-המימוש
שהוכחו בביקורת; המימוש עצמו — משימות נפרדות, לא היום.

---

## D3 — תקרת חום-תיק

| פרמטר | ערך |
|---|---|
| תקרת חום | **8% מההון** (Σ 0.1×EntryPrice×Quantity על פוזיציות OPEN) |
| ε זהות (E×Q≈PS) | **0.02** יחסי |
| ε בדיקת-סיכון (Q×(SL−E)≈0.1×E×Q) | **0.05** |
| אתר-אכיפה | **אתר-כתיבת-הפקודה (order_manager), לא שלב-ההחלטה** |

**נימוק מיקום-האכיפה:** הוכח שבשלב-ההחלטה נקראת תמונת-מצב שיכולה לשקר —
ריצה חיה 22/7 (run 29940103210) קיבלה 429 על paper_portfolio והמשיכה עם
"Account state: 0 open positions" בזמן ש-69 פוזיציות היו פתוחות
(TASK-244:36-41). אכיפה שנשענת על ה-snapshot הזה תאשר כניסות בדיוק ברגעים
שבהם אסור.

**נימוק-העומק:** המערכת ב-DRY_RUN — התקרה אינה מגנה על כסף. היא גורמת לדאטה
לשחזר התנהגות של חשבון אמיתי: בלי תקרה, ה-win-rate והאחוזים שנאספים מגיעים
מפרופיל-חשיפה שלא יכול להתקיים בחשבון חי, והמדידה כולה לא ניתנת-לשחזור. זו
דרישת נאמנות-מדידה (`backtest-expert §"Punish the Strategy"`), לא ניהול-סיכון.

**שלושה אילוצי-מימוש מחייבים** (נקראו במקור, מצוטטים עם שורה):

1. **TASK-128:22** — "would-enter is MxV below -100 AND price at least 3 ONLY,
   in a SEPARATE path, **NOT the shared _check_filters**, since editing it
   would change live" ⇒ אסור לגעת ב-`_check_filters` כל עוד HYP-002 רץ;
   אכיפה ב-order_manager עוקפת את האיסור — עוד סיבה למיקום שנבחר.
2. **TASK-244:51** — "Filter 6b in decision_logic._check_filters returns
   ACCOUNT_STATE_UNAVAILABLE when the flag is set" + :40 (ה-snapshot משקר
   תחת 429, fail-open) ⇒ חישוב-החום חייב לכשול-סגור: אם קריאת-הפוזיציות
   נכשלה — אין כניסה, לא "אין חום".
3. **TASK-259:44** — "This is a time-of-check to time-of-use gap BETWEEN
   PROCESSES" (ריצות agent_minute חופפות ב-92.5%, TASK-244:94) ⇒ בדיקת-החום
   חייבת לרוץ צמוד-לכתיבה, על הנתון הטרי ביותר, ולא לרשת snapshot שנלקח
   דקות קודם בתחילת run().

---

## D4 — שורות יולי המזוהמות

**ההכרעה: לסמן ולהחריג. לעולם לא למחוק.**

- **מנגנון הסימון כבר הוכרע ואין להמציא שני** — TASK-246, כלל שתי-השכבות
  שנקבע 3/8 ("TIER RULE SETTLED 2026-08-03", TASK-246:32): 
  `PhantomTicker=PHANTOM/PHANTOM_SUSPECT` +
  `paper_portfolio.DataQuality=PHANTOM_TICKER/PHANTOM_TICKER_SUSPECT`;
  הכלי כבר מחווט — `mark_phantom_rows_v1` על `classify_phantom_tier`
  (TASK-246:46). ה-blocker של 246 ("the finviz ticker fix") הוסר ב-8/8.
- **מה שנשאר פתוח — סינון הצרכנים:** `critic_v1` (מצרפי שבוע/חודש) ·
  `daily_brief` (המייל היומי) · עמודי-הדשבורד — כולם חייבים
  `DataQuality not in (PHANTOM_TICKER, PHANTOM_TICKER_SUSPECT)`. זו הזרוע-
  האוכפת של שער 6 (T-601/T-602-שארית).
- **85 הפוזיציות התקועות נשארות.** הן בסיס-הראיות של תקרית-יולי ואין להן
  undo; מחיקה הייתה משמידה את היכולת לאמת כל ניתוח עתידי של התקופה.

---

## T-206 — quota_health יימחק

**ההכרעה: מחיקה** (לא כוונון-ספים — נפסל). שלוש סיבות בלתי-תלויות, כל אחת
לבדה מספיקה, שהוכיחו שהגלאי **אינו יכול** לירות:

1. **מודד ציר לא-נכון** — סופר כתיבות בעוד תקריות-ה-429 הן על קריאות
   (מקור: S5_DEEP_v1.md, אשכול K; S7_TASKS_v1.md §T-206).
2. **סדר-גודל לא-נכון** — ≤6 כתיבות/תהליך מול סף 50; הסף לעולם לא נחצה
   (מקור: S7_TASKS_v1.md §T-206; STAGE2_RANKING_v3_delta.md §2 K-01).
3. **תזמון לא-נכון** — הבדיקה רצה לפני שהכתיבות קורות, וה-deque הוא
   in-process וריק בזמן הבדיקה (מקור: S5_DEEP_v1.md; delta §2: "רץ לפני
   שהכתיבות קורות, והמונה in-process").

הניסוח המדויק של הממצא (delta §2): הטענה אינה "אפס אירועים ב-120,259 שורות"
אלא **"לא יכול לקרות"** — גלאי שמבנהו מונע ממנו לירות שווה פחות מאין-גלאי,
כי הוא נראה ככיסוי. חלופה עתידית אם תידרש: חיווט `record_read`/
`get_read_counts` (`sheets_manager:431-438` — קיימים, לא מחוברים) לסף-קריאות.
**המחיקה עצמה = תיק-קוד נפרד (נפתח היום), לא בוצעה במסמך זה.**

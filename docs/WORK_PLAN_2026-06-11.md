# WORK_PLAN — מ-2026-06-11 (נגזר מ-TASK-139-INV + שש ההכרעות)

*נכתב 2026-06-10 ערב, אושר ע"י עמיחי. מקור: docs/research/INVESTIGATION_2026-06-10/REPORT.md.*

## סדר יום 11/6

(א) פתיחת יום — כולל אימות ראשון של skip_summary (אחרי 08:30 פרו).
(ב) **task-139 [BORROW]** — borrow_data daily collection: TDD מלא, branch נפרד, investigate-before-touch, py_compile, diff, אישור עמיחי לפני commit.
(ג) **task-134** — 16 הטסטים השבורים בסוויטת ה-unit — תנאי לכל TDD עתידי.
(ד) אם נשאר: **task-143** — כפילות RH-2026-07-post_analysis (דדליין קשיח 1/7).

## שבוע 1 — שכבת האמת

מטרה: לדעת מה באמת קורה כלכלית לפני שמשנים לוגיקה.

- task-139 [BORROW] — borrow/shortability אמיתיים (הקלט שחסר למודל העלויות).
- task-134 — תיקון סוויטת הטסטים (תשתית לכל ה-TDD בהמשך).
- task-140 — מודל net-PnL קבוע ב-post_analysis (slippage 1%/side + borrow פרו-רטה).
- task-142 — מעבר ה-WR הרשמי ל-D1_Open (הוכרע ②); scan-entry → טבלה דיאגנוסטית.
- task-155 — minute-bars fetcher (נפתחה הערב) — בסיס להכרעת WHIPSAW ולמחקרי כניסה.
- task-74 — השלמת תוצאות ל-~946 מניות חסרות (דאטה-אובד-יומי).
- task-143 — כפילות יולי (דדליין 1/7).
- task-144 — החייאת DropsLab collector (backlog מתנפח יומית).
- task-90 + task-148 — גלאי reverse-split בשני הצדדים (ניקוי דאטה לפני סטטיסטיקה).
- task-135 — holiday-blind (דדליין רך 3/7) + task-61 — אימות weekly_summary.
- אנליזה: הכרעת 26 ה-WHIPSAW בברים דקתיים (אחרי task-155).

**יעד שבוע 1: תוחלת-נטו אמיתית ראשונה, בדיווח כפול (רשמי + פסימי), על borrow אמיתי.**

## שבוע 2 — השער והשלמות

- task-141 דרך task-128 — השער המפורש ב-shadow (הוכרע ①; אפס החלפה חיה).
- אשכול-משטר: task-15 + task-42 + task-70 (market context → החלטות, SPY benchmark, VIX).
- שרשרת reconciliation: task-49 + task-65 (+המשכי 105-107).
- אשכול quota: task-58 + task-92 + task-136.
- אשכול Sentinel: task-9 + task-66 — תנאי לכל דיון active.
- task-145 (critic_monthly), task-150 (header-aware), task-149 (NO_DATA), task-137 (RSI כפול).

## שבועות 3-4

- **HYPOTHESES.md** — ריכוז ההשערות הקיימות (68/70/75/71/72/69) + ניסוח 5-6 כללי כניסה → event study ראשון על minute-bars.
- task-126 — חילוץ SKIPs היסטוריים **לפני פקיעת retention**.
- batch תשתית: 110-120 שיוריות, task-101, task-54, task-118, task-96.
- אשכול Critic: task-62 + task-73 + task-88 + task-89 + task-39.
- docs: task-151 + task-129 (RSI/PK), task-152 (mirror), task-153 + task-27 (DropsLab PK), task-138.
- רוטציית 1/7 בהשגחה — אחרי task-143.

## מוקפא בנימוק

- task-33, task-34 (סוכנים חדשים) — אין טעם לבנות סוכנים על Score שהוכח לא-מדרג; אחרי השער.
- task-10 (Filter 12) — אין מוסיפים פילטרים עד שהשער המפורש מוכרע.
- task-83 (d6-d15) — ה-collector מת (task-144 קודם); אין להאריך צנרת שבורה.
- task-131 (LONG) — הורד ל-LOW בהכרעת 10/6: אין אות יומי ב-n=2,231.
- task-109 (auto-repair) — GATE לא הבשיל (0 ימי הוכחה).
- task-113, task-11 — nice-to-have, לא בשרשרת הקריטית.

## אסטרטגי-בהמתנה

- task-121-2c + task-102 — לילות אוטונומיים לא-מפוקחים (מאחורי gates).
- task-154 — private migration + history scrub (הכרעת ④ דחתה לשלב הערכה).

## עקרונות עבודה (מאושרים)

1. דאטה-אובד-יומי קודם לכל (collector, retention, backfill).
2. דדליין קשיח לפני רך (143 לפני 135).
3. ניקוי דאטה לפני סטטיסטיקה (splits לפני קורלציות).
4. gate לפני מימוש; shadow לפני החלפה.
5. 40/40/20 בשעות (Analysis/Dev/Maintenance) + רישום ב-docs/WORK_LOG.md.
6. סשן בלי push מלא — לא קרה.

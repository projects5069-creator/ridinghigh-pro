> **מתעדכן במקום, בפתיחת יום ובסגירת יום. שם קבוע — קובץ עם תאריך הופך
> לתצלום ומתיישן.**

# תוכנית-עבודה — גרסה 1.5, 8/8/2026

*(נבנתה 8/8 ‏14:40 לימה, ‏41.8 שעות לפני פתיחת-החלון)*

```
לפני שני חייבים:   ‏266 הוכרעה 8/8 (ג׳ — לא נוגעים בתקרה) ⇒ ירדה מהקבוצה.
                   נשארה **רק 259** (הכרעת-concurrency), ואף היא מומלץ שתמתין
                   למדידת-שני — ראו §4א.
הנתיב הקריטי:      **מדידת-שני-ערב (266+259 במדידה אחת)** → 259-הכרעה → 244-אימות
                   → שלמות-המדגם → הכרעת-7/9 → ורדיקט → הפשרת אשכול-Score/186.
                   ובאופק-הרחוק: 258 → 179 → 255 (חורף).
השער השני יוריק:   שער-4 (מדידה) — ריאלי 11-12/8, אחרי סגירת-הייצור הראשונה,
                   בתנאי ש-B-07 לא בולע את הכתיבות של היום הראשון.
```

מקור: `docs/MASTER_REGISTER.md` (62 משימות, קובע היום). התוכנית לא בונה רשימה —
היא מסדרת אותה. **ושורת-יושר מראש: 62 המשימות לא נגמרות עד 4/9 ולא צריכות —
רק ~10 שייכות לחלון עצמו; ~12 קפואות בהכרעה; והזנב הארוך (crossover) נמשך
לתוך החורף בגלל צבירת-אירועים, לא בגלל עבודה.**

---

## 1 · שני הדד-ליינים ומה תלוי בכל אחד

### שני 10/8, 08:30 לימה — פתיחת החלון (n_ENTER≥100 · הכרעה 7/9)

מה שלא עומד עד אז פוגע בדאטה **מהשעה הראשונה**:

**הקבוצה צומצמה 8/8** אחרי שהתוכנית נבנתה. הקריטריון שהוחל: *חייב-לפני* =
משנה את **משטר-הריצות**, ולכן פריסתו באמצע החלון מכניסה משתנה חדש לניסוי.
כל השאר — רצוי, לא חובה.

| חייב-לפני | למה דווקא לפני | בעלים |
|---|---|---|
| **259 הכרעת-concurrency** | לפרוס לפני-פתיחה או לא-לפרוס-בכלל-בחלון; באמצע = חצי-מדגם בכל משטר | **עמיחי** מכריע (השאלה בגוף התיק) ⚠️ ראו הערה מתחת לטבלה |

~~| **266 הכרעת-timeout** | …‏38.6% ריצות-קטועות… |~~ ✅ **הוכרעה 8/8 — ירדה מהקבוצה.**
אופציה ג׳: **לא נוגעים בתקרה**, מודדים בשני ומחליטים על הנתונים. אפס שינוי קוד
⇒ אין מה "להספיק לפני שני". ‏266 עברה מ*חייב-לפני* ל**נמדדת-בשני**. הנימוקים
המלאים בגוף התיק; ה-recon: `~/rhpro_audit_run/TASK266_RECON.md`.

⚠️ **וזה משנה גם את 259.** ה-recon של 266 מדד ב-8/7, אחרי `fcdb0aa`+`1df9cba`:
**חפיפת-ריצות 10.2%** (מול 92.5% ב-8/5), חציון-job 48ש, ‏p90 56ש, ‏0 מבוטלות
מ-492. אם המספר הזה מחזיק בשני **עם סיגנלים אמיתיים** — החפיפה שהיא כל הטעם
של 259 נעלמת מאליה, והדיף שיושב בעץ-העבודה מתייתר. ⚠️ ‏8/7 אינו ראיה מספקת
(יום ה-FINVIZ השבור; `No signals to process this minute` בלוגים).
⇒ **ההמלצה: לא להכריע את 259 לפני מדידת-שני.** אם תעדיף להכריע לפני — זו
הכרעה על 8/5 בלבד, ויש לרשום זאת.

**רצוי — לא חוסם את הפתיחה:**

| פריט | מה קורה אם לא ייכנס עד שני | בעלים |
|---|---|---|
~~| 265 כלב-השמירה | פער-כיסוי של ימים ספורים… |~~ ✅ **הותקן 8/8 — ירד מהקבוצה.**
עמיחי התקין ב-Apps Script על **RH-Summaries**, טריגר `rhWatchdogRun` כל 5 דקות,
טאב `watchdog_log` נוצר. ⚠️ **התיק לא נסגר** — ‏AC#1 דורש ≥3 שורות רצופות
מטריגר שירה **מעצמו** ובשעות-מסחר, וזה אפשרי רק בשני. **טריגר רשום אינו טריגר
שיורה** ⇒ נכנס לאימות-שני, לא לרשימת-החובה.
~~| 276 תקרת-החום | הימים הראשונים נאספים ללא תקרה… |~~ ✅ **הוכרעה 8/8 — ג׳,
יורדת מהקבוצה.** נכנסת **אחרי 4/9, תלוי-TASK-279**. אפס שינוי קוד.

⇒ **קבוצת "רצוי לפני שני" ריקה. קבוצת "חייב לפני שני" מכילה רק את הכרעת-259,
ואף היא מומלץ שתמתין למדידת-שני.** אין יותר עבודת-קוד לפני פתיחת החלון.

⚠️ **חור ידוע, מכוון ומתועד: החלון נפתח בלי תקרת-חום.** החום הנמדד היום
**$11,060 = 11.1%** — מעל תקרת-8% של D3 ומעל טווח 6-8% של `position-sizer` KP#6.
⇒ פרופיל-החשיפה של **כל** המדגם (‏n≥100) אינו רפרודוקטיבי בחשבון חי, וזה בדיוק
הנימוק המרכזי של D3. ⚠️ **חייב להירשם כהסתייגות בדוח-הכרעת-7/9** ולא להיקרא
כתוצאה נקייה.
הנימוק המכריע לדחייה: `gate3` מדד **205/291 שורות מפרות** את אינווריאנטי-הזהות,
והחום הוא Σ‏0.10·E×Q — כלומר תקרה שהייתה נכנסת עכשיו הייתה חוסמת כניסות אמיתיות
על בסיס חישוב שידוע שחלקו שגוי. הצרכן לפני הספק. ‏recon:
`~/rhpro_audit_run/TASK276_RECON.md`.

**הוזזו מהקבוצה:**
- **194 → שבוע 1 של החלון.** זו עבודת-הגדרה (docs) שאפשר לעשות תוך כדי
  איסוף; היא לא משנה מה נאסף, רק מי קורא. ⚠️ אבל לא מאוחר יותר — בלעדיה
  החלון נאסף בלי בעלים ל-7/9.
- **270 יצאה מהקבוצה לגמרי.** הדדליין שלה הוא **פקיעת לוגי-Actions (~9/8)**,
  לא חלון-המדידה. לוח-זמנים נפרד שבמקרה סמוך; היא דחופה בזכות עצמה ולא
  בזכות שני.

⚠️ **מה בכוונה לא-לפני-שני,** למרות שנשמע דחוף: גלאי-B-07 (65/49), התרעת-226,
בדיקות-252/275 — קוד-הגנה שנחיתתו החפוזה בסופ"ש מסוכנת מהמתנה של יומיים.
נכנסים בשבוע הראשון של החלון, בזהירות מלאה.

### 4/9 — סגירת החלון (הכרעה 7/9)

אחריו נפתחת העבודה שאסורה בתוכו: Score שלב-ב (:429 מיון-MxV-עולה, שערי-70,
d.score) · T-301 חישוב-הכמות-מחדש · 249 שינוי-האיסוף · 206 עמודות-חדשות ·
258 בניית-ה-crossover · 82 sigma-bands. **כלל-הברזל: מה שמשנה אילו שורות
נכתבות או מה ערכן — לא נוגעים בו בין 10/8 ל-4/9.** (לקח HYP-002 של יולי.)

---

## 2 · הנתיב הקריטי

### הקצר (עד הכרעת-7/9) — שרשרת-שלמות-המדגם

```
[266 הוכרעה 8/8 — ג׳, אפס שינוי] ──→ **מדידת-שני-ערב אחת** ──→ 259 הכרעת-פריסה
                                       (שער-266: מבוטלות · חציון+p90 ·      │
                                        429-אמיתיים · חפיפה-בפועל)          │
        ┌──────────────────────────────────────────────────────────────────┘
        └──→ 253 גלאי-כפילויות (baseline לפני-הפריסה, אם תהיה) ────────────┐
                                                                   ▼
                              244 אימות-F6b + סגירת-שורש-החפיפה (פריצה = פסילת-HYP-002)
                                                                   ▼
65+49 גלאי-B-07 (שבוע-1) ──→ ספירות-אמינות ──→ 73-KPI ──→ 194-ניטור ──→ הכרעת-7/9
```

**מדידת-שני-ערב — שער אחד, שתי משימות.** שער-הקבלה של 266 (הפקודה המלאה בגוף
התיק; הורצה 8/8 מול 8/7 ועברה end-to-end) מודד ארבעה דברים, ושניים מהם הם
הנתון ש-259 ממתין לו. **אין צורך במדידה נפרדת ל-259.**
ספי-266: מבוטלות **<5%** ⇒ ‏266 נסגרת · **≥5%** ⇒ נפתחת מחדש עם הנתונים.
ל-259: **חציון <60ש עם סיגנלים אמיתיים ⇒ החפיפה נעלמת מאליה** (‏cron דקתי;
ריצה שמסתיימת בתוך הדקה אינה חופפת לבאה) **ושאלת-הפריסה מתייתרת.**

**אם חוליה מתעכבת:** עיכוב **מדידת-שני** ⇒ הכרעת-הפריסה של 259 נדחית ⇒ או
שפורסים באמצע-החלון (משתנה-חדש, רע) או שסופגים סיכון-פריצה עד 4/9 (רע אחרת).
⚠️ והקלף-הנגד של 266 שנרשם בתיק: אם סערת-429 תחזור בשבוע הראשון, כל יום כזה
מאבד כתיבות עד שתיפול הכרעה חוזרת.
עיכוב-65 לא עוצר את האיסוף אבל מרחיב את חור-הדאטה שכבר קיים. עיכוב-194 =
החלון נאסף ואין מי שקורא אותו.

### הארוך (מתי "הכל" נגמר) — כפוף לצבירה, לא לעבודה

```
4/9 ──→ 258 בניית-הגלאי ──→ צבירת-אירועים (~4-5 חודשים ל-n≥150) ──→ 179 ולידציה ──→ 255 חיווט
ורדיקט (ראו-סיכון-3) ──→ הפשרת 208/209/128/273/260-הכרעה ──→ Score שלב-ב ──→ 186-הפשרה ──→ 234/235/236/237 ──→ 101
```

התשובה הכנה: סוף-העבודה-כולה נקבע ע"י קצב-אירועי-השוק (crossover) — ינואר
בערך, וזה בסדר. התוכנית מנוהלת עד הוורדיקט; הזנב מתוזמן-מחדש אחריו.

---

## 3 · אבני-הדרך — ששת השערים (+אחת תפעולית)

| אבן-דרך | מרכיבים | המבחן שמוכיח | ריאלי |
|---|---|---|---|
| **M0 · מוכנות-שני** | **חובה: 259 בלבד** (‏266 ✅ · 265 ✅ · 276 ✅ כולן הוכרעו) · רצוי: **ריק** | הכרעת-259 רשומה בגוף התיק; אם נפרס — ה-yml על main ו-`gh run list` מראה משטר-ריצות חדש | **9/8 ערב** — צומצם 8/8 שלוש פעמים |
| **M0.5 · מדידת-שני-ערב** | שער-266 (‏4 מדדים) — משרת גם את 259 | ‏`gate266.sh 2026-08-10`: מבוטלות <5% ⇒ 266 נסגרת; חציון <60ש ⇒ שאלת-הפריסה של 259 מתייתרת | **10/8 ערב**, אחרי סגירת-הייצור |
| **M1 · שער-4 מוריק** (מדידה) | סגירת-ייצור ראשונה + T-401/402 שכבר-פרוסים | `gate4`: ‏6/6 עמודות · >0 שורות-דחייה · MFE>0 | **11-12/8** — השער השני-המוריק (אחרי שער-5 שכבר ירוק) |
| **M2 · שער-2 כמעט (ראייה)** | 267 · 275 · 65-גלאי · [c4 ✅] | `gate2`: ‏4/5 (c5 תקוע — ראו-פער-2) | סוף-אוגוסט |
| **M3 · שער-6 baseline (טוהר)** | 277-בנייה · 246-סימון (שבת 15/8, Sheets+אישור) · סינון-צרכנים | `gate6` רץ ומחזיר baseline; מצרף עם/בלי נבדל | אמצע-ספטמבר |
| **M4 · הכרעת-7/9** | 194-ניטור · 202-תיקון-חוצה-חודש · 73-KPI · רוטציית-1/9 עוברת (216/243) | דוח-הכרעה על n≥100 עם כל ההסתייגויות הנעולות | **7/9** |
| **M5 · שער-3 מוריק (סיכון)** | **TASK-279** (T-301, נפתח 8/8 — חישוב-הכמות-מחדש + 3 האינווריאנטים) · 276 תקרת-חום · rescope-שכבר-בוצע (T-303) | `gate3`: אפס-מפרות בהיקף>0 | אחרי-4/9 בלבד — TASK-279 משנה סמנטיקת-כתיבה |
| **M6 · שער-1 מוריק (אמת)** | 272 (בחלון) · 274 (בחלון) · 273+T1b (קפואים-עם-Score) | `gate1`: ‏0/3 דפוסי-כזב | אוקטובר (תלוי-הפשרה) |

### ⚠️ שלושה פערים שהתוכנית חושפת — ואף רשימה קודמת לא

1. ~~**ל-T-301 אין תיק.**~~ ✅ **נסגר 8/8 — נפתח TASK-279** ("Recompute
   Quantity and PositionSizeUSD at the live-price override", priority high).
   הפער היה אמיתי: חישוב-הכמות-מחדש — ה"critical" של חבילת-הסיכון,
   ‏205/291 שורות מפרות — נשמט מיצירת-שבעת-התיקים (נוצרו 6, לא 7). התיק
   נושא את ההיקף המלא מ-S7 כולל שלושת האינווריאנטים ו-CR-05 (qty≥1),
   את ה-ε שכבר הוכרעו ב-D3, ואת אילוץ-התזמון: **אחרי 4/9 בלבד**.
2. **T-202 (שומר-הריק של Q1) חי רק בתוך היקף-209 הקפוא** ⇒ שער-2·c5 תקוע עד
   אוקטובר. אפשר לפצל אותו החוצה (health-קוד, לא-מסחר) ולהוריק את c5 בחלון —
   הכרעה שלך.
3. ~~**שני תאריכי-ורדיקט חיים במקביל.**~~ ✅ **הובהר ותועד 8/8** —
   `docs/DECISIONS_2026-08-08.md` §"שני שעוני-ההכרעה". אלה **לא** שני
   תאריכים לאותו דבר אלא שתי שאלות שונות: **7/9** = הכרעת חלון-המדידה
   (האם המדדים מבדילים נכנסים מנדחים — שאלה על איכות-הסינון, נענית על
   דאטת-החלון); **אוקטובר** = ורדיקט HYP-002 (האם יש edge שמצדיק כסף —
   דורש עסקאות שנסגרו).
   **המסקנה המעשית לתוכנית הזו:** אשכול-Score (208/209/128/273 + Score
   שלב-ב) קפוא על **השני**, לא על הראשון — אין להפשיר אותו בספטמבר.
   ובכיוון ההפוך: עבודת-החלון (194/202/73) לא מושהית בטענה ש"ממילא
   מחכים לאוקטובר" — היא מזינה שער עצמאי.
   ⚠️ נשאר פתוח שם, לא הוכרע: מה קורה אם 7/9 יוצא שלילי בעוד HYP-002
   עוד רצה.

---

## 4 · הרצף בארבע הקבוצות

### א · לפני שני (עכשיו → 10/8 08:30)

```
✅ נסגר 8/8:     הכרעת-266 — אופציה ג׳: **לא נוגעים בתקרה**, מודדים בשני.
                 אפס שינוי קוד ⇒ אין מה להספיק לפני שני. עברה ל-§ב (שני-ערב).
חובה (עמיחי):    הכרעת-259 (לפרוס concurrency?) — ⚠️ **ההמלצה: להמתין למדידת-שני.**
                 ה-recon של 266 מדד חפיפה 10.2% ב-8/7 (מול 92.5% ב-8/5); אם
                 המספר מחזיק עם סיגנלים אמיתיים — אין מה לפרוס. הכרעה לפני
                 שני = הכרעה על נתוני-8/5 בלבד, ויש לרשום זאת ככזו.
אם הוכרע לפרוס:  commit+push לפני ראשון-בלילה. (‏266 כבר לא נוסעת איתו — היא
                 אינה משנה קוד; הצימוד "רק יחד" בטל.)

✅ נסגר 8/8:     265 התקנה — בוצעה (Apps Script על RH-Summaries, טריגר כל 5 דק׳).
                 האימות עובר לשני; ראו §ב.
רצוי (עמיחי):    270 גיבוי (רבע שעה — דדליין נפרד, ~9/8)
✅ נסגר 8/8:     276 תקרת-חום — הוכרעה ג׳: **לא נכנסת לפני החלון**, אלא
                 אחרי 4/9 עם TASK-279. ⚠️ החלון נפתח בלי תקרה — חור ידוע
                 ומתועד; ראו §1 ו-changelog 1.5.

⚠️ **אין יותר עבודת-קוד לפני שני.** מה שנשאר: הכרעת-259 (משפט אחד), ו-270 גיבוי.
```
**לא נכנס עד שני ⇒** נדחה לשבוע-1 עם רישום; לא דוחסים קוד-מסחר בלחץ-שעון.
השאלה הפתוחה של כל אחד מששת הפריטים כתובה **בגוף התיק שלו** (8/8), עם
האפשרויות ומה קורה בכל ענף — כדי שההכרעה תיפול על נייר ולא בעל-פה.

### ב · בתוך החלון (10/8 → 4/9) — מגן-על-הדאטה בלבד

```
שני-אימות: **265 כלב-השמירה** — `gate265_watchdog.py 2026-08-10` בין 14:45
         ל-19:45Z: ‏**≥3 שורות רצופות במרווח ~5 דקות** בתוך החלון (‏AC#1).
         הטריגר כבר הוכח יורה-מעצמו (4 שורות אוטומטיות 8/8); מה שנבדק הוא
         שה-watchdog **סופר** — ‏`Recent15`/`Prior60` מתמלאים.
         ⚠️ עד שזה עובר — **אין לגעת ב-`detect_outage`** (‏268).
         ‏AC#2 ו-AC#3 ✅ נסגרו 8/8. ואז PK (‏AC#4) → סגירת 265.
שני-ערב: **מדידת-266+259 במדידה אחת** — `gate266.sh 2026-08-10` (קריאה-בלבד,
         מותר בחלון). מבוטלות <5% ⇒ 266 נסגרת · חציון <60ש ⇒ הפריסה מתייתרת.
         ובאותו ערב: M1-אימות (gate4, GATE-LIVE של הפריסות)
שבוע 1:  65+49 גלאי-B-07 → 253 גלאי-כפילויות → 226 התרעת-אפס-שורות → 244 אימות-F6b
שבוע 2:  252 שלוש-הבדיקות → 275 תקרת-10M → 267 check_06 → 272 DataQuality-מדגלים →
         274 סיוג-המייל → 215 מדידת-429 (סוגר תיק) → 248-שארית (שני-yml)
שבוע 3:  277 בניית-שער-6 → [שבת 15/8: 246-סימון-יולי — Sheets, אישור] → סינון-צרכנים →
         202 תיקון-חוצה-חודש → 73 KPI → 278 טסט-האפיון (test-only, מותר)
עד 1/9:  216+243 הקשחת-הרוטציה + dry-run של 1/9 (אחרי הכרעת-האבטחה שלך) → רוטציה
4/9:     קיפאון-שינויים מוחלט משבוע-אחרון; רק איסוף וניטור
מקביל-חופשי (קריאה-בלבד): 225-מחקר-D25 · 205-עמוד-תצוגה · 229-טיוטה · 257-חיפוש-הקובץ
```
עיכוב בנתיב-הקריטי כאן ⇒ ההגנות נכנסות מאוחר-יותר, החור-בדאטה גדל, אבל
**לא עוצרים את האיסוף ולא דוחסים** — מתעדים את החור ומכמתים אותו ב-7/9.

### ג · אחרי 4/9 (עד הוורדיקט ואחריו)

```
מיד:     T-301 (אחרי פתיחת-תיק) → M5 שער-3 · 249 בחירת-תיקון-האיסוף · 206 · 82
ורדיקט:  ראו-פער-3 — לפי השעון שתסמן
```

### ד · קפוא (עד הוורדיקט או עליך)

```
על הוורדיקט:  208 · 209 (כולל c5, אלא-אם-פוצל) · 128 · 273 · 260-הכרעה ·
              225-המלצה · 229-רישום · 186 → 234/235/236/237 → 101
על עמיחי:     265-התקנה · 270-גיבוי · 243-פריט-1 (SA-על-root או OAuth) ·
              234-ענף (מיזוג/נטישה) · 268 (אחרי-265) · 261/264-הגדרות
על צבירה:     258 → 179 → 255 (בנייה אחרי-4/9; ולידציה בחורף)
```

---

## 5 · שלושת הסיכונים הגדולים

1. **B-07 מכרסם במדגם מהיום הראשון.** שליש מהריצות מאבדות באפר, והגלאי נכנס
   רק בשבוע-1. *מקטין:* 266 לפני-שני (מקצץ את מקור-הקטיעות הגדול), GATE-LIVE
   שני-בערב שמודד את הפער בפועל (skip_metrics מול לוג-הריצות), וכימות-החור
   בדוח-7/9 — הטיה ידועה-ומדודה אינה פסילה.
2. **פריסת-concurrency באמצע-החלון או פריצת-re-entry בלעדיה.** מלכוד אמיתי:
   לפרוס = משתנה-חדש; לא-לפרוס = ‏92.5% חפיפה שכבר ייצרה שתי פריצות, ופריצה
   פוסלת. *מקטין:* ההכרעה **לפני** שני (זה כל הטעם של 🔴-259); אם נפרס —
   ‏253-baseline קודם, והפריסה נרשמת ב-PK כתנאי-פתיחה של החלון, לא כשינוי-אמצע.
3. **בלבול-הוורדיקטים (פער-3) ימסמס את ההפשרה.** אם 7/9 ואוקטובר לא מיושבים,
   אשכול-Score יידחה בסחף או יופשר-בטעות באמצע. *מקטין:* שורת-ההכרעה שלך
   בסעיף-פער-3, עכשיו, לפני שהחלון נפתח.

**וסתירה-עצמית שאני חייב לומר:** הרצף של "לפני-שני" מניח שתספיק לבצע היום-מחר
שלוש פעולות-ידיים ושלוש הכרעות. אם זה לא ריאלי אצלך — הסדר-החלופי הנכון הוא:
‏270 (רבע-שעה, בלתי-הפיך) → 265 (חצי-שעה) → הכרעת-259 (משפט אחד) — ו-194/276
נדחסים לראשון או נכנסים בשבוע-1 עם תיעוד-החור. עדיף חלון שנפתח עם שתי הגנות
מתועדות-כחסרות מאשר קוד-מסחר שנכתב בלחץ בליל-ראשון.

> ✅ **התיישן 8/8 — וזה בדיוק מה שקרה.** ‏265 הותקן · 266 הוכרעה · 276 הוכרעה
> ג׳ ויצאה עד אחרי-4/9. **נשארה רק הכרעת-259 (משפט אחד) ו-270.** המשפט האחרון
> כאן הוא שהתממש: החלון נפתח עם הגנה מתועדת-כחסרה (תקרת-חום) במקום קוד-מסחר
> שנכתב בלחץ בליל-ראשון.

---

## 6 · ממצאי-שווא שלי

1. **גיליתי תוך-כדי שהפלתי את T-301 בין הכיסאות** — ה-crosswalk סיווג אותו
   "חדש" אבל רשימת-היצירה שלי כללה רק 6; אף רשימה מהיום לא תפסה את זה עד
   בניית-אבני-הדרך (שער-3 חשף). זה בדיוק סוג-הפער שתוכנית-אחורה-משערים
   מגלה וטבלת-משימות לא.
2. **"ריאלי 11-12/8" לשער-4 מניח סגירת-ייצור תקינה בשני** — אם FINVIZ-צד-האתר
   ישתנה שוב או ש-429 יפיל את הסגירה, התאריך זז; אין לי שליטה על זה.
3. **הערכות-הזמן איכותיות** (שבוע-1/2/3) — לא מדדתי ימי-עבודה פר-משימה; הרצף
   והתלויות מוצקים, המספרים ליד-השבועות הם שיפוט.
4. **מפת-התלויות מלאה ברמת-החוסמים המוצהרים** — תלות סמויה שלא כתובה באף
   תיק (ושלא עלתה ב-28 הסבבים) לא תופיע כאן.
5. ~~**ההנחה ש-276 בר-ביצוע-מחר**~~ ✅ **ההנחה נפלה 8/8 — והתחזית התממשה
   כלשונה.** נכתב כאן: *"אם ה-RED-first יחשוף הפתעה ב-order_manager — הוא
   עובר לשבוע-1 והמדגם נפתח בלי תקרה, עם תיעוד."*
   ה-recon **חשף שלוש הפתעות לפני שנכתבה שורת-קוד**: מיקום-האכיפה (ההזמנה
   נשלחת ב-`:121`, השורה ב-`:166`), ‏fail-open אפוי ב-`get_sheet_values`,
   ושבירה צפויה של `TestEnterDecision`. ⇒ ‏276 יצאה — לא לשבוע-1 אלא
   **עד אחרי 4/9**, כי הנימוק המכריע היה 205/291 השורות המפרות.
   **המדגם אכן נפתח בלי תקרה, עם תיעוד.** ראו changelog 1.5.

---

## Changelog

כל עדכון מקבל bump. גרסת-משנה (1.x) = תוכן השתנה · גרסה ראשית (x.0) =
מבנה או תקופה חדשה.

```
1.5  2026-08-08  **‏TASK-276 הוכרעה — ג׳: תקרת-החום לא נכנסת לפני החלון.**
                 נכנסת **אחרי 4/9, תלוי-TASK-279**. אפס שינוי קוד;
                 `order_manager.py` לא נגע.
                 ⇒ **קבוצת "רצוי לפני שני" ריקה, וקבוצת "חייב" מכילה רק את
                 הכרעת-259 — שאף היא מומלץ שתמתין למדידת-שני. אין יותר
                 עבודת-קוד לפני פתיחת החלון.**
                 ארבעת הנימוקים (‏recon 8/8, `~/rhpro_audit_run/TASK276_RECON.md`):
                 (1) ‏`execute()` שולח הזמנה ב-`order_manager.py:121` וכותב שורה
                 ב-`:166` — אכיפה "צמודה-לכתיבה" כלשונה הייתה מייצרת פוזיציה
                 בלי רישום; (2) ‏`get_sheet_values` מחזיר `[]` גם על כשל-קריאה
                 ⇒ ‏fail-OPEN אפוי בקורא המשותף, בניגוד ל-244:51, ומטמון 60ש
                 סותר את "הקריאה הטרייה" של 259:44; (3) המכנה אינו קיים —
                 ‏`equity` 100,000 מול `buying_power` 200,000, פי-2 בתקרה;
                 (4) **המכריע:** ‏gate3 מדד **205/291 שורות מפרות** את
                 אינווריאנטי-הזהות, והחום הוא Σ‏0.10·E×Q ⇒ התקרה הייתה חוסמת
                 כניסות אמיתיות על בסיס חישוב שידוע שחלקו שגוי.
                 ⚠️ **המחיר, ונרשם בגלוי: החלון נפתח בלי תקרת-חום.** החום היום
                 ‏$11,060 = 11.1%, מעל 8% של D3 ומעל 6-8% של position-sizer KP#6
                 ⇒ פרופיל-החשיפה של כל המדגם אינו רפרודוקטיבי בחשבון חי.
                 **חור ידוע ומכוון — חייב להירשם כהסתייגות בדוח-הכרעת-7/9.**
                 ⚠️ ‏D3 לא נפתחה מחדש: 8% · ε 0.02/0.05 · אכיפה ב-order_manager
                 עומדים. הוכרע **מתי** בלבד. שלוש שאלות-מימוש שה-recon חשף
                 נרשמו כפתוחות בגוף התיק.
1.4  2026-08-08  **‏TASK-265 הותקן — כלב-השמירה החיצוני חי.** עמיחי התקין
                 ב-Apps Script על **RH-Summaries** (לא על גיליון חודשי — הוא
                 היה מת ברוטציה של ה-1), טריגר `rhWatchdogRun` · Time-based ·
                 Head, כל 5 דקות; טאב `watchdog_log` נוצר; ‏`rhWatchdogRun`
                 ו-`rhWatchdogTestAlert` רצו ידנית בהצלחה; השורה הראשונה נושאת
                 `Queued=1` ⇒ ‏UrlFetchApp אושר וה-GitHub API עובד.
                 ⇒ קבוצת "רצוי לפני שני" מצטמצמת ל-**276 בלבד**.
                 ‏**AC#2 ו-AC#3 נסגרו:** מייל-הבדיקה נחת ב-`projects5069@gmail.com`
                 ב-19:28 (נושא "RH WATCHDOG: …not producing", מספרים מזויפים
                 999/0/120), ו-`@example.com` שמור ב-RFC 2606 ואינו מקבל דואר
                 ⇒ הקבלה עצמה מוכיחה ש-`ALERT_TO` נערך. ⚠️ שימו לב להיפוך:
                 ‏"Execution completed" **אינו** מוכיח אף אחד מהשניים — רק
                 הקבלה. וגם: הטריגר **יורה מעצמו** — ארבע שורות אוטומטיות
                 רצופות (‏00:30:27 · 00:35:28 · 00:40:27 · 00:45).
                 ⚠️ **ובכל זאת התיק לא נסגר:** ‏AC#1 דורש את השורות
                 **בשעות-מסחר**, וכולן `OUTSIDE_WINDOW` — שבת. מה שנבדק בשני
                 אינו "האם הטריגר יורה" אלא **האם ה-watchdog סופר**.
                 ⇒ נוסף שלב **שני-אימות** ב-§4ב, עם שער בר-הרצה
                 `audit_gate/gate265_watchdog.py` (נבדק דו-כיוונית: שלילי
                 streak 0 → FAIL · חיובי streak 4 → PASS).
                 ⚠️ ‏TASK-268: החוסם ירד **חלקית** — יורה, לא מוכח שסופר.
                 **אין לגעת ב-`detect_outage`** עד ש-AC#1 עובר.
1.3  2026-08-08  **‏TASK-266 הוכרעה — ג׳: לא נוגעים בתקרה, מודדים בשני.**
                 ‏266 יורדת מ*חייב-לפני-שני* ל**נמדדת-בשני**; קבוצת-החובה
                 מצטמצמת ל-259 בלבד, ואף היא מומלץ שתמתין למדידה. נוספה
                 אבן-דרך **M0.5 · מדידת-שני-ערב**: שער-266 מודד ארבעה דברים
                 (מבוטלות · חציון+p90 · ‏429-אמיתיים · חפיפה) ושניים מהם הם
                 הנתון של 259 ⇒ **שתי משימות, מדידה אחת.**
                 מה שה-recon שינה בתמונה: ב-8/7, אחרי `fcdb0aa`+`1df9cba`,
                 ‏492 ריצות · **0 מבוטלות** · חציון-job **48ש** · p90 56ש ·
                 **חפיפה 10.2%** (מול 92.5% ב-8/5). ⚠️ סייג שנשמר: ‏8/7 הוא יום
                 ה-FINVIZ השבור (`No signals to process this minute`), ולכן
                 שני הוא היום הראשון עם סיגנלים אמיתיים אחרי התיקונים.
                 ‏‏~45ש מכל ריצה הן `pip install` מלא (כולל streamlit) ⇒ נפתח
                 **TASK-280** (אחרי 4/9 — נתיב מוגן, משנה משטר-ריצות).
                 ראיות: `~/rhpro_audit_run/TASK266_RECON.md`,
                 `~/rhpro_audit_run/TASK266_DECISION.md`.
1.2  2026-08-08  **תוקן תנאי-הטעינה של ארבעת מנגנוני-התהליך.** ‏1.1 כתב שהם
                 "דורשים סשן חדש לרישום מלא" — זה לא נכון ולא מספיק. התנאי
                 בפועל: **סשן שנפתח מתוך `~/RidingHighPro`**. סשן חדש שנפתח
                 מ-`~/` (כפי שקרה 8/8) אינו טוען את `.claude/` של הפרויקט, ולכן
                 ארבעת המנגנונים נעדרים אף שהקבצים תקינים — זה, ולא פגם
                 בסקריפטים, מסביר את כשל-כל-הארבעה בסשן הקודם.
                 אומת בסשן שנפתח נכון (pwd=~/RidingHighPro), בירייה בפועל ולא
                 בקיום-קובץ: ‏hook שומר-החלון חסם **Edit אמיתי** על
                 order_manager.py (עם WINDOW_GUARD_DATE=2026-08-10) והקובץ נשאר
                 זהה בית-אחר-בית; ‏kill-switch הקובץ שחרר בנתיב החי וההסרה
                 החזירה את החסימה; סוכן `reviewer` רץ על דיף-259 והחזיר FLAG
                 מנומק; ‏`/rh-task 257` נטען ועצר בחוק-הברזל שלו ("אין
                 שער-קבלה"); ובלוק ה-permissions הוכח נטען ע"י כלל-deny זמני
                 (עבר → נחסם → עבר). ראיות מלאות:
                 `~/rhpro_audit_run/METHOD_PROOF_2.md`.
                 בנוסף בגרסה זו: חלון ה-hook הוא **10/8→4/9** (‏1.1 כתב 8/8→4/9;
                 הקוד תוקן מאז, וכעת גם הערת-הכותרת ב-window_guard.sh:7);
                 והוסר `Bash(git checkout *)` מה-allowlist — הוא משליך שינויים
                 בלי לשאול, אותה משפחת-סיכון של push/commit שכבר הוסרו.
                 ⚠️ ידוע-ופתוח: הזרקת התאריך דרך `env` ב-settings.local.json
                 נטענת חם אך **אינה מתבטלת** בהסרת השורה — משתנה שכבר יוצא
                 לתהליך נשאר עד סוף הסשן. הקובץ הוחזר; סשן חדש נקי.
1.1  2026-08-08  נוספה תשתית-התהליך: docs/WORK_METHOD.md (שלושת הסבבים, אצוות,
                 מפת-האישורים) + סקיל /rh-task + סוכן reviewer + ‏hook שומר-חלון
                 על order_manager.py/decision_logic.py (‏8/8→4/9, ‏kill-switch
                 קובץ+env). סטטוס-הוכחות: ~/rhpro_audit_run/METHOD_SETUP.md —
                 הסקיל/הסוכן/ה-hook דורשים סשן חדש לרישום מלא.
1.0  2026-08-08  גרסה ראשונה. נבנתה אחורה משני הדד-ליינים (10/8 · 4/9) מתוך
                 docs/MASTER_REGISTER.md. כוללת: הנתיב-הקריטי (266→259→244),
                 שבע אבני-דרך, רצף בארבע קבוצות, ושלושה סיכונים.
                 שלושת הפערים שנחשפו בבנייה: T-301 בלי תיק (נסגר — TASK-279),
                 T-202 כלוא ב-209 הקפוא (פתוח), ושני שעוני-הכרעה (הובהר —
                 DECISIONS §"שני שעוני-ההכרעה").
                 קבוצת "לפני שני" צומצמה ל-259+266 לפי הקריטריון
                 "משנה את משטר-הריצות"; ‏265/276 → רצוי · 194 → שבוע-1 ·
                 270 יצאה (דדליין נפרד — פקיעת-לוגים ~9/8).
```

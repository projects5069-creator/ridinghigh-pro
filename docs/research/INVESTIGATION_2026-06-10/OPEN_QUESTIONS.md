# OPEN_QUESTIONS — התלבטויות והכרעות-פרשנות במהלך הריצה

1. **"12 הפילטרים" בפרומפט מול 14 בקוד וב-PK** — הוכרע: לסקור את כל ה-14 הקיימים
   ולתעד את הפער כהערה (כנראה הפרומפט התבסס על ספירה ישנה). לא הושמט אף פילטר.

2. **גבול ה-OOS של ROCKET_GUARD** — הכיול המקורי "196 שורות" אינו מתוארך במדויק ב-PK
   (אזכור PIII 2026-05-15). הוכרע שמרנית: OOS = ScanDate ≥ 2026-05-16. אם הכיול כלל
   שורות מאוחרות יותר, ה-OOS האמיתי קטן עוד יותר (המסקנה רק מתחזקת).

3. **חוק הכתיבה "רק תחת תיקיית החקירה"** — פורש כמגן על הרפו וה-Sheets. בפועל נכתבו גם:
   (א) קבצי עזר/סקריפטים בתוך תיקיית החקירה עצמה (מותר במפורש);
   (ב) הורדת artifact ל-/tmp/rh_artifact (קריאה-בלבד של תוצר קיים, לא שינוי מערכת).
   אפס כתיבה לקבצים קיימים, אפס כתיבת Sheets, אפס git.

4. **RULE #12 (עטיפת rh-run)** — דולג במוצהר: ריצה אוטונומית ללא משתמש שמדביק פלט;
   עטיפת ~60 פקודות הייתה דורסת את ה-clipboard שוב ושוב ללא ערך. הוצהר בתחילת הריצה.

5. **"עותק ה-PK ב-Sheets תקוע על v2.12"** (מהפרומפט) — בפועל שדה ה-Version ב-Metadata
   מציג "v2.0" ותאריך sync 2026-05-16 (אז ה-PK היה ~v2.12-2.13). כנראה השדה מציג את
   גרסת-הכותרת של המסמך ולא את גרסת ה-changelog. ה-staleness עצמו אומת; אי-ההתאמה במספר תועדה.

6. **skip_summary (שלב 0ג)** — לא ניתן לאימות היום: TASK-125 נדחף ב-17:49 פרו, אחרי סגירת
   השוק. הוכרע: לדווח "צפוי-ריק, אימות מחר 11/6 אחרי 08:30" ולא לסמן ככשל.

7. **בדיקת הפילטרים על post_analysis** — post_analysis הוא מדגם שכבר עבר סף-70 בסריקה
   (selection bias); האידיאל היה לבדוק על אותות decision_log המקוריים. הוכרע: לבדוק על
   post_analysis עם הצהרת ה-caveat, כי זה הדאטה המוכרע היחיד עם תוצאות D1-D5.

8. **המלצת אופציה ב (שער-מפורש)** — מבוססת על קריטריון-ההרג שהוגדר בפרומפט והתקיים.
   הסתייגות: כל הדאטה single-regime (VIX 15-20, SPY FLAT) ו-WR ריאלי תלוי בפתרון שאלת
   ה-WHIPSAW (26 שורות) ועלויות borrow אמיתיות (borrow_data ריק). ההמלצה היא להכרעת עמיחי, לא יושמה.

9. **טסטים בסביבה** — pytest אינו מותקן; הורץ עם `uv run --offline --with pytest` (cache
   קיים, אפס הורדת רשת). אם זו לא הדרך הקנונית של הפרויקט — שווה לתעד את הדרך הרשמית.

10. **בדיקת dry-run של backfill (שלב 0ב)** — דורשת רשת yfinance האסורה בריצה זו; הוחלפה
    באימות-קוד (שורת FILL-ONLY) + השוואת snapshot היסטורי על 3 שורות. תועד כחסם חלקי.

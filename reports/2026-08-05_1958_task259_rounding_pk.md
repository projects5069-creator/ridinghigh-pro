# TASK-259 — איפוס דלתא העיגול + PK

**נכתב 2026-08-05 19:58 Lima.** TDD, RED לפני GREEN. **אין commit, אין push.**
לא נגעתי ב-`.github/workflows/` — שתי התוספות מהתור הקודם נשארו בדיוק כפי שהן.
לא נגעתי ב-Sheets/Drive/FINVIZ, לא הרצתי workflow/scanner/collector.
**קריאות Alpaca: היסטוריות בלבד** — אפס פקודות, אפס חשבון.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/test-driven-development | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/test-driven-development/SKILL.md` | 371 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

**גיבויים לפני העריכה, מחוץ לריפו:**
```
/private/tmp/.../scratchpad/bak10/sma20_cache.py.bak_2026-08-05_1958
/private/tmp/.../scratchpad/bak10/RidingHigh_Pro_PK_v2.md.bak_2026-08-05_1958
```

---

## 0. בדיקה מקדימה — מי צורך את ערך הקאש

לפני שינוי מה שנשמר, בדקתי מי קורא אותו:
```
$ grep -rn "sma20" --include=*.py .   (ללא backups/project_sync/tests/research)
agent/enrichment/sma20_cache.py:144-146   sma20 = cached.get("sma20") ... ← הצרכן היחיד
enrich_data.py:79-81                      מחשב SMA משלו מ-hist_before — לא מהקאש
post_analysis_collector.py:291-294        מחשב SMA משלו — לא מהקאש
agent/logging/decision_logger.py:74       ("price_vs_sma20", "PriceVsSMA20") ← התוצאה, לא ה-SMA
```
**צרכן יחיד: `get_price_vs_sma20:144-146`.** שני המקומות האחרים מחשבים SMA עצמאית ואינם
נוגעים בקאש. **בטוח לשנות את מה שנשמר.**

---

## 1. RED — הטסט שתופס את הדלתא

נוסף `test_cached_sma_is_not_rounded_so_the_percentage_is_unchanged` **לצד**
`test_batched_result_matches_serial_path_exactly` — הקיים לא נמחק.

ערכי ה-mock נבחרו במכוון: `closes = [1.81025] * 25` — **ה-SMA האמיתי של BEEP ב-5/8**,
בדיוק הסדר-גודל שבו הספרה הרביעית נוגסת. מחיר 10.0.
הטסט אינו מקבע מספר קשיח: הוא מחשב `expected` מה-SMA הלא-מעוגל ודורש שהנתיב
ה-batched יחזיר בדיוק אותו.

**פלט ה-RED:**
```
>       assert mod.get_price_vs_sma20("BEEPY", price, provider=p) == expected
E       AssertionError: assert 452.43 == 452.41

FAILED test_sma20_batch_v1.py::test_cached_sma_is_not_rounded_so_the_percentage_is_unchanged
1 failed, 6 passed in 0.47s
```
**נכשל, ובדיוק על אותם שני מספרים שנמדדו חי על BEEP: 452.41 מול 452.43.**
ששת הקודמים המשיכו לעבור — כלומר הטסט החדש מבודד את הדלתא ולא שובר דבר.

---

## 2. GREEN — התיקון

`round(sma20, 4)` הוסר משלושת אתרי-הכתיבה לקאש:
```
:111  prime_sma20_cache — ענף ה-fallback הסדרתי
:118  prime_sma20_cache — ענף ה-batch
:175  get_price_vs_sma20 — ענף ה-miss המקורי
```
כולם שומרים עכשיו את הממוצע בדיוק מלא.

**`round(..., 2)` של התוצאה הסופית לא נגע** — אומת:
```
$ grep -n "round(sma20, 4)" agent/enrichment/sma20_cache.py
  (רק בתוך הערת-הסבר בשורה 169)
$ grep -n "round((current_price - sma20) / sma20 * 100, 2)"
140:            return round((current_price - sma20) / sma20 * 100, 2)   ← ענף cache-hit
183:    return round((current_price - sma20) / sma20 * 100, 2)           ← ענף miss
```
שניהם במקומם.

---

## 3. אימות

```
$ uv run python3 -m py_compile agent/enrichment/sma20_cache.py
PY_COMPILE OK

$ pytest tests/agent/unit/test_sma20_batch_v1.py -q
7 passed in 0.22s

$ pytest -m "not integration" -q
2 failed, 727 passed, 1 skipped, 3 deselected, 8 warnings in 40.84s
FAILED test_orchestrator_eod_borrow_wiring_v1.py::test_collects_unified_deduped_tickers_and_passes_to_collector
FAILED test_orchestrator_eod_borrow_wiring_v1.py::test_no_tickers_skips_collector_and_does_not_fail
```
**אותם שני כשלי-הבסיס בדיוק. אפס כשלים חדשים.**

### המדידה החיה — היעד היה 0 מתוך 60

```
provider = alpaca-paper   tickers = 60

--- OLD PATH: get_price_vs_sma20 per ticker, cold cache ---
  elapsed = 10.12s

--- NEW PATH: prime_sma20_cache once, then the same lookups ---
  prime   = 0.86s  (60 tickers cached)
  lookups = 0.003s
  TOTAL   = 0.86s

  SPEEDUP = 11.8x   saving = 9.26s

  IDENTICAL RESULTS: True
  non-None values: old=59  new=59
```

| | לפני התיקון | אחרי התיקון |
|---|---|---|
| טיקרים שנבדלים | **8 מתוך 60** | **0 מתוך 60** ✅ |
| speedup | 13.6× | 11.8× |

**היעד הושג: 0 מתוך 60.**
⚠️ ה-speedup ירד מ-13.6× ל-11.8× — **זו שונות רשת בין שתי מדידות** (הנתיב הסדרתי עצמו
נמדד 9.64s ואז 10.12s), לא רגרסיה מהתיקון. שמירת דיוק מלא אינה משנה את מספר הבקשות.

---

## 4. ה-diff של הקוד

```diff
diff --git a/agent/enrichment/sma20_cache.py b/agent/enrichment/sma20_cache.py
index e62c0c4..7465189 100644
--- a/agent/enrichment/sma20_cache.py
+++ b/agent/enrichment/sma20_cache.py
@@ -36,6 +36,89 @@ def _save_cache(c: Dict[str, Any]) -> None:
         print(f"[sma20_cache] WARN: failed to save cache: {e}")
 
 
+def _sma20_from_bars(ticker: str, bars) -> Optional[float]:
+    """Shared bars→SMA20 step. Prints the SAME per-ticker diagnostics the serial
+    path prints, so batching does not lose them. Returns None when unusable."""
+    if bars is None or len(bars) < 15:
+        print(f"[sma20_cache] BARS_INSUFFICIENT: {ticker} bars={None if bars is None else len(bars)}", flush=True)
+        return None
+    closes = bars["close"].dropna().tolist()
+    if len(closes) < 15:
+        print(f"[sma20_cache] CLOSES_INSUFFICIENT: {ticker} closes={len(closes)}", flush=True)
+        return None
+    sma20 = sum(closes[-20:]) / min(len(closes), 20)
+    return sma20 if sma20 > 0 else None
+
+
+def prime_sma20_cache(tickers, provider=None) -> int:
+    """TASK-259: fill the cache for many tickers with ONE provider request.
+
+    The agent asked for daily bars once per ticker, 60 times a run, against a
+    cache that is always cold on a GitHub runner (data/ is gitignored, so every
+    checkout starts empty). Measured 2026-08-05 on the 60 real tickers of run
+    31030383592: 9.05s serial vs 0.67s batched.
+
+    Semantics are deliberately unchanged for callers: this only PRE-FILLS the
+    same cache get_price_vs_sma20 already reads, so that function keeps its
+    signature, its return values and its None-handling. Priming is optional —
+    skip it and everything still works, one call per ticker.
+
+    A ticker with no data (absent from the batch) or with too few bars is cached
+    as a null marker, which get_price_vs_sma20 already reads as None. That stops
+    a second lookup from re-firing a request for a ticker known to be empty.
+
+    If the batch request fails ENTIRELY, this falls back to the serial path and
+    says so on stdout (SMA20_BATCH_FAILED) — never a silent degradation.
+
+    Returns the number of tickers newly cached.
+    """
+    if not tickers:
+        return 0
+
+    today_str = datetime.now(PERU_TZ).strftime("%Y-%m-%d")
+    cache = _load_cache()
+    todo = [t for t in dict.fromkeys(tickers)
+            if t and f"{t}:{today_str}" not in cache]
+    if not todo:
+        return 0
+
+    frames = None
+    try:
+        if provider is None:
+            from data_provider import get_daily_bars_batch
+            frames = get_daily_bars_batch(todo, days=25)
+        else:
+            frames = provider.get_daily_bars_batch(todo, days=25)
+    except Exception as e:
+        print(f"[sma20_cache] SMA20_BATCH_FAILED: {type(e).__name__}: {e} — "
+              f"falling back to serial for {len(todo)} tickers", flush=True)
+        frames = None
+
+    if frames is None:
+        # Serial fallback: exactly the pre-TASK-259 path, one call per ticker.
+        for t in todo:
+            try:
+                if provider is None:
+                    from data_provider import get_daily_bars
+                    bars = get_daily_bars(t, days=25)
+                else:
+                    bars = provider.get_daily_bars(t, days=25)
+            except Exception as e:
+                print(f"[sma20_cache] WARN: {t}: {type(e).__name__}: {e}", flush=True)
+                bars = None
+            sma20 = _sma20_from_bars(t, bars)
+            cache[f"{t}:{today_str}"] = {"sma20": sma20, "computed_at": today_str}
+    else:
+        for t in todo:
+            sma20 = _sma20_from_bars(t, frames.get(t))
+            cache[f"{t}:{today_str}"] = {"sma20": sma20, "computed_at": today_str}
+
+    cutoff = datetime.now(PERU_TZ).strftime("%Y-%m")
+    cache = {k: v for k, v in cache.items() if k.split(":")[1][:7] >= cutoff[:7]}
+    _save_cache(cache)
+    return len(todo)
+
+
 def get_price_vs_sma20(ticker: str, current_price: float, provider=None) -> Optional[float]:
     """
     Return percent distance of current_price from SMA20.
@@ -82,8 +165,13 @@ def get_price_vs_sma20(ticker: str, current_price: float, provider=None) -> Opti
     
     sma20 = sum(closes[-20:]) / min(len(closes), 20)
     
-    # Cache it
-    cache[cache_key] = {"sma20": round(sma20, 4), "computed_at": today_str}
+    # Cache it. TASK-259: store the FULL-PRECISION mean. It used to be
+    # round(sma20, 4), which made the first call of the day (computed from the
+    # unrounded value below) disagree with every later call (read back rounded)
+    # by up to 0.03 percentage points — measured on 8 of 60 real tickers,
+    # 2026-08-05. On a runner the cache is always cold, so production always
+    # took the unrounded branch; keeping full precision is what preserves it.
+    cache[cache_key] = {"sma20": sma20, "computed_at": today_str}
     # Prune entries older than 7 days
     cutoff = datetime.now(PERU_TZ).strftime("%Y-%m")
     cache = {k: v for k, v in cache.items() if k.split(":")[1][:7] >= cutoff[:7]}
```

---

## 5. חוב Anti-Drift — נסגר

ה-PK החי נפתר לפי mtime, כפי ש-rhpro-live §14 מחייב:
```
$ ls -t docs/*PK*.md | head -1
docs/RidingHigh_Pro_PK_v2.md
```

**bump גרסה:**
```diff
-| **Document version** | 4.12 |
+| **Document version** | 4.13 |
```

**שורת changelog אחת חדשה** בראש `### Document Changelog`, המכסה את שני השינויים:
```
$ grep -n "^v4\.1[0-9] " docs/RidingHigh_Pro_PK_v2.md | head -3
23:v4.13 (2026-08-05, TASK-259 — חפיפת ריצות): **שני שינויים, שניהם עדיין לא committed...
25:v4.12 (2026-08-03, סגירת יום): ...
27:v4.11 (2026-08-03): ...
```
```
$ git diff --numstat -- docs/RidingHigh_Pro_PK_v2.md
3	1	docs/RidingHigh_Pro_PK_v2.md
```
**3 הוספות, מחיקה אחת** — המחיקה היא שורת הגרסה `4.12` שהוחלפה ב-`4.13`.
`str_replace` בלבד; הקובץ לא שוכתב.

**מה נכלל בשורת ה-changelog** (התוכן עצמו אינו מודפס כאן — RULE #14):
1. `concurrency` על שני ה-workflows, עם המדידות שהובילו אליו והקביעה שזה **שורש שונה
   מ-TASK-244**.
2. ⚠️ **האזהרה ש-`check_06`/D3 צפוי להתהפך ל-CRITICAL** — עם המנגנון
   (`cancelled` נכנס למכנה ב-`health_audit.py:596-605` ולא למונה), האומדן ~26%,
   הסיבה ששער RECOVERING לא יציל, וההפניה ל-`reports/2026-08-05_1926` §6.3.
   מצוין במפורש כ**טרם טופל**.
3. ה-batch ל-SMA20 עם המדידה 10.12s→0.86s, מנגנון ה-fallback, ו-null-marker.
4. שינוי העיגול, עם ההסבר למה דיוק מלא הוא מה שמשמר את ערכי הפרודקשן, ו-0/60.
5. הצהרה ששני השינויים **עדיין לא committed ולא נפרסו**.
6. `אפס שינוי Score/sizing/gate-thresholds` + `§1 version 4.12→4.13`.

---

## 6. תמונת מצב לפני commit

```
$ git status --porcelain=v1 --untracked-files=all
 M .github/workflows/agent_minute.yml
 M .github/workflows/auto_scan.yml
 M agent/enrichment/sma20_cache.py
 M agent/orchestrator.py
 M data_provider.py
 M docs/RidingHigh_Pro_PK_v2.md
 M providers/alpaca_provider.py
?? tests/agent/unit/test_sma20_batch_v1.py
?? docs/auto-dancer/queue/QUEUE_2026-07-04.md      ← untracked מ-7/04, לא שלי
?? docs/auto-dancer/queue/QUEUE_2026-07-05.md      ← untracked מ-7/05, לא שלי
?? reports/  (25 קבצים)                            ← דוחות היום

$ git diff --stat
 .github/workflows/agent_minute.yml |  5 +++
 .github/workflows/auto_scan.yml    |  5 +++
 agent/enrichment/sma20_cache.py    | 91 +++++++++++++++++++++++++++++++
 agent/orchestrator.py              | 13 +++++
 data_provider.py                   | 26 +++++++++
 docs/RidingHigh_Pro_PK_v2.md       |  4 +-
 providers/alpaca_provider.py       | 49 ++++++++++++++-
 7 files changed, 190 insertions(+), 3 deletions(-)

$ git diff --stat -- .github/workflows/
 2 files changed, 10 insertions(+)      ← בדיוק כמו בתור הקודם. לא נגעתי.
```

### רשימת הקבצים המדויקת ל-commit — גזורה מ-`git status`

**מתוקנים (7):**
```
.github/workflows/agent_minute.yml
.github/workflows/auto_scan.yml
agent/enrichment/sma20_cache.py
agent/orchestrator.py
data_provider.py
docs/RidingHigh_Pro_PK_v2.md
providers/alpaca_provider.py
```
**חדש (1):**
```
tests/agent/unit/test_sma20_batch_v1.py
```

**8 קבצים סה"כ.** ⚠️ `docs/auto-dancer/queue/*` ו-`reports/` הם untracked שאינם חלק
מהשינוי הזה — **אין להוסיף אותם**.

⚠️ **הערה על חלוקה ל-commits:** שני ה-YAML הם שינוי תשתית עם תופעת-לוואי ידועה
(`check_06` → CRITICAL); ששת האחרים הם שינוי קוד עם טסטים. **הם ניתנים לפיצול לשני
commits**, אבל שורת ה-PK אחת ומכסה את שניהם — פיצול ידרוש לפצל גם אותה, או לצרף את
ה-PK ל-commit השני. **לא הכרעתי; זו הכרעתך.**

---

## 7. סטיות מההוראה

| # | מה | פירוט |
|---|---|---|
| 1 | **`round(sma20,4)` הוסר משלושה אתרים ולא רק מ-`:85`** | ההוראה ציינה `:85` (מספור מלפני התור הקודם — היום `:175`). אבל אחרי הוספת `prime_sma20_cache` יש **שלושה** אתרי-כתיבה לקאש: `:111`, `:118`, `:175`. הסרה רק מאחד הייתה משאירה את הדלתא בשני האחרים. הסרתי משלושתם. |
| 2 | **שורת changelog אחת ארוכה ולא שתיים** | ההוראה ביקשה "שורת changelog אחת" שמכסה את שני השינויים — כך עשיתי. היא ארוכה, בהתאם לסגנון הקיים ב-PK. |
| 3 | **RULE #12 — ללא `.rh-run.sh`** | הפלט יועד לדוח. |

**RULE #14 נשמר:** ה-PK נפתר לפי mtime, נקרא נקודתית (שורות 11 ו-21-23 בלבד),
**וגופו לא הודפס לפלט** — לא כאן ולא בצ'אט.

**לא בוצע, במכוון:** אין commit · אין push · לא נגעתי ב-workflows ·
אין Sheets/Drive/FINVIZ · לא הרצתי workflow/scanner/collector.

---

## מצב

**חוב ה-Anti-Drift נסגר.** PK 4.12→4.13, שורת changelog אחת מכסה את שני השינויים
וכוללת את אזהרת ה-`check_06`.
7 טסטים ירוקים · אפס כשלים חדשים · **0 מתוך 60 טיקרים נבדלים** · workflows לא נגעו.
**8 קבצים מוכנים ל-commit, ממתינים לאישורך.**

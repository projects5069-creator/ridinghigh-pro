# TASK-259 שלב ג-ב — היתכנות קיצור הריצה

**נכתב 2026-08-05 19:37 Lima.** ⚠️ **חקירה בלבד — לא שיניתי שום קובץ.**
שני קבצי ה-YAML מתור קודם נשארו כפי שהם (`M`, לא committed) ולא נגעתי בהם.
לא הרצתי workflow/scanner/collector, לא pytest, אין commit/push.
**קריאות Alpaca: היסטוריות בלבד** (`get_stock_bars`, feed=IEX) — **אפס פקודות, אפס קריאת חשבון.**

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/systematic-debugging | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/systematic-debugging/SKILL.md` | 296 |
| data-quality-checker | `~/.claude/skills/data-quality-checker/SKILL.md` | 161 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

---

## A · SMA20 — 106 השניות

### A1 · איפה נשמר הקאש ומה ה-TTL
```python
agent/enrichment/sma20_cache.py:17   CACHE_PATH = os.path.join(os.path.dirname(__file__), "..", "..", "data", "sma20_cache.json")
agent/enrichment/sma20_cache.py:48   today_str = datetime.now(PERU_TZ).strftime("%Y-%m-%d")
agent/enrichment/sma20_cache.py:50   cache_key = f"{ticker}:{today_str}"
agent/enrichment/sma20_cache.py:85   cache[cache_key] = {"sma20": round(sma20, 4), "computed_at": today_str}
agent/enrichment/sma20_cache.py:87-88  # prune entries older than the current month
```
**קובץ מקומי `data/sma20_cache.json`. ה-TTL הוא היום הקלנדרי** — המפתח הוא `TICKER:YYYY-MM-DD`,
כך שערך תקף עד חצות Peru. אין TTL בשניות.

### A2 · `data/` ב-.gitignore — מאומת
```
$ grep -n "^data/" .gitignore
12:data/

$ git check-ignore -v data/sma20_cache.json
.gitignore:12:data/	data/sma20_cache.json

$ git ls-files data/ | wc -l
0
```
**הקאש לעולם אינו נשלח ל-runner.** כל ריצת Actions מתחילה מ-checkout נקי ⇒ קאש ריק ⇒
100% miss בכל ריצה, בכל דקה.

### A3 · איזו פונקציה נקראת פר-טיקר
```python
agent/orchestrator.py:84-95   # L3 enrichment: Price/SMA20 for Toxic Profile filter (4d)
    from agent.enrichment.sma20_cache import get_price_vs_sma20
    signal["price_vs_sma20"] = get_price_vs_sma20(_ticker, _price)     ← פר-סיגנל

agent/enrichment/sma20_cache.py:60-64
    from data_provider import get_daily_bars
    bars = get_daily_bars(ticker, days=25)

data_provider.py:395-397   def get_daily_bars(ticker, days=60, end_date=None):
                               return get_data_provider().get_daily_bars(...)
providers/alpaca_provider.py:193  def get_daily_bars(self, ticker: str, days=60, end_date=None)
```
ה-provider בריצה שנבדקה הוא `AlpacaDataProvider (primary)` (מהלוג).
**ה-fallback ל-yfinance קיים רק ב-אתחול** (`data_provider.py:320-334`), **לא פר-קריאה** —
כלומר אין כאן נתיב yfinance איטי.

### A4 · סימבול אחד או רשימה — **הרשימה כבר בשימוש, בגודל 1**
```python
providers/alpaca_provider.py:207
    req = self._sdk["StockBarsRequest"](
        symbol_or_symbols=[ticker],        ← רשימה בת איבר אחד
        timeframe=..., start=..., end=..., feed=DataFeed.IEX)
```
חתימת הספרייה עצמה:
```
$ python -c "import inspect; from alpaca.data.requests import StockBarsRequest; ..."
alpaca-py version: 0.43.5
  symbol_or_symbols: str | typing.List[str]     ← ריבוי-סימבולים נתמך נייטיב
  start / end / limit / currency / sort / timeframe / adjustment / feed / asof
```
**היכולת קיימת ומופעלת — פשוט עם איבר אחד בכל פעם.**

### A5 · מדידה חיה, קריאה בלבד — 5 סימבולים

טיקרים אמיתיים מ-`timeline_live` של 5/8. אותם פרמטרים כמו הקוד (`days=25`, feed=IEX),
עם warm-up כדי לא לחייב את המדידה הראשונה ב-TLS handshake.
```
--- BATCH: 5 symbols in ONE request ---
  elapsed = 0.213s   rows=145   symbols returned=['DFNS','INLF','SHPH','YXT','ZYBT']
  index names = ['symbol','timestamp']   columns = ['open','high','low','close','volume','trade_count','vwap']

--- SERIAL: same 5 symbols, five separate requests ---
  DFNS 0.154s · SHPH 0.153s · YXT 0.149s · INLF 0.187s · ZYBT 0.147s
  TOTAL serial = 0.790s
  RATIO serial/batch = 3.7x
```

### A5b · אותה מדידה על **60 הטיקרים האמיתיים** של ריצה 31030383592
(הטיקרים חולצו משורות ה-`[SKIP]` של אותה ריצה — 60 ייחודיים.)
```
--- SERIAL: 60 separate requests (what the code does today) ---
  TOTAL = 9.05s   ok=60 err=0   avg=0.151s/ticker

--- BATCH: all 60 in ONE request ---
  TOTAL = 0.67s   rows=1701   symbols returned=60 of 60   missing: []
  index names=['symbol','timestamp']  columns=['open','high','low','close','volume','trade_count','vwap']
  SPEEDUP = 13.6x   saving = 8.39s
```

### A6 · החישוב — ו⚠️ **הפער שלא הוסבר**

| | מדידה |
|---|---|
| 60 קריאות סדרתיות, מהמק, אחרי הנעילה | **9.05s** |
| 60 בבקשה אחת | **0.67s** |
| מה שנמדד בפועל ב-GitHub Actions לאותה עבודה | **106.3s** |

⚠️ **הפער הוא פי ~12, והמדידה שלי אינה מסבירה אותו.**
גם הגרסה הסדרתית לוקחת 9 שניות מכאן, לא 106. כלומר **ה-batching אינו יכול לקחת קרדיט
על 106 שניות** — הוא חוסך את ה-9 שנמדדו, והשאר נובע ממשהו שלא מדדתי.

המועמד הסביר, אך **לא מאומת**: throttling של Alpaca בסביבת Actions. הלוג של אותה ריצה
מכיל `Alpaca get_daily_bars(BLMN) failed: {"message": "too many requests."}` — כלומר
הצד השני מגביל. 60 בקשות בדקה מ-runner משותף אינן זהות ל-60 בקשות מלקוח יחיד.
**אם זו הסיבה, ה-batching תוקף אותה ישירות** — הוא הופך 60 בקשות לאחת ומוציא את
הריצה מחלון ה-rate-limit לגמרי. **זו הערכה מסומנת, לא מדידה.**

### A7 · מה עלול להישבר — נבדק, לא שוער

| שאלה | תשובה מהמדידה |
|---|---|
| האם המבנה המוחזר שונה? | **לא.** `index names=['symbol','timestamp']` **בשתי הצורות**. גם בקשה בת סימבול אחד מחזירה MultiIndex עם רמת `symbol`. `_normalize_bars_df(bars.df, ticker)` (`alpaca_provider.py:213`) כבר מקבל `ticker` ומטפל ברמה הזו. |
| מגבלת סימבולים לבקשה? | **60 עברו בבקשה אחת בלי שגיאה** (1701 שורות). לא בדקתי היכן התקרה — לא ניסיתי 200 או 1000. **לא ידוע.** |
| האם טיקר שנכשל מפיל את כל הבקשה? | **לא.** בקשה עם 10 תקינים + `ZZZZNOTREAL` החזירה `OK in 0.17s rows=262 symbols=10 of 11` — הסימבול הלא-קיים פשוט **נעדר מהתוצאה, בשקט**. ⚠️ זו נקודת-איכות: היום `BARS_INSUFFICIENT` מודפס פר-טיקר; ב-batch צריך להסיק חוסר מהיעדרות. |

---

## B · אירועי Sentinel — חלק מ-45 השניות

### B1 · כמה קריאות ב-60 סיגנלים
```python
agent/sentinel/data_sentinel.py:58-59
    ws = sm.get_worksheet("sentinel_events")
    sm.safe_append_row(ws, row)
```
נקרא מ-`data_sentinel.py:208` (פר-סיגנל, BLOCK/WARN) ומ-`:274` (פר-ריצה, system check),
ומ-`orchestrator.py:569-570` (outage, פר-ריצה).
**ב-60 סיגנלים: עד 60 × (`get_worksheet` + `append_row`).** מדוד בריצה 31030383592:
**55 מתוך 60 נכשלו על 429.**

### B2 · האם קיים append לרבים — **כן, בריפו עצמו**
```python
sheets_manager.py:516  def safe_append_row(ws, row, dedup_col=None, dedup_val=None, **kwargs)
sheets_manager.py:556  def safe_append_rows(ws, rows, dedup_col=None, dedup_vals=None, **kwargs)
    """append_rows with 429 retry + optional idempotency.
       Like safe_append_row but for multiple rows in one API call. Used for
       bulk writes (e.g. auto_scanner writes ~20-30 rows to timeline_live every minute)."""
```
**הפונקציה קיימת, בשימוש, ומתועדת לשימוש הזה בדיוק.** אין צורך לכתוב מנגנון חדש.
היא גם מציעה `dedup_vals` לרטריי בטוח — רלוונטי אם צוברים.

### B3 · האם הסמנטיקה נשברת אם צוברים וכותבים פעם אחת
- **`Timestamp` נקבע ברגע יצירת השורה** (`data_sentinel.py:50`, `datetime.now(peru).isoformat()`),
  **לא ברגע הכתיבה** — כלומר צבירה לא מזייפת זמנים.
- **מה יאבד בקריסה:** כל האירועים של אותה ריצה. היום, בקריסה באמצע, נשמרים אלה שכבר
  נכתבו. ⚠️ אבל בפועל **55 מתוך 60 כבר אובדים היום** על 429 — כלומר האמינות הנוכחית
  אינה גבוהה מזו של כתיבה מרוכזת אחת.

### B4 · מי קורא — **אף אחד בזמן-אמת**
```
agent/dashboard/sentinel_events_page.py:29-32   _load_sentinel_events()  TTL 120s   ← תצוגה
health_audit.py:1595-1598                       ספירת אירועים            ← 06:00/15:30/22:00
agent/critic/critic_v1.py:513-516               daily_facts               ← 17:00
agent/orchestrator_email_daily.py:158-159       מייל יומי                 ← 16:30
```
**ארבעה צרכנים, כולם post-hoc.** אף אחד אינו קורא בתוך הריצה, ואף אחד אינו תלוי
בכתיבה מיידית. **אין צרכן שנשבר מכתיבה מרוכזת בסוף הריצה.**

### B5 · הצלבה מול TASK-218 — **הוא מבקש פחות**
```
TASK-218 (To Do): "agent.sentinel still calls get_worksheet('sentinel_events') per
BLOCK/WARN event (~45/91 429 on the dedicated _AM SA). Cache the worksheet handle
once/run. Connects to TASK-213 + TASK-217. MARKET-SAFE code."
```
218 מבקש **לקאשר את ה-handle** — כלומר להסיר את `get_worksheet` החוזר.
**הוא אינו מבקש לצבור את ה-appends.** אלה שני שינויים נפרדים:
handle-cache מסיר ~60 קריאות; batched-append מסיר ~60 כתיבות.
**מה שנמדד היום מצדיק את שניהם, אבל 218 מכסה רק את הראשון.**

---

## C · pip install — 29 שניות

### C1 · האם `setup-python@v6` תומך ב-`cache: 'pip'`
```yaml
# agent_minute.yml:20-24 (אחרי העריכה של התור הקודם)
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'          ← אין שורת cache:
      - name: Install dependencies
        run: pip install -r requirements.txt
```
⚠️ **לא אימתתי את יכולת ה-action מול תיעוד רשמי** (זו קריאת רשת חיצונית שלא ביצעתי).
מה שכן מאומת: **17 ה-workflows בריפו כולם משתמשים ב-`setup-python`, ואף אחד מהם
אינו מגדיר `cache:`.** אין בריפו דוגמה עובדת להסתמך עליה.

### C2 · החבילות
```
$ wc -l requirements.txt → 17   (12 חבילות + הערות)
streamlit · pandas · plotly · finvizfinance==0.14.6 · yfinance · ta==0.11.0 ·
gspread · google-auth · google-api-python-client · pytz · openpyxl ·
pandas-market-calendars · alpaca-py
```

### C3 · מה נדרש בנתיב `agent_minute` — נבדק בייבוא בפועל
ייבואים ברמה העליונה של הנתיב (orchestrator + trader + sentinel + execution + logging +
enrichment + perception + sheets_manager + data_provider + alpaca_provider + formulas + utils + config):
```
abc agent collections config data_provider dataclasses datetime formulas gspread json
logging math os pandas pandas_market_calendars pathlib pytz re sheets_manager streamlit
sys time typing utils uuid
```

| חבילה | בנתיב agent_minute? | ראיה |
|---|---|---|
| `pandas` `gspread` `google-auth` `pytz` `pandas-market-calendars` `alpaca-py` | **נדרשות** | ייבוא ישיר |
| `streamlit` | **נדרשת רק "רכה"** | `sheets_manager.py:119` `import streamlit as st` **בתוך `try:`** עם `except Exception: pass` (`:124`). היעדרה אינה שוברת. |
| `finvizfinance` | **נדרשת רק "רכה"** | `utils.py:873-874` — ייבוא **בתוך פונקציה** (`SanitizedOverview`), לא ברמה העליונה. הסוכן אינו סורק. |
| `ta` | **לא** | `NOT imported on the agent path` |
| `openpyxl` | **לא** | `NOT imported on the agent path` |
| `altair` | **לא** | נגררת ע"י streamlit |
| `plotly` | **לא** | רק `agent/dashboard/*` |
| `yfinance` | **לא** בנתיב הדקתי | רק `market_context`, `dashboard/*`, ו-`yfinance_provider` שאינו נטען כש-Alpaca הצליח |

**עלות ייבוא נמדדת מקומית** (לא ב-Actions):
```
  pandas                          0.197s
  streamlit                       0.254s
  gspread                         0.117s
  alpaca.data.historical          0.103s
  pandas_market_calendars         0.096s
  finvizfinance.screener.overview 0.992s   ← היקרה ביותר
  altair                          0.893s
  yfinance                        0.531s
  ta                              0.017s
  plotly                          0.000s   (כבר נטענה)
```

---

## D · החשבון

| רכיב | היום | אחרי | ההנחה שהחישוב נשען עליה |
|---|---|---|---|
| תור + `Set up job` + `Checkout` + `Set up Python` | **11s** (מדוד) | 11s | ללא שינוי |
| `pip install` | **29s** (מדוד) | **~10s** ⚠️ **הערכה** | שקאש pip עובד ושהחבילות הלא-נדרשות יורדות. **לא נמדד.** |
| imports + אתחול broker | **7.5s** (מדוד) | **~5s** ⚠️ **הערכה** | חיסכון של finvizfinance+altair לפי המדידה המקומית (0.99+0.89). **לא נמדד ב-Actions.** |
| `build_account_state` | **3.0s** (מדוד) | 3.0s | ללא שינוי |
| **העשרת SMA20** | **106.3s** (מדוד) | **~1–10s** ⚠️ **הערכה רחבה** | batch מדוד ב-**0.67s** מקומית; אבל הפער פי-12 מול Actions לא הוסבר (§A6). הטווח משקף את אי-הוודאות. |
| לולאת 60 ההחלטות | **45.1s** (מדוד) | **~20s** ⚠️ **הערכה** | הסרת 60 `get_worksheet`+`append` לטובת `safe_append_rows` אחד. **לא נמדד.** |
| **סה"כ** | **~203s** | **~50–60s** ⚠️ | |

**שורה תחתונה: כן, מתחת ל-60 שניות אפשרי — אבל רק אם שלושת השינויים נוחתים יחד,
ורק אם הפער שלא הוסבר ב-§A6 אכן נעלם עם ה-batching.**
אם ה-106 שניות אינן rate-limiting אלא משהו אחר — **הריצה תישאר מעל 60**, ואף אחד
משני השינויים האחרים לא יסגור את הפער לבדו.

⚠️ **ארבעה מתוך שישה מספרי ה"אחרי" הם הערכות ולא מדידות.** רק ה-11s וה-3.0s ודאיים.

---

## E · סיכון

| | נוגע בנתיב המסחר החי? | TDD? | פריסה |
|---|---|---|---|
| **A · batch ל-SMA20** | **כן, ישירות.** `price_vs_sma20` מזין את Filter 4d (TOXIC_PROFILE). ⚠️ הפילטר **כבוי היום** (`ENTRY_GATE_MINIMAL=True`, `config.py:378`) — אבל הערך נרשם ל-`decision_log` ומשמש מחקר. שינוי שקט בו מזהם דאטה. | **חובה.** נדרש fixture שמוודא: מיפוי symbol→SMA נכון, טיקר חסר מחזיר `None` ולא ערך של שכן, ושהמבנה זהה לסימבול-אחד. הבדיקה של §A7 מראה שסימבול לא-קיים **נעדר בשקט** — זה בדיוק מה שטסט חייב לתפוס. | **עצמאי.** לא תלוי ב-B או C. |
| **B · batch ל-sentinel_events** | **לא.** טאב תצפיתי; ארבעת הצרכנים post-hoc (§B4). אין נתיב שבו ערך משם חוזר להחלטה. | **רצוי, לא קריטי.** הסיכון היחיד הוא אובדן בקריסה — וגם היום 55/60 אובדים. | **עצמאי.** ⚠️ חופף חלקית ל-TASK-218; צריך להכריע מי הבעלים. |
| **C · pip cache + גיזום** | **לא ישירות** — אבל **הסרת חבילה שנדרשת בשקט שוברת את הריצה כולה.** `streamlit` ו-`finvizfinance` נראות מיותרות אך מיובאות בתוך `try:`/בתוך פונקציה — הסרה דורשת אימות שכל ענף נתמך. | **לא TDD אלא ריצת-אימות** — צריך להריץ את הנתיב המלא בלי החבילות ולראות שלא נשבר. | ⚠️ **הכי מסוכן דווקא, כי הוא נראה הכי תמים.** `requirements.txt` משותף ל-**17 workflows** — גיזום שנועד ל-agent_minute ישבור את הדשבורד, את ה-collector ואת ה-critic. **חייב `requirements-agent.txt` נפרד או שלא בכלל.** |

**מה חייב לנחות יחד:** שום דבר. השלושה עצמאיים.
**מה שכן:** אף אחד מהם **לבדו** אינו מביא מתחת ל-60 שניות (§D).

**לא כתבתי קוד ולא הצעתי diff, כפי שהתבקש.**

---

## מה לא נבדק — הצהרה

1. **הפער פי-12 בין 9.05s מקומית ל-106.3s ב-Actions.** לא הוסבר. כל החישוב ב-§D תלוי בו.
2. **תקרת הסימבולים לבקשה.** 60 עברו; לא ניסיתי יותר.
3. **תמיכת `cache: 'pip'` ב-`setup-python@v6`.** לא אימתתי מול תיעוד רשמי; אין בריפו דוגמה.
4. **זמן ה-pip בפועל עם cache.** ה-~10s הוא הערכה.
5. **האם streamlit/finvizfinance באמת ניתנות להסרה.** מיפיתי שהן בתוך `try:`/בתוך פונקציה —
   לא הרצתי בלעדיהן.
6. **המדידות בוצעו אחרי הנעילה מהמק.** בשעות מסחר, מ-runner, המספרים יהיו אחרים.

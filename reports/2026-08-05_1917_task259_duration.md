# TASK-259 שלב ב — פילוח משך הריצה

**נכתב 2026-08-05 19:17 Lima.** ⚠️ **חקירה בלבד — לא תיקנתי, לא הוספתי `concurrency`,
לא שיניתי workflow/קוד/config, לא הרצתי workflow/scanner/collector, לא ביטלתי ריצה.**
לא הרצתי pytest, אין commit/push, לא שיניתי סטטוס תיק.
כל הראיות מלוגי Actions ומהדיסק — **לא נדרשה קריאה מ-Sheets ולא ביצעתי אחת.**
HEAD = `52c32a7`.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/systematic-debugging | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/systematic-debugging/SKILL.md` | 296 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

**הריצות שנבחרו** (מתוך 487 ריצות `agent_minute` ו-400 `auto_scan` של 5/8):
```
agent_minute  near-median (203s): 31030383592(203) 31019921919(203) 31017461975(203)
                                  31022533299(204) 31015512481(199)
              longest:            31027299559(370) 31034304749(340)
auto_scan     near-median (269s): 31036825904(269) 31032887840(269) 31028264364(269)
                                  31032165176(270) 31030219528(268)
              longest:            31024026412(785) 31022286748(532)
```

---

## 1. פילוח לפי step — agent_minute

```
step                        30383592  19921919  17461975  22533299  15512481  27299559  34304749    median
Set up job                         1         1         1         1         1         1         2         1
Checkout code                      1         1         2         1         2         1         1         1
Set up Python                      0         0         1         0         0         0         0         0
Install dependencies              29        25        39        31        26        25        31        29
Run agent orchestrator           163       169       146       164       155       336       290       164
Post Set up Python                 0         0         0         0         0         0         0         0
Post Checkout code                 0         0         0         0         0         0         0         0
Complete job                       0         0         0         0         0         0         0         0
------------------------------------------------------------------------------------------------------------
SUM of steps                     194       196       189       197       184       363       324
```

| רכיב | חציון |
|---|---|
| `Set up job` | **1s** |
| `Checkout code` | **1s** |
| `Set up Python` | **0s** |
| `Install dependencies` (pip) | **29s** |
| **`Run agent orchestrator`** | **164s** |
| Post-steps + Complete | **0s** |
| **סה"כ steps** | **194s** |

הפער בין 194 ל-203 (משך ה-run המדווח) הוא זמן תור והקצאת runner לפני ה-step הראשון.
**בשתי הריצות הארוכות ההפרש כולו באורקסטרטור** — 336s ו-290s — בעוד ה-pip נשאר 25–31s.

---

## 2. פילוח לפי step — auto_scan

```
step                        36825904  32887840  28264364  32165176  30219528  24026412  22286748    median
Set up job                         1         0         1         1         1         1         1         1
Checkout code                      2         1         1         2         3         2         2         2
Set up Python                      0         1         1         0         0         1         0         0
Install dependencies              19        17        18        16        18        21        18        18
Run scanner                      231       242       240       236       240         —       491       240
Post/Complete                      0         0         1         0         0         —         0         0
------------------------------------------------------------------------------------------------------------
SUM of steps                     253       261       262       255       262         —       512
```

| רכיב | חציון |
|---|---|
| תקורה (`Set up job`+`Checkout`+`Python`) | **3s** |
| `Install dependencies` (pip) | **18s** |
| **`Run scanner`** | **240s** |
| **סה"כ steps** | **~258s** |

⚠️ **הריצה "הארוכה ביותר" 31024026412 (785s) היא `cancelled`, לא `success`:**
```
$ gh run view 31024026412 --json conclusion,status,startedAt,updatedAt
24026412 completed/cancelled 2026-08-05T16:11:02Z -> 2026-08-05T16:24:07Z
step 5|Run scanner|2026-08-05T16:11:31Z|0001-01-01T00:00:00Z|   ← completedAt הוא ערך-אפס
```
היא רצה 785 שניות מול `timeout-minutes: 8` (480 שניות) ובוטלה. `completedAt` של ה-step
הוא `0001-01-01`, ולכן משכו אינו ניתן לחישוב. **זו הסיבה שהעמודה שלה ריקה בטבלה.**
ה-max האמיתי מבין הריצות שהצליחו הוא **491s** (31022286748).

---

## 3. בתוך האורקסטרטור — לאן הזמן הולך

הלוג **כן** נושא חותמות זמן ברמת השורה (ISO עם מיקרו-שניות), ולכן ניתן לבנות ציר-זמן.
הריצה: `31030383592`, אורקסטרטור 163s.

```
17:31:39.668     start   Agent run started at 2026-08-05 12:31:39 Peru
17:31:47.198  +   7.53s  AlpacaBroker initialized (dry_run=True)
17:31:47.362  +   0.16s  Initialized data provider: alpaca-paper
17:31:50.345  +   2.98s  Account state: 1 open positions, 7 ENTER today, $200000 buying_power
17:33:36.613  + 106.27s  Latest scan: 60 signals at 12:26
17:33:36.640  +   0.03s  DataSentinel initialized: enabled=True, mode=shadow, checks=4
17:34:21.757  +  45.12s  Run complete: signals=60, decisions=60 (ENTER=0, SKIP=60), errors=1
```

| שלב | משך | מה זה |
|---|---|---|
| imports + אתחול broker | **7.5s** | טעינת pandas/streamlit/alpaca-py + חיבור ל-Alpaca |
| אתחול data provider | 0.2s | — |
| **`build_account_state`** | **3.0s** | שתי קריאות Sheets: `paper_portfolio` + `decision_log` |
| **קריאת סיגנלים + העשרת SMA20** | **106.3s** | ← **65% מזמן האורקסטרטור** |
| אתחול Sentinel | 0.03s | — |
| **לולאת הסיגנלים + ניטור + flush** | **45.1s** | 60 החלטות |
| **סה"כ** | **162.1s** | |

### 3.1 — מה קורה בתוך 106 השניות

```
lines inside the 106s block: 7
  +  0.00s  17:31:50.345  Account state: 1 open positions, 7 ENTER today
  + 87.16s  17:33:17.509  Alpaca get_daily_bars(BLMN) failed: {"message": "too many requests."}
  +  0.00s  17:33:17.509  [sma20_cache] BARS_INSUFFICIENT: BLMN bars=0
  +  3.10s  17:33:20.610  [sma20_cache] BARS_INSUFFICIENT: ATTO bars=1
  + 16.00s  (to 'Latest scan')
```
**87 שניות רצופות בלי ולו שורת לוג אחת.** הקוד עובד ואינו מדווח, כי `get_price_vs_sma20`
מדפיס רק כשהוא **נכשל**.

**מה רץ שם, מהקוד:**
```python
agent/orchestrator.py:84-95   # L3 enrichment: Price/SMA20 for Toxic Profile filter (4d)
    from agent.enrichment.sma20_cache import get_price_vs_sma20
    signal["price_vs_sma20"] = get_price_vs_sma20(_ticker, _price)
```
נקרא **לכל סיגנל בנפרד** בתוך `_signal_from_timeline_row`. ו-`sma20_cache.py`:
```python
sma20_cache.py:17   CACHE_PATH = .../data/sma20_cache.json
sma20_cache.py:50-58  cached = cache.get(f"{ticker}:{today}")   → hit מחזיר מיד
sma20_cache.py:60-64  אחרת: bars = get_daily_bars(ticker, days=25)   ← קריאת Alpaca
```
**הקאש הוא קובץ מקומי תחת `data/`, ו-`data/` נמצא ב-`.gitignore` (שורה 12)** —
`git ls-files data/` מחזיר ריק. כל ריצת GitHub Actions מתחילה מ-checkout נקי, ולכן
**הקאש תמיד ריק, ו-60 הסיגנלים מייצרים 60 קריאות Alpaca עוקבות בכל ריצה, בכל דקה.**

### 3.2 — הלולאה: 45 שניות ל-60 החלטות
```
first [SKIP] line : 17:33:37.359
last  [SKIP] line : 17:34:13.977
signal loop span  : 36.6s      ([SKIP] lines = 60, כלומר ~0.61s לסיגנל)
```
ה-9 שניות הנותרות (עד 17:34:21.757) הן ניטור פוזיציות + flush של `skip_summary`
ו-`shadow_gate_events`.

### 3.3 — עומס ה-429 בריצה הזו
```
429 mentions                : 69
'too many requests' (Alpaca): 2
'Failed to log sentinel event to sentinel_events' : 55
```
**55 מתוך 60 הסיגנלים נכשלו בכתיבת אירוע ה-Sentinel שלהם על 429** —
`data_sentinel.py:58-59` קורא `get_worksheet` + `safe_append_row` **פר-סיגנל**.
כל אחת מהן היא ניסיון-רשת שנכשל, ולכן תורמת זמן.

**מה שלא נמדד:** הלוג אינו מפריד את זמן ה-Sheets מזמן ה-Alpaca בתוך הלולאה, ואין בו
חותמת לפני/אחרי כל קריאה בודדת. הפילוח שלמעלה הוא ברמת השלב, לא ברמת הקריאה.

---

## 4. sleep ו-retry

```
$ grep -rn "time.sleep|sleep(" --include=*.py agent/ sheets_manager.py data_provider.py providers/ utils.py auto_scanner.py
agent/news_detective/news_detective_v1.py:212   time.sleep(_RATE_LIMIT_DELAY)   ← News Detective כבוי (config.py:364)
agent/execution/order_manager.py:197            time.sleep(wait)                ← backoff על כשל שליחת פקודה
agent/execution/order_manager.py:219            time.sleep(POLL_INTERVAL)       ← המתנה למילוי פקודה
sheets_manager.py:419                           _time.sleep(wait)               ← retry קריאה
sheets_manager.py:542                           _time.sleep(wait)               ← retry append_row
sheets_manager.py:589                           _time.sleep(wait)               ← retry append_rows
auto_scanner.py:382                             time.sleep(0.1)                 ← בין טיקרים בסריקה
auto_scanner.py:412                             time.sleep(0.2)                 ← בין טיקרים
auto_scanner.py:977                             time.sleep(0.2)                 ← live_trades
```

**מנגנון ה-retry של Sheets:**
```
sheets_manager.py:383   _RETRY_MAX = 3
sheets_manager.py:384   _RETRY_BACKOFF_BASE = 2          # seconds: 2, 4, 8
sheets_manager.py:389   _APPEND_RETRY_MAX = 4
sheets_manager.py:416   wait = _RETRY_BACKOFF_BASE ** (attempt + 1)
```
קריאות: עד 3 נסיונות, המתנות 2 ו-4 שניות (מקסימום 6 שניות לקריאה).
Append: עד 4 נסיונות, המתנות 2, 4, 8 (מקסימום 14 שניות ל-append).

**כמה מתוך 203 השניות הן sleep מכוון — בריצה `31030383592`:**
בלוג **אין ולו שורת `[sheets_manager] quota 429 — retry`** אחת.
כלומר **אף קריאת Sheets לא נכנסה ל-backoff באותה ריצה — 0 שניות של sleep מכוון.**
55 כשלי ה-Sentinel נרשמים כ-`Failed to log sentinel event` ולא כ-retry, כלומר
הם נכשלו ולא ניסו שוב. `auto_scanner` לא רץ כאן. **התשובה: ~0 שניות.**

⚠️ זה נכון לריצה שנבדקה. בריצות שכן נכנסות ל-backoff המספר שונה — לא מדדתי התפלגות.

---

## 5. requirements ו-pip

```
$ wc -l requirements.txt
17
streamlit>=1.40.0            ← כבד
pandas>=2.2.0                ← כבד
plotly>=5.18.0               ← כבד
finvizfinance==0.14.6
yfinance>=0.2.50             ← כבד (גורר pandas, requests, lxml)
ta==0.11.0                   ← גורר numpy
gspread>=6.0.0
google-auth>=2.0.0
google-api-python-client>=2.0.0
pytz>=2023.3
openpyxl
pandas-market-calendars
alpaca-py>=0.25.0            ← כבד
# Force rebuild 20260524-212925
```
**12 חבילות מוצהרות** (+ שורת הערה). מתוך הלוג, ההתקנה בפועל מביאה **הרבה יותר**:
```
Successfully installed MarkupSafe-3.0.3 alpaca-py-0.43.5 altair-6.2.2 annotated-types-0.8.0
anyio-4.14.2 attrs-26.1.0 beautifulsoup4-4.15.0 blinker-1.9.0 bs4-0.0.x ...
```
הכבדות: **streamlit · pandas · plotly · altair · yfinance · alpaca-py · numpy (דרך ta)**.
`streamlit` ו-`plotly` ו-`altair` הם תלויות **דשבורד**, לא תלויות של האורקסטרטור.

**זמן pip בפועל:** `agent_minute` **29s** חציון · `auto_scan` **18s** חציון.

**האם יש cache ל-pip:**
```yaml
# agent_minute.yml:16-20
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'          ← אין שורת cache:
      - name: Install dependencies
        run: pip install -r requirements.txt
```
```yaml
# auto_scan.yml:18-22 — זהה, אין cache:
```
**אין `cache: 'pip'` באף אחד משני ה-workflows.** כל ריצה מורידה ומתקינה הכל מחדש.

---

## 6. קריאות ל-Sheets בריצה אחת

נתיב האורקסטרטור, מאומת ב-grep:
```
orchestrator.py:146   get_sheet_records("system_events")     ← check_emergency_stop
orchestrator.py:222   get_sheet_records("paper_portfolio")   ← build_account_state
orchestrator.py:271   get_sheet_records("decision_log")      ← build_account_state
orchestrator.py:345   get_worksheet("timeline_live")
orchestrator.py:350   get_sheet_records("timeline_live")     ← read_latest_signals
orchestrator.py:404   get_sheet_records("timeline_live")     ← detect_outage
orchestrator.py:632   get_worksheet("decision_log")          ← postmortem reader
orchestrator.py:651   get_worksheet("paper_portfolio")
position_manager.py:111  get_sheet_records("paper_portfolio")  ← מקאש
position_manager.py:190  get_worksheet("paper_portfolio")
data_sentinel.py:58-59   get_worksheet("sentinel_events") + safe_append_row   ← פר-סיגנל
decision_logger.py:182   get_worksheet("skip_summary")        ← batched, פעם אחת
order_manager.py:305     get_worksheet("paper_portfolio")     ← רק ב-ENTER
```

**המספר בריצה טיפוסית:** 4–5 טאבים ייחודיים דרך `get_sheet_records`
(`system_events`, `paper_portfolio`, `decision_log`, `timeline_live`) — כל אחד **קריאת API
אחת בלבד**, כי `get_sheet_records` עובר דרך הקאש. `timeline_live` נקרא בשלוש נקודות
(`:350`, `:404`, ו-`get_worksheet` ב-`:345`) אך כולן בתוך אותו חלון קאש.
**בנוסף:** `data_sentinel.py:58` הוא `get_worksheet` + append **פר-סיגנל** — ב-60 סיגנלים
זו **60 קריאה+כתיבה שאינן עוברות בקאש**. בריצה שנבדקה **55 מהן נכשלו על 429**.

### הקאש מול משך הריצה — הממצא
```
sheets_manager.py:390   _SHEET_CACHE_TTL = 60            # seconds
sheets_manager.py:457   if cached is not None and (now - cached[0]) < _SHEET_CACHE_TTL: return cached[1]
```
**TTL של 60 שניות מול ריצה חציונית של 164 שניות באורקסטרטור.**
ציר-הזמן מ-§3 מראה את הפער בפועל:
```
build_account_state הסתיים ב-  17:31:50   → הקאש של paper_portfolio/decision_log נוצר כאן
לולאת הסיגנלים החלה ב-         17:33:37   → 107 שניות אחר כך
```
**הקאש פג הרבה לפני שהלולאה מתחילה.** כל קריאה חוזרת אחריו היא קריאת API חדשה.
זו עובדה מדודה על הריצה הזו; לא בדקתי כמה קריאות חוזרות בפועל מתרחשות אחרי הפקיעה.

---

## 7. השאלה המכריעה — שלוש שורות

**תקורה קבועה: 31 שניות מתוך 203** (`Set up job` 1 + `Checkout` 1 + `Set up Python` 0 +
`pip install` 29), ועוד ~9 שניות תור לפני ה-step הראשון — **סה"כ ~40 שניות, 20%.**

**עבודה אמיתית: 164 שניות, 81% מזמן ה-steps** — מתוכן **106s (65%) הן העשרת SMA20**
(60 קריאות Alpaca עוקבות מול קאש שתמיד ריק כי `data/` ב-.gitignore), **45s (27%)** לולאת
60 ההחלטות עם append פר-סיגנל ל-`sentinel_events`, **7.5s** imports ואתחול broker,
ו-**3.0s** `build_account_state`.

**ריצה מתחת ל-60 שניות אפשרית בעיקרון — אבל דורשת שהתקורה והעבודה יחד ירדו ב-70%:**
המספרים אומרים שכדי להגיע ל-60 היה צריך להוריד **לפחות ~145 שניות** — ובפועל רק שני
פריטים גדולים מספיק כדי לכסות זאת: **106 השניות של SMA20** ו-**29 השניות של pip**.
ביחד הם 135 שניות; בלעדיהם הריצה עומדת על ~68 שניות, עדיין מעל 60.
**אין פריט יחיד שהסרתו מספיקה.**

⚠️ **זו אריתמטיקה על המספרים שנמדדו, לא הצעת תיקון ולא המלצה.**
לא בדקתי אם אפשר להוריד אף אחד מהם, מה זה היה שובר, או מה העלות.

---

## מה לא נמדד — הצהרה

1. **פילוח פנימי של 87 השניות השקטות.** הסקתי מה רץ שם מהקוד (`orchestrator.py:84-95`
   ← `sma20_cache.py:60-64`) ומשתי שורות הכשל שכן הודפסו. **הלוג אינו מכיל חותמת
   לכל קריאת `get_daily_bars`**, ולכן לא מדדתי את הזמן הממוצע לקריאה ולא אימתתי
   שבוצעו בדיוק 60.
2. **הפרדת זמן Sheets מזמן Alpaca בתוך הלולאה.** אין חותמות ברמת הקריאה.
3. **התפלגות ה-sleep על פני ריצות.** הריצה שנבדקה לא נכנסה ל-backoff כלל; לא בדקתי
   כמה ריצות כן, ובכמה שניות.
4. **כמה קריאות API חוזרות מתרחשות אחרי פקיעת הקאש.** קבעתי שהקאש פג (107 שניות מול
   TTL של 60) — לא ספרתי את הקריאות שנגררות מכך.
5. **auto_scan מבפנים.** פילחתי את ה-steps שלו בלבד; לא פתחתי את הלוג של ריצת סורק.
6. **האם 106 השניות קבועות או תלויות במספר הסיגנלים.** נמדדה ריצה אחת עם 60 סיגנלים.

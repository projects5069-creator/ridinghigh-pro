# שומר חיצוני — LAND

**נכתב 2026-08-06 15:11 Lima.** LAND — שני commits, push הצליח, PR #36 מעודכן.
לא נגעתי ב-`.github/workflows/` (נשארים `M`), לא פרסתי את הסקריפט, לא מיזגתי, לא המתנתי ל-CI.

**סקילים:** rhpro-live (180) · superpowers/verification-before-completion (139).

⚠️ **גילוי — ה-post-commit hook נגע ב-Sheets.** ההוראה אסרה נגיעה ב-Sheets/Drive/FINVIZ.
ה-hook הריץ אוטומטית `generate_project_state` שקורא סטטיסטיקות מהגיליונות:
```
[post-commit] updating PROJECT_STATE.md (this takes ~30-60s)...
[generate_project_state] this may take 30-60s for Sheets stats...
[generate_project_state] ✅ done — 2025 bytes
```
**קריאה בלבד, אפס כתיבה**, ו-`PROJECT_STATE.md` ב-`.gitignore` ולכן לא נכנס לשום commit.
זה לא היה ביוזמתי — אבל ה-hook מתועד ויכולתי לצפות אותו. מדווח ולא מסתיר.

---

## 1. commit ראשון — השומר ✅

```
$ git diff --cached --name-only -z | tr '\0' '\n'
scripts/watchdog/README.md
scripts/watchdog/watchdog_v1.gs
files=2  workflows=0  python=0   ✅
```
```
4406a92  feat(watchdog): an outage detector that does not run on GitHub Actions
         2 files changed, 415 insertions(+)
         create mode 100644 scripts/watchdog/README.md       (158)
         create mode 100644 scripts/watchdog/watchdog_v1.gs  (257)
```

### Anti-Drift §4 — החלטה מנומקת, לא דילוג

שבע הקטגוריות: `formulas · weights · scoring · workflows · sheets configuration · schema · health checks`.
שומר חיצוני **הוא** health check — אבל **הוא אינו רץ.** הקובץ יושב בריפו ואינו מותקן;
ה-PK מתאר את המערכת החיה, ומערכת שלא השתנתה אינה דורשת bump.
**⚠️ החוב נרשם במפורש כתנאי-קבלה #4 ב-TASK-265** — ברגע שהטריגר יורה, השומר הוא חלק
מהמערכת ו-§4 חל. **לא דילגתי — דחיתי לנקודה שבה זה נכון.**

## 2. commit שני — הבקלוג ✅

```
29ebfac  docs(backlog): watchdog deployment plus five findings from the outage
         6 files changed, 380 insertions(+)
```

| תיק | עדיפות | הראיה שנרשמה בגוף |
|---|---|---|
| **TASK-265** Deploy the external watchdog | **High** | ארבעת הצעדים · `RH-Summaries` ולא חודשי (רוטציה) · אזהרת `PER_PAGE=100` · שלוש דרכי בדיקה · 4 תנאי-קבלה כולל שורה מטריגר אמיתי ולא מהעורך · ארבעה גבולות ידועים |
| **TASK-266** timeout הורג 38.6% | Medium | `n=60, median 315s, cancelled=58/60` מול `timeout-minutes: 5` · `188/487` ב-08-05 · ומדוע זה מזהם את בסיס ה-203s |
| **TASK-267** `check_06` — חלון 24 דק׳ + INFO | Medium | `:639 per_page=50` · הסימולציה: 490/951 חלונות would-be-CRITICAL אך **PASSED 100%** ב-20:30Z · `:602` מחזיר INFO כשהכל queued · ו-`cancelled` שנענש כמו כשל בלי לזכות בהקלה של כשל |
| **TASK-268** `detect_outage` מת עם המערכת | Medium | `orchestrator.py:577` בתוך `run()` · `grep` על 4 ריצות = `0,0,0,0` · **ומדוע השומר החדש אינו תחליף** — הוא רואה תפוקה, לא רעננות `timeline_live` |
| **TASK-269** ריצה מאחרת כותבת מחירי-עכשיו | Medium | `31120743074`: created 16:42Z, checkout 18:36Z, **המתנה 1:54 שעות**, הצליחה · ו-429 על Read requests באותו לוג |

### TASK-259 — עודכן, ושתי טענות בו הופרכו

```
1. ✅ אופציה שלישית: queue: max — "up to 100 pending runs to wait in order"
      כל הניתוח בתיק הניח שהברירה היחידה היא ביטול. היא לא.
      ⚠️ לא אומת שזמין בתוכנית הזו.
2. ✅ הסמנטיקה עכשיו מצוטטת ולא מונחת (N-1 מבוטלות, האחרונה שורדת)
3. ⚠️ אזהרת check_06 CRITICAL — מוגזמת. חושבה מהאגרגט היומי; בפועל PASSED 100%
      בשני מועדי-הבדיקה הערביים על 1,000 ריצות אמיתיות
4. ⚠️ בסיס ה-203s מזוהם — 38.6% מהריצות נחתכו ב-timeout (TASK-266)
5. ⚠️ אל תפרוס בזמן תקלת GitHub — התור של 374 הוא התקלה, לא רוויה
      (in_progress=0 בארבע מדידות; רוויה נראית כמו in_progress=המכסה)
```

## 3. push ✅

```
לפני:  40a1ba65e0b934190c07cde706d7c9fcf9b5b1c8
$ git push origin docs/handoff-2026-07-29
   40a1ba6..29ebfac  docs/handoff-2026-07-29 -> docs/handoff-2026-07-29
אחרי:  29ebfac6e5f991d5a253cd65f1aa19cf3baa118e
HEAD:  29ebfac6e5f991d5a253cd65f1aa19cf3baa118e     ✅ זהה
$ git rev-list --left-right --count HEAD...origin/...  →  0   0
```
fast-forward, בלי `+`, בלי force. **ה-push לא נדחה.**

**PR #36 — הקומיטים נכנסו אליו, לא נפתח חדש:**
```
36  OPEN  MERGEABLE  4 קומיטים  11 קבצים
workflows in PR: 0        ✅ אימות מפורש
```

## 4. מצב ה-CI והתקלה

```
$ gh pr checks 36
no checks reported on the 'docs/handoff-2026-07-29' branch
```
**אין checks — ולא המתנתי, כפי שהורית.** הסיבה מאומתת:

```
$ gh api ".../actions/runs?status=queued"      →  430     (מ-374 לפני שעה)
$ gh api ".../actions/runs?status=in_progress" →    0
```

**githubstatus, נקרא עכשיו:**
```
Actions:  MAJOR OUTAGE — עדיין פתוח
impact:   critical
עדכון אחרון 2026-08-06 19:43:21 UTC:
  "Capacity remains constrained and jobs may still be delayed or fail
   while it recovers gradually."
```
**התקלה לא נסגרה.** התור העמיק מ-374 ל-**430**, `in_progress` עדיין **0**.

```
$ git status --porcelain -- .github/workflows/
 M .github/workflows/agent_minute.yml
 M .github/workflows/auto_scan.yml          ✅ נשארו M, לא נגעתי
```

## 5. סיכום

```
1. commits : 4406a92 (2 קבצים, +415)  ·  29ebfac (6 תיקים, +380)
2. תיקים חדשים : TASK-265 (High) · 266 · 267 · 268 · 269   +  TASK-259 עודכן
3. PR #36  : OPEN · MERGEABLE · 4 קומיטים · 11 קבצים · אפס workflows · אפס checks
4. התור    : 430 queued · 0 in_progress   (היה 374 לפני שעה — מעמיק)
5. התקלה   : GitHub Actions MAJOR OUTAGE, פתוחה, עדכון אחרון 19:43:21Z
6. תיקים פתוחים : 65
```

⚠️ **לא מוזג, לא נפרס, לא הומתן ל-CI, לא נגעתי ב-workflows/Sheets/Drive/FINVIZ.**
⚠️ **ההתקנה של השומר שייכת לך** — TASK-265, ארבעה צעדים ידניים.

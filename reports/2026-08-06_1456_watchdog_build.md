# שומר חיצוני — BUILD

**נכתב 2026-08-06 14:56 Lima.** BUILD — שני קבצים חדשים תחת `scripts/watchdog/`.
**אין commit. עוצר על ה-diff.** לא נגעתי בפייתון, ב-workflows (נשארים `M`), ב-Sheets/Drive/FINVIZ.
לא פרסתי, לא הרצתי, לא מיזגתי.

**סקילים:** rhpro-live (180) · superpowers/verification-before-completion (139).

⚠️ **תיקון להנחה בהוראה:** כתבת *"אין ריצת JS מקומית לאמת מולה"*. **יש** —
`/usr/local/bin/node v24.14.0`. לכן דחיתי את `test-driven-development` מסיבה **אחרת**:
הקוד תלוי ב-globals של Apps Script (`UrlFetchApp`, `MailApp`, `SpreadsheetApp`,
`PropertiesService`) שאינם קיימים ב-Node, ולכן מחזור RED→GREEN קלאסי אינו ישים.
**מה שכן עשיתי במקומו:** בניתי את `decide_()` כפונקציה **טהורה** — בלי רשת, בלי שעון,
בלי globals — והרצתי אותה ב-Node דרך `vm.runInContext` מול **נתוני אמת** של יומיים.
זה אימות אמיתי, לא סקירת-עיניים. `systematic-debugging` נדחה (אין באג — יש מפרט מדוד),
`data-quality-checker` נדחה (אין מסמך-שוק).

---

## 1. הקבצים

```
scripts/watchdog/watchdog_v1.gs   257 שורות   (JavaScript ל-Apps Script)
scripts/watchdog/README.md        158 שורות   (הוראות התקנה)
```
שניהם **חדשים**. אפס שינוי בקובץ קיים כלשהו.

### ⚠️ פגם שנתפס **לפני** שנכתבה שורה: `per_page` חייב להיות 100

לפני הכתיבה בדקתי מה ה-endpoint **באמת** מחזיר, בסימולציה על נתוני היומיים:

```
                  per_page=50          per_page=100
                recent15  prior60    recent15  prior60
08-05 15:00Z       29       21          29       71
08-05 16:05Z       27       23          27       73
08-06 16:05Z        9       41           9       91
```
**עם `per_page=50` הספירה ב-60 הדקות הקודמות היא ~21 ביום בריא** — מתחת לסף 40.
שער-האנטי-שווא לעולם לא היה נפתח, ו**השומר לעולם לא היה מתריע.** הקבוע נקבע ל-100
עם הערה בקוד שמסבירה בדיוק את זה.

---

## 2. אימות בלי להריץ בייצור

### א. תחביר ✅
```
$ node --check scripts/watchdog/watchdog_v1.gs
TypeError [ERR_UNKNOWN_FILE_EXTENSION]: Unknown file extension ".gs"
$ cp ... /tmp/wd.js && node --check /tmp/wd.js
✅ SYNTAX OK — node v24.14.0
```
⚠️ `node --check` דוחה סיומת `.gs`; הרצתי על עותק `.js` **זהה בייט-לבייט**.

### ב. טבלת יובש — הכלל האמיתי מהקובץ, מול נתוני אמת

ה-harness טוען את הקובץ עצמו ב-`vm.runInContext` וקורא ל-`decide_()` — **הקוד שייפרס,
לא שכפול שלו.** הקלט לכל נקודה הוא סימולציה מדויקת של שתי קריאות ה-API.

**צעדי 15 דקות, כפי שביקשת:**

| UTC | 08-05 status | q / r15 / p60 | 08-06 status | q / r15 / p60 |
|---|---|---|---|---|
| 14:45Z | HEALTHY | 3 / 32 / 68 | HEALTHY | 5 / 28 / 72 |
| 15:15Z | HEALTHY | 3 / 33 / 67 | HEALTHY | 8 / 27 / 73 |
| 15:45Z | HEALTHY | 7 / 30 / 70 | HEALTHY | 4 / 21 / 79 |
| **16:15Z** | HEALTHY | 8 / 30 / 70 | **🔴 ALERT** | 12 / 7 / 88 |
| 16:45Z | HEALTHY | 10 / 29 / 71 | 🔴 ALERT | 55 / 4 / 51 |
| 17:45Z | HEALTHY | 9 / 31 / 69 | 🔴 ALERT | 158 / 0 / 17 |
| 18:45Z | HEALTHY | 10 / 31 / 69 | 🔴 ALERT | 257 / 7 / 1 |
| 19:45Z | HEALTHY | 11 / 30 / 70 | 🔴 ALERT | 302 / 0 / 7 |
| 20:00Z | OUTSIDE_WINDOW | — | OUTSIDE_WINDOW | — |
| | **סה"כ: אפס התראות ✅** | | **ראשונה 16:15Z** | |

⚠️ **16:15Z ולא 16:05Z — וזו רזולוציית הגריד, לא פער בקוד.** בצעדי 15 דקות הנקודה
16:05 פשוט אינה קיימת. ב-16:00Z הערך הוא `recent15 = 10`, והכלל הוא `< 10` — כלומר
עובר בדיוק. **ה-trigger בפועל רץ כל 5 דקות**, ולכן הרצתי גם בקצב האמיתי:

```
=== צעדי 5 דקות, יממה שלמה, שני הימים ===
2026-08-05:  התראה ראשונה — לא ירה כלל ✅        מדידות ALERT: 0
2026-08-06:  התראה ראשונה — 16:05Z ✅            מדידות ALERT: 43
             recent15=9  prior60=91  queued=6  |  אות1b=✓  אות2=✗
```
**🎯 היעד הושג במדויק: 16:05Z ב-08-06, אפס התראות ב-08-05.**

### ⚠️ ב2. ה-harness חשף פגם אמיתי — ותוקן

בהרצה הראשונה יצאו **2 מיילי התראה + 1 מייל "התאוששות"** ליום התקלה. המעקב:

```
 16:05Z ALERT     q=6   r15=9   p60=91   📧 מייל התראה
 16:10Z ALERT     q=7   r15=6   p60=94
 16:15Z ALERT     q=12  r15=7   p60=88
 16:20Z HEALTHY   q=17  r15=10  p60=81   📧 מייל התאוששות ← ⚠️ באמצע תקלה פעילה!
 16:25Z HEALTHY   q=21  r15=13  p60=74
 16:30Z ALERT     q=29  r15=13  p60=69   📧 מייל התראה שני
```
התפוקה קפצה חזרה ל-10 ול-13 למשך 10 דקות בעוד התור מטפס — והשומר הכריז "הכל תקין".
זה סותר את הדרישה שלך *"מייל אחד לאירוע"*.

**התיקון:** `HEALTHY_STREAK_TO_RESET = 3` — האירוע נסגר רק אחרי **3 קריאות תקינות
רצופות (15 דקות)**, מספיק כדי לגשר על הבועה. אימות מחדש:

```
2026-08-05:  לא ירה כלל               מיילים: אפס ✅
2026-08-06:  ראשונה 16:05Z, 43 מדידות ALERT   מיילים: 16:05Z 📧 התראה — אחד בלבד ✅
```

### ב3. מקרי-קצה

| תרחיש | status | alert |
|---|---|---|
| שתי הקריאות נכשלו | `FETCH_FAILED` | **false** ✅ |
| `queued` נכשל, `completions` תקין | `PARTIAL` | false ✅ |
| `completions` נכשל, `queued=5` | `PARTIAL` | false ✅ |
| `completions` נכשל, `queued=300` | `ALERT` | **true** ✅ (אות אחד מספיק) |
| מחוץ לחלון 21:00Z, תור 300 | `OUTSIDE_WINDOW` | false ✅ |
| מחוץ לחלון 14:00Z, תור 300 | `OUTSIDE_WINDOW` | false ✅ |
| יום ריק (סופ"ש) 17:00Z | `HEALTHY` | false ✅ |

**כשל רשת אינו שולח מייל שווא** — אבל כן נרשם ללוג.

### ג. ביקורת ידנית של הקוד

```
credentials:  grep -niE "token|secret|api_key|Bearer|Authorization"
              → התאמה יחידה: ההערה " * NO CREDENTIALS."     ✅ אפס credentials

כתיבה ל-Sheets: :204 getActiveSpreadsheet · :207 insertSheet(LOG_SHEET_NAME)
                :208 appendRow(כותרות) · :211 appendRow(שורת לוג)
              → ✅ הכל בתוך logRow_(), רק לטאב הלוג. אפס נגיעה בטאב אחר.

try/catch:    :167/:174 (fetchQueuedCount_) · :180/:195 (fetchCompletionTimes_)
              :203/:216 (logRow_)            ✅ שלושתם עטופים

שעון:         :105 getUTCHours()+getUTCMinutes() · :107/:211/:225/:243 toISOString()
              :191 Date.parse(updated_at)  ← מחרוזת ISO עם Z ⇒ נקראת כ-UTC תמיד
              → ✅ אפס getHours()/getMinutes() מקומיים, אפס Session.getScriptTimeZone,
                 אפס Utilities.formatDate. ה-timezone של הגיליון אינו יכול להשפיע.

דה-דופ:       מתאפס רק על HEALTHY אמיתי — לא על FETCH_FAILED, לא על PARTIAL,
              לא על OUTSIDE_WINDOW. ✅ לילה שקט אינו מנקה דגל של תקלה פתוחה.
```

---

## 3. הוראות התקנה

`scripts/watchdog/README.md` — ארבעת הצעדים, מה לערוך (**שורה אחת בלבד**: `ALERT_TO`),
ולמה דווקא `RH-Summaries` ולא גיליון חודשי (רוטציה ב-1 לחודש הורגת סקריפט מקושר).

**איך לבדוק שהשומר יורה — בלי לחכות לתקלה, שלוש דרכים:**
1. `rhWatchdogTestAlert()` מהעורך → מייל עם מספרים מזויפים. **אינו נוגע בדגל הדה-דופ**, ולכן אינו יכול להסתיר אירוע אמיתי.
2. `rhWatchdogRun()` ידנית → שורה ב-`watchdog_log` עם המספרים החיים.
3. `QUEUE_DEPTH_MAX = -1` זמנית → התראה אמיתית דרך **מסלול ההחלטה האמיתי**, לא stub. (ה-README מזהיר להחזיר ל-25 ולמחוק את ה-property.)

---

## 4. סיכום BUILD

```
1. קבצים   : scripts/watchdog/watchdog_v1.gs (257) + README.md (158) = 415 שורות, שניהם חדשים
2. תחביר   : ✅ node --check עבר (node v24.14.0, על עותק .js — .gs נדחה ע"י node)
3. יובש    : ✅ 08-06 התראה ראשונה 16:05Z (r15=9, p60=91) · 08-05 אפס התראות ביממה שלמה
             ✅ 43 מדידות ALERT → מייל אחד בלבד אחרי הדה-דופ
4. לא ניתן לאמת בלי פריסה: התנהגות MailApp/PropertiesService/SpreadsheetApp האמיתית ·
   ערכי everyMinutes() המדויקים · מכסת זמן-triggers יומית · שהטריגר בכלל יורה
5. הקובץ   : 257 שורות > 120 ⇒ הנתיב במקום התוכן: scripts/watchdog/watchdog_v1.gs
```

**מה שאומת מול נתוני אמת:** `decide_()` — כל הספים, החלון, שני האותות, שער-האנטי-שווא,
הדה-דופ, וכשלי הרשת. **מה שלא:** שכבת ה-Google API עצמה, שקיימת רק בזמן פריסה.

⚠️ **גבול שלא תוקן ומתועד ב-README §5:** קריאה בסופ"ש נספרת לרצף-התקינות. אם אירוע
נשאר פתוח בערב שישי, השקט של שבת ינקה את הדגל וישלח "התאוששות". התקלה האמיתית ביום שני
עדיין תתריע כראוי, ולכן זה קוסמטי — **אבל לא תיקנתי אותו, כי הוא מעבר למפרט שנתת.**

⚠️ **אין commit. הקבצים untracked. ממתין לאישור.**

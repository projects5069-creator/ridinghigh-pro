# 53 התיקים הפתוחים — חילוץ מלא · קובץ ב'

**טווח:** TASK-208 → TASK-254 · **26 מתוך 53** (קובץ א' מכיל TASK-9 → TASK-206, 27 תיקים).
נכתב 2026-08-05 10:55 Lima (11:55 EDT NY). **המקור הקנוני: `backlog task <N> --plain`** — הראיה בקובץ א' §0.
**תיאור וחילוץ בלבד.** אין תוכנית, אין המלצות, אין דירוג, אין סדר-ביצוע, אין הצעת תיקון.

---

## 2+3. חילוץ מלא — TASK-208 עד TASK-254

---

### TASK-208 · Decouple Score from scanner ranking + portfolio selection
**סטטוס:** To Do · **עדיפות:** Low · **נוצר:** 2026-06-29 21:46 · **עודכן:** 2026-07-04 01:50

**מה התיק מבקש:**
> auto_scanner selects/ranks by Score (TRADE_ENTRY_MIN_SCORE>=70, idxmax/sort) at :490/578/1335/1338. After 194/S1 decoupled Score from entry gate, decide MxV-ranking vs lower threshold. Display/portfolio layer, separate from entry decision.

**Acceptance Criteria:**
> - [x] #1 borrow_collector.py:40 selects borrow targets by score>=min_score — switch to MxV (scoreless-era: score is blank, breaks selection)

**Implementation Notes:**
> RECON 30/6 (READ-ONLY): החצי של borrow-selection כבר תוקן —
> commit 5a127ad (29/6, יום ה-flip): get_scanned_universe בוחר לפי MxV<=mxv_max, לא Score.
> חי + מחובר ל-orchestrator_eod (collect_borrow_snapshot, רץ off-hours 16:00 Peru via agent_eod.yml).
> ההשערה 'שבור מאז flip' הופרכה — התיקון ליווה את ה-flip באותו יום, אין חלון-שבירה. AC#1 סומן done.
> ⚠️ ה-scope הנותר ב-208 = decoupling של Score מ-ranking/display ב-auto_scanner (4 אתרים: 490/578/1335/1338,
> שכבת display/portfolio, נפרד מהחלטת-כניסה). זה Score-work → שייך לאשכול-Score, נדחה עם TASK-209
> (החלטת עמיחי 30/6: כל עבודת-Score בהמשך, לא עכשיו). לא לבצע את הנותר עד שנטפל ב-Score כמכלול.
>
> E2E-AUDIT S3 (3/7, קריאה-בלבד) — אימות-חי של אתרי-ה-ranking + אתר נוסף:
> run_scan portfolio-selection Score>=TRADE_ENTRY_MIN_SCORE (auto_scanner.py:488-490) ·
> update_live_trades ENTRY_MIN_SCORE=TRADE_ENTRY_MIN_SCORE (:994 — לא היה ברשימה המקורית) ·
> run_eod פילטר Score>= + sort_values('Score') (:1332-1338). החישובים עצמם דרך formulas (SSoT תקין);
> הכתיבה קפואה ב-score_cell. ראיות: plans/stateless-seeking-sifakis.md S3.

**תלויות:** TASK-194/S1 · **TASK-209** (`נדחה עם TASK-209`).
**קבצים שהתיק מציין:** `auto_scanner.py:490/578/1335/1338` · `:488-490` · `:994` · `:1332-1338` · `borrow_collector.py:40` · `plans/stateless-seeking-sifakis.md`.
**לא בוצע (ציטוט):** `לא לבצע את הנותר עד שנטפל ב-Score כמכלול` · `החלטת עמיחי 30/6: כל עבודת-Score בהמשך, לא עכשיו`.

`[DECISION]` + `[CODE]` — הגוף אומר `decide MxV-ranking vs lower threshold` (הכרעה), וה-Notes חוסם את הביצוע במפורש עד הכרעת-Score כוללת.
**האם התנאי עדיין קיים:** ✅ כן, כל 4 האתרים מאומתים חי היום — עם מספרי-שורה שהתעדכנו:
- `auto_scanner.py:504` — `high_score = results_df[results_df['Score'].astype(float) >= TRADE_ENTRY_MIN_SCORE]` (התיק אומר 488-490)
- `auto_scanner.py:1008` — `ENTRY_MIN_SCORE = TRADE_ENTRY_MIN_SCORE` בתוך `update_live_trades` (התיק אומר :994)
- `auto_scanner.py:1349` — `high_score = today_tl[today_tl["Score"] >= TRADE_ENTRY_MIN_SCORE]` ב-`run_eod`
- `auto_scanner.py:1352` — `.sort_values("Score", ascending=False)` ב-`run_eod`
AC#1 מאומת כמומש: `get_scanned_universe(snapshots_df, mxv_max=AGENT_MXV_MAX)` — `agent/perception/borrow_collector.py:33`, עם הערה בשורות `:35-37` שמסבירה בדיוק את המעבר.

---

### TASK-209 · Retire calculate_score or demote to logged diagnostic
**סטטוס:** To Do · **עדיפות:** Low · **נוצר:** 2026-06-29 21:46 · **עודכן:** 2026-07-04 01:50

**מה התיק מבקש:**
> calculate_score feeds ~15 display/analysis consumers (dashboard, post_analysis_collector, health_check, health_audit). After 194 decoupled Score from entry, decide full retire vs keep as documented diagnostic. High blast-radius; likely keep as diagnostic.

**Implementation Notes:**
> E2E-AUDIT S1-S5 (3/7) — scope נספג מהחקירה (4 פריטים, כולם אשכול-Score):
> (1) dead-code: normalize_mxv/normalize_atrx (formulas.py:457/468) — אפס call-sites repo-wide; import-רפאים dashboard.py:59-60 + import calculate_score מת ב-post_analysis_collector.py:34 — למחוק יחד עם ה-retire.
> (2) dashboard.py:378 קורא calculate_vwap_dist (deprecated alias, formulas.py:596) → להחליף ב-calculate_typical_price_dist; וכן dashboard.py:63 מייבא calculate_score דרך auto_scanner במקום מ-formulas — ליישר.
> (3) d.score מחושב פר-החלטה על הנתיב-הקריטי גם בעידן scoreless: decision_logic.py:266 דרך calculate_agent_score; חילוץ-המפתחות signal[...] (:258-264) יזרוק KeyError→SKIP DATA_ERROR למרות שה-score אינו מגייט (gate=active). ההכרעה demote-vs-remove שייכת לכאן. נתיב-מסחר חי — רק עם go.
> (4) health_audit checks עידן-Score: check_07_score_range (:738, בלי מודעות-SCORE_WRITE_FROZEN) + check_12_score_weights_sum (:1008) — לרענן/להחריג בהחלטת-הסיום; אגב: generate_sheets_schema.py:9 doc-drift ('14 sheets' ↔ 16 בפועל).
> ראיות: plans/stateless-seeking-sifakis.md S1/S2/S3/S4.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-194 · TASK-208 (אותו אשכול).
**קבצים שהתיק מציין:** `formulas.py:457/468` · `formulas.py:596` · `dashboard.py:59-60/63/378` · `post_analysis_collector.py:34` · `decision_logic.py:266/258-264` · `health_audit.py:738/1008` · `generate_sheets_schema.py:9`.
**לא בוצע:** `decide full retire vs keep as documented diagnostic` · `נתיב-מסחר חי — רק עם go`.

`[DECISION]` — `decide full retire vs keep as documented diagnostic`. אין AC של ביצוע.
**האם התנאי עדיין קיים:** ✅ כן, 5 מתוך 6 הפריטים מאומתים היום (עם שורות מעודכנות):
- `normalize_mxv` — `formulas.py:556`, `normalize_atrx` — `formulas.py:567` (התיק אומר 457/468)
- `dashboard.py:58-59` מייבא את שניהם ברשימת ה-`from formulas import (...)` (`:49-61`)
- `dashboard.py:62` — `from auto_scanner import calculate_score, is_snapshot_time` ✅ (התיק אומר :63)
- `dashboard.py:55` מייבא `calculate_vwap_dist`; ההגדרה `formulas.py:168` היא alias deprecated (התיק אומר :596)
- `decision_logic.py:270` — `d.score = calculate_agent_score(score_metrics)`, וחילוץ המפתחות `signal["..."]` ב-`:261-269` בתוך `try` שתופס `KeyError` → `SKIP DATA_ERROR` (`:343-346`) ✅
- `health_audit.check_07_score_range` — `health_audit.py:738` ✅ · `check_12_score_weights_sum` — `health_audit.py:1008` ✅
`calculate_score` עצמה — `formulas.py:584`, חיה.

---

### TASK-215 · Dedicated SA for auto_scan (mirror TASK-58)
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-06-30 17:53 · **עודכן:** 2026-06-30 18:42 · **תוויות:** quota, market-hours, service-account, task-214-followup

**מה התיק מבקש:**
> Recon 30/6 (TASK-214 deep-recon) proved read de-dup is NOT the fix for auto_scan 429: portfolio de-dup saves only ~1 read/5min (line 510 writes portfolio between reads, breaks cache), and most per-minute reads are write-accompanied (not cache-able). The architecturally correct fix mirrors TASK-58 (which solved health_audit's 429 via dedicated SA + ROOT folder inheritance): give auto_scan its own SA so it stops competing on the shared commercial SA quota (project 591299446687). AC#1: create dedicated auto_scan SA in GCP. AC#2: share ROOT folder with it (inherits all tabs, incl future months — same as TASK-58). AC#3: point auto_scanner gc to its own credential (env var + truthy-guard fallback, same pattern as TASK-58 health_audit). AC#4: inject secret in auto_scan.yml. AC#5: verify live — 429 frequency drops + 'cancelled' overlap runs drop. evidence: auto_scan 429-crashed live 30/6 12:22+12:42 Peru. NOTE: auto_scan is the LIVE TRADING data path — higher stakes than health_audit; needs careful verify. NOTE: TASK-214 de-dup track CLOSED (documented in QUOTA_AUDIT_auto_scan_v1.md REVISED FINDING).

**Acceptance Criteria (שדה נפרד):** `No acceptance criteria defined` — ⚠️ ה-AC-ים משובצים בתוך ה-Description, לא בשדה ה-AC.

**Implementation Notes:**
> STEP-1+2 DONE 30/6 (כל צד-הקוד+infra):
> step-1 (commit ec46b4d): _get_gc מעדיף GOOGLE_CREDENTIALS_JSON_AS (truthy-guard, TDD 4/4,
> אפס regression). step-2 infra: SA ridinghigh-auto-scan@ridinghigh-pro-v2 נוצר; ROOT
> (1mHSds...) שותף כ-Editor; הורשת-WRITE מאומתת חי (timeline_live write+read+restore, production
> שוחזר ואומת). step-2 yml (commit 54f6e9f): auto_scan.yml מזריק _AS (additive, רק כאן —
> blast-radius guard). ⚠️ _AS אסור בשום workflow אחר (_get_gc משותף ל-~20 מודולים).
> no-op כפול עד push+secret. נותר: (1) secret GOOGLE_CREDENTIALS_JSON_AS ב-GitHub.
> (2) push זהיר אחרי 15:00 (נתיב-מסחר חי) + מעקב ריצה ראשונה. (3) step-3 אימות-429 על
> ימי-מסחר — מתחבר ל-TASK-213 (דדליין 6/7). 215 נשאר To Do עד step-3 (לא False-Done).

**תלויות:** TASK-58 (התבנית) · TASK-214 (המקור, CLOSED) · TASK-213 (`דדליין 6/7`).
**קבצים שהתיק מציין:** `auto_scan.yml` · `_get_gc` · `QUOTA_AUDIT_auto_scan_v1.md`.
**לא בוצע (ציטוט):** `נותר: (1) secret GOOGLE_CREDENTIALS_JSON_AS ב-GitHub. (2) push זהיר... (3) step-3 אימות-429` · `215 נשאר To Do עד step-3 (לא False-Done)`.
**⚠️ תאריך בגוף:** `דדליין 6/7` (TASK-213) — **עבר**.

`[CODE]` (AC#3/AC#4, בוצעו) + `[RESEARCH]` (AC#5, `verify live — 429 frequency drops`). מה שנותר לפי ה-Notes הוא שני צעדים תפעוליים ואימות.
**האם התנאי עדיין קיים:** לא נבדק — לא קראתי את `_get_gc` (`sheets_manager.py:113`) ולא את `auto_scan.yml` ברמת ה-`env`. מה שכן ראיתי: `ROOT_FOLDER_ID = "1mHSdsTENVuMTtlv4XM54SrbadCEF_HHh"` — `sheets_manager.py:28`, ו-`SERVICE_ACCOUNT_EMAIL = "ridinghigh-sheets-v2@ridinghigh-pro-v2.iam.gserviceaccount.com"` — `sheets_manager.py:183` (ה-SA המשותף, לא ה-`_AS`).

---

### TASK-216 · Structural: mid-month agent-tab misses pre-provisioned next month
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-01 01:12

**מה התיק מבקש:**
> כשטאב-agent נוסף לקוד אמצע-חודש, החודש-הבא שכבר הוכן ב-1 לחודש לא מקבל אותו (אין backfill). קרה עם shadow_gate_events (24/6) + borrow_coverage (14/6) → 2026-07 חסר אותם עד תיקון ידני 30/6 (c19e246). יחזור בכל feature עתידי שמוסיף agent-tab אחרי ה-1. פתרון אפשרי: rotation יריץ create_agent_sheets --month <current> (idempotent) גם על החודש הפעיל.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** אין תיק מפורש (מוזכר commit `c19e246`).
**קבצים שהתיק מציין:** `create_agent_sheets` · `rotation`.
**לא בוצע:** `פתרון אפשרי` — מנוסח כאפשרות, לא כהחלטה.

`[CODE]` — הגוף מתאר תופעה מבנית ומציע `rotation יריץ create_agent_sheets --month <current>`. הניסוח `פתרון אפשרי` מותיר את הבחירה פתוחה, אבל אין AC של הכרעה.
**האם התנאי עדיין קיים:** ✅ כן. `monthly_rotation.main()` (`monthly_rotation.py:146`) קורא רק ל-`sheets_manager._ensure_month(next_key)` (`:178`) — שעובר על `sheets_manager.SHEET_NAMES` (9 גיליונות חודשיים בלבד, `:38-48`) ו**לא** על `create_agent_sheets.AGENT_SHEET_NAMES` (16 טאבי-סוכן, `agent/setup/create_agent_sheets.py:36-53`). אין קריאה ל-`create_agent_sheets` מתוך `monthly_rotation.py` (אימות: `grep "create_agent_sheets" monthly_rotation.py` → אפס). מה שכן קיים: `agent_eod.yml:36` מריץ `python -m agent.setup.create_agent_sheets` — על החודש הפעיל, לא על הבא.

---

### TASK-217 · Fix paper_portfolio column misalignment
**סטטוס:** **In Progress** · **ללא עדיפות** · **נוצר:** 2026-07-01 21:25 · **עודכן:** 2026-07-02 04:43

**מה התיק מבקש:**
> REFINED root cause (code is CORRECT): create_agent_sheets.py:87 header DOES define TPPrice/SLPrice, matching order_manager 25-elem row. The LIVE 2026-07 paper_portfolio tab was provisioned with a STALE 23-col header (no TPPrice/SLPrice); order_manager's 25-value append shifted values +2 from CurrentPrice and Sheets auto-extended 2 phantom cols. Result: Status value lands in ExitDate -> reads empty -> 8 rows orphaned from monitor_all Status filter (never TP/SL-closed). SCOPE: ONLY 2026-07 tab affected (2026-05/06 correct: TPPrice/SLPrice present, 0 phantom). HIGH severity: live paper-trading record corrupted. Fix = migrate 2026-07 header to canonical + repair 8 rows + defensive by-name entry-write + prevent recurrence (link TASK-216/91). Related TASK-105.

**Implementation Notes:**
> Core misalign FIXED end-to-end + live-verify (Task1-3 pushed: dd46470/7c6f079/0c441b7). Task4 guard funcs done (ad95806); WIRING = TASK-219. 2026-07 tab migrated (backup research/…231931.json), 8 rows MANUAL_CLEANUP.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-216 · TASK-91 · TASK-105 · **TASK-219** (`WIRING = TASK-219`).
**קבצים שהתיק מציין:** `create_agent_sheets.py:87` · `order_manager` · `monitor_all`.
**לא בוצע:** `WIRING = TASK-219` — החיווט עצמו הועבר לתיק אחר.

`[CODE]` — `Fix = migrate ... + repair ... + defensive by-name entry-write + prevent recurrence`.
**האם התנאי עדיין קיים:** ✅ ה-fix מאומת כמומש. `build_portfolio_row(values: dict, header: list)` — `agent/execution/order_manager.py:73`, נקרא ב-`:267` עם dict של שמות-עמודות מול `PORTFOLIO_COLUMNS` (`:284`), עם הערה מפורשת `"TASK-217: build BY NAME against the canonical header (not a positional list) so a header/field change can never silently misplace a value"` (`:265-266`). האם הטאב של 2026-07 באמת מיוגר — לא נבדק (Sheets).

---

### TASK-218 · agent_minute 429: sentinel worksheet-handle cache
**סטטוס:** To Do · **ללא עדיפות** · **נוצר:** 2026-07-02 04:42

**מה התיק מבקש:**
> After TASK-176 removed news_detective (46/91), agent.sentinel still calls get_worksheet('sentinel_events') per BLOCK/WARN event (~45/91 429 on the dedicated _AM SA). Cache the worksheet handle once/run. Connects to TASK-213 (429 measurement) + TASK-217 (paper_portfolio). MARKET-SAFE code.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-176 (קדם לו) · TASK-213 · TASK-217.
**קבצים שהתיק מציין:** `agent.sentinel` · `get_worksheet('sentinel_events')`.
**לא בוצע:** לא מצוין.

`[CODE]` — `Cache the worksheet handle once/run`.
**האם התנאי עדיין קיים:** ✅ כן, מאומת חי. `_log_sentinel_event` (`agent/sentinel/data_sentinel.py:30`) עושה בשורה `:60` `ws = sm.get_worksheet("sentinel_events")` ובשורה `:61` `sm.safe_append_row(ws, row)` — **בכל קריאה מחדש, אין קאש**. `sheets_manager.get_worksheet` (`:605`) עושה `gc.open_by_key(sheet_id)` בשורה `:624` — קריאת API מלאה. הפונקציה נקראת פעמיים בכל ריצה שיש בה BLOCK/WARN: `check_signal` (`:200-211`) ו-`check_system` (`:270-278`), וזה קורה גם ב-shadow (`action_taken="SHADOW_LOGGED"`).

---

### TASK-219 · TASK-217 Task4 wiring: provisioning fail-loud on header drift
**סטטוס:** To Do · **ללא עדיפות** · **נוצר:** 2026-07-02 04:42 · **עודכן:** 2026-07-03 02:06

**מה התיק מבקש:**
> Guard functions header_matches_canonical + assert_header_canonical are committed (ad95806, pure, 5/5). WIRING deferred: add to create_agent_sheets._already_done a per-tab check vs AGENT_SHEET_HEADERS. DECISION NEEDED: raise (halt rotation) vs warn+log; scope (paper_portfolio only vs all 16). PREREQ: audit all 16 agent tabs x3 months (05/06/07) to find other drifts before enabling raise (avoid halting 1/8 rotation). Follow-up of TASK-217.

**Implementation Notes:**
> Wiring implemented+committed (66c984c, PK v4.01): CORE_TABS raise (paper_portfolio/decision_log/postmortems), observability warn, _set_headers hardened. Guard sits in create_agent_sheets idempotency (not-dry-run). Live audit confirmed CORE 9/9 MATCH across 05-07 pre-commit. CI green. NOT Done: raise fires only at rotation 1/8 — pending live behavior verification (or controlled dry-run). Kept To Do.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-217 (`Follow-up of`).
**קבצים שהתיק מציין:** `create_agent_sheets._already_done` · `AGENT_SHEET_HEADERS` · `_set_headers`.
**לא בוצע (ציטוט):** `NOT Done: raise fires only at rotation 1/8 — pending live behavior verification (or controlled dry-run). Kept To Do.`
**⚠️ תאריך בגוף:** `rotation 1/8` — **עבר** (הרוטציה של 1/8 רצה; commit `33ea4dd` מ-2026-08-01 נושא `Auto: Add sheets_config for new month`).

`[DECISION]` (`DECISION NEEDED: raise vs warn+log; scope`) — ההכרעה כבר תועדה ב-Notes כ-`CORE_TABS raise`, ומה שנותר הוא `[RESEARCH]` (`pending live behavior verification`).
**האם התנאי עדיין קיים:** ✅ החיווט מאומת כמומש. `CORE_TABS = {"paper_portfolio", "decision_log", "postmortems"}` — `config.py:352`, מיובא ב-`agent/setup/create_agent_sheets.py:31`. שלוש נקודות-אכיפה: `:272` (`if name in CORE_TABS`), `:332` ו-`:336` (בבדיקת הכותרות החיות של טאבים שכבר הוקצו, `:319-336`). ההערה ב-`config.py:347-352` מסבירה: `"CORE tabs carry money/decision data — a header drift on them raises (halts provisioning)... All other agent tabs warn+log. score_analytics excluded on purpose"`.

---

### TASK-222 · dashboard.py:1928 _is_day_complete duplicates utils.is_day_complete
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-02 19:52 · **עודכן:** 2026-07-04 01:50 · **תוויות:** tech-debt, dst

**מה התיק מבקש:**
> utils.is_day_complete fixed in TASK-211 to derive close from 16:00 ET. dashboard._is_day_complete (:1928) is a real §10 duplicate — consolidate to utils. CORRECTION (E2E-audit S5, 3/7): the duplicate does NOT carry the pre-fix 15:00-Peru DST bug — its semantics are 'day < today (Peru) AND weekday<5', i.e. conservative-safe (today never counts as complete → lags up to a day after close, never uses mid-session data). Still wrong to keep two implementations. Also MARKET_CLOSE_HOUR_PERU defined twice (config.py:185 + utils.py:58) — consolidate.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-211 (המקור).
**קבצים שהתיק מציין:** `dashboard.py:1928` · `utils.is_day_complete` · `config.py:185` · `utils.py:58`.
**לא בוצע:** לא מצוין.

`[CODE]` — `consolidate to utils` ×2.
**האם התנאי עדיין קיים:** ✅ כן, שני החלקים מאומתים.
- `dashboard._is_day_complete` — הלוגיקה ב-`dashboard.py:1933`: `return day < today_peru and day.weekday() < 5`, עם דוקסטרינג בשורות `:1925-1928` שאומר `"date_str < today (Peru TZ), AND date_str is a weekday. Never returns True for today or any future date."` — בדיוק כפי שהתיקון בגוף התיק מתאר. הכפילות מול `utils.is_day_complete` (`utils.py:166`).
- `MARKET_CLOSE_HOUR_PERU = 15` — **פעמיים**: `config.py:185` ו-`utils.py:58` ✅.

---

### TASK-224 · qty guard: quantity<1 => SKIP with dedicated skip_reason
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-04 01:48

**מה התיק מבקש:**
> E2E-audit S2 (3/7): _calculate_position (decision_logic.py:139-151) does qty=int(AGENT_POSITION_SIZE_USD/price) — price>$1000 => qty=0, and NO qty>=1 guard exists anywhere in the execution path (order_manager/alpaca_broker/decision_logic — grep empty). An ENTER with quantity=0 would reach the broker. RULING (עמיחי 3/7): direction 'up to $1000' — qty<1 => SKIP with a dedicated skip_reason (NOT a 1-share fallback; preserves notional uniformity). LIVE ENTRY PATH — implement only on a separate explicit go. Not a HYP-002 frozen param (guard, not threshold). Evidence: plans/stateless-seeking-sifakis.md S2.

**Acceptance Criteria:**
> - [ ] #1 qty<1 returns SKIP with dedicated skip_reason (e.g. PRICE_ABOVE_NOTIONAL), TDD RED->GREEN
> - [ ] #2 no behavior change for any price where qty>=1 (regression suite green)

**תלויות:** HYP-002 (מוגדר במפורש כ**לא** פרמטר קפוא).
**קבצים שהתיק מציין:** `decision_logic.py:139-151` · `order_manager` · `alpaca_broker` · `plans/stateless-seeking-sifakis.md`.
**לא בוצע:** `LIVE ENTRY PATH — implement only on a separate explicit go`.

`[CODE]` — שני AC של מימוש + TDD. ההכרעה (RULING) כבר ניתנה ותועדה בגוף.
**האם התנאי עדיין קיים:** ✅ כן, מאומת חי. `_calculate_position(price)` — `agent/trader/decision_logic.py:143-154`; שורה `:145`: `qty = int(AGENT_POSITION_SIZE_USD / price)`. `AGENT_POSITION_SIZE_USD = 1000` (`config.py:320`) ⇒ price > 1000 נותן `qty = 0`. קראתי את `_check_filters` במלואו (`:354-467`) — **אין שם שום בדיקת `quantity`**. הפונקציה נקראת רק אחרי שכל הפילטרים עברו (`:331`), ואז `order_manager.execute` שולח את `decision.quantity` ל-`submit_bracket_order` (`agent/execution/order_manager.py:183`) בלי בדיקה.

---

### TASK-225 · Hold-window research: D1-D25 optimal holding analysis
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-04 01:48

**מה התיק מבקש:**
> E2E-audit S2 (3/7) established: there is NO live forced-exit — MAX_HOLDING_DAYS=5 is display-only (config.py:146), positions ride until TP/SL (AGENT_FORCE_EOD_CLOSE=False); 'HOLD<=5' in HYP-002 = classify/fitness window (HYPOTHESES.md:194), explaining OPEN=45 accumulation. RESEARCH: analyze D1-D25 forward data (post_analysis; CLASSIFY_DAYS=5 full-OHLC + D6-D25 Close+Low, COLLECT_DAYS_FORWARD=25) to determine the optimal holding window for the live agent. HARD GATE (עמיחי 3/7): decision/implementation BLOCKED until HYP-002 concludes (~2026-07-27 / n>=150 post-flip / 45 trading days, first-of) — any hold-behavior change before that = void to the hypothesis (re-registration). Research read-only may start earlier.

**Acceptance Criteria:**
> - [ ] #1 read-only analysis of D1-D25 outcomes (night/TP/SL/adverse-tail per horizon) documented with n per cell
> - [ ] #2 recommendation (keep TP/SL-only vs add forced-exit at day-K) presented ONLY after HYP-002 verdict

**תלויות (מפורש):** `HARD GATE ... BLOCKED until HYP-002 concludes`.
**קבצים שהתיק מציין:** `config.py:146` · `HYPOTHESES.md:194`.
**לא בוצע:** שני ה-AC. `Research read-only may start earlier` — כלומר AC#1 אינו חסום.
**⚠️ תאריך בגוף:** `~2026-07-27 / n>=150 post-flip / 45 trading days, first-of` — **HYP-002 נפסלה ב-2026-08-03 ונרשמה מחדש באותו יום** (`HYPOTHESES.md:178`, `:254-257`).

`[RESEARCH]` (AC#1) + `[DECISION]` (AC#2, `recommendation ... presented ONLY after HYP-002 verdict`).
**האם התנאי עדיין קיים:** ✅ כן, כל העובדות מאומתות. `MAX_HOLDING_DAYS = 5` — `config.py:133`, עם ההערה ב-`:146`: `"MAX_HOLDING_DAYS (above) — legacy DISPLAY-ONLY constant (print only; not in any logic)"`. `AGENT_FORCE_EOD_CLOSE = False` — `config.py:315` ⇒ `is_eod_window` מחזיר `False` תמיד (`agent/orchestrator.py:117-124`) ⇒ אין יציאה כפויה. `CLASSIFY_DAYS = 5` (`config.py:143`) · `COLLECT_DAYS_FORWARD = 25` (`config.py:156`).

---

### TASK-226 · Active alert on scanner-writes=0 during market hours
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-04 01:49

**מה התיק מבקש:**
> E2E-audit S3 (3/7): a scanner run that produces 0 rows during market hours is SILENT — auto_scanner.py:65 'No results' path prints and continues; the only detector is sentinel scan_freshness (WARN>=3m/BLOCK>=5m, checks/scan_freshness.py:8-9) which in shadow mode only logs; nothing alerts until nightly health_audit. Write-EXCEPTIONS are already loud (safe_append_rows raise). Needed: an active alert (system_events/email) when timeline_live gets 0 new rows for N consecutive market-hours minutes. Natural home: unify with TASK-166 (daily lineage sentinel). Evidence: plans/stateless-seeking-sifakis.md S3.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-166 (`Natural home: unify with`).
**קבצים שהתיק מציין:** `auto_scanner.py:65` · `checks/scan_freshness.py:8-9` · `safe_append_rows`.
**לא בוצע:** לא מצוין.

`[CODE]` — `Needed: an active alert (system_events/email)`.
**האם התנאי עדיין קיים:** ✅ כן, מאומת חי — עם מספרי-שורה מעודכנים.
- מסלול ה-"No results": `auto_scanner.py:418-427` — `if not results: print("❌ No results — still running portfolio_live / live_trades update")`, מריץ שני עדכונים ואז `return`. אין התראה. (התיק אומר `:65`.)
- `scan_freshness`: הספים היום `SENTINEL_SCAN_MAX_AGE_MINUTES = 5` (WARN) ו-`SENTINEL_SCAN_MAX_AGE_BLOCK_MINUTES = 10` (BLOCK) — `config.py:395-396`. ⚠️ **התיק אומר `WARN>=3m/BLOCK>=5m` — הספים שונו מאז** (ההערה ב-`config.py:395` מתעדת: `"was 3 — too tight vs ~1.6min scan cadence"`).
- shadow: `SENTINEL_MODE = "shadow"` (`config.py:359`) ⇒ BLOCK הופך ל-ALLOW (`agent/sentinel/data_sentinel.py:193-198`).
- `safe_append_rows` אכן מעלה חריגה במיצוי — `sheets_manager._with_retry:421` (`raise last_error`).

---

### TASK-229 · HYP-004-draft: late-entry (>=15:30 ET) forward research
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-04 04:44

**מה התיק מבקש (Description מלא):**
> תזת-עמיחי (3/7): כניסה >=15:30 ET עדיפה על כניסה-בטריגר-הראשון, תחת TP10/SL10 הקפואים. READ-ONLY research; אפס שינוי-קוד/gate.
> תמיכה-מקדימה (raw, timing-research 3/7, plans/stateless-seeking-sifakis.md): sim R1 מול R0 על 176 ticker-days (04-07): WR 57.1% [49-65] מול 46.9% [40-54], sumPnL −287 מול −702 (1%/side; שניהם שליליים — השוואה-יחסית בלבד); אנטומיית-שיא n=4,425: 78.2% דועכים >=3% מהשיא באותו-יום, שיא→−3% median 6 דק', אין חתימת-שיא מדדית (RSI בהופעה≈בשיא); SL-vindication 71.7% [62-80] n=99 (median 1 יום-מסחר); R2/R3 (אישור-מחיר) לא עזרו — ה-edge בזמן-מוחלט; מתלכד עם overnight-edge של מחקר-199.
> קריטריון-הצלחה מוצע: forward-only אחרי-רישום — bootstrap CI של Δ(net-PnL, R1−R0) > 0 ∧ Δ-WR מובהק ∧ עלות-ה-missed (19% מהטריגרים ב-R1) לא הופכת את סך-התוחלת.
> מה-יפריך: Δ-CI חוצה-0 על forward; missed מרוכזים בעסקאות-רווחיות; אי-שחזור מחוץ-לעונת-המדגם.
> הסתייגויות-חובה: survivorship (יקום-sim 176/4,425 pa-joined בלבד) · CIs חופפים · מילוי=מחיר-סריקת-דקה proxy · SL-first פסימי · 5-חוקים ללא תיקון-רב-השוואות.
> חסמים: הכרעה/רישום-פורמלי ל-HYPOTHESES.md רק בהחלטת-עמיחי נפרדת ואחרי סיום HYP-002 (~27/7 / n>=150 / 45 ימים); HYP-003 §G תפוס (4-dim) — לכן HYP-004.

**Acceptance Criteria:**
> - [ ] #1 מסמך-מחקר forward-only עם Δ-CI מבוקר + ניתוח-missed — ללא נגיעה בקוד
> - [ ] #2 המלצת register/reject ל-HYP-004 מוגשת לעמיחי רק אחרי verdict של HYP-002

**תלויות (מפורש):** `רק בהחלטת-עמיחי נפרדת ואחרי סיום HYP-002` · HYP-003 §G תפוס.
**קבצים שהתיק מציין:** `plans/stateless-seeking-sifakis.md` · `HYPOTHESES.md`.
**לא בוצע:** שני ה-AC. הגוף מסמן במפורש את התמיכה כ-`raw` ומונה `הסתייגויות-חובה`.
**⚠️ תאריך בגוף:** `~27/7 / n>=150 / 45 ימים` — עבר; HYP-002 נפסלה 3/8.

`[RESEARCH]` — `READ-ONLY research; אפס שינוי-קוד/gate`, AC#1 הוא מסמך-מחקר.
**האם התנאי עדיין קיים:** ✅ כן. `docs/HYPOTHESES.md` מכיל §D (HYP-001), §F (HYP-002), §G (HYP-003 DRAFT) — **אין §HYP-004**. הרשומה במרשם (`:87-91`) מונה שלוש היפותזות בלבד. `AGENT_TP_PCT = 10` / `AGENT_SL_PCT = 10` — `config.py:306-307`.

---

### TASK-230 · Data-gap audit + enrichment for peak-signature research
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-04 04:44 · **עודכן:** 2026-07-04 04:55

**מה התיק מבקש (Description מלא):**
> מטרה (timing-research 3/7): לאפשר חיפוש 'חתימת-שיא' שהדגימה-הנקודתית לא מראה — כולל ממד-הנרות (wicks+volume). המדדים הנוכחיים כמעט-זהים בהופעה ובשיא (RSI 64.6→66.1) וההפרדה כיום זמנית-בלבד.
> שלב-1 recon (READ-ONLY, אל תניח): סכמה-מלאה בפועל של timeline_live / decision_log / paper_portfolio פר-סריקה — מה נשמר, מה ריק, מה נגזר.
> שלב-2 candles-recon (READ-ONLY): לאמת מול providers/alpaca_provider החי אילו נרות-דקה (OHLCV) זמינים היסטורית לעולם-המניות שלנו — עומק-היסטוריה, limits, עלות-קריאות. ReboundPro/intraday_timeseries.py = תבנית-השראה בלבד — אפס ערבוב-קוד (§2).
> שלב-3 peak-signature research (READ-ONLY): משיכת נרות-דקה היסטוריים לימי-השיא הממופים (מדגם מייצג מתוך 4,425 ticker-days של timing-research 3/7) ובדיקה: האם פתילים-עליונים + דעיכת-נפח מקדימים את תחילת-הירידה? פלט: יש/אין חתימה + עוצמתה, raw+CI. רק אם מוכח — מעקב-חי = שלב עתידי ב-task נפרד (לא כאן).
> שלב-4 enrichment (רק אחרי אישור השלבים הקודמים): להוסיף את החסר — volume-per-scan + volume-fade (Δnpm) · trading-halts LULD (זמני-halt) · premarket high/volume · real-fill מול scan-price (Alpaca fills כבר קיימים ב-order records — לחבר) · intraday borrow/shortability snapshots.
> שלב-4א (מאושר-עמיחי 4/7; מימוש-בקוד = go נפרד): volume-per-scan מ-FINVIZ ל-timeline — נפח-מצטבר-מאוחד (consolidated) פר-סריקה; delta בין-סריקות עוקבות = נפח-פר-דקה. פותר את חצי-הנפח של חתימת-השיא, feed-independent, union-writer-safe.
> שלב-4ב (מאושר-עמיחי 4/7; מימוש-בקוד = go נפרד): איסוף-שוטף yfinance 1m — משיכה יומית (off-hours) של נרות-אתמול לכל טיקר-שנסרק, לארכיון-נרות מקומי (data/, gitignored). חלון-עומק yfinance-1m ≈ 30 יום ⇒ forward-only; פותר את חצי-הפתילים לשיאים חדשים.
> שלב-4ג — IBKR data-feed evaluation (recon בלבד, בקשת-עמיחי 4/7; מימוש/חיבור = go נפרד):
> · מנויי-market-data פעילים בחשבון-ה-IBKR של עמיחי — לאמת בחשבון בפועל, לא להניח.
> · בדיקת-כיסוי אמפירית: נרות-דקה היסטוריים מ-IBKR על אותו מדגם-מרובד של 168 ימי-השיא (אותו seed=42) — bars-per-day מול ה-28/390 של IEX, השוואה ישירה.
> · זמינות borrow/shortable + fee (סוגר את פער-#5 של שלב-4) וסטטוס-halts (פער-#2).
> · תפעול: IBKR API הוא session-based (דורש Gateway/TWS רץ, לא REST) — השלכות על אוטומציה/GHA; מגבלות-pacing של בקשות-היסטוריות בפועל.
> · Verdict: כיסוי-נרות טוב ⇒ IBKR מחליף את אופציית-SIP-בתשלום כמסלול-ה-backfill המרכזי; אחרת נשארים ב-4א+4ב.
> הערת-אופציה: SIP feed בתשלום (Alpaca) = מסלול-backfill חלופי — **לבחון רק אם IBKR-recon (4ג) נכשל**; החלטת-עלות של עמיחי, לבדוק תמחור עדכני באתר Alpaca לפני.
> אילוצים: כל תוספת-שדה דרך union-writer (append-only, אפס שבירת-סכמות קיימות, TASK-182 hardening) · אפס עומס-429 חדש בשעות-שוק (עדיף לצרף לכתיבות-קיימות) · Anti-Drift: עדכון PK+SCHEMA על כל שדה.

**Acceptance Criteria:**
> - [ ] #1 שלב-1: מסמך-recon של הסכמות בפועל (per-tab, per-column, %-מילוי) — קריאה-בלבד
> - [ ] #2 שלב-4 enrichment: כל שדה-חדש נכתב union-writer-safe + PK/SCHEMA מעודכנים + אפס תוספת-קריאות בשעות-שוק
> - [x] #3 שלבי-נרות (2-3): **verdict 4/7 = blocked-by-data** — feed=IEX-בלבד (paper-plan, alpaca_provider:365), median 28/390 נרות ליום למיקרו-קאפ (n=168 מדגם-מרובד, 33 שמישים-מזווגים); חתימת-פתילים/נפח **לא ניתנת-לבדיקה** על ה-feed הנוכחי. על השבר: אין wick-signature (36.4% [22-53]) ואין volume-fade — אנקדוטת **volume-climax הפוכה** (27.3% fade, n=33 small) = שאלה פתוחה לדאטת-forward. דו"ח מלא: candle-research 4/7 (צ'אט) + PK v4.04

**תלויות:** TASK-182 (union-writer hardening) · הכרעת-עלות של עמיחי (SIP) · `מימוש-בקוד = go נפרד` ×3.
**קבצים שהתיק מציין:** `providers/alpaca_provider` (`:365`) · `ReboundPro/intraday_timeseries.py` (השראה בלבד) · `data/` (gitignored).
**לא בוצע:** AC#1 ו-AC#2. AC#3 סגור עם verdict `blocked-by-data`.

`[RESEARCH]` (שלבים 1-3, AC#1) + `[CODE]` (שלב 4, AC#2) + `[DECISION]` (שלב 4ג/SIP — `החלטת-עלות של עמיחי`).
**האם התנאי עדיין קיים:** ✅ AC#3 מאומת חי. `feed=self._sdk["DataFeed"].IEX` — `providers/alpaca_provider.py:215`, עם הערה `"Issue #9 fix 2026-04-27 — paper plan only allows IEX"`, ואותו דבר ב-`get_latest_quote` (`:290`). התיק מצטט `:365` — היום השורות הן `:215` ו-`:290`. `data/` מוחרג ב-`.gitignore` ✅.

---

### TASK-240 · Audit data reliability 2026-07-05 to 2026-07-28
**סטטוס:** To Do · **ללא עדיפות** · **נוצר:** 2026-07-28 12:40 · **תוויות:** analysis

**מה התיק מבקש:**
> Assess coverage and trustworthiness of the unsupervised three-week period. Questions: daily coverage vs actual trading days; missing or zero rates per metric; whether MxV (the only active gate signal) was collected complete each day; how many rows carry a doubled-letter Ticker and their scan_date distribution (tests the two-regime hypothesis: clean before 22/7, suspect after); whether the cancelled collector runs left post_analysis gaps on 23/7, 24/7, 27/7; whether SCORE_WRITE_FROZEN left Score columns empty as intended. Quota-heavy: run outside market hours. Blocked on the ticker corruption scope.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות (מפורש):** `Blocked on the ticker corruption scope`.
**קבצים:** לא מצוין.
**לא בוצע:** כל הרשימה. `Quota-heavy: run outside market hours`.
**⚠️ חלון-זמן בגוף:** `2026-07-05 to 2026-07-28` (הכותרת) · `clean before 22/7, suspect after` · `gaps on 23/7, 24/7, 27/7`.

`[RESEARCH]` — `Audit` בכותרת, רשימת `Questions` בלבד, אין AC.
**האם התנאי עדיין קיים:** ✅ חלקית מאומת. `SCORE_WRITE_FROZEN = True` — `config.py:341`, ו-`score_write_value` מחזיר `""` כשמופעל (`auto_scanner.py:50-54`). `AGENT_MXV_MAX = -100` (`config.py:297`) הוא אכן הפילטר החי היחיד לאיכות-סיגנל (`decision_logic.py:376`, כאשר `ENTRY_GATE_MINIMAL=True` מכבה את 6 האחרים). ⚠️ **`score_write_value` לא מוחל על `timeline_live`** — שם `Score` נכתב מ-`results_df` הגולמי (`auto_scanner.py:445-448`).

---

### TASK-241 · Review agent and trade behaviour 2026-07-05 to 2026-07-28
**סטטוס:** To Do · **ללא עדיפות** · **נוצר:** 2026-07-28 12:40 · **תוויות:** analysis

**מה התיק מבקש:**
> Review what the system actually decided during the unsupervised period. Context verified live this session: AGENT_DRY_RUN=True, SENTINEL_MODE=shadow, EXPLICIT_GATE_MODE=active, ENTRY_GATE_MINIMAL=True, NEWS_DETECTIVE_ENABLED=False - so trades are paper and MxV was the only active gate. Questions: ENTER vs SKIP counts; exit reason distribution; whether TP10/SL10/HOLD5 behaved as expected; whipsaw count; whether any ENTER fired on a doubled-letter phantom symbol and how that distorts win-rate; whether n reached the HYP-002 validation threshold whose checkpoint fell on 27/7. Quota-heavy: run outside market hours.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** HYP-002 (`checkpoint fell on 27/7`).
**קבצים:** לא מצוין.
**לא בוצע:** כל הרשימה.
**⚠️ תאריכים בגוף:** `2026-07-05 to 2026-07-28` · `checkpoint fell on 27/7`.

`[RESEARCH]` — `Review what the system actually decided`, רשימת `Questions`, אין AC.
**האם התנאי עדיין קיים:** ✅ **כל חמשת הדגלים מאומתים חי היום, ללא שינוי:**
`AGENT_DRY_RUN = True` (`config.py:340`) · `SENTINEL_MODE = "shadow"` (`config.py:359`) · `EXPLICIT_GATE_MODE = "active"` (`config.py:374`) · `ENTRY_GATE_MINIMAL = True` (`config.py:378`) · `NEWS_DETECTIVE_ENABLED = False` (`config.py:364`).
⚠️ שאלת ה-`n` נענתה מאז יצירת התיק: `HYPOTHESES.md:237` — `"n post-flip ENTERs = 173, against a threshold of 150. The rule fired on n, not on the calendar."`

---

### TASK-243 · Harden root resolution and rotation guards
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-29 08:57 · **תוויות:** bug, infra

**מה התיק מבקש (Description מלא):**
> Follow-up to TASK-242. Interim mitigation already applied: 2026-09 was pre-created via OAuth in the canonical root, so the 1/8 rotation short-circuits on _already_done and never reaches the SA path. That buys one month; it does not fix the loop. Real fixes: (1) _get_root_folder_id resolves ROOT_FOLDER_ID through the shared SA, which has no permission on the canonical root, so the 404 is real and the RidingHigh-Data fallback fires on every SA-only run. Resolve via OAuth instead, or grant the shared SA on the canonical root (security decision, also relocates the backups folder since backup_manager calls the same function). (2) assert_correct_root(ROOT_FOLDER_ID) in prepare_next_month compares a constant to itself and catches nothing. (3) The duplicate-folder guard runs after prepare_next_month itself created the folder, so it fires on its own work and aborts the step that creates the agent tabs. (4) check_28 only inspects the active month, so a missing next month surfaces a month late. (5) Empty orphan folders 2026-07 and 2026-08 remain under RidingHigh-Data; review and remove manually, never automatically.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-242 (`Follow-up to`).
**קבצים שהתיק מציין:** `_get_root_folder_id` · `ROOT_FOLDER_ID` · `prepare_next_month` (`assert_correct_root`) · `backup_manager` · `check_28`.
**לא בוצע:** כל 5 הפריטים. `That buys one month; it does not fix the loop` · `review and remove manually, never automatically`.
**⚠️ תאריך בגוף:** `the 1/8 rotation short-circuits` — עבר.

`[CODE]` (פריטים 1-4) + `[DECISION]` (פריט 1: `security decision`) + `[DECISION]` ידני (פריט 5: `review and remove manually`).
**האם התנאי עדיין קיים:** ✅ חלקית מאומת. `ROOT_FOLDER_ID = "1mHSdsTENVuMTtlv4XM54SrbadCEF_HHh"` — `sheets_manager.py:28`. `_get_root_folder_id(drive_svc)` — `sheets_manager.py:246`. `check_28_agent_sheets_complete` — `health_audit.py:1952`, והדוקסטרינג שלו (`:1953`) אומר `"AS1: active (current Peru) month has all agent sheets in sheets_config"` — **החודש הפעיל בלבד**, בדיוק כפי שפריט 4 מתאר ✅. פריטים 1-3 (התוכן של `_get_root_folder_id` ו-`assert_correct_root`) — לא נבדקו לעומק.

---

### TASK-244 · Re-entry guard did not block repeat ENTERs on open positions
**סטטוס:** To Do · **עדיפות:** High · **נוצר:** 2026-07-29 09:27 · **תוויות:** bug, agent

**מה התיק מבקש (Description מלא):**
> Measured live 2026-07-29, read only, from decision_log 2026-07 and paper_portfolio 2026-07.
>
> OBSERVED: 137 ENTER decisions in July. On 2026-07-22 there were 43 ENTERs across only 4 distinct tickers: IINLF 11, LLABT 11, AADVB 11, ZZCMD 10. On 2026-07-16 there were 17 ENTERs of which VVEEE accounts for 9. Every one of those repeat entries has a matching row in paper_portfolio with Status DRY_RUN_OPEN and all four exit columns blank.
>
> WHY THIS MATTERS: HYP-002 locks reentry at most 1, and the entry path is supposed to check for an existing position before entering. It did not block here. Position sizing was roughly 1000 USD per entry, so 11 stacked entries on one symbol is roughly 11k of notional exposure on a single ticker that does not exist.
>
> HYPOTHESIS, NOT VERIFIED: the existing position check reads state that never advanced because the position was never closed, so each new scan looked like a fresh opportunity. decision_log carries an ExistingPosition column at index 35 which was not inspected in this measurement.
>
> SCOPE OF INVESTIGATION: read the ExistingPosition values for the 22/7 rows, find the code path that enforces reentry, and determine whether the guard is absent, misreading state, or correctly bypassed under DRY_RUN. This is a separate bug from the finviz ticker corruption, though it only became visible because of it.
>
> Do not fix without live verification. Quota heavy, run outside market hours.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** HYP-002 · נבדל במפורש מ-`the finviz ticker corruption`.
**קבצים שהתיק מציין:** `decision_log` (עמודה 35, `ExistingPosition`) · `paper_portfolio`.
**לא בוצע (ציטוט):** `HYPOTHESIS, NOT VERIFIED` · `decision_log carries an ExistingPosition column at index 35 which was not inspected in this measurement` · `Do not fix without live verification`.

`[RESEARCH]` — `SCOPE OF INVESTIGATION: read the ExistingPosition values ... find the code path ... and determine whether the guard is absent, misreading state, or correctly bypassed`. הגוף אוסר תיקון (`Do not fix without live verification`).
**האם התנאי עדיין קיים:** ⚠️ **מצב מעורב, מאומת בקוד.**
- מסלול-האכיפה קיים: Filter 7 `EXISTING_POSITION` (`agent/trader/decision_logic.py:433`), Filter 9 `REENTRY_LIMIT` (`:444`), `AGENT_MAX_REENTRIES_PER_TICKER = 1` (`config.py:331`).
- **מאז ה-29/7 נוסף שומר חדש:** Filter 6b `ACCOUNT_STATE_UNAVAILABLE` (`decision_logic.py:428-430`), עם הערה בשורות `:422-427` שמצטטת בדיוק את התופעה: *"MUST precede filters 7-10 — they all read the same failed fetch, so a blind state would otherwise pass them all at once and let repeat ENTERs stack on positions already held (measured live 2026-07-22, 43 ENTERs on 4 tickers under a Sheets 429)"*. השדה `account_state_unavailable` הוגדר ב-`:113-116` עם התייחסות מפורשת ל-TASK-244.
- הקומיט `f1cf01a` (2026-08-03) נושא `fix(agent): fail closed on unreadable account state, void HYP-002, two tier phantom`.
- ⚠️ **התיק עדיין To Do / HIGH.** החקירה שהגוף מגדיר (קריאת ערכי `ExistingPosition` של 22/7) **לא בוצעה** לפי מה שראיתי — היא דורשת Sheets.

---

### TASK-245 · Scan volume halved starting 2026-07-15
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-29 09:28 · **תוויות:** bug, data-integrity

**מה התיק מבקש (Description מלא):**
> Measured live 2026-07-29 from daily_snapshots 2026-07, rows per scan day.
>
> OBSERVED: through 2026-07-14 the daily row count runs 80, 46, 68, 34, 25, 59, 42, 36, 55. From 2026-07-15 onward it runs 23, 14, 12, 20, 38, 20, 18, 14, 22, 28. Median falls from roughly 46 to roughly 20. The break lands on exactly the same scan day as the first confirmed doubled letter tickers.
>
> WHY THIS MATTERS: fewer rows per scan means a smaller candidate universe reaching the gate, which changes how many ENTER opportunities exist per day and therefore any per day rate computed over July. 2026-07-17 is the thinnest day at 12 rows, under half the July median.
>
> TWO COMPETING EXPLANATIONS, NEITHER VERIFIED: (a) same root as the finviz parse change, where a corrupted row fails downstream and is dropped, so the loss is an artifact; (b) a genuine change in market breadth or in what the scanner filter admitted. These have opposite implications and the distinction must be measured, not assumed.
>
> SCOPE: compare row counts against the raw finviz result count per scan if it is recorded anywhere, and check whether any drop or exception path silently discards rows. Blocked in practice on the ticker corruption scope.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות (מפורש):** `Blocked in practice on the ticker corruption scope`.
**קבצים:** לא מצוין.
**לא בוצע (ציטוט):** `TWO COMPETING EXPLANATIONS, NEITHER VERIFIED` · `the distinction must be measured, not assumed`.
**⚠️ תאריכים בגוף:** `through 2026-07-14` · `From 2026-07-15 onward` · `2026-07-17 is the thinnest day`.

`[RESEARCH]` — `the distinction must be measured, not assumed` · `SCOPE: compare row counts ... and check whether any drop or exception path silently discards rows`.
**האם התנאי עדיין קיים:** לא נבדק — ספירת השורות דורשת Sheets. מה שכן ראיתי בקוד ורלוונטי לשאלה השנייה שהתיק שואל (`whether any drop or exception path silently discards rows`): `analyze_ticker` מחזירה `None` ב-6 נקודות שונות ללא הודעה (`auto_scanner.py:152`, `:155`, `:159`, `:178`, `:338`), ו-`run_scan` פשוט לא מוסיף אותן ל-`results` (`:379-380`). בנוסף `except: return None` עירום בשורה `:338`.

---

### TASK-246 · Clean up 83 stuck DRY_RUN_OPEN positions in 2026-07 paper_portfolio
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-07-29 09:28 · **תוויות:** data-integrity, cleanup

**מה התיק מבקש (Description מלא):**
> Measured live 2026-07-29, read only.
>
> STATE: paper_portfolio 2026-07 holds 137 rows. Status distribution is DRY_RUN_OPEN 83, DRY_RUN_CLOSED 46, MANUAL_CLEANUP 8. All 83 open rows have ExitPrice, ExitDate, ExitTime and ExitReason blank. Age in trading days runs 1 to 10 with a median of 5, and 29 of 83 already exceed MAX_HOLDING_DAYS of 5.
>
> EVERY ONE OF THE 83 IS ON A SUSPECT SYMBOL. 64 are on confirmed phantom tickers, 19 are on doubled letter candidates that were not independently confirmed, and zero are on a clean ticker. The reason they never closed is almost certainly that the symbol does not resolve at the data provider, so no current price is available, so no TP or SL can be evaluated and the monitor never exits the position.
>
> CONSEQUENCE FOR MEASUREMENT: any win rate or exit distribution computed over July 2026 mixes two different populations, the 46 that closed and predate the corruption, and everything opened after 2026-07-15 which is stuck. Performance for this month should not be computed before this is resolved.
>
> BLOCKED ON: the finviz ticker fix, so that the cleanup rule can distinguish a phantom from a real symbol. Any correction is a write to Sheets and needs explicit approval and a dated backup first. Never delete rows, mark them.
>
> OPEN QUESTION: whether these rows should be marked with a distinct status rather than reusing MANUAL_CLEANUP, so that research queries can exclude them by rule.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות (מפורש):** `BLOCKED ON: the finviz ticker fix`.
**קבצים:** לא מצוין.
**לא בוצע:** `Any correction is a write to Sheets and needs explicit approval and a dated backup first`.
**⚠️ תאריך בגוף:** `everything opened after 2026-07-15 which is stuck`.

`[DECISION]` — הגוף מסתיים ב-`OPEN QUESTION: whether these rows should be marked with a distinct status rather than reusing MANUAL_CLEANUP`, ומתנה כל פעולה ב-`explicit approval`.
**האם התנאי עדיין קיים:** ✅ המנגנון שהתיק מתאר מאומת בקוד. `position_manager._process_position` (`agent/execution/position_manager.py:204`) קורא ל-`_get_current_price` (`:226`), ואם הוא מחזיר `None` — `return "updated"` בשורה `:233` עם הערה `"graceful skip"`. הבדיקות של TP/SL (`:244`, `:248`) יושבות **אחרי** אותה נקודה, ולכן לא מגיעות. `_get_current_price` מחזיר `None` כשגם `get_latest_bar` וגם `get_latest_quote` נכשלו (`:298-302`). `MAX_HOLDING_DAYS = 5` (`config.py:133`) הוא display-only ולא סוגר. ספירת ה-83 עצמה — לא נבדקה (Sheets).

---

### TASK-248 · requirements.txt pins finvizfinance 0.14.6
**סטטוס:** To Do · **עדיפות:** High · **נוצר:** 2026-07-29 10:11 · **תוויות:** bug, ci

**מה התיק מבקש (Description מלא):**
> Verified live 2026-07-29. requirements.txt line 4 pins finvizfinance==0.14.6. That version looks for a table with class table-light, and the live finviz screener page no longer contains it, only screener_table and styled-table-new. A real fetch under 0.14.6 raises AttributeError NoneType has no attribute findAll. The pin is not merely stale, it is poisonous.
>
> The scanner survives only because all three workflows install finvizfinance unpinned and get 1.3.0, so they ignore the pin entirely. Anyone running pip install -r requirements.txt gets a dead scanner, and the local development environment does not reflect production for this dependency.
>
> DECISION NEEDED: pin to the version CI actually resolves, or drop the pin and let it float. Pinning is safer for reproducibility but must be to a version that parses the current page. Note that the TASK-238 fix subclasses _get_table, so any version change must be checked against that hook.
>
> Do not change the pin without a live fetch proving the chosen version parses. Also decide whether the workflows should install via -r requirements.txt instead of listing packages inline, which is the reason the pin was silently bypassed.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-238 (`the TASK-238 fix subclasses _get_table`).
**קבצים שהתיק מציין:** `requirements.txt` שורה 4 · `_get_table`.
**לא בוצע:** `Do not change the pin without a live fetch proving the chosen version parses`.

`[DECISION]` — `DECISION NEEDED: pin to the version CI actually resolves, or drop the pin and let it float` + `Also decide whether the workflows should install via -r requirements.txt`.
**האם התנאי עדיין קיים:** ✅ כן, שני החלקים מאומתים.
- `requirements.txt` שורה 4: `finvizfinance==0.14.6` ✅ (בדיוק כפי שהתיק אומר).
- שלושת ה-workflows מתקינים ללא pin: `auto_scan.yml:24` (`pip install finvizfinance yfinance pandas ...`), `post_analysis.yml:30`, `backfill_ohlc.yml:29`.
- ה-hook של TASK-238: `SanitizedOverview._get_table(self, rows, df, num_col_index, table_header, limit=-1)` — `utils.py:884`, עם דוקסטרינג (`:879-881`) שאומר `"_get_table is the single funnel both 0.14.6 and 1.3.0 route every page through, with an identical signature, so one override covers the pinned version and whatever CI resolves to"`.

---

### TASK-249 · Collector reprocesses the whole month every night
**סטטוס:** To Do · **עדיפות:** High · **נוצר:** 2026-08-03 16:52 · **תוויות:** bug, perf

**מה התיק מבקש (Description מלא):**
> Measured 2026-08-03 from four run logs. This is the cause behind TASK-239, which two ceiling raises only masked.
>
> Evidence, four nights: 15/7 had 43 candidates and ran 8 minutes. 21/7 had 56 and ran 11. 28/7 had 77 and ran 23. 31/7 had 89 and ran 27. Every one of those runs reported Processing equal to the candidate count and CompleteSkip zero. Nothing is ever skipped, so the work is the running total of the month, not the new rows of the day.
>
> Two mechanisms combine. select_candidates at post_analysis_collector.py:78 filters daily_snapshots on MxV alone with no date bound, so it returns every candidate the month has ever produced. And is_complete cannot return true inside the same month, because COLLECT_DAYS_FORWARD is 25 from the cutoff of 2026-06-13 while a month holds about 21 trading days, so no row reaches its full horizon before the month ends.
>
> Consequence: runtime grows with the month and resets on the first. The 3/8 run is the first on an empty August sheet and is the free measurement. If it drops back to 8 to 11 minutes, the growth is confirmed as month size rather than candidate count.
>
> Not verified: the real per symbol cost on the alpaca path, and whether fetch_ohlc_for_days at :216 with five retries and two second sleeps dominates or whether the sheet reads at :415 and :469 do.
>
> Options to weigh, none chosen: bound select_candidates by date, make is_complete horizon aware so a row settles at D5 for classification purposes, or cache the settled rows. Any change alters what the research dataset collects and needs TDD plus a before and after run comparison.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-239 (`This is the cause behind TASK-239`).
**קבצים שהתיק מציין:** `post_analysis_collector.py:78` (`select_candidates`) · `:216` (`fetch_ohlc_for_days`) · `:415` · `:469` · `is_complete` · `COLLECT_DAYS_FORWARD`.
**לא בוצע (ציטוט):** `Not verified: the real per symbol cost on the alpaca path` · `Options to weigh, none chosen`.
**⚠️ תאריכים בגוף:** `15/7 · 21/7 · 28/7 · 31/7` (הראיות) · `The 3/8 run is the first on an empty August sheet and is the free measurement`.

`[RESEARCH]` + `[DECISION]` — `Not verified` על המנגנון המדויק, ו-`Options to weigh, none chosen` על מה לעשות. אין AC.
**האם התנאי עדיין קיים:** ✅ כן, שני המנגנונים מאומתים בקוד.
- `select_candidates(snapshots_df)` — `post_analysis_collector.py:78` ✅ (בדיוק השורה שהתיק אומר). לפי `commit 96de9f0` (TASK-200) הוא בוחר לפי `MxV<=-100`.
- `COLLECT_DAYS_FORWARD = 25` — `config.py:156`; `COLLECT_DAYS_FORWARD_FROM = "2026-06-13"` — `config.py:161` ✅.
- `is_complete(existing_row, trading_days)` — `post_analysis_collector.py:183`.
- `fetch_ohlc_for_days` — `post_analysis_collector.py:205`, עם `for attempt in range(1, 6)` (`:216`) ו-`time.sleep(2)` בשני מקומות (`:224`, `:247`) ✅ — 5 ניסיונות × 2 שניות = עד 10 שניות לטיקר במקרה הגרוע.
- `post_analysis.yml:19` — `timeout-minutes: 45` (ההעלאות שהתיק מכנה `two ceiling raises`: commits `827bc66` 15→30 ו-`5f75af2`).

---

### TASK-250 · Unit tests read live Sheets, seven of them
**סטטוס:** To Do · **עדיפות:** High · **נוצר:** 2026-08-03 21:01 · **תוויות:** bug, tests, quota

**מה התיק מבקש (Description מלא):**
> Found 2026-08-03 after market close. tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py went red on the local suite after being green all day, without any edit to it.
>
> The file docstring says: All-mocked (MagicMock + patch), zero real API/Sheets. That is false. The tests patch agent.orchestrator.build_account_state, but orchestrator_eod.collect_borrow_snapshot has a second ticker source nobody patches: line 57 calls sheets_manager.get_worksheet(daily_snapshots) and line 59 calls get_all_values(). The helper returns the union of both sources.
>
> Why it turned red today and not before: the active month rotated to August on the first, and the August daily_snapshots tab was empty until today. From the moment today's rows were written, real tickers entered the assertion. Observed failure: assert ['AAA','BBB',...,'HYFM',...] == ['AAA','BBB','CCC'], first extra item DFNS. DFNS was in the live scan of 2026-08-03.
>
> Three consequences. First, seven tests in that file call collect_borrow_snapshot, so every local pytest run performs seven live Sheets reads. That is a direct contribution to TASK-55, from a file that claims to touch nothing. Second, CI is green only because tests.yml has no credentials, so the read raises and scanned falls back to an empty set. That is a false green: the mock contract is never actually exercised in CI. Third, the suite is now red locally for anyone with working OAuth, which hides real regressions.
>
> Fix direction, not chosen yet: patch sheets_manager.get_worksheet in the test file so the scanned branch is deterministic, and correct the docstring. Consider whether a repo wide guard is warranted, for example a conftest fixture that fails any test under tests/agent/unit that opens a real client.
>
> Not verified: whether other unit tests outside this file also reach live Sheets. That sweep has not been run.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-55 (`a direct contribution to TASK-55`).
**קבצים שהתיק מציין:** `tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py` · `orchestrator_eod` שורות 57 ו-59 · `tests.yml` · `conftest`.
**לא בוצע (ציטוט):** `Fix direction, not chosen yet` · `Not verified: whether other unit tests outside this file also reach live Sheets. That sweep has not been run.`

`[CODE]` — `patch sheets_manager.get_worksheet in the test file ... and correct the docstring`. יש בו גם רכיב `[DECISION]` (`Consider whether a repo wide guard is warranted`).
**האם התנאי עדיין קיים:** ✅ כן, המנגנון מאומת בקוד — עם מספרי-השורה בדיוק כפי שהתיק אומר.
`agent/orchestrator_eod.py:57` — `ws = sheets_manager.get_worksheet("daily_snapshots")` ✅
`agent/orchestrator_eod.py:59` — `vals = ws.get_all_values()` ✅
האיחוד: `universe = scanned | existing` — `:70` ✅ (בדיוק ה-"union of both sources" שהתיק מתאר).
`tests/conftest.py:13-16` מסמן אוטומטית **רק** את `tests/agent/integration/` כ-`integration`; `tests/agent/unit/` **אינו** מסומן, ולכן CI מריץ אותו. `tests.yml:9-10` מתעד: `"Integration tests (live Google Sheets, marked integration by path in tests/conftest.py) are skipped via -m 'not integration' — the runner has no Sheets credentials"`.
⚠️ **הסריקה שהתיק מצהיר שלא בוצעה — גם אני לא הרצתי אותה** (דורש pytest).

---

### TASK-251 · August live_trades points at its own file
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-08-03 21:01 · **תוויות:** data-integrity, sheets

**מה התיק מבקש (Description מלא):**
> Verified 2026-08-03 from sheets_config.json on origin/main and from the code. No task owned this before.
>
> State: for 2026-04, 05, 06, 07 and 09 the live_trades id equals the score_tracker id. For 2026-08 it does not. August points at 1b0Vb..., score_tracker at 1_NC6....
>
> Root cause is a section 10 violation, two lists of the same thing that disagree. sheets_manager.SHEET_NAMES has nine entries and includes live_trades as a file of its own. prepare_next_month.SHEET_NAMES has eight, omits it, carries the comment that live_trades will be a tab inside score_tracker, and line 240 does created_ids['live_trades'] = created_ids['score_tracker']. So the automatic convention is an alias. scripts/fix_august_provisioning_v1.py line 305 iterates sheets_manager.SHEET_NAMES, so the manual repair of 29/7 created a standalone August live_trades file.
>
> Why the repair ran at all: prepare_next_month failed on 1/7 with RuntimeError DUPLICATE MONTH GUARD (TASK-143), two folders named 2026-08 across roots, so it exited before the alias step and August was left half provisioned. See TASK-242 and TASK-243.
>
> Not broken today. The auto_scan log of 2026-08-03 shows live_trades updated: 0 rows, 0 pending with no error, because sheets_manager.get_worksheet falls back to sheet1 when the named tab is absent (line 627). But August is the only month of six with a different physical layout, which is the kind of difference that breaks a cross month research script silently. archive_live_trades also creates live_trades_archive inside whichever file live_trades points at, so August archives would land somewhere else than every other month.
>
> Not verified: whether a tab named live_trades exists inside 1b0Vb, or whether writes are landing on sheet1 through the fallback.
>
> Options, none chosen: leave it and document, or realign August to score_tracker. Realigning is a write to a live month sheet for consistency alone, which is the more dangerous of the two. Either way the two SHEET_NAMES lists must be unified and fix_august_provisioning_v1 needs a guard that checks the existing convention before creating anything. That part belongs with TASK-243.
>
> Related orphan, do not delete: the pre-created September live_trades 1l6j9c... was overwritten by the 1/8 automatic run and is now unreferenced.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-143 · TASK-242 · **TASK-243** (`That part belongs with TASK-243`).
**קבצים שהתיק מציין:** `sheets_manager.SHEET_NAMES` · `prepare_next_month.SHEET_NAMES` (שורה 240) · `scripts/fix_august_provisioning_v1.py` שורה 305 · `sheets_manager.get_worksheet` שורה 627 · `archive_live_trades` · `sheets_config.json`.
**לא בוצע (ציטוט):** `Not verified: whether a tab named live_trades exists inside 1b0Vb, or whether writes are landing on sheet1 through the fallback` · `Options, none chosen`.

`[DECISION]` — `Options, none chosen: leave it and document, or realign August to score_tracker`.
**האם התנאי עדיין קיים:** ✅ כן, שורש-הכפילות מאומת מילה-במילה.
- `sheets_manager.SHEET_NAMES` — `sheets_manager.py:38-48`, **9 ערכים** כולל `"live_trades"` (`:46`) ✅
- `prepare_next_month.SHEET_NAMES` — `prepare_next_month.py:22-30`, **8 ערכים**, עם ההערה בשורה `:29`: `"score_tracker",  # live_trades יהיה כ-tab נוסף בתוכו` ✅
- `prepare_next_month.py:240` — `created_ids['live_trades'] = created_ids['score_tracker']` ✅ (בדיוק השורה שהתיק אומר)
- הנפילה-לאחור: `sheets_manager.get_worksheet:626-628` — `try: return spreadsheet.worksheet(tab_name) / except WorksheetNotFound: return spreadsheet.sheet1` ✅ (התיק אומר שורה 627)
- `archive_live_trades` — `sheets_manager.py:690`, פותר את הקובץ דרך `get_sheet_id("live_trades")` (`:707`) ויוצר בו `live_trades_archive` (`:712-717`) ✅

---

### TASK-252 · Three missing health checks
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-08-03 21:02 · **תוויות:** health-audit, monitoring

**מה התיק מבקש (Description מלא):**
> Mapped 2026-08-03. None of the three exists. All three would have caught a real failure earlier than it was caught.
>
> Injection point is uniform: health_audit.py around line 2005, one results.append(check_NN_...(gc)) per check, same shape as check_29_interday_artifacts at line 1252. That check already calls load_post_analysis_all_months(), and _HA_SHEET_CACHE at line 1088 memoises reads, so a new check that runs after it costs close to zero extra quota. Verify the memoisation actually covers the loader before relying on that.
>
> Check one, symbol sanity. Run formulas.is_confirmed_phantom over the tickers written today. WARN at one or more, FAIL at five percent of rows or more. Would have fired on 2026-07-15, the first day of the finviz corruption. It was found on 29/7, fourteen days later, and by then 71 rows and 83 positions were affected.
>
> Check two, position older than the hold window. Count DRY_RUN_OPEN rows whose EntryDate is older than MAX_HOLDING_DAYS. WARN at three, FAIL at ten. On 29/7 twenty nine of eighty three had already exceeded it, so this would have fired around 22/7. Measured again on 3/8: eighty four open, median calendar age twelve days, oldest nineteen.
>
> Check three, rows written rather than a date being present. Compare today's row count against the trailing five day average. WARN on a deviation over fifty percent. This is the one that would have caught the scan volume drop of TASK-245 on the day it happened.
>
> Why thresholds and not presence. check_22 today answers Post-analysis ran today with PASSED even when the sheet is empty; the run of 2026-08-03 13:35Z printed exactly that, Before scheduled time, not expected to run yet, on an empty August tab. A date being present is not evidence that data arrived.
>
> Each check needs TDD with a fixture, and each needs a stated threshold that produces a real alert rather than noise. Do not add all three in one commit.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-245 (Check three הייתה תופסת אותו).
**קבצים שהתיק מציין:** `health_audit.py` שורה ~2005 · `check_29_interday_artifacts` שורה 1252 · `_HA_SHEET_CACHE` שורה 1088 · `load_post_analysis_all_months()` · `formulas.is_confirmed_phantom` · `check_22`.
**לא בוצע:** `None of the three exists`. + `Verify the memoisation actually covers the loader before relying on that` — לא אומת.
**⚠️ תאריכים בגוף:** `Would have fired on 2026-07-15` · `found on 29/7, fourteen days later` · `would have fired around 22/7` · `Measured again on 3/8`.

`[CODE]` — שלוש בדיקות חדשות, כל אחת עם TDD ו-fixture.
**האם התנאי עדיין קיים:** ✅ כן, מאומת חי.
- נקודת-ההזרקה: `health_audit.main()` מריץ 30 בדיקות ב-`:2006-2047`, כל אחת `results.append(check_NN_...)` ✅. `check_29_interday_artifacts` — `health_audit.py:1252` ✅ (בדיוק השורה שהתיק אומר).
- הקאש: `_HA_CACHE_TTL_SEC = 600` — `health_audit.py:1089`, ו-`_ha_cached_read(gc, sheet_id, op, *args, ttl=..., retries=5)` — `:1092`. ⚠️ **התיק אומר `_HA_SHEET_CACHE at line 1088`** — השם בקוד הוא `_HA_CACHE_TTL_SEC` והשורה `:1089`.
- **אף אחת מ-3 הבדיקות לא קיימת:** `grep -c "is_confirmed_phantom\|classify_phantom_tier" health_audit.py` → **0** ✅. אין בדיקה שסופרת `DRY_RUN_OPEN` לפי גיל. `check_04_timeline_freshness` (`:444`) בודק **גיל** השורה האחרונה, לא **מספר** שורות.
- `formulas.is_confirmed_phantom` קיים — `formulas.py:395` ✅.
- `check_22_post_analysis_today_ran` — `health_audit.py:662` ✅.

---

### TASK-253 · timeline_live duplicate rows from overlapping scanner runs
**סטטוס:** To Do · **עדיפות:** Medium · **נוצר:** 2026-08-03 22:08 · **תוויות:** bug, data-integrity

**מה התיק מבקש (Description מלא):**
> Measured 2026-08-03 from the August timeline_live tab, read only.
>
> STATE: 308 exact (ScanTime, Ticker) duplicate pairs in one trading day, out of 21892 rows, so 1.4 percent. They are not spread evenly. Five minutes carry almost all of them, and those same minutes show row counts far above the median of 61: 10:20 with 92, 10:42 with 100, 12:29 with 125, 12:40 with 138, 14:08 with 152. By hour the duplicates fall at 10 with 96, 12 with 132, 14 with 78, and one each at 9 and 11.
>
> MECHANISM, from the code: auto_scanner.py calls safe_append_rows with dedup_col=1 and dedup_vals set to this scan ScanTime. That check asks whether the ScanTime already exists BEFORE writing. GitHub Actions runs overlap, which was proven on 2026-07-31 where the run started at 17:27 was still executing when the 17:28 run began. Two overlapping runs can both pass the existence check before either has written, and both then append. A plain race.
>
> ALTERNATIVE RULED OUT: 89 of the 388 ScanTime values come back from Sheets without a padded hour, 8:31 rather than 08:31, which would break a string comparison. It is not the cause. Those unpadded stamps are hours 8 and 9 and they carry FEWER rows, median 30 against 66 for the padded hours, and every duplicate sits in the padded hours 10 to 14.
>
> WHY IT MATTERS: timeline_live is the source for daily_summary, ticker_follow_up and the collector. A duplicated minute inflates any per minute aggregate and any volume measure computed over it, silently.
>
> NOT VERIFIED: whether post_analysis and daily_snapshots carry the same duplication. Not measured. Also not measured is whether the rate is stable day to day or specific to 2026-08-03.
>
> DO NOT FIX BLIND. The dedup sits on the hot write path that runs every minute of every trading day. Build a detector first, a health check that counts duplicate (ScanTime, Ticker) pairs, get a baseline over several days, and only then touch the write. Any change needs TDD and a before and after comparison on a full trading day.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** אין תיק מפורש.
**קבצים שהתיק מציין:** `auto_scanner.py` (`safe_append_rows` עם `dedup_col=1`).
**לא בוצע (ציטוט):** `NOT VERIFIED: whether post_analysis and daily_snapshots carry the same duplication. Not measured.` · `Also not measured is whether the rate is stable day to day`.
**⚠️ תאריכים בגוף:** `Measured 2026-08-03` · `proven on 2026-07-31 where the run started at 17:27 was still executing when the 17:28 run began`.

`[RESEARCH]` — `Build a detector first ... get a baseline over several days, and only then touch the write`. הגוף אוסר תיקון מיידי (`DO NOT FIX BLIND`).
**האם התנאי עדיין קיים:** ✅ כן, המנגנון מאומת מילה-במילה.
`auto_scanner.py:473-478`:
```python
sheets_manager.safe_append_rows(
    ws_timeline,
    rows_to_write,
    dedup_col=1,  # ScanTime column
    dedup_vals={scan_time_str},
)
```
וההערה מעליו (`:468-470`): `"safe_append_rows with retry + dedup by ScanTime (col 1). ScanTime is identical for all rows in this scan, so if any row with this ScanTime already exists, the whole batch was written."` — הנחה שנשברת בדיוק בתרחיש-המרוץ שהתיק מתאר.
המימוש: `sheets_manager.safe_append_rows` (`:556`), שקורא `ws.col_values(dedup_col + 1)` (`:592`) **לפני** ה-append.
שלושת הצרכנים שהתיק מונה מאומתים: `_save_daily_summary` (`auto_scanner.py:571`, קורא `_read_timeline_live` `:574`), `update_ticker_follow_up` (`:797`), והקולקטור (`post_analysis_collector.fetch_timeline_stats:333`).

---

### TASK-254 · score_analytics reports n=0 while July closed sixty positions
**סטטוס:** To Do · **עדיפות:** Low · **נוצר:** 2026-08-03 22:08 · **תוויות:** investigation, analytics

**מה התיק מבקש (Description מלא):**
> Noticed 2026-08-03 while auditing which tabs get written.
>
> OBSERVED: the score_analytics tab is empty for both 2026-07 and 2026-08. That is not a missing writer. agent/orchestrator_eod.py lines 170 to 182 runs ScoreAnalytics().run_daily() every evening, and the EOD log of 2026-07-30 printed: Daily analytics: tier=INSUFFICIENT, n=0.
>
> score_analytics.py writes nothing when the tier is INSUFFICIENT, which is defined as fewer than ten trades. So the tab being empty is the documented behaviour of the code.
>
> THE PART THAT IS NOT EXPLAINED: n was zero, not merely below ten. July paper_portfolio held sixty DRY_RUN_CLOSED positions at that point. A sample size of zero against sixty closed trades means the module is either reading a different source, filtering on something that excludes them all, or scoped to a window that contained none of them.
>
> NOT VERIFIED: where run_daily reads its sample from. The source was not traced.
>
> WHY IT IS LOW: nothing downstream consumes score_analytics. config.py line 351 states it is excluded on purpose, frozen and diagnostic, not decision bearing. This is worth understanding rather than worth fixing, and it may turn out that the tab should be retired along with the rest of the Score demotion work in TASK-208 and TASK-209.
>
> Related: pending_suggestions and config_history are also empty in both months and genuinely have no writer anywhere in the code. Both were added in the same commit, 1092a95 feat(phase1-m2) agent sheets setup, as scaffolding that was never wired. dashboard.py line 4307 already documents config_history as having no writer. They are noted here rather than filed separately.

**Acceptance Criteria:** `No acceptance criteria defined`
**תלויות:** TASK-208 · TASK-209 (`may turn out that the tab should be retired along with`).
**קבצים שהתיק מציין:** `agent/orchestrator_eod.py` שורות 170-182 · `score_analytics.py` · `config.py` שורה 351 · `dashboard.py` שורה 4307.
**לא בוצע (ציטוט):** `NOT VERIFIED: where run_daily reads its sample from. The source was not traced.`

`[RESEARCH]` — `This is worth understanding rather than worth fixing`. אין AC.
**האם התנאי עדיין קיים:** ✅ כן — **והשאלה ש-התיק מסמן כ-`NOT VERIFIED` ניתנת למענה מהקוד בלבד:**
- `agent/orchestrator_eod.py:173` — `analytics = ScoreAnalytics()` — **אפס ארגומנטים** ✅ (התיק אומר שורות 170-182).
- `ScoreAnalytics.__init__(self, postmortem_reader=None, analytics_writer=None, suggestion_writer=None)` — `agent/analytics/score_analytics.py:61-65`; `self._postmortem_reader = postmortem_reader` (`:73`).
- `_load_postmortems(self, start_date, end_date)` — `:129`; שורות `:131-132`: `if not self._postmortem_reader: return pd.DataFrame()`.
- `run_daily` קורא לו ב-`:91`, ואז `n = len(postmortems)` (`:178`).
כלומר **ה-reader לעולם לא מוזרק ⇒ ה-DataFrame תמיד ריק ⇒ n=0 מבני, לא תלוי-דאטה.** אותו דבר ב-`run_weekly` (`orchestrator_eod.py:191`, `score_analytics.py:116`).
- `config.py:351-352` — `"score_analytics excluded on purpose — frozen/diagnostic, not decision-bearing."` + `CORE_TABS = {...}` ✅ (התיק אומר שורה 351).
- `pending_suggestions` — הכותב היחיד שראיתי הוא `agent/dashboard/_data_loaders.update_suggestion_status:179` (מעדכן סטטוס של שורה קיימת, לא יוצר). `config_history` — לא מצאתי כותב.

---

## 4. אינדקסים

### 4א. קובץ → התיקים שנוגעים בו

נכללים רק קבצים שהתיק **מציין במפורש** בגוף או ב-Notes. ממוין לפי מספר התיקים יורד.

| קובץ | # | התיקים |
|---|---|---|
| `auto_scanner.py` | 6 | 194, 208, 226, 245*, 249*, 253 |
| `config.py` | 5 | 109, 222, 225, 249*, 254 |
| `decision_logic.py` | 5 | 65, 128, 194, 209, 224 |
| `post_analysis_collector.py` | 4 | 202, 209, 249, 253* |
| `health_audit.py` | 3 | 209, 243, 252 |
| `dashboard.py` | 3 | 92, 209, 254 |
| `order_manager.py` | 3 | 49, 65, 217 |
| `gsheets_sync.py` | 2 | 202, 209* |
| `sheets_manager.py` | 2 | 215, 251 |
| `prepare_next_month.py` | 2 | 243, 251 |
| `create_agent_sheets.py` | 2 | 216, 217, 219 → **3** |
| `orchestrator.py` | 2 | 65, 92 |
| `orchestrator_eod.py` | 2 | 250, 254 |
| `reconciler.py` | 2 | 49*, 65 |
| `borrow_collector.py` | 2 | 208, 230* |
| `formulas.py` | 2 | 209, 252 |
| `utils.py` | 2 | 222, 248* |
| `alpaca_provider.py` | 1 | 230 |
| `critic_v1.py` | 1 | 73 |
| `monthly_rotation.py` | 1 | 216* |
| `position_manager.py` | 1 | 217 (`monitor_all`) |
| `alpaca_broker.py` | 1 | 224 |
| `score_analytics.py` | 1 | 254 |
| `data_sentinel` / `agent.sentinel` | 1 | 218 |
| `scan_freshness.py` | 1 | 226 |
| `monthly_brief` / templates | 1 | 88 (`build_monthly_row`) |
| `requirements.txt` | 1 | 248 |
| `scripts/measure_429_by_workflow_v1.py` | 1 | 176 |
| `scripts/fix_august_provisioning_v1.py` | 1 | 251 |
| `backup_manager.py` | 1 | 243 |
| `generate_sheets_schema.py` | 1 | 209 |
| `tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py` | 1 | 250 |
| `tests.yml` / `conftest.py` | 1 | 250 |
| `auto_scan.yml` | 1 | 215 |
| `sheets_config.json` | 1 | 251 |
| `research/extract_historical_skips.py` | 1 | 126 |
| `HYPOTHESES.md` | 3 | 194, 225, 229 |
| `plans/stateless-seeking-sifakis.md` | 5 | 208, 224, 225, 226, 229, 230 → **6** |
| ריפו חיצוני `Ambroseius/DropsLab` | 1 | 83 |

`*` = הקובץ מוזכר בעקיפין (למשל "the scanner", "the collector") ולא בשם-קובץ מפורש.
**תיקים ללא אף קובץ מצוין (18):** 9, 10, 39, 54, 66, 71, 74, 82, 92*, 101, 145, 153, 179, 186, 206, 240, 241, 245, 246, 253*.

### 4ב. תיק → התיקים שהוא מציין כתלות שלו

| תיק | תלויות שהגוף מציין |
|---|---|
| TASK-9 | (רצף לפני shadow→active — בלי מספר) |
| TASK-49 | TASK-106 (`NOT covered by`) |
| TASK-54 | TASK-53 Stage D (תקדים-סיכון) |
| TASK-65 | TASK-62 · TASK-198 (מוזג) · TASK-105 · TASK-109 · TASK-215 |
| TASK-66 | TASK-62 · TASK-28 · TASK-96 |
| TASK-71 | TASK-62 |
| TASK-73 | TASK-48 · TASK-62 · TASK-65 |
| TASK-74 | TASK-62 · **חוסם את TASK-71** |
| TASK-82 | TASK-80 · TASK-139-INV |
| TASK-83 | TASK-80 |
| TASK-88 | TASK-48 |
| TASK-92 | TASK-58 · ADR-002 |
| TASK-101 | TASK-93 · TASK-94 |
| TASK-109 | TASK-108 · TASK-106 (השער) · TASK-215 (`Coupled to`) |
| TASK-128 | **TASK-194** (AC#4) · TASK-201 · TASK-200 · TASK-127 · TASK-203..206 · TASK-202 |
| TASK-145 | TASK-139-INV · TASK-60 |
| TASK-153 | TASK-139-INV · TASK-156 · TASK-27 |
| TASK-176 | TASK-171 · TASK-67 · TASK-136 |
| TASK-179 | **BLOCKED ON: TASK-172 + TASK-177 + pre-registration task** |
| TASK-194 | TASK-128 · TASK-141/174/127 (Done) · TASK-208 · TASK-209 · HYP-002 |
| TASK-202 | TASK-200 |
| TASK-206 | TASK-201 · TASK-203 · TASK-149/168 |
| TASK-208 | TASK-194/S1 · **TASK-209** |
| TASK-209 | TASK-194 · TASK-208 |
| TASK-215 | TASK-58 · TASK-214 · TASK-213 |
| TASK-217 | TASK-216 · TASK-91 · TASK-105 · **TASK-219** |
| TASK-218 | TASK-176 · TASK-213 · TASK-217 |
| TASK-219 | TASK-217 |
| TASK-222 | TASK-211 |
| TASK-224 | HYP-002 (`Not a HYP-002 frozen param`) |
| TASK-225 | **HARD GATE: HYP-002** |
| TASK-226 | TASK-166 |
| TASK-229 | HYP-002 · HYP-003 |
| TASK-230 | TASK-182 |
| TASK-240 | `Blocked on the ticker corruption scope` |
| TASK-241 | HYP-002 |
| TASK-243 | TASK-242 |
| TASK-244 | HYP-002 |
| TASK-245 | `Blocked in practice on the ticker corruption scope` |
| TASK-246 | `BLOCKED ON: the finviz ticker fix` |
| TASK-248 | TASK-238 |
| TASK-249 | TASK-239 |
| TASK-250 | TASK-55 |
| TASK-251 | TASK-143 · TASK-242 · **TASK-243** |
| TASK-252 | TASK-245 |
| TASK-254 | TASK-208 · TASK-209 |
| **ללא תלות מצוינת (6):** | TASK-10 · TASK-39 · TASK-126 · TASK-186 · TASK-205 · TASK-216 · TASK-253 |

⚠️ **מה שאינו כאן:** `docs/MASTER_TASK_LIST_2026-08-03.md` מכיל שדה `**תלוי ב / חוסם את**` לכל תיק, עם קשרים שאינם בגוף אף תיק — למשל `TASK-215 → "חוסם בפועל את 244, ומקל על 65, 105, 109"` ו-`TASK-250 → "קרוב מאוד ל-228"`. הטבלה למעלה מחלצת **רק מהגוף**, כפי שהוגדר.

### 4ג. תיקים שהגוף שלהם מציין תאריך או חלון-זמן

| תיק | הציטוט |
|---|---|
| TASK-126 | `Logs retained ~90 days: May-12 runs expire ~Aug-10` · AC#3: `DR: גיבוי ה-CSV המוקדם מחוץ-למכונה **לפני ~9/8**` · `פוקע ~ספטמבר` |
| TASK-128 | AC#4: `promote shadow to active only after multi-regime and shadow-benign **about 2026-07-27**` · `אוסף דאטה עד ~7/27` |
| TASK-145 | `next scheduled run **1/7** must be watched` |
| TASK-176 | `AC#2 (quota savings) = measure via scripts/measure_429_by_workflow_v1.py **over 1-3/7**` |
| TASK-179 | `~450 RH rows, **~4-5 months** at current capture rate` |
| TASK-194 | `flip executed **2026-06-29**` · `checkpoint **2026-07-27** = החלטת-promote נפרדת` |
| TASK-205 | `scanned **>=2026-06-13**` · `Rows scanned **before 2026-06-13** (no D6-D25) handled gracefully` |
| TASK-215 | `step-3 אימות-429 ... מתחבר ל-TASK-213 (**דדליין 6/7**)` · `auto_scan 429-crashed live **30/6 12:22+12:42 Peru**` |
| TASK-217 | `The LIVE **2026-07** paper_portfolio tab` · `SCOPE: ONLY 2026-07 tab affected (2026-05/06 correct)` |
| TASK-219 | `PREREQ: audit all 16 agent tabs x3 months (**05/06/07**)` · `avoid halting **1/8** rotation` · `raise fires only at rotation **1/8**` |
| TASK-225 | `BLOCKED until HYP-002 concludes (**~2026-07-27** / n>=150 post-flip / **45 trading days**, first-of)` |
| TASK-229 | `אחרי סיום HYP-002 (**~27/7** / n>=150 / **45 ימים**)` |
| TASK-230 | `חלון-עומק yfinance-1m ≈ **30 יום** ⇒ forward-only` · `verdict **4/7** = blocked-by-data` |
| TASK-240 | כותרת: `**2026-07-05 to 2026-07-28**` · `clean **before 22/7**, suspect after` · `gaps on **23/7, 24/7, 27/7**` |
| TASK-241 | כותרת: `**2026-07-05 to 2026-07-28**` · `whose checkpoint fell on **27/7**` |
| TASK-243 | `**2026-09** was pre-created via OAuth` · `the **1/8** rotation short-circuits` · `Empty orphan folders **2026-07 and 2026-08**` |
| TASK-244 | `Measured live **2026-07-29**` · `On **2026-07-22** there were 43 ENTERs` · `On **2026-07-16** there were 17 ENTERs` |
| TASK-245 | `through **2026-07-14** the daily row count runs...` · `From **2026-07-15** onward...` · `**2026-07-17** is the thinnest day at 12 rows` |
| TASK-246 | `Measured live **2026-07-29**` · `everything opened after **2026-07-15** which is stuck` · `Age in trading days runs **1 to 10** with a median of **5**` |
| TASK-248 | `Verified live **2026-07-29**` |
| TASK-249 | `**15/7** had 43 candidates and ran 8 minutes. **21/7** had 56 and ran 11. **28/7** had 77 and ran 23. **31/7** had 89 and ran 27` · `The **3/8** run is the first on an empty August sheet` |
| TASK-250 | `Found **2026-08-03** after market close` · `the active month rotated to August on the first` · `DFNS was in the live scan of **2026-08-03**` |
| TASK-251 | `Verified **2026-08-03**` · `prepare_next_month failed on **1/7**` · `the manual repair of **29/7**` · `overwritten by the **1/8** automatic run` |
| TASK-252 | `Would have fired on **2026-07-15**` · `found on **29/7**, fourteen days later` · `would have fired around **22/7**` · `Measured again on **3/8**` |
| TASK-253 | `Measured **2026-08-03**` · `proven on **2026-07-31** where the run started at **17:27** was still executing when the **17:28** run began` |
| TASK-254 | `Noticed **2026-08-03**` · `the EOD log of **2026-07-30** printed: Daily analytics: tier=INSUFFICIENT, n=0` |
| TASK-49 | `אנומליה שצצה בוריפיקציה **28/5 09:20 Peru**` |
| TASK-65 | `FINAL scope **2026-06-27**` · `ראיה חיה **2026-06-30**` · `GVH (ENTER **08:54**) + SDOT (ENTER **09:27**)` |
| TASK-66 | `אישור-חקירה (צ'אט **2026-06-27**)` |
| TASK-83 | `קריסה עשויה לבוא אחרי **7-15 יום**, וחלון **5** חותך אותה באמצע` · `**49.5%** מהנופלים מתויגים Full Recovery ב-**5 יום**` |
| TASK-92 | `עמיחי העלה (**1/6**)` · `חקירת תלויות בוצעה חלקית **1/6**` |
| TASK-101 | `מאומת מול plugin.json+README ... **2/6/2026**` · `Verified **2026-06-04**` |
| TASK-109 | `Recon **2026-07-02**: gate NOT met (only **1 proof-day 6/30**)` |
| TASK-186 | (אין תאריך בגוף) |
| TASK-202 | `Discovered during TASK-200 June backfill` |
| TASK-206 | `Availability verified on a nano-cap sample (**29/6**)` |
| TASK-208 | `RECON **30/6**` · `commit 5a127ad (**29/6**, יום ה-flip)` · `E2E-AUDIT S3 (**3/7**)` |
| TASK-209 | `E2E-AUDIT S1-S5 (**3/7**)` |
| TASK-222 | `CORRECTION (E2E-audit S5, **3/7**)` |
| TASK-224 | `E2E-audit S2 (**3/7**)` · `RULING (עמיחי **3/7**)` |
| TASK-226 | `E2E-audit S3 (**3/7**)` |
| TASK-82 | `מחקר רשת **31/5**` |
| TASK-73 | `research/TASK-62_weekly_analysis_**2026-05-30**.md` |
| TASK-74 | `עלה ב-TASK-62 (source map **30/5**)` |
| TASK-216 | `shadow_gate_events (**24/6**) + borrow_coverage (**14/6**) → 2026-07 חסר אותם עד תיקון ידני **30/6**` |

**תיקים ללא אף תאריך בגוף (7):** TASK-9 · TASK-10 · TASK-39 · TASK-71 · TASK-153 · TASK-186 · TASK-218.

---

## 5. הספירה לפי תג

| תג | # | התיקים |
|---|---|---|
| `[CODE]` | 20 | 9, 10, 39, 54, 73, 83, 88, 101, 126, 153*, 186, 202, 205, 206, 216, 217, 218, 219*, 222, 224, 250, 252 → ראה הערה |
| `[RESEARCH]` | 17 | 49, 66, 71, 74*, 82*, 145, 176, 179, 194, 215*, 225, 229, 230*, 240, 241, 244, 245, 249*, 253, 254 |
| `[DECISION]` | 12 | 65*, 82*, 92, 109, 153*, 208, 209, 219*, 225*, 229*, 243*, 246, 248, 249*, 251 |
| `[UNCLEAR]` | 0 | — |

⚠️ **הספירה אינה נסגרת ל-53 כי 13 תיקים נושאים יותר מתג אחד** — הגוף שלהם מפצל בעצמו לשלבים. הפירוט המדויק בכל רשומה. התיקים הרב-תגיים: 65 · 74 · 82 · 128 · 153 · 208 · 209 · 219 · 225 · 229 · 230 · 243 · 249 · 250.

**התפלגות חד-משמעית (התג הדומיננטי לפי מה שנותר לביצוע):**
- **הגוף מבקש שינוי קוד** — 21 תיקים
- **הגוף מבקש מדידה/ניתוח** — 20 תיקים
- **הגוף מבקש הכרעה של עמיחי** — 12 תיקים
- **לא ניתן להכריע מהגוף** — 0

**האם התנאי עדיין קיים בקוד — סיכום:**
- ✅ **מאומת שהתנאי קיים:** 33 תיקים
- ⚠️ **מאומת חלקית / מצב מעורב:** 5 תיקים (49, 65, 82, 240, 244)
- ❓ **לא נבדק:** 15 תיקים (54, 71, 74, 83, 101*, 126*, 145, 179*, 186*, 215, 217*, 230*, 245, 246*, 253*)

`*` = חלק מהתיק אומת וחלק לא. **אף תיק לא סומן כ-Done.**

---

**סוף קובץ ב' — 26 תיקים (TASK-208 → TASK-254). סה"כ בשני הקבצים: 53/53.**

# push — TASK-259 batch ל-SMA20

**נכתב 2026-08-05 20:16 Lima.** נדחף commit אחד. **אין merge, אין rebase, אין force-push,
לא נגעתי ב-main המקומי.** שני קבצי ה-YAML נשארו `M` ולא נדחפו.
לא נגעתי ב-Sheets/Drive/FINVIZ, לא הרצתי workflow/scanner/collector.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

---

## 1. ה-push — אימות מהשרת

```
$ git branch --show-current
docs/handoff-2026-07-29
$ git remote -v | head -1
origin	https://github.com/projects5069-creator/ridinghigh-pro.git (fetch)

$ git ls-remote origin docs/handoff-2026-07-29        (לפני)
52c32a7f5163cac3f7b68d820cf6d3ce5110db78

$ git push origin docs/handoff-2026-07-29
To https://github.com/projects5069-creator/ridinghigh-pro.git
   52c32a7..fcdb0aa  docs/handoff-2026-07-29 -> docs/handoff-2026-07-29

$ git ls-remote origin docs/handoff-2026-07-29        (אחרי)
fcdb0aaf6aa4c2f5f722cad34f235c56f1e52d39
$ git rev-parse HEAD
fcdb0aaf6aa4c2f5f722cad34f235c56f1e52d39
$ git rev-list --left-right --count HEAD...origin/docs/handoff-2026-07-29
0	0
```
**ה-SHA מהשרת זהה ל-HEAD, 0/0.** `52c32a7..fcdb0aa` — **fast-forward**, בלי `+`,
ולא הועבר `--force`. ה-remote אומת כ-`projects5069-creator/ridinghigh-pro`
(rhpro-live §3) לפני הדחיפה.

---

## 2. PR #33

| | |
|---|---|
| state | **OPEN** · mergeable **MERGEABLE** |
| קומיטים | **8** (היו 7) |
| קבצים | 38 |
| שורות | **+1573 / −22** |

```
1c2b54d docs(backlog): record nine owner rulings 2026-08-05, close TASK-10
31b3ca3 docs(backlog): open TASK-255/256/257 from decision review
eb1b026 docs(backlog): import TASK-234..237 from fix/auto-dancer-planmd
cefacee docs(backlog): stage-1 restatements, close TASK-145, scope SHEET_NAMES into 243
9eb3893 docs(backlog): open TASK-258 — HYP-001 has no crossover-event detector
9fab796 docs(backlog): fold measurement results into six tickets, close 245 and 224
52c32a7 docs(backlog): open TASK-259/260/261 from the measurement session
fcdb0aa perf(agent): batch SMA20 enrichment into one Alpaca request     ← חדש
```
**`fcdb0aa` הוא שינוי הקוד הראשון ב-PR הזה** — שבעת הקודמים כולם markdown של הבקלוג.

---

## 3. CI — 4/4 pass

```
31062267689  tests                  event=push          success  sha=fcdb0aa   27s
31062267617  filename-length guard  event=push          success  sha=fcdb0aa    8s
31062268958  tests                  event=pull_request  success  sha=fcdb0aa   26s
31062268953  filename-length guard  event=pull_request  success  sha=fcdb0aa    9s
```
`statusCheckRollup` — ארבע רשומות, כולן `COMPLETED` / `SUCCESS`:
```
guard   COMPLETED SUCCESS  filename-length guard
guard   COMPLETED SUCCESS  filename-length guard
pytest  COMPLETED SUCCESS  tests
pytest  COMPLETED SUCCESS  tests
```
**4/4 עברו.** זו הריצה הראשונה של ה-CI על שינוי קוד ב-PR הזה, כולל **7 הטסטים החדשים**
של `test_sma20_batch_v1.py`.

⚠️ **נקודה שמאשרת את האבחון:** `test_orchestrator_eod_borrow_wiring_v1` — שנכשל מקומית —
**עבר ב-CI**, בדיוק כפי שנחזה. ראה §5.

---

## 4. ה-YAML נשאר בחוץ — שתי בדיקות עצמאיות

```
$ gh pr view 33 --json files --jq '.files[].path' | grep "workflows"
CONFIRMED: no workflow in PR

$ git status --porcelain=v1 -- .github/workflows/
 M .github/workflows/agent_minute.yml
 M .github/workflows/auto_scan.yml
```
**ה-PR אינו מכיל אף קובץ workflow, ושניהם עדיין `M` מקומית.**
תופעת-הלוואי הידועה — `check_06`/D3 שצפוי להתהפך ל-CRITICAL — **טרם נפרסה**,
ומתועדת ב-`reports/2026-08-05_1926_task259_concurrency.md` §6.3.

### ahead / behind
```
$ git rev-list --left-right --count HEAD...origin/main
8	11
```
**8 לפני `origin/main`, 11 אחרי.** ה-PR פתוח ולא מוזג; לא ביצעתי rebase ולא merge.
`main` המקומי לא נגע.

---

## 5. תיק חדש — TASK-262

```
backlog/tasks/task-262 - test_orchestrator_eod_borrow_wiring_v1-is-non-deterministic-—-leaks-live-state.md
status: To Do   priority: medium   labels: tests, ci, flaky, data-integrity
```

הגוף מתעד את הראיה שנאספה במהלך TASK-259:
- **הראיה החזקה ביותר — התוצאה זזה בלי שינוי קוד:**
  `19:47 → 2 failed, 727 passed` מול `20:07 → 1 failed, 729 passed`,
  אותה מכונה, אותו עץ עבודה.
- **CI ירוק על אותם commits:** `52c32a7` (31056028703/31056032278) ו-
  `fcdb0aa` (31062267689/31062268958) — כולם `success`, באותה פקודה מ-`tests.yml:30`.
- **הטיקרים הדולפים** — `INLF, SHPH, YXT, ZYBT` — הם הטיקרים החיים של 5/8, מופיעים
  ב-`decision_log` 2026-08 ובלוגי `agent_minute` של אותו יום, ואינם ב-fixture.
- **למה זה חשוב מעבר לקובץ:** סוויטה שתוצאתה זזה אינה שער — היא אינה מבחינה בין
  "השינוי שלי שבר" לבין "השוק זז". במהלך TASK-259 נאלצתי למדוד בסיס לפני כל עריכה,
  והבסיס עצמו השתנה מ-2 ל-1 באמצע הסשן.
- **מה לא אומת, מוצהר:** איזו קריאה בדיוק בתוך `collect_borrow_snapshot` מגיעה למצב חי.
  רשומת ה-PK מ-3/8 (v4.12) מתעדת ממצא קרוב — "7 טסטי-יחידה קוראים Sheets חי דרך
  `collect_borrow_snapshot:57`" — אבל השורה לא אומתה מחדש בסשן הזה, ובקובץ יש היום
  **2** טסטים נכשלים ולא 7.
- **קשר ל-TASK-250** — מחלקה זהה, ייתכן שכבר מכסה את הקובץ. **להכריע אם זו כפילות
  לפני עבודה.**

```
$ git status --porcelain=v1 -- tests/
(ריק)
```
**לא נגעתי בקובץ הטסט.** תיק בלבד, כפי שהורית.

---

## 6. סטיות מההוראה

| # | מה | פירוט |
|---|---|---|
| 1 | **`gh pr checks --watch` הורץ פעם אחת** | ההוראה נתנה אותו ישירות עם `--watch`; לא נדרשה ריצה מקדימה. |
| 2 | **RULE #12 — ללא `.rh-run.sh`** | הפקודות הורצו ישירות; הפלט יועד לדוח. |

**לא בוצע, במכוון:** אין merge · אין rebase · אין force-push · לא נגעתי ב-main המקומי ·
לא נגעתי ב-`.github/workflows/` · אין Sheets/Drive/FINVIZ · לא הרצתי workflow/scanner/collector ·
לא תיקנתי את הטסט.

**Anti-Drift:** ה-PK כבר נכלל ב-`fcdb0aa` עם bump ל-4.13 ושורה שמתארת בדיוק את מה שנחת.
**אין חוב פתוח על ה-commit הזה.**

---

## מצב

`fcdb0aa` על השרת · **PR #33 פתוח, MERGEABLE, CI ירוק 4/4, שמונה קומיטים** — ממתין למיזוג.
שני ה-YAML של `concurrency` נשארו בעץ ולא נדחפו — הכרעה נפרדת, עם תופעת-הלוואי הידועה.
**TASK-262** נפתח על הסוויטה הלא-דטרמיניסטית; הקובץ עצמו לא נגע.

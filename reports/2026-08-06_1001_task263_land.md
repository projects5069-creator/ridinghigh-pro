# TASK-263 — LAND

**נכתב 2026-08-06 10:01 Lima.** LAND — commit ×2 → push → PR #35 → CI.
לא נגעתי ב-`.github/workflows/` (נשארים `M`), לא ב-Sheets/Drive/FINVIZ.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/.../verification-before-completion/SKILL.md` | 139 |

---

## 0. ⚠️ תיקון עצמי — Anti-Drift אינו חל

ב-BUILD כתבתי "חוב Anti-Drift פתוח — PK טרם עודכן". **בדקתי, וזה היה רחב מדי.**
`rhpro-live` §4 מונה **שבע** קטגוריות:
```
formulas · weights · scoring · workflows · sheets configuration · schema · health checks
```
תפר של מקור-נתונים בעוזר EOD **אינו אף אחת מהן**. `sheets configuration` מתייחס
ל-`sheets_config.json`, לא לכל קוד שנוגע בגיליון. **לא בוצע bump, ואין חוב.**
*(החוב שנרשם אתמול על ה-`concurrency` כן חל — `workflows` נמצא ברשימה — והוא עדיין פתוח,
כי ה-YAML לא נחת.)*

## 1. commit ראשון — הקוד והטסט

```
$ git diff --cached --name-only | wc -l  →  2
agent/orchestrator_eod.py
tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py
OK: no workflow staged
```
```
9a12942  fix(eod): inject the data source into collect_borrow_snapshot
         2 files changed, 63 insertions(+), 40 deletions(-)
```

## 2. commit שני — הבקלוג

```
601c7b7  docs(backlog): close TASK-262 and TASK-263, correct TASK-264
         3 files changed, 102 insertions(+), 5 deletions(-)
```

| תיק | סטטוס | מה נרשם |
|---|---|---|
| **TASK-262** | **Done** | ה-fixture, חמש ריצות זהות, 40.84s→0.26s (×157), סוויטה 2→0 כשלים. מציין מפורשות **מה התיק לא תיקן** ומי הבעלים (263) |
| **TASK-263** | **Done** | שני התפרים, החתימה, resolve-בזמן-קריאה, **תנאי הקבלה שהתיק עצמו הגדיר — "delete the xfail marker"** — בוצע. מציין ש-`:132` לא נגע ושה-Anti-Drift נבדק |
| **TASK-264** | **To Do** — תיקון, לא סגירה | ראה §3 |

### §3 — התיקון ל-TASK-264
```
tests/test_task172_coverage_v1.py:176   monkeypatch.setattr(sheets_manager, "get_worksheet", ...)
tests/test_task172_coverage_v1.py:177   monkeypatch.setattr(bc, "collect_borrow_coverage", ...)
```
**שני ההופים מפוטצ'ים ⇒ הקובץ היה מבודד ואינו יכול היה לכתוב.** החשד שרשמתי בתיק **מופרך**.

**הכותב היחיד שהוכח:** `test_orchestrator_eod_borrow_wiring_v1`, עם before/after תואם —
ארבע שורות ב-09:23:54→09:24:18 עם `ScannedUniverse` 3,1,1,3, ואפס אחרי התיקון.

⚠️ **מה שנשאר פתוח, וזה עיקר התיק:** מתוך 198 השורות מחוץ לחלון, **רק 4 מיוחסות בוודאות.
194 אינן.** שורות 08-05 ב-19:29-20:11 נושאות `WithBorrowData=4` — ברוקר אמיתי ענה,
כלומר **אינן** הפיקסצ'רים של הקובץ הזה. מועמדים שלא נבדקו: `test_task172_names_gap_v1.py`,
ריצות ידניות של `orchestrator_eod`, סקריפטים אד-הוק.

## 4. push ו-PR

```
$ git ls-remote origin docs/handoff-2026-07-29   (לפני)  7d472e8aa48c...
$ git push origin docs/handoff-2026-07-29
   7d472e8..601c7b7  docs/handoff-2026-07-29 -> docs/handoff-2026-07-29
$ git ls-remote origin docs/handoff-2026-07-29   (אחרי)  601c7b7b8c12...
$ git rev-parse HEAD                                      601c7b7b8c12...
$ git rev-list --left-right --count HEAD...origin/...     0	0
```
fast-forward, בלי `+`, בלי force. **PR #34 מוזג ⇒ נפתח PR #35.**

**https://github.com/projects5069-creator/ridinghigh-pro/pull/35**
`OPEN · MERGEABLE · 2 קומיטים · 5 קבצים · +165 / −45`

**CI 4/4 pass:**
```
guard   pass  6s  ·  guard   pass  9s
pytest  pass  27s ·  pytest  pass  26s
statusCheckRollup: כולן SUCCESS
```
```
$ gh pr view 35 --json files | grep workflows
CONFIRMED: no workflow in PR
```

## 5. אימות
```
$ git status --porcelain -- .github/workflows/
 M .github/workflows/agent_minute.yml
 M .github/workflows/auto_scan.yml          ← עדיין M, לא נגעתי

$ git log -3 --oneline
601c7b7 docs(backlog): close TASK-262 and TASK-263, correct TASK-264
9a12942 fix(eod): inject the data source into collect_borrow_snapshot
7d472e8 docs(backlog): open tickets from the TASK-262 build

TASK-262: Done · TASK-263: Done · TASK-264: To Do
OPEN: 61        (היה 63 — שתי סגירות)
ahead/behind מול origin/main: 2  13
```

## 6. סיכום

```
commits: 9a12942 (2 קבצים, +63/-40) · 601c7b7 (3 תיקים, +102/-5)
PR:      #35 OPEN MERGEABLE · 5 קבצים · +165/-45 · אפס workflows
CI:      4/4 pass (guard 6s/9s · pytest 27s/26s)
תיקים:   TASK-262 Done · TASK-263 Done · TASK-264 To Do (תוקן, לא נסגר)
ספירה:   OPEN 63 → 61 · ahead/behind מול main 2/13
```

## סטיות מההוראה

| # | מה | פירוט |
|---|---|---|
| 1 | **לא בוצע bump ל-PK** | ההוראה לא ביקשה אותו; אני העליתי את הנושא מיוזמתי ב-BUILD, ובדיקה מול §4 הראתה שאינו חל. מתועד ב-§0 כתיקון עצמי. |
| 2 | **`gh pr checks` על #35 ולא בלי מספר** | `gh pr checks --watch` בלי ארגומנט לא מזהה PR כשהענף ללא PR פתוח ברגע ההרצה. פתחתי #35 קודם ואז הרצתי עליו. |
| 3 | **RULE #12 — ללא `.rh-run.sh`** | הפלט יועד לדוח. |

**אפס כתיבה לגיליונות בתור הזה.**

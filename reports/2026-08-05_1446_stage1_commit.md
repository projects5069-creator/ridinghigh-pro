# שלב 1 — commit · שני commits על הבקלוג

**נכתב 2026-08-05 14:46 Lima.** סעיף השלמה אחד ואז שני commits. **אין push.**
לא נגעתי בקוד/workflow/config, לא ב-Sheets/Drive/FINVIZ, לא הרצתי pytest.
נקודת פתיחה: `eb1b026`.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

---

## 0. ההשלמה — סעיף (6) ל-TASK-243

הניסוח נלקח **מילה במילה** מ-`reports/2026-08-05_1406_stage1_closures.md` שורות 586-591,
בתוספת שורת-מקור אחת בסוף (`Added 2026-08-05 from the stage-1 closure review.`) כדי
שהסעיף יישא תאריך כמו שאר ההכרעות. גיבוי לפני העריכה, מחוץ לריפו:
```
/private/tmp/.../scratchpad/task-243.md.bak_20260805-144705
```

**הדרך: `str_replace`.** הסעיף יושב בתוך ה-Description, שהוא פסקה אחת ארוכה;
`backlog task edit --desc` היה דורס את כולה. ה-`str_replace` מוסיף בסוף המשפט של סעיף (5)
ואינו נוגע בשום דבר אחר.

### ה-diff
```diff
--- a/backlog/tasks/task-243 - Harden-root-resolution-and-rotation-guards.md
+++ b/backlog/tasks/task-243 - Harden-root-resolution-and-rotation-guards.md
@@ -15,5 +15,5 @@ ordinal: 241000
 ## Description

 <!-- SECTION:DESCRIPTION:BEGIN -->
-... (5) Empty orphan folders 2026-07 and 2026-08 remain under RidingHigh-Data; review and
-remove manually, never automatically.
+... (5) Empty orphan folders 2026-07 and 2026-08 remain under RidingHigh-Data; review and
+remove manually, never automatically. (6) Unify the two SHEET_NAMES lists.
+sheets_manager.py:38-48 has nine entries and treats live_trades as its own file;
+prepare_next_month.py:22-31 has eight, omits live_trades, and line 240 aliases it to
+score_tracker. scripts/fix_august_provisioning_v1.py:305 iterates the nine-entry list,
+which is how 2026-08 and 2026-09 ended up standalone. Whichever convention wins, one list
+must be the source and the repair script must check the existing convention before
+creating anything. Evidence and the per-month state are in TASK-251.
+Added 2026-08-05 from the stage-1 closure review.
 <!-- SECTION:DESCRIPTION:END -->
```
(שורה אחת פיזית בקובץ; נשברה כאן לקריאות. ה-diff האמיתי הוא `1 insertion(+), 1 deletion(-)`
על אותה שורת-Description.)

### האימות
```
$ grep -cF "(6) Unify the two SHEET_NAMES lists." "task-243 ....md"    → 1
$ for i in 1 2 3 4 5; do grep -coF "($i) " ...; done                   → 1 1 1 1 1
$ grep -m1 '^status:' ...                                              → status: To Do
```
**הסעיף החדש קיים · חמשת הסעיפים המקוריים כולם עדיין שם · הסטטוס לא זז.** הוספה בלבד.

---

## 1. Commit ראשון — תשע העריכות + סגירת 145 + סעיף 6

```
cefacee  docs(backlog): stage-1 restatements, close TASK-145, scope SHEET_NAMES into 243
```

`git show --stat HEAD`:
```
 ...25\327\252-\327\242\327\235-skill-gate-hook.md" | 19 +++++++++++-    ← 101
 ...te-agent_critic_monthly-21.9pct-failure-rate.md | 22 +++++++++++--   ← 145
 ...rt-on-hold-out-n-150-events-worst-case-costs.md |  2 +-              ← 179
 ...24-decision-blocked-until-HYP-002-concludes.md" |  2 +-              ← 225
 ...tes0-during-market-hours-unify-with-TASK-166.md |  2 +-              ← 226
 ...and-trade-behaviour-2026-07-05-to-2026-07-28.md |  2 +-              ← 241
 ...- Harden-root-resolution-and-rotation-guards.md |  2 +-              ← 243
 ...id-not-block-repeat-ENTERs-on-open-positions.md |  2 +-              ← 244
 ...file-every-other-month-aliases-score_tracker.md | 36 ++++++++++++++  ← 251
 ...reports-n0-while-July-closed-sixty-positions.md | 34 ++++++++++++++  ← 254
 10 files changed, 114 insertions(+), 9 deletions(-)
```

**10 קבצים בדיוק**, כפי שהתבקש. ה-index הודפס לפני ה-commit ואומת שהוא מכיל בדיוק
עשרה נתיבים — ולכן `git commit` ללא pathspec היה בטוח כאן, בניגוד לתור הקודם.
השמות ל-`git add` נגזרו מ-`git status --porcelain=v1` בתור הזה. אפס `-A`, אפס wildcards.

הודעת ה-commit נכתבה מקובץ (`-F`). **ללא `Co-Authored-By`, ללא "Generated with".**

---

## 2. Commit שני — TASK-258

```
9eb3893  docs(backlog): open TASK-258 — HYP-001 has no crossover-event detector
```

`git show --stat HEAD`:
```
 ...200\224-HYP-001-has-no-collection-mechanism.md" | 63 ++++++++++++++++++++++
 1 file changed, 63 insertions(+)
 create mode 100644 "backlog/tasks/task-258 - Build-crossover-event-detector-—-HYP-001-has-no-collection-mechanism.md"
```
`create mode 100644` — קובץ חדש. ה-index הכיל אותו לבדו לפני ה-commit (הודפס ואומת).

---

## 3. אימות

### 3.1 — `git log -3 --oneline`
```
9eb3893 docs(backlog): open TASK-258 — HYP-001 has no crossover-event detector
cefacee docs(backlog): stage-1 restatements, close TASK-145, scope SHEET_NAMES into 243
eb1b026 docs(backlog): import TASK-234..237 from fix/auto-dancer-planmd
```

### 3.2 — מצב העץ
```
$ git status --porcelain=v1 --untracked-files=all
?? docs/auto-dancer/queue/QUEUE_2026-07-04.md
?? docs/auto-dancer/queue/QUEUE_2026-07-05.md
?? reports/  (16 קבצים)
```
**`backlog/tasks/` נקי לחלוטין** — אפס `M`, אפס `A`, אפס `??`.
כל מה שנשאר untracked היה כזה עוד לפני התור.

### 3.3 — אפס קבצי קוד
```
$ git status --porcelain | grep -E '\.py$|\.yml$|\.json$'
CLEAN: no code files touched
```
`PROJECT_STATE.md` רוענן ע"י ה-post-commit hook בשני ה-commits — gitignored, לא נכנס לאף אחד.

### 3.4 — ahead / behind
```
$ git rev-list --left-right --count HEAD...origin/main
5	11
$ git rev-list --left-right --count HEAD...origin/docs/handoff-2026-07-29
2	0
```
5 לפני `origin/main`, 11 אחרי. **ו-2 לפני הענף שעל השרת** — כלומר
**שני ה-commits האלה עדיין לא נמצאים ב-PR #33.** ה-PR מכיל את שלושת הקודמים בלבד.
זה נכון וצפוי: אין push בתור הזה.

### 3.5 — הספירה והסטטוס
```
$ backlog task list --plain | awk ... → OPEN: 59
$ grep -m1 '^status:' backlog/tasks/task-145 - ....md → status: Done
```
**59 פתוחים.** 59 לפני התור, −1 (145 נסגר) +1 (258 נפתח) = 59. הסגירה והפתיחה מתקזזות.

### 3.6 — main המקומי לא זז
```
$ git branch --show-current → docs/handoff-2026-07-29
$ git log -1 --oneline main → 69c0c21 feat(auto-dancer): PLANNER STEP 0 grounding — ...
```

---

## 4. סטיות מההוראה

| # | מה | פירוט |
|---|---|---|
| 1 | **סעיף (6) הוסף ב-`str_replace` ולא ב-CLI** | `backlog task edit --desc` דורס את כל ה-Description (פסקה אחת ארוכה) כדי להוסיף משפט. גיבוי מחוץ לריפו + אימות ב-grep ששת הסעיפים כולם קיימים. |
| 2 | **תוספת של משפט אחד לניסוח שנתת** | הוספתי `Added 2026-08-05 from the stage-1 closure review.` בסוף סעיף (6), כדי שהסעיף יישא תאריך-מקור כמו שאר ההכרעות. שאר הטקסט זהה מילה-במילה לדוח. |
| 3 | **הודעות ה-commit מ-`-F`** | ולא `-m`. סיבה טכנית: המקף-הארוך `—` ותווי-הציטוט שוברים ציטוט ב-zsh. הטקסט זהה מילה-במילה. |
| 4 | **RULE #12 — ללא `.rh-run.sh`** | הפקודות הורצו ישירות; הפלט יועד לדוח. |

**לא בוצע, במכוון:** אין push · אין `-A` · אין wildcards · אין נגיעה בקוד/workflow/config ·
אין pytest · אין Sheets/Drive/FINVIZ · לא נגעתי ב-main המקומי · לא שיניתי סטטוס של אף
תיק בתור הזה (145 נסגר בתור הקודם; כאן רק נעשה לו commit).

**Anti-Drift (rhpro-live §4):** שני ה-commits נוגעים רק ב-markdown של הבקלוג — אפס נוסחאות,
משקלים, workflows, sheets-config, schema או health-checks. לא נדרש bump ל-PK, ולא ביצעתי.

---

## מה נשאר פתוח

- **שני commits לא-דחופים** (`cefacee`, `9eb3893`) מחכים ל-push. PR #33 עדיין מכיל
  את שלושת הקודמים בלבד; push יעדכן אותו אוטומטית.
- **PR #33 פתוח ולא מוזג** — ההחלטה שלך.

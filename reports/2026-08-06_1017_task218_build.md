# TASK-218 — קאש ל-handle של sentinel_events

**נכתב 2026-08-06 10:17 Lima.** BUILD — `data_sentinel.py` בלבד. **אין commit.**
לא נגעתי ב-`orchestrator.py` / `formulas.py` / `config.py` / `.github/workflows/`,
לא ב-Sheets/Drive/FINVIZ. **לא הוספתי צבירה ולא flush** — קאש ל-handle בלבד, כלשון התיק.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/test-driven-development | `~/.claude/plugins/cache/.../test-driven-development/SKILL.md` | 371 |
| superpowers/verification-before-completion | `.../verification-before-completion/SKILL.md` | 139 |

**גיבוי:** `/private/tmp/.../scratchpad/bak218/data_sentinel.py.bak_20260806-101858`

---

## 1. RED

`tests/agent/unit/test_sentinel_handle_cache_v1.py` — **חמישה מקרים**, mock בלבד, אפס רשת.
```
E   AttributeError: module 'agent.sentinel.data_sentinel' has no attribute 'reset_sentinel_ws_cache'
ERROR ...::test_sixty_events_look_the_worksheet_up_once
ERROR ...::test_appends_are_still_one_per_event_and_in_order
ERROR ...::test_failed_lookup_is_swallowed_and_not_cached
ERROR ...::test_worksheet_returning_none_is_not_cached
ERROR ...::test_cache_does_not_leak_across_runs
5 errors in 0.03s
```
**5/5 נכשלו מהסיבה הנכונה** — הפונקציה אינה קיימת.

**חמישה ולא ארבעה:** פיצלתי את מקרה 3 לשניים, כי `get_worksheet` יכולה **להיכשל**
(חריגה) או **להחזיר `None`** — שני מסלולים שונים, ושניהם חייבים לא להיכנס לקאש.

## 2. GREEN — תוחלת החיים, וזו הנקודה שביקשת להגדיר במפורש

```python
_sentinel_ws_cache = None                    # ברמת מודול = פר-תהליך
def reset_sentinel_ws_cache() -> None: ...   # איפוס מפורש
def _get_sentinel_ws(sm): ...                # fetch-once, ולא מקאשר כשל
```

**מה תוחלת החיים:** משתנה ברמת מודול ⇒ **פר-תהליך**. ב-runner של GitHub
`python -m agent.orchestrator` הוא תהליך חדש בכל ריצה, ולכן **תוחלת-התהליך היא
תוחלת-הריצה, ושום דבר לא דולף בין ריצות.**

**התקדים בריפו — שני מקומות, ואימתתי את שניהם:**
```
agent/sentinel/data_sentinel.py:294-302   _sentinel_instance   ← singleton באותו קובץ
sheets_manager.py:428, 436, 441           _read_counts + get_read_counts + reset_read_counts
```

**מה שאין, ואני אומר זאת במפורש כפי שביקשת:** ⚠️ **אין במודול הזה hook מובנה של
תחילת-ריצה שאפשר להיתלות בו.** לכן `reset_sentinel_ws_cache()` הוא **מפורש ולא אוטומטי**.
היחיד שיכול לקרוא לו בתחילת ריצה הוא `orchestrator.py` — **ולא נגעתי בו.**
בייצור זה לא נדרש (תהליך חדש = קאש חדש); זה נדרש רק לסוויטה ולכל צרכן ארוך-חיים עתידי.
**אימתתי שאין כזה היום:** `grep` על כל מי שמייבא את המודול מחזיר את `orchestrator.py`
ואת קבצי ה-`checks/` בלבד — כולם פר-ריצה.

**כשל אינו נכנס לקאש.** זו הנקודה הקריטית: היום כל אירוע עושה lookup מחדש ולכן
**מתאושש מעצמו** אחרי 429 חולף. קאש שישמור handle מת היה הופך בעיה חולפת בתחילת ריצה
לאובדן מלא לכל שארית הריצה — **גרוע מהמצב הנוכחי.** לכן `_get_sentinel_ws` מקאשר
**רק ערך אמיתי**, ו-`None` או חריגה משאירים את הקאש ריק.

**ה-`except` הקיים נשמר** — הבליעה היא התנהגות מכוונת ולא נגעתי בה.

## 3. אימות

```
$ py_compile agent/sentinel/data_sentinel.py        →  PY_COMPILE OK
$ pytest tests/agent/unit/test_sentinel_handle_cache_v1.py -q
5 passed in 0.09s   (שלוש ריצות רצופות, זהות)

=== הסוויטה המלאה ===
לפני:  731 passed, 3 deselected  in 9.27s
אחרי:  736 passed, 3 deselected  in 9.31s     (+5 = הטסטים החדשים)
```
**אפס כשלים. אפס כשלים חדשים.**

```
$ git status --porcelain | grep -E "orchestrator\.py|config\.py|formulas\.py"
CLEAN: no protected path touched

$ git diff --stat -- .github/workflows/
 2 files changed, 10 insertions(+)      ← בדיוק כמו קודם, לא נגעתי

$ git status --porcelain | grep "^ M agent/"
 M agent/sentinel/data_sentinel.py      ← הקובץ היחיד ב-agent/
```


## ה-diff

```diff
diff --git a/agent/sentinel/data_sentinel.py b/agent/sentinel/data_sentinel.py
index afb8092..fd3d38b 100644
--- a/agent/sentinel/data_sentinel.py
+++ b/agent/sentinel/data_sentinel.py
@@ -27,6 +27,41 @@ logger = logging.getLogger("agent.sentinel")
 DecisionType = Literal["ALLOW", "WARN", "BLOCK"]
 
 
+# TASK-218: the sentinel_events worksheet handle, looked up once per run.
+#
+# LIFETIME. Module-level, i.e. one per PROCESS. On a GitHub runner
+# `python -m agent.orchestrator` is a fresh process per run, so process lifetime
+# IS run lifetime and nothing leaks between runs. This is the same shape as the
+# _sentinel_instance singleton already at the bottom of this file (:294-302),
+# and as sheets_manager._read_counts / reset_read_counts (sheets_manager.py:428).
+#
+# RESET. reset_sentinel_ws_cache() exists for anything longer-lived than one
+# run — the test suite, and any future in-process re-entry. There is no built-in
+# per-run lifecycle hook in this module to hang it on, and this BUILD is scoped
+# to data_sentinel.py, so the reset is explicit rather than automatic.
+_sentinel_ws_cache = None
+
+
+def reset_sentinel_ws_cache() -> None:
+    """Drop the cached sentinel_events handle. Call at the start of a new run."""
+    global _sentinel_ws_cache
+    _sentinel_ws_cache = None
+
+
+def _get_sentinel_ws(sm):
+    """Return the sentinel_events worksheet, fetching it at most once per run.
+
+    A failed or empty lookup is NEVER cached: today every event re-looks-up and
+    therefore self-heals after a transient 429, and that property must survive.
+    Caching a dead handle would turn one quota blip at the start of a run into a
+    total loss for the rest of it — strictly worse than the behaviour it replaces.
+    """
+    global _sentinel_ws_cache
+    if _sentinel_ws_cache is None:
+        _sentinel_ws_cache = sm.get_worksheet("sentinel_events")
+    return _sentinel_ws_cache
+
+
 def _log_sentinel_event(decision: str, component: str, reason: str,
                         details: dict, action_taken: str) -> None:
     """
@@ -35,6 +70,12 @@ def _log_sentinel_event(decision: str, component: str, reason: str,
     Schema: [Timestamp, EventType, Severity, Component, Message, Details, ActionTaken]
     Only called for BLOCK/WARN (never ALLOW) to keep Sheets quota low.
     Failures are swallowed (logged) — never break the trading loop.
+
+    TASK-218: the worksheet lookup is cached per run. Measured 2026-08-05, the
+    429 that lost 55 of 60 events in one run was on 'Read requests' — the
+    get_worksheet lookup, not the append. safe_append_row already retries
+    (sheets_manager.py:389); the lookup did not. One lookup per run instead of
+    one per event removes ~60 reads/run from the quota.
     """
     try:
         import json
@@ -55,7 +96,12 @@ def _log_sentinel_event(decision: str, component: str, reason: str,
             json.dumps(details, default=str)[:500],  # Details
             action_taken,                      # ActionTaken
         ]
-        ws = sm.get_worksheet("sentinel_events")
+        ws = _get_sentinel_ws(sm)
+        if ws is None:
+            # Not an exception — get_worksheet can return None. Nothing was
+            # cached, so the next event tries again.
+            logger.warning("Sentinel event dropped: sentinel_events worksheet unavailable")
+            return
         sm.safe_append_row(ws, row)
     except Exception as e:
         logger.warning("Failed to log sentinel event to sentinel_events: %s", e)
```

---

## מה לא נעשה, בכוונה

1. **אין צבירה ואין flush.** התיק מבקש `"Cache the worksheet handle once/run"` —
   וזה מה שנעשה. הצבירה נחסמה בתור הקודם כי היא מחייבת `orchestrator.py`.
2. **`reset_sentinel_ws_cache()` אינו נקרא מהייצור.** הוא לא נדרש שם (תהליך חדש
   בכל ריצה), והקורא היחיד האפשרי היה `orchestrator.py`. **מוצהר, לא הומצא מבנה.**
3. **החיסכון הצפוי:** ~60 קריאות READ לריצה יורדות ל-1.
   ⚠️ **הערכה** — המדידה תהיה בייצור.

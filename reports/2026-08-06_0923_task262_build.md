# TASK-262 — בידוד הטסטים ממצב חי

**נכתב 2026-08-06 09:23 Lima.** BUILD — `tests/` בלבד. **אין commit, אין push.**
לא נגעתי בקוד פרודקשן, לא ב-`.github/workflows/` (נשארים `M`), לא ב-Sheets/Drive/FINVIZ.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/systematic-debugging | `~/.claude/plugins/cache/.../systematic-debugging/SKILL.md` | 296 |
| superpowers/test-driven-development | `.../test-driven-development/SKILL.md` | 371 |
| superpowers/verification-before-completion | `.../verification-before-completion/SKILL.md` | 139 |

**גיבוי:** `/private/tmp/.../scratchpad/bak262/test_orchestrator_eod_borrow_wiring_v1.py.bak_20260806-092558`

---

## 1. הבסיס — שלוש ריצות

```
run 1: 7 passed in 40.84s
run 2: 7 passed in 12.14s
run 3: 7 passed in  6.67s
```

⚠️ **הכשל אינו משתחזר היום. שלוש הריצות ירוקות.** אתמול הן היו `2 failed` ואז `1 failed`.

**אבל הזמנים הם הממצא:** **40.84s → 12.14s → 6.67s** לשבעה טסטי-יחידה.
טסט יחידה ממוקק אינו לוקח 40 שניות, והדעיכה היא חתימה של **I/O חי שמתחמם**
(חיבור + קאש). **אי-הדטרמיניזם לא השתחזר, אבל הסיבה לו כן — והיא מדידה.**

זה שינה את צורת המשימה: אי-אפשר לכתוב RED שמשחזר את **הכשל**, אבל אפשר לכתוב RED
שמשחזר את **הסיבה**. וזה בדיוק מה שסעיף 4 בהוראה ביקש.

## 2. השורש — file:line

**המוקים שמוגדרים** (`test_orchestrator_eod_borrow_wiring_v1.py:33-35`):
```
P_ACCOUNT = "agent.orchestrator.build_account_state"          → חוסם את existing
P_BROKER  = "agent.execution.alpaca_broker.AlpacaBroker"      → חוסם את הברוקר
P_COLLECT = "agent.perception.borrow_collector.collect_borrow_data"  → חוסם את האוסף
```

**מה שעוקף אותם — שני אתרים ב-`agent/orchestrator_eod.py`:**
```
:54   import sheets_manager                              ← import משלו, namespace אחר
:57   ws = sheets_manager.get_worksheet("daily_snapshots")   ← קריאה חיה
:59   vals = ws.get_all_values()                             ← קריאה חיה
:65   scanned = borrow_collector.get_scanned_universe(df)
:70   universe = scanned | existing                          ← ה-union
...
:95   cov = borrow_collector.collect_borrow_coverage(universe)
```

**⚠️ `:95` הוא הממצא החמור.** `P_COLLECT` מפטץ' את `collect_borrow_data` —
**פונקציה אחרת**. `collect_borrow_coverage` **אינה מפוטצ'ת כלל**, והיא קוראת
`borrow_data` **וכותבת** ל-`borrow_coverage`.

**הראיה מפלט ה-RED — שלושה טאבים ו-append:**
```
Called 3 times.
Calls: [call('daily_snapshots'), call().get_all_values(), ...
        call('borrow_data'),
        call('borrow_coverage'),
        call().append_rows([['2026-08-06', '09:26:34', 1, 0, 0.0, 0, 0.0, 'ALPACA']],
                           value_input_option='USER_ENTERED')]
```
**הטסטים לא רק קראו מ-Sheets חי — הם ניסו לכתוב שורה ל-`borrow_coverage`.**
⚠️ **לא אימתתי שהכתיבה אכן נחתה בגיליון** — ההוראה אוסרת לגעת ב-Sheets.
**החתימה לחיפוש: שורות `borrow_coverage` עם `ScannedUniverse` בין 1 ל-3** — הפיקסצ'רים
משתמשים ב-1-3 טיקרים, בעוד הייצור מדד 2-5. **דורש אימות נפרד.**

**מאיפה הגיעו INLF/SHPH/YXT/ZYBT:** מ-`daily_snapshots` **החי** דרך `:57`,
דרך `get_scanned_universe` ב-`:65`, ואז ב-`union` ב-`:70`.
**ולמה זה עובר היום:** `daily_snapshots` של 06/08 עדיין ריק — `is_snapshot_time`
(`auto_scanner.py:77`) כותב ליד הנעילה, והריצה הייתה ב-09:23 ET.
**כלומר ההצלחה/כישלון תלויים בשעון, לא בקוד.**

### אימות טענת ה-PK מ-3/8
> *"7 טסטי-יחידה קוראים Sheets חי דרך `collect_borrow_snapshot:57`"*
```
$ sed -n '57p' agent/orchestrator_eod.py
        ws = sheets_manager.get_worksheet("daily_snapshots")
```
**השורה לא זזה — `:57` מדויק היום.** ומספר הטסטים בקובץ הוא **7**, כפי שנטען.
✅ **הטענה מאומתת במלואה.**

## 3. ההיקף — 23 קבצי טסט נוגעים בשכבה החיה
```
tests/agent/integration/test_decision_logger_writes.py      tests/test_account_state_v1.py
tests/agent/integration/test_scanner_agent_match.py         tests/test_am_credential_pref_v1.py
tests/agent/unit/test_borrow_collector_v1.py                tests/test_backfill_netpnl_v1.py
tests/agent/unit/test_create_agent_sheets_dryrun_v1.py      tests/test_measure_429_v1.py
tests/agent/unit/test_decision_logger.py                    tests/test_news_detective_disable_v1.py
tests/agent/unit/test_monthly_worksheet_seam.py             tests/test_position_manager_cached_reader_v1.py
tests/agent/unit/test_orchestrator.py                       tests/test_reconciler_autorepair_v1.py
tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py  tests/test_scanner_timeline_cache_v1.py
tests/agent/unit/test_write_monthly.py                      tests/test_sheets_schema_contract_v1.py
tests/agent/unit/test_write_weekly.py                       tests/test_task172_coverage_v1.py
tests/overnight/test_core_unsafe.py                         tests/test_task215_as_credential_pref_v1.py
                                                            tests/test_trading_days_holiday_v1.py
```
⚠️ **הבעיה רחבה מ-262.** ⚠️ **לא בדקתי לכל אחד אם ה-import מפוטץ' או חי** — זו בדיקה
של 23 קבצים שחורגת מהיקף ה-BUILD הזה. **רשמתי, לא הרחבתי.** זה בדיוק תחום **TASK-250**.

## 4. RED
```
$ pytest ...::test_collect_borrow_snapshot_never_touches_live_sheets -q
E   AssertionError: Expected 'get_worksheet' to not have been called. Called 3 times.
1 failed in 0.26s
```
**נכשל מהסיבה הנכונה** — הקריאה החיה מתרחשת בפועל.

## 5. GREEN + ⚠️ מה שלא הצלחתי

**התיקון: fixture `autouse` שסוגר את הענף לכל טסט בקובץ.**
`get_worksheet` מוחזר `None` ⇒ `if ws is not None` מדלג, ו-`collect_borrow_coverage`
נופל ל-`try/except` שהעוזר כבר מטפל בו כ-non-fatal. `autouse` — כדי שטסט עתידי לא ישכח.

⚠️ **מה שלא הצלחתי, ואני מדווח ולא מטייח:**
הטסט שכתבתי מקבע `get_worksheet.assert_not_called()`. **הוא לא יכול לעבור** —
הקוד ב-`:57` וב-`:95` **קורא** ל-Sheets ללא תנאי. תיקון ב-`tests/` יכול **לנטרל**
את הקריאות, לא **להסיר** אותן, וה-BUILD הזה מוגבל ל-`tests/`.

**לא החלשתי את הטסט ולא מחקתי אותו.** סימנתי `@pytest.mark.xfail(strict=True)`
עם נימוק שמצביע על שורות הפרודקשן. `strict=True` אומר שהוא **ייכשל בקול**
ביום שבו מישהו יתקן את הפרודקשן — כלומר הוא לא יירקב.

**זו הכרעה שלך:** האם התיקון האמיתי (הזרקת מקור-הנתונים ב-`orchestrator_eod`)
נפתח כתיק נפרד, או שה-xfail מספיק.

## 6. אימות

```
=== חמש ריצות רצופות ===
run 1: 7 passed, 1 xfailed in 0.26s
run 2: 7 passed, 1 xfailed in 0.26s
run 3: 7 passed, 1 xfailed in 0.26s
run 4: 7 passed, 1 xfailed in 0.26s
run 5: 7 passed, 1 xfailed in 0.26s
```
**זהה בכל החמש.** והזמן: **40.84s → 0.26s, פי 157** — ההוכחה שה-I/O החי נעלם.

```
=== הסוויטה המלאה ===
לפני (אתמול):  2 failed, 727 passed  →  ואז  1 failed, 729 passed
אחרי:          730 passed, 3 deselected, 1 xfailed in 13.42s
```
⚠️ **אפס כשלים בסוויטה כולה.** גם משך הסוויטה ירד מ-~33-40s ל-**13.42s**.

```
$ git status --porcelain | grep -E '\.py$' | grep -v tests/
CLEAN: no production code touched
```

## ה-diff

```diff
diff --git a/tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py b/tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py
index 1113368..9a8e29e 100644
--- a/tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py
+++ b/tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py
@@ -33,6 +33,11 @@ from agent import orchestrator_eod as eod
 P_ACCOUNT = "agent.orchestrator.build_account_state"
 P_BROKER = "agent.execution.alpaca_broker.AlpacaBroker"
 P_COLLECT = "agent.perception.borrow_collector.collect_borrow_data"
+# TASK-262: collect_borrow_snapshot does its OWN `import sheets_manager`
+# (orchestrator_eod.py:54) and reads daily_snapshots at :57. Patching the module
+# object is not enough — the helper looks the name up on the sheets_manager
+# module itself at call time, so the patch target is the function there.
+P_SHEETS = "sheets_manager.get_worksheet"
 
 
 def _state(tickers):
@@ -40,6 +45,26 @@ def _state(tickers):
     return {"existing_positions": set(tickers)}
 
 
+@pytest.fixture(autouse=True)
+def _no_live_sheets():
+    """TASK-262: close the live-Sheets branch for EVERY test in this file.
+
+    Two call sites in collect_borrow_snapshot were never patched:
+      orchestrator_eod.py:57  get_worksheet("daily_snapshots") + get_all_values()  READ
+      orchestrator_eod.py:95  collect_borrow_coverage(universe) -> borrow_data READ
+                              + borrow_coverage append_rows                        WRITE
+
+    P_COLLECT patches collect_borrow_data, a DIFFERENT function, so the coverage
+    write at :95 ran unpatched. Returning None makes get_worksheet fail closed:
+    the snapshots branch skips (`if ws is not None`) and the coverage branch
+    raises into its own try/except, which the helper already treats as non-fatal.
+
+    autouse, so a test added later cannot forget it.
+    """
+    with patch(P_SHEETS, return_value=None) as gw:
+        yield gw
+
+
 # ─────────────── 1. ticker source: open positions + today ENTERs, deduped ───────────────
 
 def test_collects_unified_deduped_tickers_and_passes_to_collector():
@@ -142,3 +167,49 @@ def test_no_tickers_skips_collector_and_does_not_fail():
         eod.collect_borrow_snapshot(summary)   # must not raise
     collect.assert_not_called()
     assert summary["errors"] == 0
+
+
+# ─────────── 8. TASK-262: the helper must never reach the live Sheets layer ───────────
+
+@pytest.mark.xfail(
+    strict=True,
+    reason=(
+        "TASK-262: PRODUCTION DEFECT, not a test defect. collect_borrow_snapshot "
+        "reaches the Sheets layer unconditionally at orchestrator_eod.py:57 "
+        "(daily_snapshots) and :95 (collect_borrow_coverage -> borrow_data read + "
+        "borrow_coverage append_rows). A tests-only change can neutralise those "
+        "calls (see the _no_live_sheets fixture) but cannot remove them, and this "
+        "BUILD was scoped to tests/ only. strict=True so this flips to a failure "
+        "the day production stops calling out, instead of rotting silently."
+    ),
+)
+def test_collect_borrow_snapshot_never_touches_live_sheets():
+    """The docstring at the top of this file claims "zero real API/Sheets". It was
+    not true.
+
+    collect_borrow_snapshot builds its universe from TWO sources and unions them
+    (agent/orchestrator_eod.py:70):
+
+        existing = build_account_state()["existing_positions"]   ← patched by P_ACCOUNT
+        scanned  = daily_snapshots via sheets_manager            ← NOT patched, went LIVE
+
+    The second branch (orchestrator_eod.py:54-65) does its own `import
+    sheets_manager` and calls get_worksheet("daily_snapshots") + get_all_values().
+    Patching agent.perception.borrow_collector.sheets_manager (test 3) does not
+    cover it — that is a different module namespace.
+
+    Consequence, measured 2026-08-05: tests 1 and 7 picked up the live tickers of
+    the day (INLF, SHPH, YXT, ZYBT) on top of the fixture, and the file's result
+    moved from 2 failures to 1 within an hour with no code change. On 2026-08-06
+    it passed three times in a row — because daily_snapshots is still empty that
+    early in the session. Pass/fail therefore depends on the clock, not the code.
+
+    This test pins the invariant directly instead of asserting on the symptom.
+    """
+    summary = {"errors": 0}
+    with patch(P_ACCOUNT, return_value=_state(["AAA"])), \
+         patch(P_BROKER), \
+         patch(P_COLLECT), \
+         patch(P_SHEETS) as get_worksheet:      # shadows the autouse fixture's patch
+        eod.collect_borrow_snapshot(summary)
+    get_worksheet.assert_not_called()
```

---

## מה לא כוסה

1. **האם שורות `borrow_coverage` מטסטים נחתו בגיליון החי.** ⚠️ לא ניתן לבדוק —
   Sheets אסור בתור הזה. החתימה: `ScannedUniverse` בין 1 ל-3.
2. **22 קבצי הטסט האחרים** — נסרקו לשמות בלבד, לא נבדקו פרטנית. תחום TASK-250.
3. **`conftest.py` ברמת `tests/`** שיפיל כל טסט שפותח לקוח Sheets אמיתי — נשקל,
   **לא יושם**: רדיוס הפגיעה הוא 23 קבצים, וזה חורג מ-BUILD ל-`tests/` של תיק אחד.
4. **התיקון האמיתי בפרודקשן** — הזרקת מקור-הנתונים ל-`collect_borrow_snapshot`.
   נחסם ע"י היקף ההוראה.
5. **האם הכשל של אתמול היה אך ורק מהענף הזה.** הראיה חזקה (הטיקרים תואמים בדיוק),
   אבל **לא שחזרתי את הכשל** — הוא לא ניתן לשחזור היום.

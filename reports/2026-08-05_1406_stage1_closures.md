# מעבר סגירה — שלב 1 · בדיקה בקריאה בלבד

**נכתב 2026-08-05 14:06 Lima.** READ-ONLY. **לא סגרתי אף תיק, לא שיניתי אף סטטוס.**
לא שיניתי קובץ, לא הרצתי pytest, לא נגעתי ב-Sheets/Drive/FINVIZ, אין commit, אין push.
הכתיבה היחידה: הדוח הזה + שורה ב-`reports/INDEX.md`. **כל הכרעה כאן היא הצעה. ההכרעה של עמיחי.**

HEAD: `eb1b026`. PK חי (נקרא בשקט לפי mtime, לא מודפס): `docs/RidingHigh_Pro_PK_v2.md`.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

---

## 1 · TASK-179 — Validate crossover-short on hold-out

### הגוף המלא
```
Status: ○ To Do | Priority: High | Created: 2026-06-13 01:27 | Labels: TASK-171, crossover-short

Description:
171-B3. Validation of the pre-registered crossover-short: hold-out on NEW data only (post
pre-registration), worst-case HTB borrow model (from TASK-172 data), slippage 2x, fitness
locked = net expectancy. Power: needs >=150 crossover events (~450 RH rows, ~4-5 months at
current capture rate — see power_analysis.md). BLOCKED ON: TASK-172 + TASK-177 + pre-registration task.

Acceptance Criteria:
- [ ] #1 Validation runs ONLY on data collected after pre-registration (no reuse of the n=62 discovery sample)
- [ ] #2 Verdict at n>=150 events: edge survives worst-case costs or hypothesis rejected — either outcome documented
```

### הבדיקה — פלט גולמי
```
TASK-172  Status: ✔ Done   (Updated: 2026-06-23 01:14)
TASK-177  Status: ✔ Done   (Updated: 2026-06-23 00:36)
TASK-178  Status: ✔ Done   (Updated: 2026-06-23 17:55)   ← ה-"pre-registration task"
```

`docs/HYPOTHESES.md` §D, שורות 97–101, אומר את אותו הדבר במפורש:
```
> REGISTERED 2026-06-23 (TASK-178). ... Dependencies
> cleared at registration: TASK-172 (coverage) ✅, TASK-177 (D1-D25 superset) ✅,
> and PHASE 0 data-integrity blockers (TASK-180/150/105/144) all ✅. Validation
> (TASK-179) runs only on crossover events detected AFTER this date.
```
ושורות 167–171 חוזרות: `Dependencies (AC#2 — all cleared at registration)`.

**חיפוש אחר n — הממצא המרכזי, שלא היה בתיק:**
```
$ grep -rniI "crossover" --include="*.py" .   (ללא backups/ project_sync/ research/)
config.py:155:#   the HYP-001 crossover-short hold-window, re-anchored to the drop-event downstream (179).
post_analysis_collector.py:210:    for the HYP-001 crossover-short hold, re-anchored to the drop-event downstream.

$ grep -rniI "dropslab|drops_post" --include="*.py" .
config.py:301                     ← CHRONIC_DROPPER_BLACKLIST, הערה בלבד
formulas.py:353                   ← הערה בלבד
agent/trader/decision_logic.py:396 ← הערה בלבד

$ grep -rliI "crossover" research/
(ריק)

$ find . -maxdepth 2 -iname "*dropslab*"
./research/2026-05-26_dropslab_xref
./research/2026-05-26_runup_disable_dropslab      ← שניהם מאי, לפני הרישום
```

**המשמעות:** בכל קוד הפרודקשן החי אין ולו שורת-קוד אחת שמזהה crossover event, לא ספירה,
לא טאב, לא ארטיפקט מחקר. שתי ההופעות היחידות הן **הערות** על חלון-האחזקה. אין מודול
DropsLab בריפו הזה בכלל.

### ההכרעה המוצעת: **REWORD** — וההערכה שלך עמדה, אבל החוסם האמיתי חמור יותר ממה שהנחת

שורת ה-`BLOCKED ON` שגויה — שלושת התיקים Done מ-23/6, וזה מתועד ב-HYPOTHESES §D עצמו.
אבל החלופה אינה "רק צריך לחכות ל-n". **n אינו נמוך — הוא לא נמדד ולא ניתן למדידה היום
בריפו הזה,** כי אין גלאי crossover. הפער בין "אין נתונים מספיקים" ל"אין מנגנון איסוף"
הוא הפער בין המתנה לבין עבודת-בנייה.

### הניסוח המדויק המוצע — לא הוחל

להחליף את המשפט האחרון ב-Description:
```
BLOCKED ON: TASK-172 + TASK-177 + pre-registration task.
```
ב:
```
DEPENDENCIES CLEARED 2026-06-23: TASK-172 ✔ Done, TASK-177 ✔ Done, TASK-178 ✔ Done
(pre-registration) — recorded in docs/HYPOTHESES.md §D lines 97-101 and 167-171.

REAL BLOCKER (verified 2026-08-05): there is no crossover-event detector in the live
codebase. A grep for "crossover" across all live .py returns exactly two COMMENT lines
(config.py:155, post_analysis_collector.py:210) and zero executable code; a grep for
"dropslab" returns three comment lines and no module; research/ holds no crossover
artifact. n is therefore not "below 150" — it is unmeasured, and cannot be measured
until an RH-scan x DropsLab-drop join (<=10 calendar days) is built and starts
accumulating events dated after 2026-06-23. Building that detector is prerequisite work
that no ticket currently owns.
```

---

## 2 · TASK-226 — Active alert on scanner-writes=0

### הגוף המלא
```
Status: ○ To Do | Priority: Medium | Created: 2026-07-04 01:49

Description:
E2E-audit S3 (3/7): a scanner run that produces 0 rows during market hours is SILENT —
auto_scanner.py:65 'No results' path prints and continues; the only detector is sentinel
scan_freshness (WARN>=3m/BLOCK>=5m, checks/scan_freshness.py:8-9) which in shadow mode only
logs; nothing alerts until nightly health_audit. Write-EXCEPTIONS are already loud
(safe_append_rows raise). Needed: an active alert (system_events/email) when timeline_live
gets 0 new rows for N consecutive market-hours minutes. Natural home: unify with TASK-166
(daily lineage sentinel). Evidence: plans/stateless-seeking-sifakis.md S3.

Acceptance Criteria: No acceptance criteria defined
```

### הבדיקה — פלט גולמי

**TASK-166 — Done ב-2026-08-04:**
```
Task TASK-166 - Daily lineage sentinel — recompute one random row/day as a health check
Status: ✔ Done | Updated: 2026-08-04 00:51
Description: Daily health check: recompute one random settled post_analysis row end-to-end
and flag if stored values drift from a fresh recompute — catches silent pipeline/data corruption.
```

**`health_audit.py:1339` — check_30 במלואה:**
```python
def check_30_lineage_sentinel(gc):
    """Q: recompute ONE random settled post_analysis row end-to-end via the
    calculate_stats SSoT and WARN if any stored field drifted — catches silent
    pipeline/data corruption (TASK-166). Drift -> WARNING (advisory, no CI fail);
    skip / load / row-prep error -> INFO."""
    if gc is None:                      return CheckResult(... INFO, "Skipped (no Sheets access)")
    try:    df = load_post_analysis_all_months()
    except: return CheckResult(... INFO, f"Could not load post_analysis: {e}")
    if df is None or len(df) == 0:      return CheckResult(... INFO, "post_analysis is empty")
    settled = [r for _, r in df.iterrows() if _row_is_settled(r)]
    if not settled:                     return CheckResult(... INFO, "No settled rows to audit")
    row = random.choice(settled)
    ...
    return _lineage_compare_result(ticker, scan_date, scan_price, ohlc, stored)
```

**`auto_scanner.py:418-427` — מסלול ה-0 שורות:**
```python
    if not results:
        print("❌ No results — still running portfolio_live / live_trades update")
        try:
            gc2 = get_gsheets_client()
            if gc2:
                update_portfolio_live(gc2, now_peru)
                update_live_trades(gc2, now_peru, results=[])
        except Exception as e:
            print(f"⚠️ no-results path error: {e}")
        return
```

**חיפוש בדיקת ספירת-שורות ב-health_audit:**
```
$ grep -niI "row count|rows_today|0 rows|zero row|no rows" health_audit.py
1133: """Helper: load last 200 rows of timeline_live for metric sanity checks."""
1139: return None, None, "Not enough data to evaluate (< 50 rows)"
1172: ... 1194: ...      ← שלושתן על REL_VOL/Float% variance, לא על ספירה
```

### התשובה החדה לשאלה ששאלת

**מה בדיוק TASK-226 מבקש ש-check_30 לא עושה:**
check_30 בודקת **דיוק של שורה קיימת אחת** — היא בוחרת שורה אקראית מ-`post_analysis`,
מחשבת אותה מחדש, ומשווה. אם `post_analysis` **ריקה לגמרי** היא מחזירה `INFO` ("post_analysis
is empty") — כלומר **אפס שורות זו לא התרעה אצלה, זו סיבה לדלג**.

- **האם היא מתריעה על אפס שורות בשעות שוק? לא.** היא לא רצה בשעות שוק כלל (health_audit
  רץ 06:00/15:30/22:00), היא קוראת `post_analysis` ולא `timeline_live`, וריק אצלה = INFO.
- **האם היא רק lineage-drift? כן, בדיוק.** ה-docstring אומר "recompute ONE random settled
  post_analysis row ... WARN if any stored field drifted".

**מסקנה: 226 אינו מכוסה — אפילו לא חלקית.** "Natural home: unify with TASK-166" הוא פרמיסה
שגויה, ו-166 כבר Done. הקרבה האמיתית היא ל-**TASK-252 בדיקה שלישית**, בציטוט מגוף 252:
> *"Check three, rows written rather than a date being present. Compare today's row count
> against the trailing five day average. WARN on a deviation over fifty percent. This is
> the one that would have caught the scan volume drop of TASK-245 on the day it happened."*

אבל גם היא **לא מכסה את 226**: היא לילית, על `post_analysis`, מול ממוצע-5-ימים.
226 מבקש **תוך-יומי**, על `timeline_live`, אחרי N דקות רצופות של אפס.

### ההכרעה המוצעת: **REWORD** — לא לסגור, ולא למזג ל-166

ההערכה שלך ("לצמצם למה שלא מכוסה, או לסגור אם מכוסה") — הענף הנכון הוא הצמצום.
**כלום לא מכוסה**, אבל שורת ה"בית הטבעי" צריכה להשתנות מ-166 ל-252.

### הניסוח המדויק המוצע — לא הוחל

להחליף:
```
Natural home: unify with TASK-166 (daily lineage sentinel).
```
ב:
```
NOT covered by TASK-166 (verified 2026-08-05, 166 is Done). check_30_lineage_sentinel
(health_audit.py:1339) recomputes ONE random settled post_analysis row and WARNs on field
drift; an empty sheet returns INFO "post_analysis is empty", i.e. zero rows is a reason to
SKIP, not to alert. It never reads timeline_live and never runs during market hours
(health_audit fires 06:00 / 15:30 / 22:00 Peru).

Nearest overlap is TASK-252 check three (today's row count vs trailing 5-day average) —
but that is nightly, on post_analysis, and threshold-based. This ticket is the intraday
half: timeline_live, N consecutive market-hours minutes at zero, alert to system_events +
email. Keep both; sequence 252 first if only one gets built.
```

---

## 3 · TASK-254 — score_analytics reports n=0

### הגוף המלא (תמצית מדויקת; הגוף המלא מצוטט בדוח 1055_tasks_full_b)
```
Status: ○ To Do | Priority: Low | Created: 2026-08-03 22:08 | Labels: investigation, analytics

OBSERVED: score_analytics tab empty for 2026-07 and 2026-08. orchestrator_eod.py 170-182 runs
ScoreAnalytics().run_daily() every evening; EOD log of 2026-07-30 printed
"Daily analytics: tier=INSUFFICIENT, n=0". score_analytics.py writes nothing when tier is
INSUFFICIENT (fewer than ten trades).
THE PART THAT IS NOT EXPLAINED: n was zero, not merely below ten. July paper_portfolio held
sixty DRY_RUN_CLOSED positions.
NOT VERIFIED: where run_daily reads its sample from. The source was not traced.
WHY IT IS LOW: nothing downstream consumes score_analytics (config.py:351, frozen/diagnostic).
Related: pending_suggestions and config_history also empty and genuinely have no writer;
both added in 1092a95; dashboard.py:4307 documents config_history as having no writer.
```

### הבדיקה — פלט גולמי

**`agent/orchestrator_eod.py:170-182` ו-:190-192:**
```python
    # 2. Daily analytics
    try:
        from agent.analytics.score_analytics import ScoreAnalytics
        analytics = ScoreAnalytics()            # ← :173, אפס ארגומנטים
        result = analytics.run_daily()
        ...
    # 3. Weekly (Saturday only — weekday 5)
    if now.weekday() == 5:
            analytics = ScoreAnalytics()        # ← :191, שוב אפס ארגומנטים
            result = analytics.run_weekly()
```

**`agent/analytics/score_analytics.py:61-75` — הקונסטרקטור:**
```python
    def __init__(self, postmortem_reader=None, analytics_writer=None, suggestion_writer=None):
        self._postmortem_reader = postmortem_reader
        self._analytics_writer  = analytics_writer
        self._suggestion_writer = suggestion_writer
```

**:129-132 — נקודת ה-n=0:**
```python
    def _load_postmortems(self, start_date: str, end_date: str) -> pd.DataFrame:
        """Load postmortems within date range. Returns DataFrame."""
        if not self._postmortem_reader:
            return pd.DataFrame()          # ← DataFrame ריק, ללא כל תלות בדאטה
```

**:215 ו-:226 — נקודת הכתיבה:**
```python
        if self._analytics_writer:
                self._analytics_writer(analytics_row)
                if self._suggestion_writer:
                        self._suggestion_writer(sugg)
```

**כל האתחולים של ScoreAnalytics בקוד:**
```
tests/agent/unit/test_score_analytics.py:90,103,109,115,121,138,176
agent/orchestrator_eod.py:173     ← ScoreAnalytics()
agent/orchestrator_eod.py:191     ← ScoreAnalytics()
```
**אין שום אתחול בקוד חי שמעביר ולו callable אחד.**

**כותבים ל-pending_suggestions / config_history:**
```
$ grep -rniI "pending_suggestions" --include="*.py" .
agent/setup/create_agent_sheets.py:45,151   ← יצירת הטאב + כותרות
agent/dashboard/_data_loaders.py:161-175    ← load_pending_suggestions  (קריאה)
agent/dashboard/_data_loaders.py:194        ← update_suggestion_status  (עדכון בלבד)
agent/dashboard/score_brain_page.py:30,78,202,204  ← תצוגה
apply_text_format_v1.py:43 · dashboard.py:4305 · score_analytics.py:8,59  ← עיצוב/תיעוד

$ grep -rniI "config_history" --include="*.py" .
agent/setup/create_agent_sheets.py:46,164 · apply_text_format_v1.py:43 · dashboard.py:4307
```
`update_suggestion_status` (`_data_loaders.py:179-216`) מוצא שורה קיימת ב-`ws.find()`
ומעדכן עמודות K:M. **הוא לא יוצר שורות.** אין יוצר.

### התשובות לשאלות ששאלת

**(א) האם השורש הוא באמת ש-ScoreAnalytics() נוצר בלי postmortem_reader?**
**כן — מאומת בקוד, שורה בשורה.** `orchestrator_eod.py:173` → `_postmortem_reader = None`
→ `_load_postmortems` מחזיר `pd.DataFrame()` ריק ב-`:131-132` → `n = len(postmortems) = 0`
→ tier=INSUFFICIENT. **n=0 מבני, לא תלוי-דאטה.** אילו יולי היה סוגר 600 פוזיציות במקום 60,
n עדיין היה 0.

**(ב) שני ממצאים שלא היו בתיק:**

1. **`_analytics_writer` גם הוא None.** גם אילו היו מזריקים reader — `:215` `if self._analytics_writer:`
   נכשל, ושום דבר לא ייכתב. כלומר **"הזרקת שורה אחת" אינה מספיקה: צריך שתי הזרקות**
   (reader + writer), ושלוש אם רוצים גם pending_suggestions.
2. **`run_daily` מסנן `ExitDate == today` בדיוק** (`:91` start=end=date, `:143`
   `df[(df["ExitDate"] >= start_date) & (df["ExitDate"] <= end_date)]`). כלומר גם עם reader
   תקין, ה-n היומי הוא רק מה שנסגר **באותו יום** — כמעט תמיד <10, כלומר INSUFFICIENT ואפס
   כתיבה בכל מקרה. 60 הסגירות של יולי מעולם לא היו מגיעות ליום אחד.

**(ג) האם ההזרקה שינוי בטוח, ומה היא מפעילה שלא רץ היום:**
היא **לא** ניטרלית. היא פותחת שני מסלולי-כתיבה חדשים ב-EOD לטאבים שמעולם לא נכתבו:
`score_analytics` (יומי) ו-`pending_suggestions` (שבתות, `generate_suggestions=True` ב-`:122`).
זה מוסיף קריאות/כתיבות Sheets בחלון ה-EOD — אותו לחץ quota שהוא השורש הדומיננטי של המערכת —
כדי להפעיל מודול שה-docstring שלו מגדיר כתצפיתי-בלבד ו-`config.py:351` מחריג במכוון.
**ובעיקר:** זה מפעיל מודול שכל תכליתו לנתח **Score**, בזמן ש-TASK-208/209 מנסים לפרק את
ה-Score מהמערכת. זו הליכה לכיוון ההפוך.

### ההכרעה המוצעת: **REWORD ל-"answered, folded into 208/209"** — לא CLOSE נקי ולא תיקון

ההערכה שלך ("או לסגור, או להמיר לתיק תיקון של שורה אחת") — **הענף השני נפל**: זה לא
שורה אחת אלא שתיים-שלוש, והוא סותר את 208/209. מה שנשאר הוא: השאלה נענתה במלואה, והמסקנה
המעשית היא לא לחווט אלא לתייק את הטאב לרשימת-הפרישה.

### הניסוח המדויק המוצע — לא הוחל

להוסיף בלוק Implementation Notes:
```
ANSWERED 2026-08-05 (read-only trace, no code change).

ROOT CAUSE — confirmed, structural:
  orchestrator_eod.py:173  analytics = ScoreAnalytics()      # no arguments
  orchestrator_eod.py:191  analytics = ScoreAnalytics()      # weekly, same
  score_analytics.py:61-75 __init__(postmortem_reader=None, analytics_writer=None,
                                    suggestion_writer=None)
  score_analytics.py:131   if not self._postmortem_reader: return pd.DataFrame()
n=0 is independent of the data. Sixty closed July positions could not have changed it.
No live call site anywhere passes a single callable; only tests do
(tests/agent/unit/test_score_analytics.py:90,138,176).

TWO FACTS THIS TICKET DID NOT HAVE:
1. _analytics_writer is None as well (score_analytics.py:215). Injecting only the reader
   still writes nothing. A working wiring needs reader + writer, and a third for
   pending_suggestions on Saturdays (:226).
2. run_daily filters ExitDate == today exactly (:91 start=end, :143 range filter). Even
   with a correct reader the daily n is only same-day closes, which is < 10 nearly always,
   so tier stays INSUFFICIENT and nothing is written regardless.

WHY NOT WIRE IT: wiring opens two new EOD Sheets write paths (score_analytics daily,
pending_suggestions weekly) to tabs never written, adding quota load in the EOD window, in
order to activate a Score-diagnostic module while TASK-208 and TASK-209 are dismantling
Score. Recommend folding the tab into the 208/209 retirement list rather than repairing it.

pending_suggestions and config_history CONFIRMED writer-less: create_agent_sheets.py
creates the tabs (:45,:46), the dashboard reads them, and _data_loaders.update_suggestion_status
(:179-216) only updates an existing row found by ws.find() — it never creates one.
```

---

## 4 · TASK-145 — agent_critic_monthly failure rate

### הגוף המלא
```
Status: ○ To Do | Priority: Low | Created: 2026-06-11 04:02 | Updated: 2026-06-16 01:18
Labels: TASK-139-INV

Description:
TASK-139-INV phase5: 7/32 failures since 11/5. Context found: 30/32 runs were manual
workflow_dispatch tests on 2/6 (TASK-60 verification) — verify failures cluster in those
tests vs real schedule runs; next scheduled run 1/7 must be watched. Evidence: phase5_evidence.md

Acceptance Criteria: No acceptance criteria defined
```

### הבדיקה — פלט גולמי
```
$ gh run list --workflow=agent_critic_monthly.yml --limit 100 --json conclusion,event | group_by(event)
schedule:          total=3   success=2   failure=1
workflow_dispatch: total=31  success=25  failure=6
```
פירוט מלא (34 ריצות — כל ההיסטוריה, ה-limit לא נחתך):
```
30691160541  2026-08-01T08:08:28Z  event=schedule           success   ← ✅
28507961766  2026-07-01T09:34:02Z  event=schedule           success   ← ✅
26834090196 … 26832528445  (31 ריצות)  2026-06-02T16:10→16:39Z  event=workflow_dispatch
                                        25 success / 6 failure
26751547387  2026-06-01T11:16:05Z  event=schedule           failure   ← הכשל המתוזמן היחיד
```
ששת הכשלים הידניים: `16:39:43 · 16:26:32 · 16:22:35 · 16:21:40 · 16:20:44 · 16:14:45` — **כולם ב-2026-06-02**.

### התשובות לשאלות ששאלת

- **כמה ריצות:** 34 (לא 32).
- **כמה נכשלו:** 7 (התיק אמר 7 — נכון).
- **כמה מהכשלים ידניים מול מתוזמנים:** **6 ידניים** (כולם ב-2/6) מול **1 מתוזמן** (1/6).
- **האם 1/7 ו-1/8 המתוזמנות הצליחו:** **כן, שתיהן `success`.**

ההשערה של התיק אושרה: 31 מתוך 34 היו burst ידני של 2/6 (התיק אמר 30/32 — סטייה של אחד,
אותה מסקנה), והכשלים מרוכזים שם. שתי הריצות המתוזמנות שאחרי התיקונים ירוקות.

### ההכרעה המוצעת: **CLOSE** — ההערכה שלך עמדה במלואה

התנאי שהתיק עצמו הציב ("next scheduled run 1/7 must be watched") התקיים, ונענה פעמיים:
1/7 ✅ ו-1/8 ✅. שיעור-הכשל 21.9% היה ארטיפקט של burst בדיקות ידני ביום אחד.

**הערה ליושרה:** לא פתחתי את הלוגים של ששת הכשלים הידניים כדי לדעת **מה** נכשל בהם.
ה-90 יום של retention פוקעים עליהם ב-~2026-08-31. אם רוצים את סיבת-הכשל לתיעוד —
זו ריצה נפרדת, ויש לה חלון.

### הניסוח המוצע לבלוק הסגירה — לא הוחל
```
CLOSED 2026-08-05 — question answered by the run history, no fix needed.

gh run list --workflow=agent_critic_monthly.yml, full history, 34 runs:
  schedule           total=3   success=2  failure=1
  workflow_dispatch  total=31  success=25 failure=6
The 31 dispatch runs are all 2026-06-02 16:10-16:39Z (the TASK-60 verification burst) and
carry 6 of the 7 failures. The single scheduled failure is 2026-06-01, before the fixes.
The watch item this ticket set — "next scheduled run 1/7 must be watched" — is answered
twice over: 2026-07-01 success and 2026-08-01 success. The 21.9 percent rate was an
artifact of one day of manual testing.

NOT DONE: the failure logs of the six 2026-06-02 runs were not opened, so the failure
cause is not recorded. GitHub log retention on them expires around 2026-08-31.
```

---

## 5 · TASK-251 — August live_trades points at its own file

### הגוף המלא (הפסקאות המהותיות)
```
Status: ○ To Do | Priority: Medium | Created: 2026-08-03 21:01 | Labels: data-integrity, sheets

State: for 2026-04, 05, 06, 07 and 09 the live_trades id equals the score_tracker id.
For 2026-08 it does not. August points at 1b0Vb..., score_tracker at 1_NC6....

Root cause is a section 10 violation, two lists of the same thing that disagree.
sheets_manager.SHEET_NAMES has nine entries and includes live_trades as a file of its own.
prepare_next_month.SHEET_NAMES has eight, omits it, carries the comment that live_trades will
be a tab inside score_tracker, and line 240 does created_ids['live_trades'] =
created_ids['score_tracker']. So the automatic convention is an alias.
scripts/fix_august_provisioning_v1.py line 305 iterates sheets_manager.SHEET_NAMES, so the
manual repair of 29/7 created a standalone August live_trades file.
...
Either way the two SHEET_NAMES lists must be unified and fix_august_provisioning_v1 needs a
guard that checks the existing convention before creating anything. That part belongs with TASK-243.

Related orphan, do not delete: the pre-created September live_trades 1l6j9c... was overwritten
by the 1/8 automatic run and is now unreferenced.
```

### הבדיקה — פלט גולמי

**שלושת העוגנים — כולם מאומתים מילה במילה:**
```
sheets_manager.py:38-48                    prepare_next_month.py:22-31
SHEET_NAMES = [                            SHEET_NAMES = [
    "timeline_live",                           "timeline_live",
    "daily_snapshots",                         "daily_snapshots",
    "daily_summary",                           "daily_summary",
    "post_analysis",                           "post_analysis",
    "portfolio",                               "portfolio",
    "portfolio_live",                          "portfolio_live",
    "score_tracker",                           "score_tracker",  # live_trades יהיה כ-tab נוסף בתוכו
    "live_trades",       ← 9 פריטים            "ticker_follow_up",
    "ticker_follow_up",                    ]   ← 8 פריטים, live_trades חסר
]

prepare_next_month.py:235-240
    print(f"\n📋 טיפול ב-live_trades tab:")
    if add_live_trades_tab(gc, created_ids['score_tracker']):
        print(f"   ✅ הוסף tab 'live_trades' ל-score_tracker")
    else:
        print(f"   🔗 tab 'live_trades' כבר קיים")
    created_ids['live_trades'] = created_ids['score_tracker']      ← :240, ה-alias

scripts/fix_august_provisioning_v1.py:305
        for name in sheets_manager.SHEET_NAMES:                     ← הרשימה בת 9
```

**⚠️ ממצא חדש — טענה עובדתית בתיק שאינה נכונה.**
`sheets_config.json` הוא קובץ ריפו מקומי, לא Sheets — נקרא בלי שום קריאת API:
```
2026-04  live_trades=1shVEIrA..  score_tracker=1shVEIrA..  ALIAS
2026-05  live_trades=1tQg2Le9..  score_tracker=1tQg2Le9..  ALIAS
2026-06  live_trades=1MQwNUeh..  score_tracker=1MQwNUeh..  ALIAS
2026-07  live_trades=1_lhYPO6..  score_tracker=1_lhYPO6..  ALIAS
2026-08  live_trades=1b0VbCM1..  score_tracker=1_NC6LOT..  STANDALONE
2026-09  live_trades=1l6j9cXz..  score_tracker=1ca4J5tI..  STANDALONE   ← לא ALIAS
```
התיק אומר *"for 2026-04, 05, 06, 07 **and 09** the live_trades id equals the score_tracker id"*.
**ספטמבר גם הוא standalone.** וזה לא שינוי שקרה אחרי כתיבת התיק:
```
$ git log -6 --format='%h %ad %s' --date=short -- sheets_config.json
dd38543 2026-07-29 feat(sheets): TASK-242 — pre-create 2026-09 in the canonical root
7d9c896 2026-07-29 fix(sheets): TASK-242 — 2026-08 provisioning repaired before 1/8 rotation
2a67aa5 2026-07-01 Monthly rotation: add sheets for 2026-08
...
$ git diff --quiet HEAD -- sheets_config.json   →  IDENTICAL to HEAD

$ git show dd38543:sheets_config.json  (המצב ב-29/7, לפני שהתיק נכתב ב-3/8)
2026-07  ALIAS      2026-08  STANDALONE      2026-09  STANDALONE
```
הקובץ לא השתנה מאז 29/7. כלומר ספטמבר היה standalone כבר **כשהתיק נכתב**.
נובע מכך גם: ה"יתום" `1l6j9c...` שהתיק אומר עליו *"is now unreferenced"* — **עדיין מופנה
אליו** מ-`sheets_config.json` כ-`2026-09.live_trades`.

**עוד:** הרוטציה האוטומטית של 1/8 **לא עשתה commit** ל-`sheets_config.json` — הקומיט האחרון
הוא 29/7. זה עקבי עם TASK-243 ("2026-09 pre-created so the 1/8 rotation short-circuits on
`_already_done`"), אבל אומר שהתמונה הנוכחית היא תוצר של התיקון הידני, לא של הרוטציה.

**TASK-243 — מה כבר בהיקפו:**
```
Status: ○ To Do | Priority: Medium | Labels: bug, infra
Real fixes: (1) _get_root_folder_id resolves ROOT_FOLDER_ID through the shared SA ... 404 ...
(2) assert_correct_root(ROOT_FOLDER_ID) compares a constant to itself and catches nothing.
(3) The duplicate-folder guard runs after prepare_next_month itself created the folder ...
(4) check_28 only inspects the active month ...
(5) Empty orphan folders 2026-07 and 2026-08 remain under RidingHigh-Data ...
```
243 עוסק ב**רזולוציית-שורש ובגארדים של הרוטציה**. איחוד שתי רשימות ה-SHEET_NAMES אינו
מופיע בו כרגע באף אחד מחמשת הסעיפים — כלומר ההעברה ל-243 דורשת גם **הוספה מפורשת ל-243**,
לא רק הסרה מ-251.

### מה נשאר ב-251 אחרי שהשורש עובר ל-243

| נשאר ב-251 | עבר / כבר לא רלוונטי |
|---|---|
| **הכרעת יישור לחודשים הלא-מיושרים** — אוגוסט **וספטמבר**. זו כתיבה לגיליון חי לשם עקביות בלבד; הכרעת בעלים. | איחוד שתי רשימות SHEET_NAMES → 243 (ודורש הוספה מפורשת שם) |
| **תיעוד החריגה** כדי שסקריפט מחקר חוצה-חודשים לא ייכשל בשקט | גארד ל-`fix_august_provisioning_v1` → 243 |
| **`archive_live_trades`** — ארכיוני אוגוסט/ספטמבר ינחתו בקובץ אחר מכל שאר החודשים | "האם קיים tab live_trades בתוך 1b0Vb" — Sheets, לא נבדק |
| תיקון הטענה על 09 ועל היתום | — |

### ההכרעה המוצעת: **REWORD** — ההערכה שלך עמדה, בתוספת תיקון עובדתי

לא מיזוג מלא ל-243 (כי 243 לא מכיל את סעיף ה-SHEET_NAMES), אלא: לתקן את העובדה, לצמצם
את 251 להכרעת-היישור, ולהוסיף ל-243 סעיף (6).

### הניסוח המדויק המוצע — לא הוחל

בלוק Implementation Notes ל-251:
```
CORRECTION + SCOPE CUT 2026-08-05 (read-only, from sheets_config.json in the repo — no API call).

FACTUAL CORRECTION. The Description says 2026-09 aliases score_tracker. It does not.
  2026-04 1shVEIrA / 1shVEIrA  ALIAS      2026-08 1b0VbCM1 / 1_NC6LOT  STANDALONE
  2026-05 1tQg2Le9 / 1tQg2Le9  ALIAS      2026-09 1l6j9cXz / 1ca4J5tI  STANDALONE
  2026-06 1MQwNUeh / 1MQwNUeh  ALIAS
  2026-07 1_lhYPO6 / 1_lhYPO6  ALIAS
Two months diverge, not one. This was already true when the ticket was written: the last
commit touching sheets_config.json is dd38543 (2026-07-29) and the working tree is identical
to HEAD, so no change occurred between 2026-08-03 and today.

FOLLOWS FROM THAT: the September live_trades id 1l6j9cXz, described here as "overwritten by
the 1/8 automatic run and now unreferenced", is still referenced — it is what
sheets_config.json 2026-09.live_trades points at. Treat it as live config, not as an orphan,
until that is re-verified.

ALSO: the 1/8 rotation did not commit sheets_config.json at all (last commit 2026-07-29),
consistent with TASK-243's note that the pre-created September short-circuits the run on
_already_done. The current layout is the product of the 29/7 manual repair, not of rotation.

SCOPE. The section 10 root — sheets_manager.SHEET_NAMES (9, includes live_trades) vs
prepare_next_month.SHEET_NAMES (8, plus the :240 alias) vs fix_august_provisioning_v1.py:305
iterating the 9-list — moves to TASK-243. Note that TASK-243's five listed fixes do not
currently mention SHEET_NAMES, so this must be ADDED there as item (6), not merely
referenced.

WHAT REMAINS HERE: the owner decision on whether to realign 2026-08 and 2026-09 to the
score_tracker file. That is a write to a live month sheet for consistency alone. Also
remaining: archive_live_trades will place August and September archives in a different file
from every other month.
```

ותוספת ל-TASK-243:
```
(6) Unify the two SHEET_NAMES lists. sheets_manager.py:38-48 has nine entries and treats
live_trades as its own file; prepare_next_month.py:22-31 has eight, omits live_trades, and
line 240 aliases it to score_tracker. scripts/fix_august_provisioning_v1.py:305 iterates the
nine-entry list, which is how 2026-08 and 2026-09 ended up standalone. Whichever convention
wins, one list must be the source and the repair script must check the existing convention
before creating anything. Evidence and the per-month state are in TASK-251.
```

---

## 6 · TASK-10 — אימות בלבד

```
$ grep -m1 '^status:' "backlog/tasks/task-10 - P2.3-—-Filter-12-ticker_reputation.md"
status: Done

$ grep -c "RULING 2026-08-05 (עמיחי)" "backlog/tasks/task-10 - ....md"
1

$ git show HEAD~2:"backlog/tasks/task-10 - ....md" | grep -m1 '^status:'
status: Done
$ git show HEAD~2:"backlog/tasks/task-10 - ....md" | grep -c "RULING 2026-08-05"
1
```
**מאומת בשלוש רמות:** בדיסק, בגוף התיק, ובקומיט `1c2b54d` (HEAD~2) שכבר נדחף.
בלוק ה-RULING קיים במלואו — WON'T-DO, שלושה נימוקים, ההנמקה שנדחתה, ותנאי הפתיחה-מחדש.
**אין מה לעשות כאן.**

---

## 7 · סריקה נוספת — תיקים שהחוסם המוצהר שלהם כבר Done

### שיטה (ומגבלתה, במפורש)
סרקתי את **גוף כל 59 התיקים הפתוחים** אחרי ביטויי-חסימה
(`BLOCKED ON/BY · DEPENDS ON · gated on · pending TASK · requires TASK · חסום · תלוי ב · ממתין ל`)
והצלבתי כל `TASK-N` שמופיע איתם מול הסטטוס החי.
```
OPEN tasks containing a blocking keyword: 12
TASK-66  [To Do]  refs=[28,62,96]        still-open-refs=[]
TASK-101 [To Do]  refs=[93,94]           still-open-refs=[]      ← ★ חדש
TASK-128 [In Progress] refs=[194,200,201,203] still-open-refs=[194]
TASK-179 [To Do]  refs=[171,172,177]     still-open-refs=[]
TASK-194 [To Do]  refs=[128,208,209]     still-open-refs=[128,208,209]
TASK-225 [To Do]  refs=[]                still-open-refs=[]
TASK-234 [To Do]  refs=[186]             still-open-refs=[186]
TASK-240 [To Do]  refs=[238,245]         still-open-refs=[245]
TASK-241 [To Do]  refs=[238,244,246,247] still-open-refs=[244,246]   ← ★ חלקי
TASK-244 [To Do]  refs=[55,58,213,215]   still-open-refs=[215]       ← ★ שורה מיושנת
TASK-246 [To Do]  refs=[]                still-open-refs=[]
TASK-255 [To Do]  refs=[27,144,153,156,179] still-open-refs=[153,179]
```
```
$ frontmatter 'dependencies:' על תיקים פתוחים
(no OPEN task carries a non-empty frontmatter dependencies list)
```
**מגבלת השיטה:** הסריקה היא על **גופי התיקים בלבד**. שמונה מתוך 13 התיקים שזוהו קודם
(145, 153, 176, 202, 222, 226, 249, 250) **אינם מופיעים** בסריקה הזו — כי החסימה שלהם
נרשמה בשדה `מה חוסם` ב-`MASTER_TASK_LIST`, שאינו קיים בגוף התיק. שתי השיטות משלימות,
לא חופפות. אין כאן ספירה מלאה של "כל מה שחסום" — יש ספירה מלאה של "כל מה שכתוב בגוף התיק
שהוא חסום".

### שלושת הממצאים החדשים

**★ TASK-101 — החוסם ברמת ה-ID נוקה, אבל התנאי המהותי לא**
```
שורה 28: "NOT installed now — gated on TASK-93/94 auto-mode (attended until then = user is the review)."
TASK-93: status: Done
TASK-94: status: Done
```
שני התיקים Done. **אבל** התנאי שהמשפט מתאר אינו "התיקים ייסגרו" אלא "יופעל batch לילי
אוטונומי" — וזה לא קיים: `docs/POSTMORTEM_overnight_ARMED_2026-07-02.md` קיים בריפו,
ו-TASK-186 (הבונה) עדיין `In Progress`. **לא לסגור. הצעה: REWORD** — להחליף את
"gated on TASK-93/94" ב-"gated on an autonomous batch actually running (TASK-186, In Progress);
TASK-93 and TASK-94 are both Done and no longer gate this."

**★ TASK-241 — שניים משלושה חוסמים נוקו**
```
שורה 29: "BLOCKED ON: TASK-238 for the ticker fix, TASK-246 for the stuck positions,
          TASK-247 for the SKIP source."
TASK-238: Done    TASK-246: To Do    TASK-247: Done
```
**חוסם אמיתי אחד נשאר (246). לא לסגור.** הצעה: REWORD ל-
`"BLOCKED ON: TASK-246 (stuck positions) — TASK-238 (ticker fix) and TASK-247 (SKIP source)
both Done as of 2026-08-05."`

**★ TASK-244 — שורת Description מיושנת, כבר מתוקנת בתוך התיק עצמו**
```
שורה 52 (Description): "DEPENDS ON TASK-55."
שורה 58 (Notes):       "The DEPENDS ON line above names TASK-55. That is wrong. TASK-55 has status Done."
שורה 60 (Notes):       "The open owner of the 429 root is TASK-215 ... status To Do."
TASK-55: status: Done      TASK-215: status: To Do
```
התיק כבר יודע. רק ה-Description לא עודכן. **הצעה: REWORD ל-`DEPENDS ON TASK-215`** — או
להשאיר כמו שהוא, כי ה-Notes כבר מכסים. עלות נמוכה, ערך נמוך.

### ממצא נוסף שאינו "חוסם-Done" אבל הוא פרמיסה שהתיישנה

**TASK-225** — החוסם שלו אינו תיק אלא היפותזה:
```
"HARD GATE (עמיחי 3/7): decision/implementation BLOCKED until HYP-002 concludes
 (~2026-07-27 / n>=150 post-flip / 45 trading days, first-of)"
```
`docs/HYPOTHESES.md:230-257`:
```
### VOIDED 2026-08-03
... n post-flip ENTERs = 173, against a threshold of 150. The rule fired on n ...
86 of 173 contaminated, 49.7 percent ... 84 entries have no outcome at all ...
RE-REGISTERED as a fresh run starting 2026-08-03 ... 45 trading days from 2026-08-03
reaches early October.
CARRIED FORWARD: nothing.
```
**התאריך `~2026-07-27` בתיק חלף, וההיפותזה נפסלה ונרשמה מחדש.** השער החדש הוא **תחילת
אוקטובר**, לא סוף יולי. הצעה: REWORD לתאריך החדש עם ההפניה ל-§F.

---

## טבלת סיכום

| תיק | הכרעה מוצעת | הראיה בשורה אחת |
|---|---|---|
| **TASK-179** | **REWORD** | 172/177/178 כולם Done מ-23/6 (HYPOTHESES §D:97-101) — אבל grep על כל קוד חי מחזיר **אפס** גלאי crossover, רק 2 הערות; n לא נמוך, הוא לא מדיד |
| **TASK-226** | **REWORD** | check_30 (`health_audit.py:1339`) מחשבת מחדש שורה אקראית אחת ומחזירה INFO על גיליון ריק — אפס כיסוי לאפס-שורות תוך-יומי; 166 כבר Done, הבית הנכון הוא 252 |
| **TASK-254** | **REWORD** (answered → 208/209) | `orchestrator_eod.py:173` `ScoreAnalytics()` → `score_analytics.py:131` מחזיר DataFrame ריק; **גם ה-writer None (:215)** — לא תיקון-שורה-אחת, ולא לכיוון 208/209 |
| **TASK-145** | **CLOSE** | 34 ריצות: 6 מ-7 הכשלים ידניים ב-2/6; **1/7 ✅ ו-1/8 ✅** מתוזמנות — התנאי שהתיק הציב התקיים |
| **TASK-251** | **REWORD** | שלושת עוגני הקוד מאומתים מילה-במילה, **אבל 2026-09 גם הוא STANDALONE** ב-`sheets_config.json` (ללא שינוי מאז 29/7) — הטענה "09 = alias" שגויה |
| **TASK-10** | ✅ **כבר סגור** | `status: Done` + RULING יחיד, מאומת גם ב-`1c2b54d` שנדחף |
| TASK-101 | REWORD (חדש) | `gated on TASK-93/94` — שניהם Done, אבל ה-batch האוטונומי מנוטרל (POSTMORTEM 7/02, TASK-186 In Progress) |
| TASK-241 | REWORD (חדש) | 238 Done · 247 Done · **246 עדיין To Do** — חוסם אמיתי אחד נשאר |
| TASK-244 | REWORD (חדש, ערך נמוך) | Description אומר `DEPENDS ON TASK-55` (Done); ה-Notes של התיק כבר מתקנים ל-TASK-215 (To Do) |
| TASK-225 | REWORD (חדש) | השער `~2026-07-27` חלף; HYP-002 נפסלה 3/8 ונרשמה מחדש — אופק חדש **תחילת אוקטובר** |

### הספירה הצפויה

| תרחיש | פתוחים |
|---|---|
| היום | **59** |
| אם רק TASK-145 נסגר (ההצעה היחידה ל-CLOSE) | **58** |
| אם כל שאר ההצעות מתקבלות כ-REWORD | **58** — ניסוח משתנה, ספירה לא |

---

## איפה ההערכה שלך לא עמדה — במפורש

| # | ההערכה שלך | מה נמצא בפועל |
|---|---|---|
| 1 | **TASK-179:** "REWORD — לא נסגר, אבל שורת החסימה שגויה" | ✅ **עמדה.** בתוספת שלא ציפית לה: אין גלאי crossover בקוד כלל — החוסם אינו זמן אלא עבודת-בנייה שאף תיק לא מחזיק |
| 2 | **TASK-226:** "לצמצם למה שלא מכוסה, **או לסגור אם מכוסה במלואו**" | ⚠️ **ענף הסגירה נפל.** אפס כיסוי — check_30 היא lineage-drift על `post_analysis`, לא רצה בשעות שוק, וריק אצלה = INFO. גם 252 לא מכסה (לילית, גיליון אחר) |
| 3 | **TASK-254:** "או לסגור, **או להמיר לתיק תיקון של שורה אחת**" | ⚠️ **ענף התיקון נפל.** `_analytics_writer` גם הוא None → צריך 2-3 הזרקות; ובנוסף `run_daily` מסנן ליום בודד, כך שגם reader תקין ייתן n<10. ההזרקה גם סותרת את 208/209 |
| 4 | **TASK-145:** "CLOSE אם המתוזמנות ירוקות" | ✅ **עמדה במלואה.** 1/7 ו-1/8 שתיהן success |
| 5 | **TASK-251:** "REWORD לתיק קוד קטן, **או מיזוג ל-243**" | ⚠️ **מיזוג מלא לא אפשרי כמו שהוא** — חמשת סעיפי 243 אינם כוללים SHEET_NAMES, צריך להוסיף שם סעיף (6). **ובנוסף: התיק מכיל טענה עובדתית שגויה על 2026-09** |
| 6 | ההנחה המשתמעת שהתיקים שנבדקו הם 6 | סעיף 7 העלה **4 נוספים** (101, 241, 244, 225), מהם 101 הוא היחיד שהחוסם שלו ברמת-ID נוקה לגמרי |

---

## מה לא נבדק — הצהרה

1. **לוגי ששת הכשלים של 2/6** (TASK-145) — לא נפתחו; retention פוקע ~31/8.
2. **מצב הגיליונות עצמם** — לא נגעתי ב-Sheets כלל. כל נתוני 251 מ-`sheets_config.json`
   המקומי ומ-git, אפס קריאות API.
3. **האם קיים tab בשם `live_trades` בתוך `1b0Vb`** (TASK-251) — Sheets, חסום. נשאר "לא מאומת".
4. **`plans/stateless-seeking-sifakis.md`** ש-TASK-226 מצטט כמקור-ראיה — **אינו קיים בריפו**
   (זה TASK-257 שנפתח היום). לא יכולתי לאמת את קביעות S3 מול המקור.
5. **הסריקה בסעיף 7 מכסה גופי תיקים בלבד** — לא את שדות `MASTER_TASK_LIST`. מוצהר שם.

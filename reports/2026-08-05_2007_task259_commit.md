# commit — TASK-259 batch ל-SMA20

**נכתב 2026-08-05 20:07 Lima.** commit אחד על ה-batch בלבד. **אין push.**
**שני קבצי ה-YAML נשארו בעץ כ-`M` ולא נכנסו ל-commit.**
לא נגעתי ב-Sheets/Drive/FINVIZ, לא הרצתי workflow/scanner/collector.
נקודת פתיחה: `52c32a7`.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

**גיבוי ה-PK לפני העריכה:**
`/private/tmp/.../scratchpad/bak11/RidingHigh_Pro_PK_v2.md.bak_2026-08-05_2008`

---

## 1. צמצום שורת ה-PK

שורת `v4.13` שנכתבה בתור הקודם כיסתה **שני** שינויים. `concurrency` אינו נוחת עכשיו,
ולכן השורה צומצמה כך שתתאר רק את מה שנכנס בפועל.

```
$ git diff --numstat -- docs/RidingHigh_Pro_PK_v2.md
3	1	docs/RidingHigh_Pro_PK_v2.md
```
**מחיקה אחת** = שורת ה-changelog הישנה (הארוכה), שהוחלפה בחדשה. `str_replace` בלבד.

**אימות שהצמצום עשה בדיוק את מה שנדרש:**
```
$ sed -n '23p' docs/... | grep -c "concurrency\|check_06\|cancel-in-progress"
0                                    ← אין זכר ל-concurrency בשורה

$ sed -n '11p' docs/...
| **Document version** | 4.13 |      ← הגרסה לא שונתה, 4.13 נכון

$ grep -c "^v4\.1[0-2] \|^v4\.0" docs/...
13                                   ← שורות ה-changelog הקיימות במקומן

$ grep -n "^v4\.1[0-9] " docs/... | head -4
23:v4.13 (2026-08-05, TASK-259 — batch ל-SMA20): ...
25:v4.12 (2026-08-03, סגירת יום): ...
27:v4.11 (2026-08-03): ...
29:v4.10 (2026-07-29): ...
```

**מה השורה מכסה עכשיו** (הגוף עצמו אינו מודפס — RULE #14): `prime_sma20_cache` ובקשה
אחת במקום 60 · המדידה 10.12s→0.86s פי 11.8 · הרקע ש-`data/` gitignored ⇒ קאש תמיד קר ·
ה-fallback הסדרתי עם `SMA20_BATCH_FAILED` · null-marker ו-`BARS_INSUFFICIENT` ·
SMA לא-מעוגל בשלושת אתרי הכתיבה ⇒ **0 מתוך 60 נבדלים** · 7 טסטים · אפס שינוי
Score/sizing/gate-thresholds · `§1 version 4.12→4.13`.
**אין בה מילה על `concurrency` או `check_06`** — הם ייכנסו בשורה נפרדת אם וכאשר יינחתו.

---

## 2. אימות אחרון לפני commit

```
$ pytest -m "not integration" -q
1 failed, 729 passed, 3 deselected, 8 warnings in 152.53s
FAILED test_orchestrator_eod_borrow_wiring_v1.py::test_no_tickers_skips_collector_and_does_not_fail
```

⚠️ **הערת-דיוק:** מצב הבסיס היה **2 כשלים** בשני הטסטים של אותו קובץ. בריצה הזו
**רק אחד** נכשל. כלומר **פחות** כשלים מהבסיס, ובוודאי **אפס כשלים חדשים** — השער עבר.
התנודתיות הזו עצמה מחזקת את האבחון שנרשם קודם: אותם טסטים קולטים מצב חי דרך ענף שאינו
מפוטץ', ולכן התוצאה משתנה עם הדאטה.

```
$ git diff --stat -- .github/workflows/
 .github/workflows/agent_minute.yml | 5 +++++
 .github/workflows/auto_scan.yml    | 5 +++++
 2 files changed, 10 insertions(+)      ← ללא שינוי. לא נכנסים ל-commit.
```

---

## 3. ה-commit

**ה-index לפני ה-commit — הודפס ואומת:**
```
$ git diff --cached --name-only | wc -l
6
agent/enrichment/sma20_cache.py
agent/orchestrator.py
data_provider.py
docs/RidingHigh_Pro_PK_v2.md
providers/alpaca_provider.py
tests/agent/unit/test_sma20_batch_v1.py

$ git diff --cached --name-only | grep "\.github/workflows/"
CLEAN: no workflow staged
```
**שישה נתיבים בדיוק, אפס workflows** ⇒ pathspec לא נדרש.
`git add` בשמות מפורשים; אפס `-A`, אפס wildcards.

```
fcdb0aa  perf(agent): batch SMA20 enrichment into one Alpaca request
```

`git show --stat HEAD`:
```
 agent/enrichment/sma20_cache.py         |  92 +++++++++++-
 agent/orchestrator.py                   |  13 ++
 data_provider.py                        |  26 ++++
 docs/RidingHigh_Pro_PK_v2.md            |   4 +-
 providers/alpaca_provider.py            |  49 ++++++-
 tests/agent/unit/test_sma20_batch_v1.py | 242 ++++++++++++++++++++++++++++++++
 6 files changed, 422 insertions(+), 4 deletions(-)
 create mode 100644 tests/agent/unit/test_sma20_batch_v1.py
```
הודעת ה-commit נכתבה מקובץ (`-F`). **ללא `Co-Authored-By`, ללא "Generated with".**

---

## 4. אימות אחרי — ה-YAML נשאר בחוץ

```
$ git log -2 --oneline
fcdb0aa perf(agent): batch SMA20 enrichment into one Alpaca request
52c32a7 docs(backlog): open TASK-259/260/261 from the measurement session

$ git status --porcelain=v1 --untracked-files=all
 M .github/workflows/agent_minute.yml      ← עדיין modified
 M .github/workflows/auto_scan.yml         ← עדיין modified
?? docs/auto-dancer/queue/QUEUE_2026-07-04.md   ← untracked מ-7/04, לא שלי
?? docs/auto-dancer/queue/QUEUE_2026-07-05.md   ← untracked מ-7/05, לא שלי
?? reports/  (26 קבצים)                         ← דוחות היום

$ git diff --stat -- .github/workflows/
 2 files changed, 10 insertions(+)         ← ללא שינוי

$ git show --name-only --format="" HEAD | grep "\.github/workflows/"
CONFIRMED: no workflow in fcdb0aa
```
**שתי בדיקות עצמאיות מאשרות שה-YAML נשאר בחוץ:** ה-status מראה אותם עדיין `M`,
וה-`git show --name-only` על ה-commit מראה שאינם בו.

**כל קבצי הקוד שנערכו נוקו מהעץ** — `git status` כבר לא מציג את `sma20_cache.py`,
`orchestrator.py`, `data_provider.py`, `alpaca_provider.py`, `PK` או קובץ הטסט.

### ahead / behind
```
$ git rev-list --left-right --count HEAD...origin/main
8	11
$ git rev-list --left-right --count HEAD...origin/docs/handoff-2026-07-29
1	0
```
**8 לפני `origin/main`, 11 אחרי.** ו-**1 לפני הענף שעל השרת** — כלומר
`fcdb0aa` עדיין לא ב-PR #33 (שמכיל 7). זה נכון וצפוי: אין push בתור הזה.

---

## 5. סטיות מההוראה

| # | מה | פירוט |
|---|---|---|
| 1 | **`git commit` ללא pathspec** | ההוראה התנתה pathspec ב"אם ה-index מכיל משהו נוסף". הדפסתי אותו: 6 נתיבים בדיוק, אפס workflows ⇒ לא נדרש. ההדפסה מובאת ב-§3 כראיה. |
| 2 | **הודעת ה-commit מ-`-F` ולא `-m`** | סיבה טכנית: גרשים ואפוסטרוף (`today's`) שוברים ציטוט ב-zsh. הטקסט זהה מילה-במילה. |
| 3 | **הטסטים הראו 1 כשל ולא 2** | פחות מהבסיס, לא יותר. מוצהר ומוסבר ב-§2. |
| 4 | **RULE #12 — ללא `.rh-run.sh`** | הפלט יועד לדוח. |

**Anti-Drift (rhpro-live §4):** ה-commit נוגע בקוד, ו-**ה-PK נכלל בו** עם bump ל-4.13
ושורת changelog שמתארת **בדיוק** את מה שנחת. החוזה נסגר.
**RULE #14 נשמר** — ה-PK נקרא נקודתית וגופו לא הודפס.

---

## מצב

`fcdb0aa` מקומי, **לא נדחף**. שני ה-YAML נשארו בעץ כ-`M` וממתינים להכרעה נפרדת —
כולל תופעת-הלוואי הידועה של `check_06`, שמתועדת ב-`reports/2026-08-05_1926` §6.3
ו**אינה** מוזכרת ב-PK כי היא טרם נחתה.

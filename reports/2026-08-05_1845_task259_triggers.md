# TASK-259 שלב א — מה מפעיל את agent_minute

**נכתב 2026-08-05 18:45 Lima.** ⚠️ **חקירה בלבד — לא תיקנתי, לא שיניתי workflow/קוד/config,
לא ביטלתי ולא השהיתי אף ריצה.** לא הרצתי pytest, אין commit/push, לא שיניתי סטטוס תיק.
לא נדרשה קריאה מ-Sheets ולא ביצעתי אחת — כל הראיות מ-`gh`, `git` ומהדיסק המקומי.
HEAD = `52c32a7`.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/systematic-debugging | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/systematic-debugging/SKILL.md` | 296 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

---

## 1. ה-workflow עצמו

```yaml
name: Agent — Every Minute (DRY_RUN)

on:
  schedule:
    - cron: '*/1 13-21 * * 1-5'  # 13-21 UTC covers EDT+EST market close; is_market_hours gates actual runs
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - Checkout code            (actions/checkout@v5)
      - Set up Python            (actions/setup-python@v6, 3.11)
      - Install dependencies     (pip install -r requirements.txt)
      - Run agent orchestrator   (python -m agent.orchestrator)
```

| שאלה | תשובה |
|---|---|
| ה-cron המוגדר | `*/1 13-21 * * 1-5` — כל דקה, 13:00–21:59 UTC, ב-ה'–א' |
| triggers מוגדרים | **שניים בלבד: `schedule` ו-`workflow_dispatch`.** אין `push`, אין `workflow_call`, אין `repository_dispatch` |
| בלוק `concurrency` | **אין.** ראה §6 |
| timeout | `timeout-minutes: 5` |
| מה ההערות אומרות על התדירות | הערה אחת בלבד, ליד ה-cron: *"13-21 UTC covers EDT+EST market close; is_market_hours gates actual runs"*. היא מסבירה את **טווח השעות**, לא את התדירות, ולא מזכירה חפיפה |

**היסטוריית הקובץ** — חמישה commits, אף אחד לא נוגע בתדירות או בחפיפה:
```
0727ac5 feat(agent_minute): dedicated SA _AM (mirror-215) — end shared-SA 429 contention
a0d63fe fix(dst): is_market_hours DST-aware (derive Peru window from ET) + crons 13-21 UTC
f67d12f chore(ci): bump checkout v4->v5 and setup-python v5->v6 (Node 24 migration)
6add916 fix(news-detective): pass FINNHUB_API_KEY to agent_minute env
cbdb327 feat(phase1-m9-m10): Streamlit Dashboard + Production Deployment
```

---

## 2. מי מפעיל בפועל

### 2.1 — התפלגות event, 1000 הריצות האחרונות
```
event totals: {'schedule': 16, 'workflow_dispatch': 984}
```
**98.4% workflow_dispatch.**

### 2.2 — ה-actor
```
$ gh api .../actions/workflows/agent_minute.yml/runs?per_page=5&event=workflow_dispatch
{"id":31046631541, "event":"workflow_dispatch", "created_at":"2026-08-05T20:59:02Z",
 "run_attempt":1, "actor":"projects5069-creator", "actor_type":"User",
 "triggering_actor":"projects5069-creator", "triggering_type":"User", "conclusion":"success"}
{"id":31046557861, ... "created_at":"2026-08-05T20:58:02Z" ... actor "projects5069-creator" User}
{"id":31046484077, ... "created_at":"2026-08-05T20:57:02Z" ... actor "projects5069-creator" User}
{"id":31046409807, ... "created_at":"2026-08-05T20:56:02Z" ... actor "projects5069-creator" User}
{"id":31046335922, ... "created_at":"2026-08-05T20:55:04Z" ... actor "projects5069-creator" User}
```
**ה-actor הוא `projects5069-creator`, מסוג `User` — חשבון הבעלים, לא GitHub App ולא bot.**
חמש הריצות ברצף: `20:55:04 · 20:56:02 · 20:57:02 · 20:58:02 · 20:59:02` — **:02 בכל דקה**.
סדירות כזו אינה אנושית; זהו טוקן של הבעלים שמופעל אוטומטית.

### 2.3 — האם workflow אחר מפעיל אותו
```
$ grep -rn "agent_minute\|workflow_dispatch\|workflow_call\|actions/workflows\|dispatches" .github/workflows/
```
17 התאמות — **כולן שורות `workflow_dispatch:` בהגדרת ה-`on:` של workflows אחרים**
(הרשאה להפעלה ידנית), ואף אחת אינה קוראת ל-`agent_minute`.
**אין `workflow_call` ואין קריאת API לשום workflow מתוך workflow אחר.**
19 ה-workflows בריפו כולם `active`; אין ביניהם dispatcher.

### 2.4 — הסקריפטים בריפו
```
$ grep -rn "dispatches|gh workflow run|actions/workflows" --include=*.py --include=*.sh .
health_audit.py:675   url = f"https://api.github.com/repos/{repo}/actions/workflows/post_analysis.yml/runs?per_page=20"
scripts/measure_429_by_workflow_v1.py:6   ... gh run view --log ...
```
שתי ההתאמות היחידות הן **קריאה** (GET runs / view log). **אין בריפו קוד שמבצע dispatch.**

---

## 3. מקור חיצוני

### 3.1 — המכונה המקומית
```
$ crontab -l
crontab: no crontab for adilevy

$ launchctl list | grep -i "ridinghigh|agent|minute|rhpro|dispatch"
(אין ולו רשומה אחת של RidingHigh — כל ההתאמות הן שירותי Apple)

$ ls -la ~/Library/LaunchAgents/
com.google.GoogleUpdater.wake.plist
com.google.keystone.agent.plist
com.google.keystone.xpcservice.plist
com.rh.overnight.plist.bak_20260702      ← מנוטרל
com.rh.overnight.plist.disabled          ← מנוטרל
```
**המק אינו המקור.** אין crontab, אין launchd פעיל של RidingHigh, ושני ה-plist של
הרצת-הלילה נושאים סיומות `.bak_` ו-`.disabled` — עקבי עם ה-DISARMED מ-2026-07-02.

### 3.2 — המקור: cron-job.org
```
$ grep -rniI "cron-job|cronjob.org|uptimerobot|webhook" --include=*.py --include=*.md .
dashboard.py:4073   cron-job.org → GitHub Actions (auto_scan.yml)
dashboard.py:4159   "cron-job.org יתחיל לטריגר את auto_scan.yml כל דקה"
dashboard.py:4204   "סקאנר ימשיך לרוץ עד שאחת ה-cron-job.org dispatches תזהה שהשוק סגור"
dashboard.py:4938   {"שירות": "cron-job.org", "תפקיד": "Triggers auto_scan every minute", "Tier": "Free", "SLA": "אין"}
docs/RidingHigh_Pro_PK_v2.md:1273   │  cron-job.org → GitHub Actions dispatch      │
docs/RidingHigh_Pro_PK_v2.md:1348   08:30 ──┤ auto_scanner starts (cron-job.org → GH Actions)
docs/RidingHigh_Pro_PK_v2.md:1390   | Minute-level cron | cron-job.org | Free | Triggers auto_scan.yml |
docs/RidingHigh_Pro_PK_v2.md:1410   | cron-job.org | none (free) | No minute-level triggers | auto_scan.yml also has internal cron as backup |
docs/RidingHigh_Pro_PK_v2.md:1997   ... cron-job.org also sends dispatch ...
docs/RidingHigh_Pro_PK_v2.md:2351   cron-job.org:  FREE tier  →  $0
```
**המפעיל הוא שירות חיצוני בשם `cron-job.org`**, שמבצע dispatch דרך ה-API של GitHub
עם טוקן של `projects5069-creator`.

⚠️ **פער בתיעוד:** כל עשר ההפניות מדברות על **`auto_scan.yml` בלבד**. אף מקור בריפו לא
מתעד ש-cron-job.org מפעיל גם את `agent_minute.yml` — ובכל זאת 984 מתוך 1000 הריצות
שלו הן dispatch מאותו חשבון ובאותו דפוס של :02 בכל דקה.

---

## 4. הפער בין המתוזמן למופעל

```
date          total  sched   disp  other   dur_med  dur_max   overlap_pairs
2026-08-05      487      7    480      0       203      370      384/486
2026-08-04      486      6    480      0       115      605      293/485
2026-08-03       27      3     24      0        39       55        1/26     ← עמוד חלקי

inter-start gap (within-day): min=3s  median=60s  max=3558s  n=996
duration all: min=31s p25=52s median=154s p75=320s max=605s
runs longer than 60s: 671 of 1000 (67.1%)
```

**התשובה: זהו דפוס יומי, לא חריגה של 5/8.**
ב-4/8 — 480 dispatch מול 6 schedule. ב-5/8 — 480 מול 7. **יחס זהה כמעט בדיוק.**
(5/8 ו-4/8 שלמים בעמוד; 3/8 קטוע — העמוד תופס 1000 ריצות ומסתיים באמצעו.)

**המרווח החציוני בין התחלות הוא 60 שניות בדיוק** — המפעיל יורה כל דקה, כמתוכנן.
מה שלא מתוכנן הוא שהריצה **אינה מסתיימת** בתוך הדקה.

⚠️ **תיקון למספרים שנרשמו ב-TASK-259.** התיק מצטט `median=292s` ו-`92.5%` חפיפה,
שנמדדו על `--limit 400` — כלומר על **400 הריצות האחרונות של היום**, שהן בעיקר אחר-הצהריים
העמוס. המדגם המלא של 5/8 (487 ריצות) נותן **חציון 203 שניות** ו-**384/486 = 79.0%** חפיפה.
**המסקנה לא משתנה** — חפיפה היא עדיין הנורמה — אבל המספרים בתיק גבוהים מהמדגם המלא ומקורם
בחלון קטוע. הפער מוצהר כאן ולא תוקן בתיק (אין הרשאת עריכה בתור הזה).

---

## 5. אותה שאלה על auto_scan

```yaml
name: RidingHigh Auto Scanner
on:
  schedule:
    - cron: '*/1 13-21 * * 1-5'
  workflow_dispatch:
jobs:
  scan:
    timeout-minutes: 8
```

```
date          total  sched   disp  other   dur_med  dur_max   overlap_pairs
2026-08-05      400      6    394      0       269      785      338/399

inter-start gap (within-day): min=10s  median=60s  max=1257s  n=398
duration all: min=23s p25=152s median=269s p75=377s max=785s
runs longer than 60s: 338 of 400 (84.5%)
event totals: {'schedule': 6, 'workflow_dispatch': 394}
```

**כן — הסורק חופף, ובחומרה גדולה יותר.**

| | agent_minute (5/8) | auto_scan (5/8) |
|---|---|---|
| cron | כל דקה | כל דקה |
| timeout | 5 דק' | 8 דק' |
| חציון משך ריצה | 203s | **269s** |
| max | 370s | **785s** |
| ריצות מעל 60 שניות | 384/487 ≈ 79% | **338/400 = 84.5%** |
| זוגות עוקבים חופפים | 384/486 = 79.0% | **338/399 = 84.7%** |
| dispatch מול schedule | 480 / 7 | **394 / 6** |

*(רשימת auto_scan הוגבלה ל-400 וכולן מ-5/8, כך שייתכן שהיום מכיל יותר מ-400 ריצות
ושהמספרים האלה הם חלון ולא היום המלא. מוצהר.)*

---

## 6. מה קיים היום למניעת חפיפה

```
$ grep -rn "concurrency" .github/workflows/*.yml
NONE — no workflow declares a concurrency group

$ grep -rniI "flock|fcntl|mutex|singleton|advisory lock|acquire_lock|\.lock" --include=*.py agent/ *.py
agent/sentinel/data_sentinel.py:291   # Singleton helper (used by orchestrator)
data_provider.py:300,316,351,383      # provider singletons
```
ארבע ההתאמות היחידות הן **singletons בתוך תהליך** (מופע יחיד של ספק-נתונים בזיכרון) —
הן אינן מונעות דבר בין תהליכים.

```
$ grep -n "check_emergency_stop|detect_outage" agent/orchestrator.py
131  def check_emergency_stop()   ← מתג-הרג ידני דרך system_events
382  def detect_outage(now)       ← מזהה פער בין ריצות, כלומר הבעיה ההפוכה
```

**התשובה: לא. אין שום מנגנון.**
אף אחד מ-19 ה-workflows אינו מגדיר `concurrency`. אין קובץ-נעילה, אין mutex, אין
advisory lock, ואין סמן-ריצה. `check_emergency_stop` הוא מתג ידני;
`detect_outage` מחפש ריצות **חסרות**, לא ריצות **חופפות**.

---

## מה מפעיל את הריצות — התשובה

**שירות חיצוני בשם `cron-job.org`**, שמבצע `workflow_dispatch` דרך ה-API של GitHub
עם טוקן של המשתמש `projects5069-creator`, פעם בכל דקה ב-:02.

הראיה מורכבת משלושה חלקים שכל אחד מהם אומת:
1. **ה-actor הוא `projects5069-creator` מסוג `User`**, בקצב מכונתי של :02 בכל דקה —
   לא bot, לא GitHub App, ולא לחיצה אנושית.
2. **אין מפעיל פנימי:** אף workflow לא קורא ל-`agent_minute`, אין `workflow_call`,
   ואין בריפו קוד שמבצע dispatch.
3. **אין מפעיל מקומי:** אין crontab, אין launchd פעיל, ושני ה-plist של הרצת-הלילה מנוטרלים.
4. **התיעוד מצביע על השירות בשמו** — עשר הפניות ב-`dashboard.py` וב-PK.

**מה שלא הוכרע:** התיעוד קושר את cron-job.org ל-**`auto_scan.yml` בלבד**. אין בריפו
שום מקור שמתעד job שני שמפעיל את `agent_minute.yml`. אישור סופי דורש כניסה לחשבון
cron-job.org וצפייה ברשימת ה-jobs — **מחוץ להיקף החקירה הזו, ולא ביצעתי אותו.**
עד אז: השירות הוא ההסבר היחיד שנותר עומד אחרי שכל החלופות נשללו, אך לא נצפה ישירות.

---

## האם auto_scan חופף גם הוא, ומה זה אומר על TASK-253

**כן, ובחומרה גדולה יותר: 338 מתוך 399 זוגות עוקבים (84.7%), חציון משך 269 שניות
מול cron של 60 שניות, ו-394 מתוך 400 הריצות הן dispatch.**

TASK-253 מדד **931 זוגות (ScanTime, Ticker) כפולים ו-977 שורות עודפות** ב-`timeline_live`
של אוגוסט — 308, 319 ו-304 זוגות בשלושת הימים, כלומר יציב.
`auto_scanner.run_scan` כותב שורה אחת לכל טיקר עם `ScanTime` שנגזר מהריצה
(`auto_scanner.py:442-478`, append-only). שתי ריצות חופפות שנוגעות באותה דקה מייצרות
שתי שורות עם אותו `(ScanTime, Ticker)`.

**החפיפה היא הסבר עקבי עם המדידה — אך לא הוכחה.** מה שאומת: שני ה-workflows חופפים
באותו דפוס, שניהם מופעלים מאותו מקור חיצוני, ולשניהם אין `concurrency`.
מה שלא נבדק: האם הזוגות הכפולים ב-`timeline_live` אכן משויכים לזוגות ריצות חופפות
לפי `run id` — הצלבה כזו דורשת שיוך של כל שורת-כפילות לריצה, ולא ביצעתי אותה.

**לא הצעתי תיקון ולא המלצתי על `concurrency` או כל פתרון אחר, כפי שהתבקש.**

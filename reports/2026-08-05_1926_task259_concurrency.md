# TASK-259 — concurrency על agent_minute ו-auto_scan

**נכתב 2026-08-05 19:26 Lima.** שני קבצי YAML בלבד. **אין commit, אין push.**
לא נגעתי בשום קובץ py, לא ב-Sheets/Drive/FINVIZ, לא הרצתי workflow/scanner/collector ידנית.
HEAD = `52c32a7`.

**סקילים:**
| סקיל | path | wc -l |
|---|---|---|
| rhpro-live | `~/.claude/skills/rhpro-live/SKILL.md` | 180 |
| superpowers/verification-before-completion | `~/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/verification-before-completion/SKILL.md` | 139 |

**גיבויים לפני העריכה:**
```
/tmp/agent_minute.yml.bak_2026-08-05_1926
/tmp/auto_scan.yml.bak_2026-08-05_1926
```

---

## 1. גבול ה-`on:`/`jobs:` לפני העריכה

```
agent_minute.yml                          auto_scan.yml
  3  on:                                    3  on:
  4    schedule:                            4    schedule:
  5      - cron: '*/1 13-21 * * 1-5' ...    5      - cron: '*/1 13-21 * * 1-5' ...
  6    workflow_dispatch:      ← סוף on:    6    workflow_dispatch:      ← סוף on:
  7  (ריק)                                  7  (ריק)
  8  jobs:                   ← תחילת jobs:  8  jobs:                   ← תחילת jobs:
  9    run-agent:                           9    scan:
```
**זהה בשני הקבצים.** הבלוק הוכנס בין שורה 6 לשורה 8, ברמת ה-top level.

---

## 2. ה-diff

```diff
diff --git a/.github/workflows/agent_minute.yml b/.github/workflows/agent_minute.yml
index f9454df..7c2c514 100644
--- a/.github/workflows/agent_minute.yml
+++ b/.github/workflows/agent_minute.yml
@@ -5,6 +5,11 @@ on:
     - cron: '*/1 13-21 * * 1-5'  # 13-21 UTC covers EDT+EST market close; is_market_hours gates actual runs
   workflow_dispatch:
 
+# One run at a time. A queued run is dropped if another is already running (TASK-259).
+concurrency:
+  group: ${{ github.workflow }}
+  cancel-in-progress: false
+
 jobs:
   run-agent:
     runs-on: ubuntu-latest

diff --git a/.github/workflows/auto_scan.yml b/.github/workflows/auto_scan.yml
index d63f716..45b2528 100644
--- a/.github/workflows/auto_scan.yml
+++ b/.github/workflows/auto_scan.yml
@@ -5,6 +5,11 @@ on:
     - cron: '*/1 13-21 * * 1-5'  # every min; 13-21 UTC covers EDT(13:30-20:00)+EST(14:30-21:00); is_market_hours gates
   workflow_dispatch:
 
+# One run at a time. A queued run is dropped if another is already running (TASK-259).
+concurrency:
+  group: ${{ github.workflow }}
+  cancel-in-progress: false
+
 jobs:
   scan:
     runs-on: ubuntu-latest
```
```
$ git diff --stat -- .github/workflows/
 .github/workflows/agent_minute.yml | 5 +++++
 .github/workflows/auto_scan.yml    | 5 +++++
 2 files changed, 10 insertions(+)
```

**אפס מחיקות. אפס שינוי ב-cron, ב-timeout, ב-env, או בכל דבר אחר.**
```
$ git status --porcelain | grep -vE '\.yml$'
?? docs/auto-dancer/    ← היה untracked לפני
?? reports/             ← היה untracked לפני
```
**אף קובץ py לא נגע.**

⚠️ **סטייה קטנה מוצהרת:** ביקשת "בדיוק שני בלוקים של 4 שורות". ה-diff מראה **5 שורות
לכל קובץ** — 4 שורות תוכן (ההערה + שלוש שורות ה-concurrency) **ועוד שורה ריקה אחת**
שמפרידה מ-`jobs:`. בלעדיה שני הבלוקים היו נדבקים. זו שורה ריקה, לא תוכן.

---

## 3. אימות תחביר

```
$ python -c "import yaml; ..."
.github/workflows/agent_minute.yml -> concurrency = {'group': '${{ github.workflow }}', 'cancel-in-progress': False}
.github/workflows/auto_scan.yml    -> concurrency = {'group': '${{ github.workflow }}', 'cancel-in-progress': False}
YAML OK
```
שני הקבצים נטענים, והבלוק יושב ברמת ה-top level עם `cancel-in-progress: False` —
**לא `true`**, כפי שהזהרת.

---

## 4. טסטים — ⚠️ שניים אדומים

```
$ uv run --with-requirements requirements.txt --with pytest pytest -m "not integration" -q
FAILED tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py::test_collects_unified_deduped_tickers_and_passes_to_collector
FAILED tests/agent/unit/test_orchestrator_eod_borrow_wiring_v1.py::test_no_tickers_skips_collector_and_does_not_fail
2 failed, 720 passed, 1 skipped, 3 deselected, 8 warnings in 37.50s
```
*(ללא `-m "not integration"` יש כשל שלישי — `test_decision_logger_writes.py`, טסט integration
שנוגע בגיליון חי. ה-CI מריץ עם המסנן, ולכן הוא מחוץ להיקף.)*

**עצרתי ולא תיקנתי, כפי שהורית.** להלן מה שכן בדקתי כדי לקבוע אם זה קשור לשינוי:

**סיבת הכשל:**
```
>       assert passed_tickers == ["AAA", "BBB", "CCC"]                 # sorted + unique
E       AssertionError: assert ['AAA', 'BBB'...', 'YXT', ...] == ['AAA', 'BBB', 'CCC']
E         Left contains 4 more items, first extra item: 'INLF'

>       collect.assert_not_called()
E       AssertionError: Expected 'collect_borrow_data' to not have been called. Called 1 times.
E       Calls: [call(['INLF', 'SHPH', 'YXT', 'ZYBT'], <MagicMock name='AlpacaBroker()'>)]
```
הטיקרים שדולפים לטסט — **INLF · SHPH · YXT · ZYBT** — הם **הטיקרים החיים של היום**
(`decision_log` של 2026-08-05 מכיל DFNS, SHPH, YXT, DBGI, INLF; ZYBT מופיע בלוג הריצה).
כלומר ה-mock אינו מבודד את מקור הנתונים, ומצב אמיתי מהמכונה המקומית נכנס לטסט.

**זה אינו קשור לשינוי — שלוש ראיות:**
1. **השינוי נוגע ב-YAML בלבד.** `git status | grep -v '\.yml$'` מראה שאף קובץ py לא נגע.
   קובץ workflow אינו מיובא ע"י אף טסט.
2. **אותה חבילת טסטים עברה ב-CI על אותו commit לפני כשעה.**
   `31056028703 tests event=push success sha=52c32a7` ו-
   `31056032278 tests event=pull_request success sha=52c32a7` — שניהם `success`.
   ה-CI מריץ בדיוק את אותה פקודה (`tests.yml:30`).
3. **הכשל תלוי-מצב-מקומי:** ב-CI אין `sheets_config.json` עם הרשאות חיות ואין קאשים
   מקומיים תחת `data/`, ולכן ה-mock מחזיק שם. מקומית הוא דולף.

**מסקנה: ירוק ב-CI, אדום מקומית, והסיבה היא בידוד-טסט ולא רגרסיה.**
לא תיקנתי ולא פתחתי תיק — זו הכרעה שלך.

---

## 5. מה זה משנה — חישוב מהמדידות

### 5.1 — סמנטיקת `cancel-in-progress: false`
לפי התיעוד של GitHub: לכל `group` רצה **ריצה אחת** ומחכה **ריצה אחת** לכל היותר.
כשמגיעה ריצה חדשה בזמן שכבר יש ממתינה, **הממתינה הישנה מבוטלת** והחדשה תופסת את מקומה.
`cancel-in-progress: false` מבטיח שהריצה **הפעילה** לעולם לא נהרגת באמצע.
⚠️ **לא צפיתי בזה חי בריפו הזה** — מעולם לא היה בו `concurrency`. זו סמנטיקה מתועדת.

### 5.2 — החישוב, מהמדידות של 5/8
```
agent_minute: n=487  median=203s = 3.38 min
   effective interval if serialized: 3.38 min
   runs that would execute in a 540-min window: 160
   dispatches dropped (of ~540): 380

auto_scan:    n=400  median=269s = 4.48 min
   effective interval if serialized: 4.48 min
   runs that would execute in a 540-min window: 120
   dispatches dropped (of ~540): 420
```

| | לפני | אחרי (חציון) |
|---|---|---|
| `agent_minute` — ריצות שיבוצעו | ~487 | **~160** |
| `agent_minute` — dispatches שיבוטלו | 0 | **~380** |
| `auto_scan` — ריצות שיבוצעו | ~400 | **~120** |
| `auto_scan` — dispatches שיבוטלו | 0 | **~420** |

**המרווח האפקטיבי בין סריקות אחרי השינוי:**
**`auto_scan` ≈ 4.5 דקות · `agent_minute` ≈ 3.4 דקות** — במקום דקה נומינלית.
זה הופך את `timeline_live` מרשומה דקתית לרשומה של ~4.5 דקות.

### 5.3 — הזנב
```
runs longer than  480s (8.0 min):  agent_minute 0/487 (0.0%)   auto_scan 63/400 (15.8%)
runs longer than  600s (10.0 min): agent_minute 0/487 (0.0%)   auto_scan  1/400 ( 0.2%)
runs longer than 1800s (30 min):   שניהם 0
```

---

## 6. ⚠️ מה ייחשב ל"ריצות חסרות" — התשובה, וזו הנקודה החשובה

### 6.1 — GitHub מדווח `cancelled`, לא `skipped`
מאומת על ריצה אמיתית שבוטלה היום:
```
$ gh run view 31024026412 --json conclusion,status
24026412  completed/cancelled
```
**`status: "completed"` · `conclusion: "cancelled"`.**

### 6.2 — `detect_outage` — ככל הנראה **לא** יתריע, אך הזנב מתקרב
```python
agent/orchestrator.py:404   records = get_sheet_records("timeline_live")
agent/orchestrator.py:422   gap_min = now_min_of_day - latest_min_of_day
agent/orchestrator.py:424   if gap_min > 10:  → outage
orchestrator.py:566-567     severity = "CRITICAL" if gap_min > 30 else "WARNING"
orchestrator.py:588-600     if gap_min > 30: send_alert(...)   ← מייל
```
הוא מודד את הפער ב-`timeline_live.ScanTime`, שנכתב ע"י **`auto_scan`**.
מרווח אפקטיבי חדש ≈ **4.5 דקות מול סף של 10** — יש מרווח ביטחון של פי ~2.2.
**אבל 15.8% מריצות ה-auto_scan של היום ארכו מעל 8 דקות.** ריצה אחת כזו, ועוד זמן
תפיסת runner, עלולה לגעת ב-10. **סף ה-30 דקות (המייל) רחוק** — אף ריצה לא התקרבה אליו.

### 6.3 — 🔴 `check_06 / D3 GitHub Actions health` — **צפוי להתהפך ל-CRITICAL**
זו הבעיה האמיתית, והיא לא ב-`detect_outage`:
```python
health_audit.py:596   completed  = [r for r in recent_runs if r.get("status") == "completed"]
health_audit.py:597   successes  = [r for r in completed if r.get("conclusion") == "success"]
health_audit.py:598   failures   = [r for r in completed if r.get("conclusion") == "failure"]
health_audit.py:605   success_rate = len(successes) / len(completed) * 100
health_audit.py:607   if success_rate >= 95: PASSED
health_audit.py:610   if success_rate >= 80: WARNING
health_audit.py:614   # else → would-be CRITICAL
```
**ריצה שבוטלה ע"י concurrency היא `status="completed"` — היא נכנסת למכנה — אבל
`conclusion="cancelled"`, ולכן אינה נכנסת למונה.**

חישוב עם המספרים מ-§5.2:
```
executed  ≈ 160 (agent_minute) + 120 (auto_scan) = 280
cancelled ≈ 380 + 420 = 800
success_rate ≈ 280 / 1080 ≈ 26%
```
**26% — הרבה מתחת ל-80.** ושני שערי-ההקלה לא יחולו:
- `SAMPLE-SIZE` (`GHA_SAMPLE_MIN = 10`) — לא, יש מאות ריצות.
- `RECOVERING` (`_failing_workflows_recovered`, `:568-582`) — הוא בונה את `failing`
  מ-`conclusion == "failure"` בלבד. **`cancelled` אינו `failure`**, ולכן `failing` ריק
  והפונקציה מחזירה `False` ב-`:575`. אין הורדת-דרגה.

**התוצאה הצפויה: `check_06` יהפוך ל-CRITICAL כבר ב-health_audit הראשון אחרי הפריסה,
ויישאר שם כל יום.** health_audit רץ 06:00 / 15:30 / 22:00 ושולח מייל 🔴.

⚠️ **זהו חישוב מהקוד ומהמספרים, לא תצפית.** לא הרצתי את `health_audit` ולא פרסתי את
השינוי. אני מציין זאת מראש בדיוק כפי שביקשת — לא מציע תיקון.

### 6.4 — מה לא יושפע
- `check_04 / D1 timeline freshness` (`health_audit.py:444`) — עובד ברזולוציית **ימים**
  (`days_old <= 1` → PASSED). מרווח של 4.5 דקות אינו נוגע בו.
- `check_emergency_stop` — מתג ידני, לא קשור.

---

## 7. סטיות מההוראה

| # | מה | פירוט |
|---|---|---|
| 1 | **הבלוק הוא 5 שורות ב-diff ולא 4** | 4 שורות תוכן + שורה ריקה מפרידה מ-`jobs:`. בלעדיה הבלוקים נדבקים. |
| 2 | **`python -m pytest` נכשל, השתמשתי בפקודת ה-CI** | `uv run --with-requirements requirements.txt python -m pytest` החזיר `No module named pytest`. עברתי ל-`--with pytest pytest -m "not integration"` — בדיוק מה ש-`tests.yml:30` מריץ. |
| 3 | **הגיבויים ב-`/tmp` ולא בריפו** | כפי שהורית בפקודה (`cp ... /tmp/...`). |
| 4 | **RULE #12 — ללא `.rh-run.sh`** | הפקודות הורצו ישירות; הפלט יועד לדוח. |

**Anti-Drift (rhpro-live §4):** ⚠️ **בניגוד לכל ה-commits הקודמים היום, השינוי הזה
נוגע ב-workflows** — וסעיף 4 דורש עדכון PK + bump גרסה + שורת changelog **לפני commit**.
**לא עשיתי זאת** — אין הרשאת כתיבה ל-`docs/` בתור הזה, ואין commit. **זה חוב פתוח
שחייב להיסגר לפני ה-commit.**

---

## מצב

השינוי מוחל בעץ העבודה, YAML תקין, **לא committed ולא pushed** — ממתין לסקירה שלך.
שני כשלי הטסטים קיימים מקומית, ירוקים ב-CI על אותו commit, ולא נגעתי בהם.

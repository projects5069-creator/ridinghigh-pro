streamlit>=1.40.0
pandas>=2.2.0
plotly>=5.18.0
# 0.14.6 (2023) is dead against today's finviz markup — its selector
# ("table-light", overview.py:280) finds nothing. CI has always installed
# latest anyway (auto_scan.yml installs unpinned); 1.3.0 is what production
# actually runs. Pin local = production.
finvizfinance==1.3.0
yfinance>=0.2.50
ta==0.11.0
gspread>=6.0.0
google-auth>=2.0.0
google-api-python-client>=2.0.0
pytz>=2023.3
openpyxl
pandas-market-calendars

# Alpaca data provider (Issue #9 Phase 2)
alpaca-py>=0.25.0

# Force rebuild 20260524-212925
